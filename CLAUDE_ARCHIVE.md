# health-shorts

건강 상식 정보를 카드뉴스 + 숏폼(캐릭터 나레이션) + 플랫폼별 캡션으로 만드는 파이프라인.
`shopping-shorts-video`(상품 쇼핑 숏폼)와는 완전히 별도 프로젝트 — 파이프라인이 섞이면
고도화가 과해진다는 판단으로 분리함(2026-07-30).

## 세션 운영 방식 — 역할 고정 폐지 (2026-08-02)

예전엔 01(신규 topic 생성)·02(기존 topic 수정)·03(Admin) 3개 세션으로 역할을 고정해서
동시에 여러 개를 빠르게 돌리는 구조였다. 그런데 SNS 업로드 자체를 플랫폼마다 하루
4개 수준으로 늦춰야 한다는 게 확인되면서("부업 정도로만 지속하는게 낫겠구먼" —
2026-08-02 사용자 확정) 콘텐츠를 여러 세션으로 빠르게 병렬 생산할 이유가 없어졌다 —
어차피 만들어봤자 배포 속도가 병목이라 대시보드에 쌓아두고 나눠 올리는 구조이기 때문.

**이제 세션을 역할로 나누지 않는다** — 세션 하나가 신규 topic 생성·기존 topic 수정·
`.claude/settings.json` 같은 설정 변경까지 그때그때 필요한 걸 전부 한다. "이건 Admin
세션에 요청해야 하나" 같은 구분 없이 요청받은 대로 바로 진행할 것.

다만 여러 세션이 우연히 동시에 켜져 있을 가능성 자체는 여전히 있으므로(예: 사용자가
탭을 여러 개 열어둔 경우), 아래 "동시 세션 락 확인" 절차는 그대로 유지한다 — 역할
구분이 없어진 것과 파일 충돌 방지 장치가 필요 없어진 것은 별개다.

## topic 폴더명 — 카테고리 + 번호로 (2026-08-02)

지금까지는 `췌장굳는음식_1`, `가슴쓰림유발음식_1`처럼 그 topic 훅 내용을 그대로 풀어쓴
긴 이름에 항상 `_1`을 붙였다. 폴더명·UI 목록에서 실제로 필요한 건 "대충 어떤
카테고리인지"뿐이고(실제 훅 문구는 `index.html` 목록에 제목으로 따로 표시됨), 복붙하기도
편하려면 짧은 카테고리명 + 번호가 낫다는 판단이다("췌장_1, 췌장_2 이런식으로... 폴더랑
UI에서 보는 리스트는 카테고리가 뭐인지만 알면 됨" — 2026-08-02 사용자 확정).

**새 topic부터 적용** — 기존 topic들은 이미 배포된 dashboard 링크가 있으니 소급
리네임하지 않고 그대로 둔다:

- 폴더명 = **핵심 카테고리 단어**(장기/증상/재료 등) + `_N`
  - `유발음식`/`부르는음식`/`해치는음식`/`방해음식` 같은 서술형 접미사는 뺀다
  - 예: "췌장 굳게 만드는 음식" 주제 → `췌장_1`, 나중에 췌장 관련 주제를 또 만들면 `췌장_2`
  - 예: "가슴이 쓰리고 신물 올라오는" 주제 → `가슴쓰림_1`
- 번호는 **같은 카테고리로 이미 있는 topic 중 제일 큰 번호 + 1** — 새 topic 시작 전에
  `ls data/ | grep "^<카테고리>_"`로 기존 번호부터 확인할 것
- 카테고리가 애매하면(여러 장기·증상이 겹치는 주제) 제일 핵심적인 키워드 하나로 좁혀서
  판단한다 — 이 기준을 코드에 고정하지 않고 매번 상황 보고 정할 것
- 폴더명이 짧아져도 실제 훅 문구(제목)는 지금처럼 `platform_captions.json`의 `title`에
  그대로 풀어서 쓴다 — 이번 변경은 **폴더명에만** 해당하고, 콘텐츠 톤·훅 작성 규칙과는
  무관하다

## 완전 자동화 모드 — 승인 없이 끝까지 진행 (2026-08-01)

사용자가 "다음 topic 해줘"류로 요청하면, 아래 전 과정을 **중간에 승인·확인 요청 없이
끝까지** 진행한다("이 파이프라인은 고도화가 잘 되어있어서 어차피 판단할 게 없다" —
사용자 확정):

1. **소재 선정** — 사용자가 URL을 주면 그걸 쓰고, 안 주면 WebSearch로 건강 트렌드·증상
   키워드를 직접 서칭해서 소재를 스스로 정한다(기존엔 매번 URL을 받아야 했지만 이제 필수
   아님)
2. 리서치 보강 → narration.txt/card_news_spec.json/platform_captions.json 작성
3. 캐릭터 일러스트(Gemini)·모션(Kling)·카드뉴스 이미지·TTS·영상 조립·대시보드 생성
4. git commit·push까지 — 커밋 메시지 확인 요청 없이 바로 진행

**비용 관련**: Gemini 이미지·Kling 영상·Typecast TTS는 topic마다 실제 과금되지만,
"필요할 때 세션 안에서 진행하는 거라 계속 찔러도 문제없다"고 사용자가 확인함(2026-08-01)
— 정기 스케줄(cron)로 무인 반복 실행하는 게 아니라 요청 시점에만 실행되므로 API 호출
횟수를 스스로 줄이거나 아낄 필요 없음.

**자동화 범위 밖(구조적 한계, 권한과 무관)**: 인스타그램·틱톡·페이스북·네이버 블로그/클립
등은 아직 이 프로젝트에 API 연동이 없어서 자동화가 안 된다 — `dashboard.html`에서
사람이 다운로드→붙여넣기→클릭하는 수동 업로드 흐름 유지. **유튜브 쇼츠만 예외** —
`lib/youtube_upload.py`로 API 업로드 연동됨(아래 "유튜브 쇼츠 자동 업로드" 절 참고).
다른 플랫폼도 "권한을 준다"고 해결되는 문제가 아니라 아직 연동 코드 자체가 없는 것.

## 유튜브 쇼츠 자동 업로드 (2026-08-02)

`lib/youtube_upload.py`가 YouTube Data API v3로 실제 업로드까지 자동화한다 —
`data/<topic>/platform_captions.json`의 "유튜브 쇼츠" 캡션("제목: ...\n\n설명란:\n...")을
파싱해서 제목·설명·해시태그 기반 태그로 나누고, `output/<topic>/<topic>_shorts.mp4`를
그대로 업로드한다.

**1회성 사전 설정(사용자가 직접 해야 함 — 대신 못 해줌)**:
1. [Google Cloud Console](https://console.cloud.google.com)에서 프로젝트 생성 →
   "YouTube Data API v3" 활성화
2. OAuth 동의 화면 구성(외부, 테스트 상태로 충분 — 본인 계정을 테스트 사용자로 추가),
   "데이터 액세스"에서 `.../auth/youtube`(YouTube 계정 관리) 스코프를 미리 추가해둘 것
3. OAuth 클라이언트 ID 발급 — 애플리케이션 유형은 **"데스크톱 앱"**
4. 발급된 클라이언트 ID/보안 비밀을 `.env`의 `YOUTUBE_CLIENT_ID`/`YOUTUBE_CLIENT_SECRET`에 저장
5. `python3 lib/youtube_auth_setup.py` 1회 실행 — 브라우저 로그인 창이 뜨면 업로드에
   쓸 유튜브 계정으로 로그인·동의 → 출력된 refresh token을 `.env`의
   `YOUTUBE_REFRESH_TOKEN`에 저장

`lib/youtube_upload.py`의 `SCOPES`는 `youtube.upload`가 아니라 **`https://www.googleapis.com/auth/youtube`**(전체 관리)를 쓴다(2026-08-02 확장) — 재생목록 생성·채널 조회 등도 이 스코프로 커버하려는 목적. 두 스크립트가 스코프를 각자 들고 있으면 어긋나기 쉬워서 `youtube_auth_setup.py`가 `youtube_upload.py`의 `SCOPES`를 그대로 import해서 쓴다 — 스코프를 또 바꿀 일이 있으면 `youtube_upload.py` 한 곳만 고칠 것.

업로드 성공 후 아래 단계를 이어서 자동 진행한다(`upload_short()` 안에서 순서대로 호출) — 실패해도 업로드 자체는 막지 않고 경고만 남긴다:

⚠️ **커스텀 썸네일 자동 설정은 뺐다**(2026-08-03) — API로 영상 첫 프레임을 썸네일로
올리면 결과물이 이상하게 나와서("너가 수정해주면 이상하게 되네" — 사용자 확인)
`_set_thumbnail`/`_extract_first_frame` 둘 다 코드에서 제거했다. 유튜브 업로드는
**일정에 맞춰 업로드 진행 → 재생목록에 추가**, 이 두 단계만 한다 — 썸네일은
유튜브가 자동 제안하는 것을 그대로 쓰거나 Studio에서 수동으로 다룰 것.

1. **재생목록**(`_add_to_category_playlist`) — topic 폴더명("카테고리_N")에서 카테고리만 뽑아(`_category_from_topic`, 위 "topic 폴더명" 절의 명명 규칙과 동일 기준) 재생목록 제목("건강정보 - 카테고리", `_playlist_title_for_category`)이 같은 재생목록이 있으면 재사용, 없으면 새로 만들어 그 영상을 추가한다. 예: `눈_1`·`눈_2`·`눈_3`은 전부 "건강정보 - 눈" 재생목록 하나에 모인다. ⚠️ 재생목록 제목에 카테고리 단어("눈", "다리쥐")만 쓰지 않는 이유(2026-08-02): 카테고리 단어만으로는 사람이나 유튜브 검색·추천 시스템이 주제를 알아보기 어렵다는 지적 — "카테고리 관련 건강정보"로 한 번 정했다가 "건강정보 - 카테고리"로 최종 확정(전체 재생목록이 "건강정보 - "로 시작해 채널 안에서 한눈에 묶여 보이도록). 새 카테고리가 생겨도 이 형식을 그대로 적용할 것. 재생목록 자체는 항상 `public`으로 만든다(안에 담긴 영상이 private이어도 그 영상은 어차피 안 보이므로 재생목록 자체의 공개 여부와 무관).
   ⚠️ **`_add_to_category_playlist` 호출 전 `_playlist_has_video`로 이미 들어있는지
   먼저 확인한다**(2026-08-02, "버그있었나본데 그래서 한도 이미 엄청빨리소진된듯?
   네개씩 너어놨네" — 사용자가 실제 재생목록에서 같은 영상이 4개씩 중복된 걸
   발견하고 직접 삭제함): 원래는 매번 조건 없이 `playlistItems().insert()`를
   호출했다 — `lib/youtube_organize_playlists.py`(이미 올라간 영상을 소급으로
   재생목록에 정리하는 스크립트)를 여러 번 돌리면 이미 들어있는 영상도 매번 다시
   insert돼서 같은 재생목록에 중복 항목이 쌓이는 버그였다. 지금은 삽입 전에
   `playlistItems().list()`로 이미 그 videoId가 있는지 먼저 확인하고, 있으면
   건너뛴다("[youtube_upload] 재생목록 '...'에 이미 있음 — 건너뜀"). `organize()`나
   `upload_short()`를 반복 실행해도 이제 안전하다. ⚠️ **방금 새로 만든 재생목록을
   곧바로 조회·수정하면 전파 지연으로 404 "playlistNotFound"가 날 수 있음**
   (2026-08-03 실측) — 그 경우 몇 초 뒤 짧게 재시도하도록 코드에 이미 재시도
   루프가 있음.
2. **완료 기록**(`_mark_youtube_uploaded`, 2026-08-02) — `output/youtube_uploaded.json`(topic
   폴더명 배열, git 추적)에 topic을 자동으로 추가한다. 루트 `index.html`의 "완성된
   콘텐츠" 목록에서 topic 전체 완료(`completed_topics.json`)와 별개로 "이 topic의
   유튜브 숏츠만 끝났는지"를 한눈에 보여주는 배지("YT" 회색 → "YT ✓" 빨강)의
   데이터 소스다("완성된 콘텐츠 목록에서 유튜브 숏츠"만" 완료되었는지를 해당
   라인에서 확인할 수 있게" 요청). ⚠️ **예약 게시(`publish_at`)도 여기서 바로
   완료 처리한다** — "완전히 업로드가 완료되는게 기준이 아니라 예약 설정해서
   업로드를 완료한 경우에 확인할 수 있게" — 실제 공개 전환 시각까지 기다리지
   않고, 업로드+재생목록 등록까지 끝나면(이 함수가 호출되는 시점, `upload_short()`
   맨 끝) 완료로 기록한다. 이 자동 기록에 의존하므로, `upload_short()`를 우회해서
   YouTube Data API를 직접 호출하는 코드를 새로 짤 땐 `_mark_youtube_uploaded(topic)`
   호출을 빼먹지 말 것.

**사용법(단일 topic)**: `python3 lib/youtube_upload.py <topic> [private|unlisted|public] [예약시각]` —
privacy_status 기본값은 **"private"**(공개 업로드는 되돌리기 어려운 작업이라 안전하게
비공개로 먼저 올리고 확인 후 다시 `public`으로 호출하는 걸 권장). 예약시각(ISO 8601 UTC,
예: `2026-08-03T09:00:00Z`)을 주면 그때 자동 공개로 전환되는 예약 게시 — YouTube API
제약상 예약 게시 영상은 privacyStatus가 강제로 `private`이 된다(두 번째 인자로 뭘
줘도 무시됨).

### 하루 배치 업로드 — `--daily-batch` (2026-08-02)

사용자가 "업로드해" 하고 지시하면(직접 트리거해야 함, 알아서 먼저 실행하지 말 것)
`python3 lib/youtube_upload.py --daily-batch [private|unlisted|public]`로 **하루치
4개를 10/13/16/19시(KST) 예약 게시로 한 번에** 올린다("10시, 11시, 14시, 17시 이렇게
네 개 토픽에 대해 영상 네 개 넣는거야" 2026-08-02 최초 확정 → 곧바로 "10 13 16 19로
해"로 최종 조정, 3시간 간격으로 더 고르게 분산).

- **topic 선택**(`select_daily_topics`): 아직 유튜브에 안 올라간 topic
  (`output/youtube_uploaded.json` 기준) 중에서, `output/posting_log.csv`에 유튜브
  쇼츠가 아닌 다른 플랫폼 포스팅 기록이 이미 있는 topic을 **우선** 고른다("이미
  올려놓은 것들이 있다면 그걸 우선순위로 유튜브 올리고") — 이런 topic은 유튜브만
  마치면 그 topic 전체가 끝나기 때문. 우선순위 topic이 4개보다 적으면 나머지는
  무작위로 채운다("아닌 경우에는 너가 randomly하게 선택해서").
- **스케줄**(`_next_daily_schedule`): 오늘 10시(KST)가 아직 안 지났으면 오늘
  10/13/16/19시, 이미 지났으면 **통째로 다음날** 10/13/16/19시로 예약한다(일부만
  오늘·나머지 내일로 흩어지지 않게).
- `posting_log.csv`가 아직 커밋 안 된 상태(빈 파일)면 우선순위 topic이 없어서
  전부 무작위 선택으로 자연스럽게 폴백한다 — 별도 처리 불필요.
- 업로드마다 기존과 동일하게 재생목록·`youtube_uploaded.json` 기록까지
  자동 진행된다(`upload_short()`를 그대로 재사용).

⚠️ **OAuth 앱이 "테스트" 상태로 남아있으면 refresh token이 7일 뒤 만료될 수 있음**(구글
정책, youtube 스코프가 민감 스코프라 그렇다) — 만료되면 `youtube_auth_setup.py`를
다시 실행해서 새 토큰을 받을 것. 완전 무인 자동화까지 가려면 나중에 OAuth 앱 인증
(verification)을 받는 걸 고려.

⚠️ **업로드 자체는 자동화됐지만 배포 페이스는 사람이 정한 대로**(위 "세션 운영 방식"
절 참고 — 플랫폼당 하루 4개 수준으로 늦추기로 확정함, "부업 정도로만 지속하는게
낫겠구먼" 2026-08-02) — **하루 배치 업로드는 위 `--daily-batch`(하루 4개, 10/13/16/19시)
형태로만 진행하고, 그 이상 밀려 있는 topic을 한 번에 몰아 올리지 말 것.** 단일
topic만 필요한 특수한 경우(재업로드 등)엔 기존 단일-topic 사용법을 그대로 쓸 것.

**승인만 생략하는 것이지 안전장치는 생략 아님** — 아래는 그대로 다 지킨다:
세션 락 확인/생성/해제, `git show --stat HEAD`로 커밋 내용 검증, 브런치·핀터레스트
제외, `comment_keyword`는 "쿠팡" 고정값 사용, 회피형
콘텐츠는 대체재로 마무리, 훅은 장기명이 아니라 구체적 증상으로, 문장 어미 "~대요" 금지 —
이 문서의 나머지 규칙 전부 동일하게 적용.

## ⚠️ 작업 시작 전 필수 — 동시 세션 락 확인 (2026-07-31)

**여러 Claude Code 세션이 같은 로컬 저장소를 동시에 쓸 수 있다** — 실제로 서로 모르고 같은
topic(`수면음식_1`)을 동시에 작업해서 캐릭터 일러스트·카드뉴스·캡션이 서로 덮어써지는 사고가
있었다. 새 topic을 시작하거나 기존 topic에 추가 작업하기 전에 **반드시**:

1. **확인**: `python3 lib/session_lock.py check <topic>` — `UNLOCKED`가 아니면(활성 락이
   있으면) 다른 세션이 지금 작업 중일 수 있다. 사용자에게 먼저 알리고 진행 여부 확인할 것
   (`python3 lib/session_lock.py list`로 전체 활성 락 목록도 확인 가능)
2. **락 생성**: 확인 후 진행하기로 했으면 즉시 `python3 lib/session_lock.py acquire <topic> "<작업 요약>"`
3. **락 해제**: 커밋·푸시까지 끝나면 `python3 lib/session_lock.py release <topic>`
4. 다른 topic이라도 `assets_library/`(공용 자산) 새로 생성하는 작업 전에는 `list`로 활성
   락이 있는지 한 번 더 확인 — 다른 세션이 지금 뭔가 활발히 하고 있다는 신호이므로
   겹칠 만한 파일(같은 품목 일러스트 등)은 피하거나 사용자에게 먼저 확인

락은 `.claude-locks/`(gitignore 처리, 3시간 지나면 자동으로 무시됨)에 저장 — 두 세션이
같은 디스크를 보고 있다는 전제라 git 커밋 없이도 파일 하나로 바로 조율된다.

### ⚠️ 락으로도 못 막는 위험 — `git add`~`git commit` 사이 레이스 (2026-08-01)

이 락 시스템은 **콘텐츠 파일 편집 충돌**만 막는다 — `git add`로 스테이징한 직후 다른
세션이 자기 파일을 `git add`(또는 `git reset`)해서 스테이징 영역(`.git/index`, 저장소
전체가 공유하는 단일 파일)을 바꿔버리면, 그 사이에 실행한 `git commit`이 내가 스테이징한
파일이 아니라 그 순간 인덱스에 있던 **다른 세션의 파일을 대신 커밋**해버릴 수 있다(실제
발생 — 당뇨유발음식_1 작업의 `git add`~`git commit` 사이에 다른 세션이 끼어들어서 커밋
메시지는 당뇨유발음식_1인데 실제 커밋 내용은 아침공복음식_1 파일이었음). 커밋 직후
`git show --stat HEAD`로 방금 커밋에 **내가 의도한 파일들이 맞는지 항상 확인**할 것 —
아니면 즉시 다시 `git add`(정확한 경로로)+`git commit`. 이미 푸시된 뒤라도 새 커밋을
얹어서 바로잡으면 되고, 잘못 들어간 다른 세션 파일은 굳이 되돌리지 않아도 된다(그 세션이
의도한 내용이라 데이터 손실은 아님 — 커밋 메시지만 부정확했던 것).

### ⚠️ → 근본 해결: 세션마다 `git worktree` 쓸 것 (2026-08-03)

위 레이스는 결국 **여러 세션이 같은 작업 디렉터리·같은 `.git/index`를 공유**해서 생긴다 —
이 세션에서 실제로 여러 번(작은 건은 매번, 한 번은 511개 파일짜리로 크게) 발생한 걸 보고
"브랜치 구조화 같은거 하면 문제 없앨만한 거 없나" 확인 후 확정. **락은 "같은 topic 내용을
동시에 편집하는 것"만 막고, git worktree는 "서로 다른 세션이 같은 git 배관(index/staging
영역)을 공유하는 것" 자체를 없앤다** — 둘은 막는 문제가 다르므로 worktree 도입 후에도 위
세션 락 확인 절차는 그대로 유지한다.

**새 작업을 시작할 때(topic 하나든, 이번처럼 여러 파일에 걸친 수정이든) 매번:**

1. 워크트리 생성 — 프로젝트 폴더 밖, 형제 디렉터리에 만든다(메인 저장소 디렉터리 안에
   중첩시키지 말 것):
   ```
   git worktree add ../health-shorts-worktrees/<짧은-설명> -b session/<짧은-설명> main
   ```
   `<짧은-설명>`은 작업 내용을 한눈에 알아볼 수 있게(예: `ja-heartburn`, `ig-cta-fix`).
   `git worktree`는 `.git` 객체 저장소는 그대로 공유하고 작업 디렉터리·index·HEAD만
   독립적으로 새로 만들어서, 풀 클론보다 훨씬 가볍고 빠르다.
2. 이후 그 워크트리 디렉터리 안에서 작업(cd해서 들어가 그 안에서 커밋까지 전부 진행) —
   같은 `main`을 보고 있는 다른 세션의 index/작업 디렉터리와 완전히 분리되므로, 그 세션이
   무슨 `git add`/`git commit`을 하든 이쪽엔 전혀 영향이 없다.
3. 작업이 끝나면 메인 워크트리(`health-shorts` 원본 디렉터리)로 가서 병합·푸시:
   ```
   cd /Users/chlwjddms16/Desktop/project/health-shorts
   git pull --ff-only   # 그 사이 다른 세션이 올린 게 있으면 먼저 받기
   git merge session/<짧은-설명>
   git push
   ```
   충돌이 나면(두 세션이 진짜 같은 파일을 동시에 건드린 경우) 이번엔 조용히 뒤섞이지
   않고 **명시적인 merge conflict**로 드러난다 — 지금까지처럼 커밋 메시지만 부정확한
   채로 조용히 넘어가는 것보다 훨씬 안전한 실패 방식이다. 직접 해결하거나 사용자에게
   확인 후 진행할 것.
4. 정리: `git worktree remove ../health-shorts-worktrees/<짧은-설명>` +
   `git branch -d session/<짧은-설명>`.

⚠️ **적용 시점**: 2026-08-03 이 시점 이후 새로 시작하는 작업부터 — 이미 진행 중이던 작업을
지금 당장 워크트리로 옮길 필요는 없다(진행 중인 작업이 있으면 그것부터 마무리). 여러 세션이
동시에 켜져 있을 가능성 자체가 여전히 있으므로 위 "동시 세션 락 확인" 절차는 worktree
사용 여부와 무관하게 계속 그대로 지킬 것 — 두 안전장치가 서로 다른 종류의 충돌을 막는다.

## 배포 — 제일 늦게 끝나는 걸 기다리지 않는다 (2026-07-30)

카드뉴스·플랫폼 캡션·숏폼 영상 중 뭐가 먼저 끝나든 순서와 무관하게 **완성되는 대로 바로
커밋·푸시**한다. 셋 다 끝날 때까지 묶어뒀다가 한 번에 올리지 않는다 — 어느 게 먼저 끝날지는
매 topic마다 다를 수 있음(영상이 항상 제일 오래 걸린다고 가정하지 말 것).

- `lib/dashboard.py`는 `video_path`가 없거나 파일이 없으면 "영상 준비 중" placeholder를
  자동으로 보여주므로, 영상이 없는 채로 먼저 푸시해도 대시보드가 깨지지 않는다
- 반대로 영상이 먼저 끝났으면(카드뉴스/캡션 작업 중이어도) 영상부터 먼저 커밋·푸시해도 된다
- 사용자는 먼저 올라온 산출물부터 바로 플랫폼 업로드를 시작하고, 나머지가 끝나는 대로
  같은 topic 폴더에 추가 → 대시보드 재생성 → 재커밋·재푸시

즉 한 topic당 커밋이 여러 번(완성되는 조각마다)으로 나뉘는 게 정상이고, 그게 의도된 흐름이다.

### 서브에이전트로 여러 topic 한꺼번에 생성할 때 — 영상은 별도 배치로 뒤로 미룬다 (2026-08-01)

여러 topic을 서브에이전트 병렬로 생성할 때(예: 10개 동시), **영상 조립이 압도적 병목**이다
— Kling 모션 클립이 topic당 여러 캐릭터면 각각 3~4분씩 걸리는데, 여러 topic이 동시에
Kling을 호출하면 429 레이트리밋까지 겹쳐서 훨씬 오래 걸린다(실제로 10개 동시 생성 시
확인됨). 그래서 아래 2단계로 나눠 진행한다("영상은 백그라운드에서 돌려놓고 다른 것들 먼저
쫙 끝내고 푸시, 영상까지 다 끝나면 다시 푸시" — 사용자 확정):

**1단계 — 영상 제외 전부**: 각 서브에이전트는 리서치 → narration.txt/card_news_spec.json/
platform_captions.json 작성 → 카드뉴스 이미지 생성 → TTS → 캐릭터 일러스트(Gemini)·
모션(Kling)까지만 진행하고, **`video_assembler.py`와 `dashboard.py`는 호출하지 않고
멈춘다**(Kling 모션 클립 자체는 나중에 영상 조립에 그대로 쓰이니 미리 만들어두는 게 맞음 —
보류하는 건 영상 조립·대시보드 두 단계뿐). 오케스트레이터는 완료되는 topic부터 검증 →
git commit·push(영상 없이 카드뉴스+캡션+나레이션만 먼저 배포).

**2단계 — 영상 조립**: 1단계가 배치 전체(또는 필요한 만큼) 끝나면, 그다음 각 topic의
영상 조립·대시보드 생성을 진행한다(이 시점엔 Kling 모션 클립이 이미 다 만들어져 있어서
API 대기 없이 빠르게 처리됨). 완성되는 대로 다시 검증 → commit·push.

즉 topic 하나당 최소 2번의 커밋·푸시(콘텐츠 1차 + 영상 2차)로 나뉘는 게 정상이다. 실사진이
필요한데 아직 소싱 전이라면(다른 세션이 Unsplash/Pexels 등에서 소싱 중인 경우) 영상 조립
자체를 그 실사진이 준비될 때까지 미루고, 소싱 완료 후 `--images`에 반영해서 2단계를
진행한다.

## 파이프라인 단계별 병렬 구조 (합의됨, 2026-07-30)

대본/내용 재구성만 판단이 필요해서 순차 진행(직접 처리, 위임 안 함). 대본 확정 이후 아래는
서로 입력이 달라 병렬 가능:

- (a) 이미지 생성 — Gemini API로 캐릭터 정지 이미지 생성 또는 `assets_library/`에서 재사용
- (b) TTS — narration.mp3/srt 생성
- (c) 플랫폼별 캡션 작성
- (d) 캐릭터 모션 클립 생성 — Kling AI로 정지 이미지 1장을 5초 루프 영상으로 변환 ((a) 완료 필요)

카드뉴스는 (a) 완료 즉시 시작 가능. 영상 조립은 (b)+(d) 둘 다 필요한 마지막 합류 지점이라
항상 제일 늦게 끝남 — 위 "배포" 규칙과 연결됨.

## 에셋 라이브러리 — 자동 재사용 (2026-07-27)

- `assets_library/illust/<품목>_illust.jpg` — AI 캐릭터 일러스트, 1024x1024, 텍스트/로고 없음,
  Gemini `gemini-3.1-flash-lite-image` (제일 저렴한 접근 가능 티어)로 생성
- `assets_library/real/<품목>_real_NN.jpg` — 실물 사진 (사용자 제공 또는 소싱)
- `assets_library/motion/<품목>_motion.mp4` — Kling으로 생성한 5초 캐릭터 움직임 루프
  (흰 배경 유지 상태로 생성 — `video_assembler.py`가 colorkey로 배경을 뺀다)
- 사용자가 새 이미지 주면 품목명 기준으로 파일명 자동 정리 + 저장, 이후 같은 품목/유사 주제엔
  재확인 없이 자동 재사용

### 실물 사진 자동 소싱 (2026-08-01)

사용자 제공 사진이 없을 때는 `lib/real_photo_sourcing.py <검색어(영어)> <후보개수> <pexels|unsplash|both>`로
Pexels·Unsplash를 같이 검색해 후보를 `assets_library/real/_candidates/<검색어>/`에 받아온 뒤, Read
도구로 직접 후보를 보고 골라서 `assets_library/real/<품목>_real_NN.jpg`로 옮긴다(나머지 후보와
`_candidates` 폴더는 삭제). 검색어는 한글이 아니라 영어로 번역해서 넘길 것 — 두 API 다 한글 검색은
결과가 거의 안 나온다.

한식·특정 국물 요리 등은 정확히 맞는 사진이 아예 없는 경우가 꽤 있다 — 이럴 땐 실패로 끝내지 말고
더 넓은 상위 개념으로 재검색해서(예: "설렁탕" 검색 결과가 부실하면 "bone broth soup"까지 넓힘)
"완전히 똑같진 않아도 톤이 맞는" 사진을 쓰기로 스스로 판단하고 진행한다 — 이 판단 기준을 코드에
고정하지 않고 매번 상황 보고 유연하게 정할 것.

#### ⚠️ 영상 배경 사진 — 최소 4장, 최대 10장 (2026-08-01, `--bg-style photo`에만 해당)

**2026-08-02부로 영상 배경 기본값이 실사진에서 칠판 스타일로 바뀌어서, 이 절은 새
topic에는 더 이상 기본 적용되지 않는다** — 아래 "영상 배경 — 칠판 스타일이 기본" 절
참고. `--bg-style photo`를 명시로 골랐을 때만(과거 topic 재조립, 실사진이 꼭 필요한
특수한 경우) 이 상하한선을 지킬 것.

품목당 `_real_01.jpg` 딱 1장만 소싱하고 그 1~2장만 `--images`에 넣는 관행이 굳어져서,
20~30초 영상에 배경 사진이 2장뿐이면 한 장이 10초 넘게 떠 있어 밋밋하다는 지적이 나왔다
("생성하는 영상들에 이미지가 좀 적은것 같은 느낌... 두개정도밖에없구나"). 아래 아래
"표준 호출 예시"의 `--images "assets_library/real/<사진1>,assets_library/real/<사진2>"`는
어디까지나 자리표시자 2개였을 뿐인데, 세션들이 그대로 따라 하면서 실제 소싱량까지 2장으로
굳어진 게 원인으로 보인다.

- **`--images`에 넣는 사진 수는 최소 4장, 최대 10장**으로 잡는다(항목 수가 그 사이면 항목
  수만큼, 항목이 10개를 넘어도 10장까지만, 항목이 4개 미만이면 겹치는 톤의 사진을 더
  소싱해서 4장을 채운다)
- 품목 하나에 여러 장(`_real_02.jpg` 등)을 소싱해서 채워도 되고, 여러 품목에서 한 장씩
  가져와도 된다 — 반드시 "품목 1개당 1장"일 필요는 없음
- 위 상하한선은 고정 상수이니 나중에 다시 지적받지 않도록 새 topic 작업 시작할 때부터
  적용할 것

### ⚠️ 새 캐릭터 만들기 전 필수 확인 — "품목명이 다르다"≠"새로 만들어야 한다" (2026-08-01)

`설렁탕곰탕_illust.jpg`와 `하얀국_illust.jpg`가 실제로 발견된 사고 사례다 — 일러스트는
어느 세션이 복사해서 재사용했는데(MD5 완전히 동일), **정작 Kling 모션 클립은 서로 다르게
두 번 생성**돼서 API 비용만 두 배로 나갔다. 이미지가 똑같아도 모션까지 자동으로 같이
재사용되는 게 아니다 — 둘 다 사람이(세션이) 판단해서 챙겨야 한다.

**새 topic에서 캐릭터가 필요할 때마다 순서:**

1. `ls assets_library/illust/`로 전체 목록을 먼저 본다
2. 지금 만들려는 품목과 **같은 시각적 카테고리**로 보일 만한 기존 파일이 있으면(예: 흰
   국물 종류, 빵류, 음료류, 견과류처럼 캐릭터화하면 겉모습이 사실상 똑같이 나올 소재)
   `Read`로 실제로 열어서 비교한다 — 이름만 보고 판단하지 않는다
3. 시각적으로 충분히 비슷하면(같은 카테고리라 캐릭터화했을 때 구분이 안 될 정도) **새로
   생성하지 않는다** — `card_news_spec.json`의 `char_file`에 기존 파일명을 그대로 쓰고,
   영상 조립의 `motion_schedule`/`--motion`에도 기존 `assets_library/motion/<기존품목>_motion.mp4`를
   그대로 쓴다. item의 표시 이름(카드 제목 등)은 이번 topic 용어(예: "사골국") 그대로 써도
   되고, 참조하는 파일명만 기존 걸 재사용하면 된다 — 파일명과 카드 표시 이름이 다른 건
   문제없음
4. 애매하면(다른 재료가 섞여 있거나 색이 다르거나 등) 새로 생성한다 — 억지로 재사용해서
   실제 다른 걸 같은 캐릭터로 퉁치지 않는다

**알려진 동일 카테고리 그룹** (발견되는 대로 이 목록에 추가할 것):

| 카테고리 | 캐논 파일(계속 재사용) | 중복 발견됨(더 이상 새로 안 만들 것) |
|---|---|---|
| 흰 국물(사골·설렁탕·곰탕류) | `설렁탕곰탕_illust.jpg` / `설렁탕곰탕_motion.mp4` | `하얀국_illust.jpg`(이미지만 동일, 모션은 별개로 낭비 생성됨 — 앞으로 하얀국 이름으로 신규 생성 금지, 흰 국물류는 전부 설렁탕곰탕 파일 재사용) |

## 캐릭터 움직임 — Kling AI 모션 루프 (2026-07-30 전환)

~~Rhubarb 립싱크(입모양 정지 이미지 3장 스위칭)~~는 폐기했다 — 대사 타이밍에 맞춰 입모양만
바뀌고 나머지 그림은 완전히 동일해서, 인트로→코너로 위치만 이동해도 "그림 자체가 안 움직여서
밋밋하다"는 피드백. 대신 캐릭터 정지 이미지 1장을 Kling AI image2video로 5초짜리 자연스러운
움직임(고개 갸웃, 눈 깜빡임 등) 영상으로 변환한 뒤, 대사 길이만큼 그냥 반복 재생한다 — 대사
내용과 입모양이 정확히 맞을 필요는 없는 캐릭터(비인간 사물)라 이 방식으로 충분함.

- API 클라이언트는 `../char-pipeline/lib/kling_client.py` (키도 그 프로젝트 `.env`에 있음,
  이 프로젝트 `.env`엔 안 둠 — char-pipeline이 "이미지→영상 변환" 담당이라는 기존 역할분담 유지)
- 캐릭터 원화는 손발이 뚜렷한 디자인이면 Kling에서 손 왜곡이 잘 나던 과거 이슈가 있었음 —
  돼지감자처럼 팔다리 없는 디자인은 이 문제에서 자유로움. 손발 있는 캐릭터면 모션 프롬프트에서
  팔다리 움직임을 최소화하도록 명시할 것("팔다리는 거의 움직이지 않고 고개/표정 위주로")
- `lib/video_assembler.py`가 모션 클립을 colorkey(단색 배경 제거) → qtrle 알파 트랙으로 변환 →
  ping-pong(정방향+역방향)으로 이어붙여 `-stream_loop`로 대사 길이만큼 반복. `--intro-duration`을
  0으로 주면 풀스크린 자기소개 구간 없이 제목 카드 직후 바로 우측 하단 코너 크기로 시작한다
  (2026-07-31 기본값 변경: "5초 뒤에 옮기지 말고 처음부터 우하단에" 요청 반영 — 새 topic도
  기본으로 `--intro-duration 0` 사용)
- ⚠️ **캐릭터 배경은 흰색 대신 크로마키 단색으로 생성할 것** — 흰 배경은 캐릭터 얼굴의 밝은
  하이라이트(이마·볼)까지 "흰색에 가깝다"고 오인해서 colorkey가 얼굴에 구멍을 뚫는 문제가
  있었다(2026-07-30, 실사진과 합성 시 눈코가 배경에 맞춰 이상하게 움직이는 것처럼 보였던
  진짜 원인). `gemini_illust.py`의 `generate_illustration()`은 `bg_color_name`/`bg_color_hex`
  파라미터로 배경색을 받는다(기본값 초록 `#00FF00`) — 새 캐릭터는 `video_assembler.py` 호출 시
  같은 색을 `--bg-color 0x00FF00`로 넘길 것(기존 흰 배경 캐릭터인 돼지감자는 기본값 그대로
  좁은 threshold로 처리됨, 하위호환 유지)
- ⚠️ **크로마키 색은 초록 고정이 아니다 — 캐릭터 색상 보고 매번 다시 판단할 것**(2026-07-31,
  반복 지적: "항상 초록색이면 안된다, 오이같은거면 초록으로 하면 씹창난다"). 캐릭터 자체가
  초록 계열(오이·상추·브로콜리·시금치·깻잎 등)이면 초록 배경과 겹쳐서 colorkey가 캐릭터
  몸통까지 지워버린다 — 이런 경우는 `bg_color_name="파란"/bg_color_hex="#0000FF"` 또는
  `bg_color_name="마젠타"/bg_color_hex="#FF00FF"`처럼 캐릭터 색과 절대 안 겹치는 색으로
  바꿔서 `generate_illustration()`을 호출하고, `video_assembler.py`의 `--bg-color`도 반드시
  같은 값으로 맞춘다. 새 캐릭터 만들기 전에 "이 채소/과일이 크로마키 배경색과 겹치는 색인가?"
  를 항상 먼저 판단할 것.

  ⚠️ **겹치는 색이 없어도 초록을 기본값으로 고정해서 쓰지 말 것**(2026-08-01, "죄다
  초록색이니까 인스타에서 보기가 좀 그렇네" 피드백 — 지금까지 만든 건 이미 과금
  완료라 재작업 안 하고, 새 캐릭터부터 적용): `lib/gemini_illust.py`의
  `pick_bg_color(avoid=[...])`를 호출해서 캐릭터 색과 겹치는 것만 `avoid`로 빼고
  나머지 중 무작위로 고른다 — 타입캐스트 보이스 랜덤 선택과 같은 패턴. 캐릭터 색이
  뭐랑 겹치는지 판단하는 건 여전히 세션 몫(자동 인식 아님), 예:
  ```python
  from lib.gemini_illust import pick_bg_color, generate_illustration
  name, hex_ = pick_bg_color(avoid=["초록"])  # 오이·상추처럼 초록 계열 캐릭터
  generate_illustration(item_name, bg_color_name=name, bg_color_hex=hex_)
  ```
  고른 색은 `video_assembler.py`의 `--bg-color`에도 그대로 맞춰야 하는 것은 동일.

  ⚠️ **후보 5색(2026-08-01, "3색? 좀더 다양화좀 안되나" 요청으로 확장)**: 초록·파란·
  마젠타·시안·보라. 노랑·주황·빨강처럼 흔한 식재료 색은 캐릭터와 겹칠 확률이 높아서
  일부러 뺐다 — 이 5색 전부 음식 캐릭터와 잘 안 겹치는 축. 단, `video_assembler.py`의
  despill 필터(가장자리 잔여 색 억제)는 ffmpeg 자체 제약으로 **green/blue 타입만
  있다** — 마젠타·시안·보라는 despill 없이 colorkey만 적용되므로(마젠타는 이미 이
  상태로 실사용 중) 가장자리에 아주 약한 색 번짐이 남을 수 있다. 완성 영상에서 캐릭터
  테두리가 이상하면 이 3색 중 하나였는지 먼저 확인할 것.

### ⚠️ 예정된 전환 — Kling 모션 자체를 폐지 (2026-08-03, 토큰 소진 후부터)

사용자 확정: "Kling은 이번 토큰 다 쓰면 그다음부터 모션 굳이 안 박고 넣어야겠다.
필요가 없어보여. 모션 넣는거자체가. 입모양도 안맞기도 하고." — 지금 남은
Kling 토큰을 다 쓸 때까지는 기존 방식(모션 루프) 그대로 두고, **토큰이
소진되는 시점부터는 새 topic에 모션 자체를 안 넣는다**. 대체 방식도 확정:
**정지 이미지로 간다**(2026-08-03, "정지 이미지로 가" — 캐릭터를 아예 빼는
옵션은 기각됨).

- 아직 토큰이 안 떨어졌으므로 지금 당장 `video_assembler.py`/`gemini_illust.py`
  코드를 바꾸지 말 것 — 이 노트는 그 시점이 왔을 때를 위한 사전 기록.
- 토큰 소진 확인되면: `assemble()`에 모션 클립(`motion_path`/`motion_schedule`)
  대신 정지 캐릭터 일러스트(`gemini_illust.py`가 이미 만들어주는 정지 PNG,
  카드뉴스 표지 등에 이미 쓰는 그 파일)를 코너에 그대로 띄우는 경로를 추가한다
  — 크로마키 제거(colorkey)·ping-pong 루프 같은 모션 전용 처리는 다 걷어내고
  PNG를 정적으로 오버레이하기만 하면 됨. 새 캐릭터를 Kling에 보내는 단계
  (`../char-pipeline` 호출)도 이 시점부터는 건너뛴다.
- ⚠️ **hex 색상 문자열을 `.upper()`로 비교할 땐 비교 대상 리터럴도 반드시 대문자로**
  (2026-07-31 실제로 터진 버그): `"0x00FF00".upper()`는 `"0X00FF00"`이 되어(x까지
  대문자화) 소문자 x가 섞인 리터럴 `"0x00FF00"`과 영원히 같아질 수 없다 —
  `_build_character_loop`의 despill 적용 분기가 이 버그로 조용히 한 번도 실행된 적이
  없었고, 초록 배경 캐릭터의 테두리에 형광 초록 라인이 남는 문제로 나타났다(despill을
  mix=1.0까지 올려도 전혀 효과 없어 보였던 진짜 원인 — despill 자체가 아니라 애초에
  호출도 안 되고 있었음). `bg_color.upper() == "..."` 패턴을 새로 추가할 때 비교
  리터럴도 전부 대문자(`"0X00FF00"`)로 맞출 것.
- ⚠️ **Kling 모션 프롬프트 — "말하듯이" 빈번한 입 개폐로 명시할 것**(2026-07-31 지적:
  "입만 계속 벌리고 있는 느낌"): Kling image2video는 나레이션 텍스트나 오디오를 읽고
  립싱크하는 게 아니라 순수 자연어 모션 프롬프트만 따른다 — 프롬프트가 "웃으며 입을
  벌린다" 식이면 한 번 크게 벌린 채 유지하는 것처럼 보인다. 대신 "입을 짧고 빈번하게
  여러 번 여닫으며 말하는 것처럼 움직인다"로 구체적으로 명시하고, negative_prompt에도
  "mouth held open, static open mouth"류를 추가해서 벌린 채 고정되는 것을 방지할 것.
  (진짜 오디오 동기화 립싱크가 필요하면 Kling image2video가 아니라 별도 립싱크 API가
  필요하다는 것도 인지 — 지금 파이프라인엔 없음, 비인간 사물 캐릭터라 대략적인 "말하는
  느낌"이면 충분하다는 전제.)
- Rhubarb(`bin/rhubarb`)는 당장 안 쓰지만 코드는 남겨둠 — 입모양 정밀 동기화가 실제로 필요한
  캐릭터(예: 사람처럼 대사를 정확히 말해야 하는 디자인)가 나오면 대안 경로로 재검토

### ⚠️ Kling 모션 생성 후 프레임 육안 검수 필수 (2026-08-03)

"이런 쓰레기같은 품질의 이미지를 스스로 판단하고 답없으면 그냥 제미나이가 생성한
움직이지 않는 이미지를 쓰게 만들어줄수있어? ... 클링이 만들어주는 영상은 입
싱크로율도 안맞고 아무런 도움도되지않는다고 판단하고 있어" — 일본어 배치 작업 중
실제로 발견: 우메보시_motion.mp4는 3.75초 지점에서 캐릭터가 옆모습으로 돌아가며
없어야 할 팔다리가 생기고 눈이 하나만 보이게 왜곡됨, 버터_motion.mp4는 손가락 달린
팔이 통째로 생겨남(원본 디자인엔 팔 자체가 없음), 된장국_motion.mp4는 아예 캐릭터가
사라지고 평범한 실사 국그릇 사진으로 바뀜 — 전부 정지 이미지(Gemini 원본)는 멀쩡한데
Kling 모션 클립만 망가진 경우였다. 둥글고 팔다리 없는 디자인일수록 Kling
image2video가 5초 안에 형태를 못 지키는 경향이 실제로 있는 것으로 보임.

**새 캐릭터마다 Kling 모션 생성 직후, 영상 조립 전에 반드시:**

1. 프레임 몇 장 뽑기: `ffmpeg -y -ss <0.5초 간격으로 3~4곳> -i <모션.mp4> -vframes 1 <출력.png>`
   (특히 3~4초 지점을 꼭 포함 — 지금까지 발견된 붕괴가 전부 이 구간에서 났음)
2. 각 프레임을 실제로 눈으로 확인(Read 도구) — 원본 정지 이미지와 비교해서 캐릭터
   정체성이 유지되는지(팔다리가 새로 생기지 않았는지, 다른 사물/실사로 안 바뀌었는지,
   비율이 안 무너졌는지) 판단. **이 판단은 픽셀 규칙으로 자동화하지 않는다** — "형태가
   무너졌다"는 눈으로 봐야 신뢰성 있게 잡히므로, 매번 실제로 프레임을 보는 세션/
   서브에이전트가 직접 판단할 것.
3. 문제 있으면(형태 붕괴가 한 프레임이라도 보이면) 그 Kling 결과는 버리고
   `lib/gemini_illust.py`의 `build_static_motion_loop(illust_path, out_path)`로
   정지 이미지 루프(캐릭터가 안 움직임, 원본 일러스트 그대로 5.1초 반복)를 만들어
   대신 쓴다 — Kling 클립과 동일 스펙(960x960, 30fps)이라 `video_assembler.py`의
   ping-pong·colorkey 처리가 그대로 동작한다.
4. 문제 없으면 Kling 클립 그대로 사용.

**비용 판단은 세션 몫** — Kling 실패율이 체감상 낮지 않은 편(일본어 1차 배치 신규
캐릭터 13개 중 3개, 약 23%)이라, 캐릭터 디자인이 팔다리 없는 둥근 형태라면 아예
Kling을 건너뛰고 처음부터 `build_static_motion_loop`로 만드는 것도 합리적인
선택이다 — 강제하지 않으니 상황 보고 판단할 것.

### 영상 배경 — 칠판 스타일이 기본 (2026-08-02)

실사진을 그대로 배경에 깔면 밋밋하고 눈에 확 안 들어온다는 지적("real 이미지 그대로
배경으로 넣고 있는데... 확 보이지가 않는다") — 카드뉴스처럼 짙은 배경에 감각적인
폰트로 나레이션을 써주는 쪽이 낫다는 판단으로 **새 topic 기본값을 실사진 슬라이드쇼에서
칠판 스타일로 바꿨다**.

- `lib/video_assembler.py`의 `assemble()`/CLI `--bg-style` 기본값이 `chalkboard`다 —
  실물 칠판 사진(`assets_library/backgrounds/칠판.png`) 배경 + 분필/마카체 자막
  (`_make_chalk_caption_png`, 반투명 박스 없이 흰 글자+옅은 그림자만) 조합.
  **`--images`를 아예 안 줘도 된다.**
- ⚠️ **처음엔 그라디언트 단색이었다가 실물 사진으로 교체**(2026-08-02, "assets의
  background 폴더에 칠판 넣어놨거든 그 이미지로 대체해" 요청): `_build_chalkboard_bg`가
  `칠판.png`(1024x1024, 나무 프레임 칠판이 흰 배경 위에 서있는 사진)의 좌우 흰 여백을
  나무 프레임 가장자리(실측 픽셀 `CHALKBOARD_CROP_LEFT=65`~`CHALKBOARD_CROP_RIGHT=962`)
  까지만 잘라서 프레임이 캔버스 가로 폭(1080)에 꽉 차게 만든다. 이 사진 자체를 다른
  걸로 바꾸지 않는 한 크롭 좌표를 다시 잴 필요는 없음.
- ⚠️ **`CHALKBOARD_ZOOM=1.35`로 추가 확대 + 세로도 함께 커짐**(2026-08-02, "가로는
  완전 꽉차게 더 크게 해야될거고 위아래로 더 키워서"): 위 CROP만으로 너비를 캔버스에
  맞추면 나무 프레임 가장자리에 흰 여백이 살짝 남는다 — 크롭 폭보다 더 확대한 뒤
  가로만 캔버스 폭(W)으로 중앙 크롭하면, 같은 배율로 세로도 비례해서 커져서
  "가로 여백 없이 꽉 차게 + 세로도 크게" 두 요청을 동시에 만족한다.
- ⚠️ **완전 정적 — zoompan 넣었다가 뺌**(2026-08-02, "왜 칠판이 움직여 ;; 이제 칠판은
  가만있고 자막만 들어가면 되는거지"): 처음엔 실사진 배경과 통일감을 주려고 미세
  zoompan(서서히 확대)을 넣었는데, 칠판은 자막을 얹는 고정 판서면이라 배경 자체가
  계속 확대되면 산만하다는 피드백 — `_build_chalkboard_bg`에서 zoompan 필터를 뺐다.
  칠판 배경은 이제 매 프레임 완전히 같은 정지 이미지, **자막만 등장/전환**한다.
- ⚠️ **상단 여백(`top_pad`)은 고정값이 아니라 배너 실제 높이(`title_h`)에 맞춰
  동적으로 계산**(2026-08-02, "글이 너무 아래로 쏠려있잖아 칠판 기준으로 중앙으로"
  피드백 대응 과정에서 함께 정리): 제목이 1줄이냐 2줄이냐에 따라 배너 높이가
  달라지는데 칠판 상단 여백이 고정값이면 배너 아래로 흰 틈이 남거나 겹칠 수 있다 —
  `assemble()`이 배너를 먼저 만들어 높이(`title_h`)를 구한 뒤
  `_build_chalkboard_bg(..., top_pad=title_h)`로 넘겨서 배너 바로 아래부터 칠판이
  시작하게 한다. `_chalkboard_display_height()`가 칠판이 실제로 화면에서 차지하는
  세로 픽셀 높이를 계산해주는데, **자막을 칠판 영역 안에서 세로 중앙 정렬할 때도
  이 값을 그대로 재사용**한다(`caption_center_y = (title_h + min(title_h +
  칠판높이, H)) / 2`, 화면 하단 기준 고정 오프셷이었던 예전 방식은 칠판이 커진
  뒤로는 자막이 아래로 치우쳐 보이는 문제가 있어서 폐기).
- ⚠️ **코너 캐릭터는 나무 프레임/받침대에 걸치도록 위치 조정**(2026-08-02, "그
  일러스트는 아래로 더 빼서 나무 틀에 걸치고"): 캐릭터 오버레이 y 오프셋을
  `main_h-overlay_h-320`에서 `main_h-overlay_h-140`으로 낮춰서, 초록 판서면 안쪽에
  붕 떠 보이지 않고 칠판 나무 프레임에 걸쳐 앉은 것처럼 보이게 했다.
- 폰트는 무료 상업적 이용 가능한 구글 폰트 **Gaegu-Bold**(SIL OFL 1.1 라이선스,
  `assets_library/fonts/Gaegu-Bold.ttf` + 라이선스 원문 `OFL-Gaegu.txt`) — 손글씨/마카
  느낌의 한글 지원 폰트. 카드뉴스 등 다른 곳에 폰트를 새로 쓸 일이 생기면 이 폰트를
  재사용할지 먼저 검토할 것(중복 다운로드 금지 — "기존 패턴 먼저 확인" 원칙)
- **기존 실사진 방식이 완전히 없어진 건 아니다** — `--bg-style photo`를 명시하면 기존
  `_build_background`/`--images` 흐름을 그대로 쓸 수 있다(과거 topic 재조립, 실사진이
  꼭 필요한 특수한 경우용). 위 "영상 배경 사진 — 최소 4장, 최대 10장" 절은 이 경우에만
  적용됨
- ⚠️ **소급 재작업 필요(2026-08-02 정정)** — 처음엔 "새 topic부터만 적용, 기존
  영상은 소급 재작업하지 않는다"로 확정했었으나("저거 잘못들어간거야" — 사용자가
  이 확정 자체가 실수였다고 정정), **기존에 실사진 배경으로 만들어둔 영상도 전부
  칠판 스타일 + 배너 실사진으로 재생성 대상**이다. 아래 "숏폼 영상 마지막 CTA"
  절의 소급 미적용 노트도 같은 이유로 함께 뒤집혔음.

### 칠판 모서리 낙서 — 옛날 교실 감성 (2026-08-02)

칠판 배경이 통짜 실사진이라 자막 없는 구간(특히 도입부)이 휑해 보인다는 지적
("파츠같은거 귀여운거 랜덤으로 칠판 모서리쪽에 추가하는게 어떨까 싶어 너무
휑하고 별로야" → "좀 많았으면 하는데... 오히려 좀 더 화려하게 갔으면 싶어" →
"나무받침대랑 분필조각, 지우개까지 다 추가하고 급훈 문구가... 도형도 이것저것
엄청 넣어보자") — 전부 PIL 도형/텍스트만으로(이미지 생성 API 없이)
`_place_chalk_doodle` 한 함수가 판서면 곳곳을 옛날 교실 감성으로 장식한다.

**장식 종류와 위치** (전부 캡션과 같은 흰색+옅은 그림자 톤 — `_stroke_shape`,
`_make_chalk_caption_png`와 동일 기법이라 손글씨 느낌이 자연스럽게 이어짐):

- **양쪽 위 모서리**: `_DOODLES`(별·하트·반짝임·음표·스마일리·구름·무지개·
  네잎클로버·말풍선·리본·발자국·꽃·풍선·우산·번개, 15종) 중 한쪽당
  `per_corner`개(기본 4개, 원래 2개)씩 서로 다른 도형을 뽑아 흩뿌린다 —
  중심거리 기반 재시도로 겹침 최소화. ⚠️ 리본은 처음에 아래로 늘어지는
  꼬리까지 그렸는데 작게 축소·회전되면 꼬리 선이 뭉개져 나비처럼 안 보였다
  (실제 렌더로 발견) — 매듭 두 날개+중앙 원만 남기고 단순화함. 신규 도형
  4종도 같은 이유로 추가 전에 항상 실제 축소·회전 렌더로 먼저 형태가
  뭉개지지 않는지 확인한다.
- **양옆 여백 띠(중간 높이)**(2026-08-02, "모서리나 양옆에 여백 구간에
  귀여운거 좀더 만들어 넣자 랜덤리하게 종류가 이빠이 많았으면 싶네" +
  "이빠이넣어 귀여운 것들 제발좀... 나중에 중복 이미지가 아니게 보이게
  할때도 좋을 것 같단말이야"): 자막(`_make_chalk_caption_png`,
  `max_width=940`, 캔버스 W=1080 중앙 정렬)은 아무리 길어도 좌우로 최소
  ~50px는 항상 비워둔다는 점을 이용해서, 가장자리에서 45px 폭짜리 얇은
  띠에 작은 도형(28~42px, 위 모서리 클러스터보다 작음) 2~3개를 추가로
  뿌린다. 위쪽 모서리 클러스터 바닥보다 60px 더 아래에서 시작해서 우상단
  아이템 라벨·하단 캐릭터 어느 쪽과도 겹치지 않는 짧은 세로 구간
  (`_SIDE_MARGIN_BAND_H=260`)에만 그린다 — `skip_right`와 무관하게 양쪽 다
  그려도 안전.
- **위쪽 중앙**: "급훈" 액자(`_doodle_text_box(..., font_size=48,
  double_border=True)`) — topic 폴더명에서 뽑은 한 단어 요약
  (`_topic_word_from_seed`, 예: "손발저림_1_shorts" → "손발저림")을 이중
  테두리 액자에 넣는다. ⚠️ 처음엔 `font_size=32`였는데 "급훈처럼 넣어놓은거
  크기도 좀 키우고 글자 폰트도 키워야지" 피드백으로 48로 확대 — 이때
  `_doodle_text_box`의 여백(`pad_x/pad_y`)도 고정값(16, 10)에서
  `font_size * 0.5 / 0.32` 비례값으로 바꿔서, 폰트만 커지고 액자 여백은
  그대로라 옹색해 보이는 문제를 같이 없앴다(주번/떠든 사람 명패도 같은 함수를
  쓰므로 자동으로 비례 적용됨).
- **왼쪽 아래**(위에서부터): "떠든 사람: OO, OO" 명단 → "주번 OOO" 명패 —
  실제 교실 칠판 모서리에 붙던 게시물을 흉내낸 사각 테두리+분필체 글자.
  ⚠️ **이름은 "성+OO" 익명 형식**(`_anon_name`, `_SURNAMES`에서 성만 뽑고
  이름 자리는 "OO")— 처음엔 "김민지"처럼 완성된 가상 이름을 썼다가 "이XX
  김XX 이런식으로... 익명느낌으로 귀엽게" 피드백으로 정정. 실존 인물 지칭
  없이도 게시물 감성은 그대로 유지됨.
- **판서면 아래 나무 받침대**(`_doodle_chalk_stick`, `_doodle_eraser`): 분필
  조각 2~3개 + 지우개를 받침대 왼쪽 절반에 얹는다 — 오른쪽 절반은 코너
  캐릭터 자리라 어차피 캐릭터에 가려지므로 왼쪽만 쓴다. ⚠️ v1(단순
  `rounded_rectangle`)은 "너무 네모반듯" 지적을, v2(디테일만 보강, 크기는
  그대로 — 하이라이트 선/나무받침 호/펠트 세로선 추가)도 "너무 애매하다"는
  지적을 받았다 — 실제 원인은 디테일 부족이 아니라 영상 실제 표시 크기(가로
  50~60px 안팎)에서 얇은 선이 뭉개지는 것이었다. v3에서 몸체를 40% 확대
  (분필 50x15→70x21, 지우개 60x30→84x42) + 선 두께를 3→4로 올려 다른
  낙서들(`_stroke_shape` 계열, width=5)과 비슷한 굵기로 맞추자 실사용
  크기에서도 형태가 바로 읽히게 됨. 실물 사진 API+배경제거는 검토 후 기각
  (Pexels/Unsplash 후보가 전부 사람 손 포함 라이프스타일 사진이라 깨끗한
  제품샷이 없었고, rembg 새 의존성 추가 + 다른 손그림 선화 낙서와의 스타일
  불일치 위험) — "선화 스타일은 괜찮을거같긴해" 확인대로 PIL 도형 자체를
  키우고 굵히는 방향으로 정리.

**배치 안전지대 원칙**: 자막은 판서면 세로 중앙에, 캐릭터는 항상 오른쪽 아래
모서리에 나온다 — 그래서 (1) 위쪽 양 모서리+중앙, (2) 왼쪽 아래, (3) 받침대
왼쪽 절반, 이 세 영역만 자막 줄 수·영상 길이와 무관하게 절대 안 겹치는
안전지대다. 모든 배치 좌표는 `_chalkboard_orig_to_canvas(x_orig, y_orig,
top_pad)` 공용 변환 함수로 계산 — 칠판.png을 픽셀 스캔해서 구한 실측 상수
(`_CHALKBOARD_GREEN_TOP_ORIG=142`, `_GREEN_BOTTOM_ORIG=866`,
`_TRAY_LEFT/RIGHT/TOP/BOTTOM_ORIG`)를 이 함수 하나로 캔버스 좌표로 옮긴다 —
크롭/줌 상수가 바뀌면 이 함수만 고치면 모든 장식이 같이 맞아떨어짐.

⚠️ **"우리 작품 안에서 안 겹침"과 "실제 유튜브 쇼츠 플레이어에서 안 겹침"은
별개**(2026-08-02, 실제 모바일 스크린샷으로 확인: "모바일에서 보니까 전체적으로
화면이 작아져서 양옆에 아이콘들이 잘리네?") — 위 안전지대는 우리 자막·캐릭터
기준으로만 계산한 거라, 유튜브 쇼츠 자체 UI(오른쪽 좋아요·댓글·공유 아이콘 열,
맨 아래 채널명·설명 캡션 띠)는 전혀 고려하지 않았었다. 실기기에서 재생하면 그
UI가 항상 그 자리를 덮고 있어서, 작품 안에서는 안전해도 실제로는 가려지거나
경쟁하는 텍스트가 겹쳐 읽기 힘들었다. `_YT_SAFE_RIGHT=150`/`_YT_SAFE_BOTTOM=320`
(픽셀, 기기·버전마다 실제 UI 위치가 조금씩 달라서 여유 있게 잡은 추정치)을
모든 오른쪽/아래쪽 배치에 추가로 적용한다:
- 위쪽 모서리 클러스터의 오른쪽 zone은 `W - 25`가 아니라 `W - _YT_SAFE_RIGHT`
  기준으로 시작.
- 양옆 여백 띠 낙서는 **왼쪽만** 그린다 — 계산해보면 "우리 자막이 비우는 자리"
  (오른쪽 가장자리 ~50px)와 "유튜브 UI가 안 가리는 자리"(오른쪽 150px보다 안쪽)
  가 아예 안 겹쳐서, 오른쪽에는 둘 다 만족하는 지점 자체가 없다.
- "떠든 사람"/"주번" 명패는 `green_bottom_canvas` 기준과 `H - _YT_SAFE_BOTTOM`
  기준 중 **더 위쪽(min)**을 쓴다 — 판서면 실측 하단에 바짝 붙이면 이 명패
  텍스트가 유튜브 채널명·설명 캡션과 겹쳐서 둘 다 읽기 힘들었다.
- 캐릭터·분필받침대는 그대로 둔다 — 텍스트가 아니라 장식적 요소라 UI와 살짝
  겹쳐도 다른 텍스트와 경쟁하는 것보다는 덜 문제가 된다는 판단(스크린샷에서도
  캐릭터는 자연스럽게 섞여 보였음).
- **재발 방지**: 이 페이지 상의 새 텍스트/아이콘 배치를 추가할 때, "우리 자막·
  캐릭터와 안 겹치는가"만 확인하지 말고 "실제 쇼츠 플레이어에서 재생했을 때도
  안 겹치는가"까지 항상 같이 확인할 것 — 가능하면 실기기 스크린샷으로.

**topic별로 결정적(seed 고정)** — `assemble()`이 `Path(out_path).stem`(예:
"손발저림_1_shorts")을 시드로 넘겨서, 같은 topic을 재조립해도 매번 낙서
종류·위치·회전각·급훈 단어·익명 이름이 안 바뀐다(`card_news.py`의
`_photo_backdrop`과 같은 결정적 랜덤 패턴).

⚠️ **`skip_right`** — motion_schedule로 캐릭터가 여러 명 번갈아 나오는 topic은
다른 세션이 만든 "구간별 아이템 배너/아이콘" 기능이 칠판 우상단에 이미 아이콘+
이름 라벨을 그린다(`item_schedule` 있을 때) — `assemble()`이
`doodle_skip_right=bool(item_schedule)`로 넘겨서 이 경우 오른쪽 위 모서리
낙서를 생략한다(왼쪽만 남김). 두 세션이 같은 자리를 동시에 손대다 git 커밋이
섞이면서 우연히 알게 된 충돌이었음 — 실제 렌더로 겹치지 않는 것까지 확인함.

`_build_chalkboard_bg(doodle_seed=...)`가 없으면(기존 호출부·테스트 호환)
장식 없이 그대로 — 새 topic부터는 `assemble()`이 항상 자동으로 넘긴다.

### 칠판 파츠 등장 자체도 랜덤화 (2026-08-03)

위 낙서 기능은 도형 **종류**만 랜덤이고 "급훈/명패/분필받침대가 뜨는지"
자체는 매 영상 고정으로 전부 나왔다 — "칠판에 있는 파츠들은 랜덤으로
들어가게 해달라했는데 걍 다 들어간상태로만 영상을 제작하는듯? 그리고 파츠들
더 다양화 하는게 좋을 것 같아" 피드백으로 `_place_chalk_doodle`을 아래처럼
바꿈:

- **급훈 액자**: 70% 확률로만 등장.
- **분필 받침대**(지우개+분필): 70% 확률로만 등장.
- **양옆 여백 띠 낙서**: 개수를 `rng.randint(2, 3)` → `rng.randint(0, 3)`으로
  바꿔서 아예 안 나오는 topic도 있게 함.
- **양쪽 위 모서리 낙서 개수**: 고정 `per_corner`(기본 4)개 → 매번
  `rng.randint(2, per_corner)`개로 topic마다 밀도가 다르게 함.
- **왼쪽 아래 명패**: "떠든 사람"+"주번" 고정 2종 조합 → `_NAMEPLATE_POOL`
  (떠든 사람/주번/우유 당번/칠판 당번/오늘의 발표자/청소 당번, 6종)에서
  `rng.sample`로 0~2개를 뽑아 쌓는 방식으로 변경 — 파츠 종류 자체도
  다양해짐.

배치 안전지대(자막·캐릭터와 안 겹치는 자리) 자체는 그대로이고, 그 자리를
"채울지 말지"와 "무엇으로 채울지"만 seed로 결정적 랜덤화했다 — topic별
재현성(같은 topic 재조립 시 결과 동일)은 그대로 유지됨.

### 도형·명패 풀 2-3배 확장 (2026-08-03)

바로 위 랜덤화 작업 직후 "파츠 더 늘렸어? 지금 갯수의 2-3배는 늘리고싶네"
피드백으로 풀 자체를 늘림:

- `_DOODLES`: 15종 → 35종(2.3배). 신규 20종(sun·crown·gift·bell·key·anchor·
  house·tree·fish·bird·diamond·apple·spiral·candle·flag·boat·moon·hourglass·
  pencil·target). ⚠️ 신규 도형도 기존 규칙대로 50~85px 회전 렌더로 먼저
  확인했고, 그중 3종은 뭉개져서 재작업함:
  - **bell**: 종 추 부분을 몸통과 분리된 작은 원으로 그렸더니 회전 시
    떨어진 점처럼 보임 → 추를 없애고 어깨선을 아치에 바로 이어붙임.
  - **butterfly**: 타원 날개 4개가 겹쳐서 그냥 얼룩처럼 보임 → 아예
    `_doodle_boat`(사다리꼴 선체+삼각 돛)로 교체.
  - **hourglass**: 삼각형을 한 줄로 이어그리니 그냥 나비넥타이(X)처럼
    보임 → 위아래에 굵은 캡 선을 추가해 "삼각형 두 개+캡" 구조를 명확히 함.
  - **pencil**: 얇은 대각선 하나뿐이라 사선처럼 안 읽힘 → 몸통을 굵은
    막대(width=16)로, 끝에 뚜렷한 삼각 촉을 붙임(`_doodle_chalk_stick`과
    같은 교훈 — 가는 선은 이 스케일에서 형태 정보를 못 줌).
- `_NAMEPLATE_POOL`: 6종 → 18종(3배). 신규 12종은 전부 실제 교실 게시물에
  흔한 당번·직책 이름(학급 회장/부회장/화분 당번/창문 당번/분리수거 당번/
  줄반장/급식 당번/이달의 독서왕/생일자/이달의 칭찬왕/우산 당번/지각생)이라
  형식(성+OO 익명, "떠든 사람"·"지각생"만 2명)이 자연스럽게 이어짐.

### ⚠️ 칠판 아래위 공간은 절대 단색으로 덮지 않는다 (2026-08-02)

**"real 배경 이미지는 칠판 외 영역에 전체로 나와야한다고 하나가 이렇게
끊기지 않고. 칠판 아래위로 공간이 지우지않아서 그런지 왜 계속 다른거로
채워지는지 모르겠네"** (여러 번 반복해서 지적됨) — `_build_chalkboard_bg`에
다른 세션이 추가했던 코드가 원인이었다: 판서면 아래 나무 받침대보다 더 아래
영역(`CHALKBOARD_CONTENT_BOTTOM=924` 기준)을 `CHALKBOARD_BOTTOM=(20,42,31)`
단색으로 덮어서 "판서면이 화면 끝까지 이어지는 것처럼" 보이게 하려는
의도였는데, 이게 바로 위 절의 "배너·칠판 뒤 배경은 실사진 하나가 끊김 없이
이어져야 한다"는 요구사항과 정면 충돌했다 — 그 영역은 이미 `board_alpha`로
투명 처리되어 실사진이 비쳐야 할 자리인데 단색으로 덮어버린 것.

**고침**: 해당 단색 채움 블록을 제거하고 자리에 이유만 주석으로 남김. 그
아래 있던 board_alpha 투명 처리가 그대로 살아있으므로, 코드를 지우기만 해도
실사진이 이어져 보인다.

⚠️ **재발 방지 — 이 영역을 손댈 일이 생기면 먼저 확인할 것**: `_build_chalkboard_bg`
안에서 `CHALKBOARD_TOP`/`CHALKBOARD_BOTTOM`/`CHALKBOARD_CONTENT_BOTTOM`
상수를 써서 칠판 밖 영역을 단색으로 채우는 코드를 다시 추가하지 말 것 — "칠판이
화면 끝까지 이어지는 것처럼" 보이게 하고 싶은 요구가 생기더라도, 실사진
배경(`shared_bg_photo`) 자체를 그 영역까지 이어지게 하는 방식으로 풀어야지
단색 덮어쓰기로 풀면 안 된다. 두 세션이 같은 파일을 동시에 건드리다 벌어진
충돌이었으므로, 비슷한 상황(다른 세션이 이 함수를 고친 흔적)을 보면 병합 전에
꼭 실제 렌더로 배경이 끊기지 않는지 프레임을 뽑아 확인한다.

### ⚠️ item_schedule(구간별 아이템 라벨)에서도 배너 사진은 항상 shared_bg_photo 하나만 (2026-08-02)

바로 위 절과 같은 종류의 버그가 다른 자리에서 또 있었다 — "맨위에 글 있는
영역보면 적당한 사진으로 잘 넣어놨는데 그 밑에 층 져가지고 이상한 사진
들어가있잖아 계속 똑같은말하는데 왜 안바뀌는지 모르겠네 동일한 사진 하나...
상단 글자의 배경으로 들어가는 이미지가 전 영역에 들어가야된다니까".

**원인**: `assemble()`에 motion_schedule로 캐릭터가 여러 명 번갈아 나오는
topic(`item_schedule` 있음)은 `_make_item_label_png`로 칠판 우상단에
구간별 아이템 아이콘+이름 라벨을 그리는 것과 별개로, 상단 배너 사진
(`title_png`)까지 구간마다 `item["real_photo"]`(모션 파일명에서 유도한
`<품목>_real_*.jpg` 글롭 매칭 결과)로 **독립적으로 다시 cover-crop해서**
덮어씌우고 있었다. 그 결과 상단 텍스트 배너는 구간 전환마다 사진이
바뀌는데, 바로 그 아래 칠판 배경(`_build_chalkboard_bg`)은 처음부터
`shared_bg_photo` 하나로 고정이라 안 바뀐다 — 배너와 칠판 배경이 서로 다른
사진을 쓰게 되면서 이어붙는 지점이 "층져서 이상한 사진"처럼 보였다.

**고침**: 배너 오버레이는 `item_schedule` 유무와 무관하게 항상 `title_png`
(shared_bg_photo로 만든 것) 하나만 나레이션 전체 구간에 건다. 구간별로
바뀌는 건 우상단의 작은 아이템 아이콘+이름 라벨뿐 — `item_schedule` 딕셔너리
에서도 이제 `real_photo` 필드 자체를 만들지 않는다(배너용 용도가 없어져서
글롭 검색 로직도 같이 제거).

⚠️ **재발 방지**: 배너·칠판 배경·(있다면) 카드뉴스 배경 등 "화면에 실사진이
연속으로 이어져 보여야 하는" 모든 자리는 topic 전체에서 **정확히 하나의
cover-crop 결과(`shared_bg_photo`)만 재사용**해야 한다는 원칙이 이 프로젝트
전반에 깔려 있음 — 새 기능(특히 구간/세그먼트별로 뭔가 바뀌는 기능)을 추가할
때 "배경 사진도 같이 바뀌면 좋겠다"는 생각이 들어도, 그건 항상 이 원칙과
충돌한다는 걸 먼저 의심할 것. 구간별로 바뀌어야 하는 요소는 아이콘·라벨처럼
작고 명확히 구획된 요소로 한정한다.

### 상단 후킹 배너 + 칠판 뒤 전체화면 배경 — 실사진 (2026-08-02)

배너 단색(핑크) 배경과 칠판 배경의 흰 여백 둘 다 없애고, topic 대표 실사진을
**하나만** 써서 채운다("상단에 분홍색 바탕 없애도 되고 바탕으로는 그 항목에 대한
real 이미지를... 배경으로 넣자" → "칠판 뒤에 배경에 전체 화면을 가득채우게 real에서
해당 아이템을 넣어"). CLI `--title-banner-photo`, `assemble()`은
`title_banner_photo_path` 파라미터 하나만 받는다 — 안 주면 기존 단색 배경으로
폴백(하위호환).

**구조(중요 — 사진을 두 번 따로 자르면 안 됨):**

1. `assemble()`이 시작할 때 `shared_bg_photo = _cover_crop_subject(title_banner_photo_path, W, H)`로
   캔버스 전체(1080x1920) 크기 실사진을 **딱 한 번만** 만든다.
2. 배너(`_make_title_png(..., photo_img=shared_bg_photo)`)는 이 이미지의 **위쪽
   `box_h`만큼만** 잘라 쓴다.
3. 칠판 배경(`_build_chalkboard_bg(..., photo_bg_img=shared_bg_photo)`)은 칠판 사진
   자체의 흰 여백(촬영 배경 흰 벽/바닥)만 투명 처리해서 같은 이미지를 통째로 깐 뒤
   그 위에 칠판을 얹는다.

⚠️ **배너와 칠판이 각자 독립적으로 photo_path를 cover-crop하면 안 됨**(2026-08-02,
"배경으로 넣는 real 사진을 글자 뒤에있는거랑 칠판 이미지 아래위랑 따로따로
넣어놨나보네?? ... 따로따로 짤려보이잖아"): 각자 다른 크기(box_h vs H)로 독립
cover-crop하면 서로 다른 배율/영역이 잘려서 이어지는 사진처럼 안 보인다 — 위처럼
**하나의 이미지를 한 번만 만들어서 위/아래 조각을 나눠 쓰는 구조**가 필수.

⚠️ **`_cover_crop_subject`는 정사각형 강제 리사이즈 금지 + 피사체 bounding box로
먼저 크롭**(2026-08-02, "정사각형으로 리사이즈해서 크롭하면 안 됨" / "어떤 물건인지
알 수 있는게 훨씬 나을거같네" / "opacity를 없애라고 아직도 흐려"): 원본 비율을
유지한 채 목표 프레임을 덮을 만큼만 확대해야 피사체가 크롭 밖으로 밀려나지 않는다.
그리고 **흰 스튜디오컷 사진은 피사체가 프레임 일부만 차지**하므로, 단순
cover-crop만 하면 위아래에 흰 여백만 남거나(사물이 안 보임) 피사체를 못 찾아서
크롭이 너무 헐거워져(전체 사진의 90%+ 를 "피사체"로 오인) 확대 배율이 거의 없어
피사체가 작고 흐릿하게 멀리 보이는 문제가 있었다(픽셀 단위 흰색 threshold는
그림자/비네팅 번짐까지 다 잡아서 bbox가 뻥튀기됨). 지금은 **행/열 밀도
프로파일**(`Image.BOX`로 1xH, Wx1로 다운샘플해서 각 행/열의 "피사체 픽셀 비율"을
구함)로 촘촘한 기준(0.30)부터 시작해 전체 면적의 8% 이상을 커버하는 첫 기준을
채택 — 그림자 같은 옅은 잡음은 걸러지고 실제 피사체 밀집 영역만 타이트하게 잡는다.
사진마다 피사체 밀도가 달라도 자동으로 적당한 크롭을 찾으므로 이 topic이든 다른
topic이든 추가 튜닝 없이 재사용 가능.

⚠️ **블러도, 흰 배경 블렌드도 넣지 않는다 — opacity는 100%**(2026-08-02, "흰색 저게
그림을 가리고있는거같잖아 opacity 안줘도 되겠다" → 강한 블러(32)도 "opacity 계속
넣네" 재지적 → "opacity 아예 없애"): 처음엔 흰 배경과 80:20 블렌드, 그다음
`card_news.py`의 `_photo_backdrop`과 동일한 블러(32)를 썼는데 둘 다 사진을
흐릿/옅게 만들어서 "opacity가 낮다"는 지적을 반복해서 받았다 — **칠판 뒤 배경은
블러·블렌드 없이 사진을 그대로(선명하게) 쓴다.** 단, **배너는 예외** — 텍스트
가독성을 위해 검은 저알파 스크림(`(0,0,0,90)`)을 유지한다("위쪽 글자 배경 색상을
왜 지웠냐, 그건 필요하다" 피드백으로 한 번 뺐다가 되돌림). 즉 **opacity 관련 요청은
칠판 뒤 전체화면 배경에만 해당**하고 배너의 텍스트 스크림과는 별개다.

칠판 사진 자체의 흰 배경(초록 판서면·나무 프레임 밖 여백)을 투명 처리하는 로직은
`ImageChops`로 R/G/B 세 밴드가 모두 threshold(225) 이상인 픽셀을 흰색으로 판정해서
`board_alpha`를 만들고, `canvas.alpha_composite(resized_rgba, (0, top_pad))`로
합성한다 — 초록 판서면·나무 프레임은 흰색과 색이 뚜렷이 달라서 그대로 남고, 흰
여백 자리에만 아래 실사진이 비친다.

배너/칠판 배경 둘 다 나레이션 전체 구간에 한 번만 만드는 고정 이미지라(세그먼트
아님, 성능상 이유), 사진도 영상 전체를 대표하는 한 장이면 된다 — 여러 품목이
번갈아 나오는 topic이어도 품목별로 바꿀 필요는 없음(코너 캐릭터가 바뀌는 것과
별개). 보통 코너 캐릭터로 가장 많이 쓰이는 품목의 실사진(`assets_library/real/`)을
쓰면 자연스럽다.

### ⚠️ `--title`에 topic 폴더명(슬러그)을 그대로 넣지 말 것 (2026-08-02)

"야 봐봐 상단에 손발저림_1 <-- 이거는 왜넣은거야"로 발견된 실수 — `--title`에
`"<훅> 손발저림_1"`처럼 topic 폴더명(`카테고리_번호` 슬러그)을 그대로 이어붙이면
그게 그대로 상단 배너에 노출된다. `--title`/`--title-card-text`엔 항상 **자연어
문구**만 넣을 것(예: `"손발 끝이 자꾸 저리고 시린가요? 손발 저림 부르는 음식"`) —
topic 폴더명은 내부 관리용 식별자일 뿐 영상에 노출되는 텍스트가 아니다.

### 표준 호출 예시 (2026-08-02 갱신 — 배경 기본값 칠판 + 배너 실사진 블러로 전환)

새 topic 영상 조립 시 기본으로 이 형태를 쓸 것 — `intro_duration`/`bg_color` 기본값은
이미 코드에도 반영돼 있지만(0, 0x00FF00 캐릭터 기준), `--title-card-char`/
`--title-banner-photo`는 선택 파라미터라 빠뜨리기 쉽다. 빠뜨려도 에러는 안 나고 그냥
단색 배경이 되니(덜 완성도 있는 정도), 매번 의식적으로 챙길 것. **`--bg-style`은
기본값이 `chalkboard`라 생략 가능하고 이때 `--images`도 필요 없다** — 위 "영상 배경 —
칠판 스타일이 기본" 절 참고:

```
python3 lib/video_assembler.py \
  --motion "assets_library/motion/<캐릭터>_motion.mp4" \
  --audio "output/<topic>/<topic>_narration.mp3" \
  --srt "output/<topic>/<topic>_narration.srt" \
  --out "output/<topic>/<topic>_shorts.mp4" \
  --title "<훅> <주제명, 자연어>" \
  --title-card-text "<훅만, 주제명 빼고>" \
  --title-card-char "assets_library/illust/<캐릭터>_illust.jpg" \
  --title-banner-photo "assets_library/real/<대표품목>_real_01.jpg" \
  --bg-color 0x00FF00
```

⚠️ **output 폴더 안 파일명에도 topic 접두어를 붙일 것**(2026-07-31, "output 폴더 안에 있는
파일들도 토픽이 추가될 수 있도록" 요청): 여러 세션이 동시에 여러 topic을 작업하다보니
`shorts.mp4`, `00_표지.jpg`, `narration.mp3` 같은 이름이 topic마다 겹쳐서 구분이 안 됐다.

- `card_news.py`의 `generate()`는 이미 자동으로 접두어를 붙인다(`out_dir.parent.name`에서
  topic을 추론) — 별도 조치 불필요
- `typecast_tts.py`의 `synthesize()`가 만드는 `narration.mp3`/`narration.srt`는 아직
  접두어가 안 붙으므로, 영상 조립 전에 `output/<topic>/narration.mp3` →
  `output/<topic>/<topic>_narration.mp3`로 이름을 바꿔주거나, `synthesize()` 결과를 받은
  뒤 바로 rename할 것
- `video_assembler.py`의 `--out`도 위 예시처럼 `<topic>_shorts.mp4`로 직접 지정할 것
  (함수 자체는 파일명을 신경 안 쓰고 호출자가 준 경로 그대로 씀)
- `dashboard.py`는 접두어 있는/없는 파일 둘 다 glob으로 찾아서 호환되지만
  (`_prefixed()` 헬퍼가 중복 접두어도 방지함), 새 topic은 처음부터 접두어를 붙여서
  시작할 것

⚠️ **위 "예전 topic까지 소급 리네임 불필요"는 틀린 설명이었다 — 실제로는 영상이 안 뜨는
버그로 이어짐**(2026-08-01 발견·수정): `_prefixed()`/`_dock_products()`가 호환하는 건
**다운로드 파일명 표시**뿐이고, `dashboard.py generate()`는 `video_path` 인자를 그대로
받아서 존재 여부만 확인할 뿐 내부적으로 영상 파일을 glob으로 찾지 않는다 — 영상을
찾는 건 **호출하는 쪽**(사람이 `ls output/<topic>/*_shorts.mp4`로 찾아서 인자로 넘기는
방식, 이 문서의 "표준 호출 예시"도 이 패턴)이다. 그래서 접두어 없는 옛 파일
(아침공복음식_1의 `shorts.mp4`)은 이 glob에 안 걸려서 인자가 빈 값으로 넘어갔고, 실제로는
영상이 존재하는데도 대시보드엔 "영상 준비 중" placeholder만 계속 떴다(사용자가 실제
대시보드에서 발견). **접두어 없는 영상/오디오 파일을 발견하면 그냥 두지 말고 반드시
`<topic>_` 접두어를 붙여 리네임한 뒤 대시보드를 재생성할 것** — 특히 여러 topic을
일괄 점검할 때는 `ls output/*/*.mp4`로 접두어 없는 파일이 있는지 먼저 훑어보는 습관을
들일 것.

### 캐릭터 여러 명(품목 3개 이상 topic) — motion_schedule (2026-07-31)

`--motion`은 캐릭터 1명짜리 topic용이다(돼지감자차_1처럼 소재가 하나). **품목이 여러 개라
캐릭터가 대사 구간마다 바뀌어야 하면(수면음식_1: 대추/체리/호두) `--motion` 대신
`--motion-schedule`을 쓴다**:

```
--motion-schedule "0-9.444:assets_library/motion/대추_motion.mp4,9.444-15.287:assets_library/motion/체리_motion.mp4,15.287-26.25:assets_library/motion/호두_motion.mp4"
```

- 형식: `시작-끝:경로` 를 쉼표로 나열, 시간은 나레이션(오디오) 기준 0초부터 — `narration.srt`에서
  각 캐릭터가 언급되는 문장의 시작 시각을 구간 경계로 잡으면 된다.
- 구간은 반드시 0초부터 나레이션 전체 길이까지 빈틈없이 이어져야 한다(마지막 구간의 끝은
  오디오 전체 길이).
- Python에서 직접 `assemble()` 호출할 때는 `motion_schedule=[(start, end, path), ...]`
  튜플 리스트로 넘긴다(`motion_path=None`으로).

### 실사진 없는 topic — 배경은 캐릭터 일러스트가 아니라 그라디언트로 (2026-07-31)

`--images`는 **실사진** 배경 슬라이드쇼용이다. 실사진이 없다고 캐릭터 일러스트(크로마키
초록 배경 포함) 파일을 `--images`에 그대로 넣으면 초록 배경이 화면 전체에 노출되는
사고가 난다(수면음식_1에서 실제 발생 — `_build_background`는 사진용이라 colorkey를
안 함). 실사진이 없으면 `lib/video_assembler.py`의 `make_gradient_bg()`로 카드뉴스와
같은 톤의 단색 그라디언트를 만들어 쓴다 — 이미 `assets_library/backgrounds/soft_gradient.jpg`
로 하나 생성해뒀으니 재사용 가능:

```python
from lib.video_assembler import make_gradient_bg
make_gradient_bg(Path("assets_library/backgrounds/soft_gradient.jpg"))  # 필요시 재생성
```

### ⚠️ 타이틀 카드 프레임레이트 — 반드시 30fps로 (2026-07-31 버그, 수정 완료)

`_make_title_card_png`로 만든 이미지를 영상으로 만들 때 `-r {FPS}`(30)를 명시하지 않으면
ffmpeg가 기본 25fps를 붙이는데, 뒤에서 본편(30fps)과 `-c copy`로 concat할 때 두 세그먼트의
프레임레이트가 달라 타임스탬프가 어긋난다(30/25=1.2배로 영상 길이가 실제보다 길게 나옴).
캐릭터 여러 명이 번갈아 나오는 영상에서는 캐릭터 전환 타이밍이 한 구간씩 밀리는 형태로
드러났다 — 이미 코드에 `-r 30` 반영해서 고쳐뒀지만, 향후 `-f concat -c copy`로 영상
세그먼트를 이어붙이는 코드를 새로 추가할 때는 **모든 세그먼트의 프레임레이트가 같은지
항상 확인**할 것(`ffprobe -select_streams v -show_entries stream=r_frame_rate`).

## TTS — 보이스 랜덤 선택 (2026-07-31)

`lib/typecast_tts.py`의 `synthesize()`는 `voice_name`을 안 넘기면(기본값 `None`)
`data/typecast_voices.json`에 등록된 보이스 중 하나를 난수로 골라 쓴다 — 매번 "상현"
고정이면 채널 전체 목소리가 단조로워진다는 피드백 반영. 특정 topic에 특정 보이스를
꼭 써야 하는 경우(예: 캐릭터 성별/톤이 명확한 경우)에만 `voice_name`을 명시로 넘길 것,
그 외엔 랜덤 기본값 그대로 둘 것.

### 캐릭터 여러 명 topic — 항목마다 다른 보이스 (2026-08-01)

`synthesize()`는 나레이션 전체를 보이스 하나로만 만든다 — 캐릭터 여러 명이 번갈아 나오는
topic(예: 60대주의음식_1의 사골국/믹스커피/과일즙)에서 화면 속 캐릭터는 바뀌는데 목소리는
안 바뀌는 어색함이 있었다(2026-08-01 사용자 지적: "각자가 다 다르게 가져가야지"). 이런
topic은 `synthesize_segments()`를 대신 쓸 것:

```
python3 lib/typecast_tts.py <topic> --multi-voice data/<topic>/narration.txt
```

narration.txt를 빈 줄 기준 문단으로 나눈 뒤, **화면 속 캐릭터(모션) 전환 지점 기준으로
문단을 묶어서** `synthesize_segments()`에 넘긴다 — 각 그룹이 TTS 세그먼트 하나가 되고,
세그먼트마다 다른 보이스가 배정된다(보이스는 안 겹치게 자동 배정, `voice_names` 인자로
직접 지정도 가능). 출력 경로(`output/<topic>/narration.mp3`/`.srt`)는 단일 보이스 버전과
동일해서 이후 `video_assembler.py` 단계는 그대로 쓰면 된다.

```python
paras = [p.strip() for p in Path("data/<topic>/narration.txt").read_text().split("\n\n") if p.strip()]
# 6문단(훅+원인+아이템1, 아이템2, 아이템3+마무리) → 캐릭터 3명 기준으로 묶기
groups = [" ".join(paras[0:3]), paras[3], " ".join(paras[4:6])]
result = synthesize_segments(topic, groups)
# result["segment_starts"]가 곧 --motion-schedule 구간 경계 시각
```

⚠️ **"문단 = 화자 전환 지점"이라고 그냥 가정하고 문단 6개를 그대로 6개 세그먼트로 넘기면
안 됨(2026-08-02 실제 버그, 사용자가 직접 발견)** — "훅 다음엔 원인 먼저" 규칙(2026-08-01)
이후 narration.txt는 훅+원인+아이템1+아이템2+아이템3+마무리 6문단인데, **화면 캐릭터는
3명뿐**이다(훅+원인+아이템1 문단 전부 아이템1의 캐릭터, 아이템3+마무리 문단 전부 아이템3의
캐릭터 — `card_news_spec.json`의 `items[].char_file`로 어느 문단이 어느 캐릭터인지 확인
가능). 문단 6개를 그대로 6개 세그먼트로 넘기면 **같은 캐릭터가 화면에 계속 떠 있는 도중에
목소리가 2~3번 바뀌는 사고**가 난다 — 정작 이 기능을 만든 이유(화면 캐릭터 전환에 맞춰
목소리도 바뀌게)와 정반대 결과. **항상 위 예시처럼 문단을 먼저 캐릭터 개수만큼(보통 3개)
묶은 뒤에 `synthesize_segments()`를 호출할 것** — 이렇게 하면 목소리 전환이 캐릭터 전환과
정확히 일치하고, `segment_starts`가 바로 `--motion-schedule` 구간 경계가 되어 손으로
SRT를 읽고 경계를 추정할 필요도 없다.

**단일 캐릭터 topic(블루베리_1, 돼지감자차_1처럼 문단이 여러 개여도 화자가 한 명인 경우)은
계속 `synthesize()`(단일 보이스)를 쓸 것** — 문단이 여러 개라고 무조건 멀티보이스로 가는
게 아니라, "이 문단이 실제로 다른 캐릭터가 말하는 구간인가"를 판단해서 고를 것.

#### ⚠️ 구버전 `synthesize_segments()`로 만든 audio/srt는 커서 드리프트 버그 — 영상만
재조립해도 안 고쳐짐, TTS부터 다시 돌릴 것 (2026-08-02)

`synthesize_segments()`가 각 세그먼트 오디오를 이어붙일 때 다음 세그먼트 시작 위치(커서)를
Typecast API가 반환한 `words[-1]["end"]`(마지막 단어 타임스탬프)로 미뤘는데, 실제 세그먼트
오디오 파일 길이와 최대 2초 가까이 어긋나는 경우가 있었다 — `_insert_sentence_pauses`가
자연스러운 여운을 위해 각 세그먼트의 마지막 문장 구간은 `-to`로 안 자르고 원본 끝까지
살리기 때문에, 실제 파일 길이가 API의 마지막 단어 타임스탬프와 일치한다는 보장이 없다.
이 오차가 세그먼트마다 누적돼서 **뒤 문단으로 갈수록 자막이 점점 더 많이 밀린다** — 영상
끝의 엔딩 카드 침범(자막이 엔딩 CTA와 겹쳐 보임)만 눈에 띄는 증상이었지만, 실제로는
문단 2 시작 시점부터 이미 자막이 오디오보다 밀려 있었다(2026-08-02, 5개 topic 배치에서
발견). `lib/video_assembler.py`의 SRT 클램프(`min(end, total_duration)`)는 **마지막
자막이 엔딩 카드 영역을 침범하지 않게 잘라내는 안전장치일 뿐**이라 이 드리프트 자체를
고치지 못한다 — 클램프만 있는 상태로 영상을 재조립하면 "마지막 캐릭터/자막은 맞아
보이는데 중간 문단들은 여전히 안 맞는" 상태가 된다(실제로 이렇게 재조립했다가 발견됨:
"이번에 만든것도 싱크 안맞는데... 그 마지막뿐만아니라 전체적인 싱크 자체가 다안맞아").

진짜 수정은 `synthesize_segments()`의 커서 계산 자체를 ffprobe로 측정한 각 세그먼트
파일의 실제 길이로 바꾼 것(단어별 개별 타임스탬프는 그대로 두고, 다음 세그먼트가 시작하는
절대 위치만 실측값 기준으로 고정) — 이 수정은 **`data/<topic>/narration.txt`로
TTS를 다시 돌려서 새 `output/<topic>/narration.mp3`/`.srt`를 만들어야만 적용된다.**
기존 audio/srt 파일을 그대로 두고 `video_assembler.py`만 재실행하면 클램프 덕분에
엔딩 카드 침범만 안 보일 뿐 중간 드리프트는 그대로 남는다. **멀티보이스로 만든 영상에서
자막 싱크 문제가 발견되면, 영상 재조립 전에 반드시**
`python3 lib/typecast_tts.py <topic> --multi-voice data/<topic>/narration.txt`**부터
다시 실행**해서 audio/srt를 새로 만들고, 그다음 영상을 재조립할 것.

#### ⚠️ 멀티보이스 기능 자체를 캐릭터 여러 명 topic에 기본으로 쓰지 않는다 (2026-08-02, 최종 결정)

위 커서 드리프트 버그를 고친 뒤에도(ffprobe 실측 기반) 실사용에서 또 다른 증상이
나왔다 — "왜 또 아이템이 바뀌지도 않았는데 목소리가 달라지며... 자막이랑 소리가
매칭이 안되며" (같은 캐릭터가 화면에 떠 있는 도중에 목소리가 바뀌거나, 세그먼트
경계가 실제 음성과 미묘하게 어긋나는 현상이 반복됨). 세그먼트를 여러 개 이어붙이는
구조 자체(개별 TTS 호출 → 문장 단위 무음 삽입 → 세그먼트 간 무음 삽입 → 최종 병합)가
근본적으로 오차가 낄 지점이 많아서, 고칠 때마다 다른 증상으로 재발했다. 결국
"어이가없긴하네 다른 사람들은 잘하던데" — 사용자가 **"이제부터 단일목소리로
가게하자"**로 확정.

**지금부터 캐릭터 여러 명(motion_schedule) topic도 `synthesize_segments()`(멀티보이스)
대신 `synthesize()`(단일 보이스, 나레이션 전체를 이어붙이지 않고 한 번의 TTS 호출로
생성)를 기본으로 쓴다** — 화면 속 캐릭터가 바뀌어도 목소리는 그대로 유지된다(과거
"화면은 바뀌는데 목소리가 안 바뀌는 어색함" 피드백보다, 반복되는 싱크 버그가 훨씬
더 큰 문제라는 우선순위 판단). `synthesize_segments()`/`--multi-voice` 코드 자체는
지우지 않고 남겨둔다(나중에 근본적으로 더 견고하게 재설계할 여지를 위해) — 다만
**새 topic이든 기존 topic 재작업이든, 특별한 이유 없이는 먼저 시도하지 말 것.**

**단일 보이스로 캐릭터 여러 명 topic을 만드는 절차**:
```python
import json
from lib.typecast_tts import synthesize
from pathlib import Path
paras = [p.strip() for p in Path("data/<topic>/narration.txt").read_text().split("\n\n") if p.strip()]
full_text = " ".join(paras)  # 문단 구분 없이 전체를 한 번에 합성
result = synthesize("<topic>", full_text)
```
그 다음 결과 SRT에서 아이템2·아이템3 문단의 **첫 문장이 시작하는 타임스탬프**를
찾아서(`grep -B1 "<아이템2 문단 첫 문장>" output/<topic>/narration.srt` 등) 그 값을
그대로 `--motion-schedule` 구간 경계로 쓴다 — 멀티보이스처럼 `segment_starts`가
자동으로 나오지 않으니 이 방식으로 손으로 경계를 잡아야 한다(2026-07-31 이전
방식으로 회귀). item_schedule(칠판 우상단 아이콘+이름 라벨, 위 "item_schedule" 절
참고)은 motion_schedule만 있으면 자동으로 파생되므로 단일 보이스 여부와 무관하게
그대로 동작한다 — 목소리와 화면 전환이 분리된 것이지, 화면 전환 관련 기능들이
없어진 게 아니다.

## 플랫폼 로테이션

브런치·핀터레스트는 항상 제외(승인장벽/콘텐츠 결/신규계정 노출 구조 문제로 확정) — 상세 이유는
Claude의 메모리(`project_health_shorts_platform_rotation`) 참조. 새 topic마다 이 둘 빼고 캡션
작성 시작.

⚠️ **인스타그램 카드뉴스도 제외(2026-08-02, "인스타그램 카드뉴스는 없애자. 릴스만
올려도 하루 4개면 빠듯해보인다 제한에")** — 플랫폼당 하루 4개 캡으로 페이스를
늦춘 뒤로는, 같은 인스타그램 계정에 릴스와 카드뉴스를 별도 게시물로 둘 다 올리면
그 캡을 릴스 혼자 다 못 채우고 카드뉴스랑 나눠 써야 한다 — 인스타그램은 **릴스만**
올린다. 새 topic 캡션 작성 시 "인스타그램 카드뉴스" 항목 자체를 만들지 말 것
(**적용 범위는 새 topic부터** — 기존 topic의 platform_captions.json에 이미 있는
"인스타그램 카드뉴스" 항목은 사용자가 별도로 정리를 요청하기 전까지 소급 삭제하지
않는다, 2026-08-02 확정 당시 36개 topic 전부에 이미 있었음).

### 플랫폼별 필수 플래그 (2026-07-31)

`platform_captions.json`의 각 플랫폼 항목에 아래 플래그를 빠짐없이 넣을 것 — 새 topic을
이전 topic 파일 복사로 시작한다면 자동으로 따라오지만, 처음부터 새로 쓸 때는 까먹기 쉽다:

- `"no_caption_link": true` — 인스타그램 릴스·틱톡(+ 기존 topic에 남아있는 인스타그램
  카드뉴스). 캡션 속 URL이 클릭되지 않는 플랫폼이라 원본 링크 대신 마켓 동적
  CTA/고지문구가 들어간다(`lib/dashboard.py` 참조)
- `"comment_dm_automation": true` — 인스타그램 릴스만(+ 기존 topic의 카드뉴스). 인포크 댓글→DM 자동화가
  실제로 연동된 플랫폼에만 붙일 것 — 이게 없는 no_caption_link 플랫폼(틱톡)은 "댓글 달면
  보내드려요" 문장 없이 고지문구만 자동 추가된다. 자동화 없는데 이 플래그를 잘못 붙이면
  실제로 안 되는 기능을 안내하는 캡션이 나간다(2026-07-31 틱톡에서 실제 발생 — 이미 있던
  "프로필에!" 안내와 모순되는 "댓글 달면" 문장이 같이 붙었었음)
- `"comment_keyword": "쿠팡"` — **모든 topic 공통 고정값(2026-08-01 정책 변경)**.
  인포크 댓글→DM 자동화가 게시물(릴스/카드뉴스) 단위로 룰이 걸리는 구조라는 게
  확인돼서, topic마다 다른 단어를 짜내고 `~/.claude/comment-keywords.md`에서 전역
  중복을 확인하던 절차를 폐지했다 — 트리거 단어가 모든 topic에서 "쿠팡"으로 똑같아도
  인포크 쪽에서 게시물마다 다른 상품 링크를 매핑하면 되기 때문. 새 topic도 항상
  `"comment_keyword": "쿠팡"`을 그대로 쓸 것, 더 이상 고유 단어를 고민하거나
  레지스트리를 확인할 필요 없음. CTA 문구도 고정: `lib/dashboard.py`의
  `_buildCtaBlock()`이 "댓글에 "쿠팡"이라고 치시면 제품 목록으로 이동할 수 있는
  링크 바로 전송해드릴게요!"로 자동 생성한다(마켓 이름은 더 이상 문장에 안 들어감).
  (2026-07-31 이전엔 topic마다 고유 키워드를 쓰고 레지스트리로 중복을 막았었음 —
  이 방식은 폐기됐고 과거 기록은 `~/.claude/comment-keywords.md`에만 남아있음)
- `"network": "naver"` — **네이버 블로그·네이버 클립 둘 다** 붙인다(2026-08-03
  재확정 — 아래 "네이버 블로그, 다시 브랜드커넥트로" 참고). 이 플래그가 있어야
  네이버 선택 시 URL 없이 상품명만 넣는 처리가 된다 — 다른 플랫폼에 잘못 붙이면
  네이버 URL이 안 들어가는 버그가 재발한다(2026-07-31 실제 발생)

### 네이버 블로그, 다시 브랜드커넥트로 (2026-08-03, 2026-08-01 결정 재번복)

2026-08-01에 "번거롭고 수익성도 덜하다"는 이유로 네이버 블로그도 쿠팡 링크를 쓰게
바꿨었는데(`"network": "naver"` 플래그 제거), **쿠팡 링크를 달면 네이버 자체
알고리즘에서 저품질(노출 저하) 판정을 받는 이슈가 있다는 게 확인돼서**("네이버
관련 플랫폼은 쿠팡 링크 달면 저품질 이슈 생기나보더라... 네이버커넥트로
돌려야할듯") 다시 브랜드커넥트 방식으로 되돌렸다 — **네이버 블로그만** 대상이고
티스토리는 무관(카카오 플랫폼이라 이 이슈와 상관없음).

`platform_captions.json`의 "네이버 블로그" 항목에 `"network": "naver"` 플래그를
다시 붙인다 — 코드(`lib/dashboard.py`의 `_buildLinkBlock`)는 이 플래그 있으면
URL 없이 상품명만 나열하는 로직을 이미 갖고 있어서(네이버 클립용으로 남겨뒀던
로직 재사용), 새로 짤 필요 없이 플래그만 되돌리면 끝이었다. ⚠️ **소급 적용** —
"캡션에 차이가 생기는거라면 기존꺼에도 수정을 해야지 당연히" 확정에 따라 기존
36개 topic의 `platform_captions.json`에도 전부 스크립트로 일괄 반영함(2026-08-03).

**네이버 클립은 원래부터 그대로 유지** — 네이버 클립은 브랜드커넥트가 아니라 클립
자체 에디터의 "상품" 버튼을 쓰는 별개 흐름이라 번거로움 문제가 없고, 8/1 변경
때도 이번 재번복 때도 계속 `"network": "naver"` 플래그를 유지해왔다. "네이버"라는
이름만 보고 두 플랫폼을 같은 규칙으로 묶지 말 것.

⚠️ 대시보드 UI의 "🔵 브랜드커넥트 검색" 입력창은 2026-08-01에 없앤 상태 그대로
안 살렸다 — `network:"naver"` 플래그가 있으면 URL 입력 여부와 무관하게 상품명만
나열하는 지금 로직으로 충분해서, UI를 되살릴 필요는 없다고 판단.

**UI도 함께 단순화**(같은 날) — 대시보드에서 플랫폼마다 쿠팡/네이버 중 골라 쓰던
마켓 토글(🛒쿠팡/🔵네이버 버튼)과 덕(dock) 패널의 "🔵 브랜드커넥트 검색" 입력창을
전부 없앴다("그 UI에서 브랜드커넥트 입력하는것도 없애자 성가시기만 하다" — 실제로
그 입력값으로 뭔가 하는 것도 없었어서 결정). 이제 상품 링크는 쿠팡 하나만 입력하면
되고, `network:"naver"`가 붙은 플랫폼(네이버 클립)은 입력 여부와 무관하게 이 topic의
전체 상품명을 조건 없이 캡션에 나열한다(`lib/dashboard.py`의 `_buildLinkBlock`
참고) — 예전처럼 "네이버 링크도 따로 입력해야 상품이 목록에 포함되는" 방식이 아님.
- `"rich_paste": true` — 네이버 블로그·티스토리처럼 클립보드 HTML 붙여넣기로 이미지까지
  같이 들어가는 리치 에디터만. 쓰레드·페이스북처럼 단순 텍스트 입력창에는 붙이지 말 것
- `"add_profile_note": true` — 링크 텍스트는 유지하되 "링크가 눌리지 않으면 프로필에서
  확인" 문구를 덧붙이고 싶을 때. 지금은 어느 플랫폼에도 안 쓰는 상태(아래 유튜브 쇼츠
  판단이 바뀌면서 대체됨) — 코드는 남겨둠, 필요해지면 재사용
- `"suppress_product_block": true` — 유튜브 쇼츠. 구독자 500명 조건을 채우기 전까지는
  설명란 링크가 아예 클릭이 안 돼서, 링크/고지문구를 넣어봤자 반감만 산다는 판단
  (2026-07-31, "구독자 500 조건 만들 때까지 링크랑 판매 관련 문구 아예 넣지 말고 팔로우
  요청만" — `add_profile_note`에서 최종적으로 이 방향으로 바뀜). 이 플래그가 있으면
  상품 링크/CTA/고지문구 전부 자동 추가 안 되고, 캡션엔 구독 유도 문구만 정적으로
  남는다. **구독자 500명 넘으면 이 플래그를 지우고 다른 플랫폼처럼
  정상적으로 링크 넣을 것**
  - ⚠️ **인스타그램(릴스·카드뉴스)도 같은 플래그로 전환(2026-08-03)** — "인스타
    쪽 쿠팡 링크 준다는 문구 없애줘 일단 채널을 키우는 데 중점을 두고 싶어.
    링크 없이 정보만 주는거로 진행할게" 확정. 지금까지 인스타는
    `comment_dm_automation: true`로 "댓글에 '쿠팡'이라고 치시면..." CTA가
    자동으로 붙었는데, 이걸 유튜브 쇼츠와 같은 이유(채널 성장 단계에서는
    판매 유도보다 팔로우 유도가 우선)로 `suppress_product_block: true`로
    바꾼다 — 정적 캡션의 "더 유용한 건강정보 궁금하면 팔로우 👆" 문구만
    남기고 CTA/링크 언급 자체를 없앤다. **채널이 충분히 성장하면(기준은
    유튜브 500명처럼 나중에 정할 것) 다시 `comment_dm_automation`으로 되돌릴
    것.**
    ⚠️ **소급 적용 보류** — 이 정책 확정 시점에 마침 다른 세션이 거의 모든
    topic의 `platform_captions.json`을 동시에 수정 중이라(git status 확인),
    지금 30여 개 파일을 일괄 수정하면 그 작업과 충돌할 위험이 커서 새 topic
    부터만 적용하고 기존 topic 일괄 반영은 그 작업이 끝난 뒤 별도로 진행하기로
    함 — 소급 필요.
- `"link_in_comment": true` — **쓰레드·페이스북**. 메타 계열은 캡션 본문에 아웃바운드
  링크가 있으면 도달을 낮추는 것으로 알려져 있다("댓글에는 링크 넣어도 돼?" — 2026-08-02
  사용자 확인 후 적용, link-in-comments라는 정석적인 우회법. 참고로 페이스북은 실제로
  본문 링크가 클릭은 된다 — 클릭 자체가 안 되는 `no_caption_link` 플랫폼과는 이유가
  다르다, 단지 도달이 낮아지니 링크만 댓글로 빼는 것). 항상 `no_caption_link`와 같이
  쓴다.
  ⚠️ **본문 자동 삽입 문구(2026-08-04 재수정)** — 처음엔 "🔗 구매 링크는 댓글에
  남겨둘게요!"라는 약속 문구가 자동으로 붙었는데, "이건 굳이 두번 넣을 필요가
  없잖아... 아예 이 멘트 자체를 없애도 될거같은데" 요청으로 이 약속 문구는 완전히
  제거했다. 실제 쿠팡 링크는 게시자가 게시 직후 댓글로 직접 추가한다(수동 게시
  흐름과 자연스럽게 이어짐 — 별도 자동화 없음). **제휴 고지문구(`COUPANG_DISCLOSURE`)는
  남긴다** — 이 약속 문구와 한 묶음으로 보고 처음엔 같이 지웠다가, "본문에는 쿠팡
  파트너스 관련 코멘트 없애도돼?" 확인 요청으로 공정위 "추천·보증 등에 관한 표시·
  광고 심사지침" 기준상 제휴 고지는 댓글에만 있으면 인정 안 되고 본문(추가 행동
  없이 보이는 위치)에 있어야 한다는 게 확인돼서 바로잡음. 위치는 **게시물 맨 아래
  한 곳에만**("아니 맨위도 아니다 걍 맨 아래 하나만 남겨줘" — 위쪽으로 먼저 갔다가
  최종 조정) — 아래 "제휴 고지문구는 맨 위·맨 아래 각각 한 번씩" 규칙의 **예외**다,
  본문에 실제 링크 자체가 없어서 위/아래 두 번 반복할 대상이 없기 때문.
  ⚠️ **"💬 댓글용 링크 텍스트 복사" 버튼**(2026-08-02, "댓글 달 때 복사해서 바로
  붙여넣을 수 있는 그런 버튼도... 위젯에있으면 좋을듯") — 상품 링크 위젯(대시보드
  우측 `position: fixed` dock) **맨 위**에 실제로 댓글에 붙여넣을 텍스트를 한 번에
  복사하는 버튼이 있다. 쓰레드·페이스북뿐 아니라 인스타그램 등 어느 플랫폼 댓글에
  붙여넣어도 된다. 복사되는 텍스트는 `_buildLinkBlock(false)`(상품명+링크+고지문구)
  앞에 안내 문장 `COMMENT_LINK_INTRO`("🔗 빠르게 이동할 수 있는 링크 목록이에요!")를
  붙인 형태 — "띡 링크만 전달하면 안될거같지않아? 어느정도 설명이 필요할것으로
  보이는데" 지적으로 추가했다. 링크만 나열하면 스팸성 댓글처럼 보이니, 캡션이
  예고한 "댓글에 남겨둘게요!"를 자연스럽게 받는 문장으로 시작하게 한 것. ⚠️ 처음엔
  "📌 오늘 소개한 상품 링크 남겨드려요!"였는데 "오늘 소개한..? 아니 그냥 목록
  전달정도니까 빠르게 이동할수있는 링크 전달하는 정도의 문구로 해야함" 지적으로
  상품 소개 톤을 빼고 바로가기 링크 모음이라는 담백한 톤으로 정정(2026-08-02).
  이 문구를 바꿀 땐 `lib/dashboard.py`의 `COMMENT_LINK_INTRO` 상수 한 곳만 고치면
  된다.

  ⚠️ **페이지 맨 아래에도 읽기 전용 사본이 있음**(2026-08-02, "야 그리고 쿠팡 링크
  맨위로 올렸잖아 맨 아래에도 추가해달라고 했는데 언제까지 안해줄거냐?") —
  `_product_links_bottom_section()`이 `{{platform_sections}}` 바로 뒤에 상품명+
  쿠팡 검색 링크+등록된 링크(있으면)를 나열한 섹션과 같은 "댓글용 링크 텍스트
  복사" 버튼을 하나 더 그린다. ⚠️ **입력창(`<input class="product-link-input">`)은
  복제하지 않는다** — 편집 가능한 링크 입력의 원본은 상단 dock 하나뿐이어야
  한다(두 벌 만들면 위/아래 값이 어긋나는 상태가 생김). 아래쪽은 항상 읽기
  전용(검색 링크 + 이미 등록된 링크의 `<a>` 태그)만 보여주고, 편집 안내는
  "링크 수정은 위쪽 '빠른 도구' 패널에서 해주세요" 문구로 위쪽을 가리킨다. ⚠️
  **"댓글용 링크 텍스트 복사" 버튼은 `id` 대신 `class="copy-comment-links"`로
  바인딩**(JS `document.querySelectorAll(".copy-comment-links").forEach(...)`) —
  원래 `id="copyCommentLinksBtn"` 하나만 보고 `getElementById`로 묶었는데, 페이지
  아래에 같은 버튼을 복제하면서 id 중복(둘째 버튼이 안 눌림)을 피하려고 class
  기준으로 바꿨다 — 위 `row-toggle`/`copy-market-link` 등 다른 상품 위젯 버튼들과
  같은 패턴.

  ⚠️ **제휴 고지문구(disclosure)는 게시물 전체의 맨 위·맨 아래에 각각 한 번씩**
  (2026-08-03, 두 번째 재작업 — 첫 fix가 "상품 블록 바로 위/아래"에만 고지를
  붙였는데, 그 블록 자체가 캡션 중간(제목 다음)에 들어가다 보니 실제로는 고지
  두 개가 캡션 **중간에 몰려있고** 게시물 맨 끝에는 하나도 없었다 — 사용자가
  스크린샷으로 재확인: "몇번을 말한건데 이거 안먹혔을까... 밑에도 없네"):
  `_buildLinkBlock`/`_buildCtaBlock`은 이제 `{text, disclosure}`만 반환하고,
  `applyProductLinks`가 맨 위(제목 다음, `AUTO_LINKS_START/END` 마커)와 맨
  아래(캡션 최종 끝, 새 `AUTO_BOTTOM_START/END` 마커)에 각각 고지문을 한 번씩
  배치한다 — 마커 쌍이 서로 겹치지 않아야 상품 링크 재입력 시 `applyProductLinks`가
  다시 돌아도 두 구간을 각각 정확히 지우고 새로 넣을 수 있다(안 그러면 고지문이
  계속 누적됨 — 이 문제 때문에 마커를 하나 더 추가한 것). ⚠️ **`link_in_comment`
  플랫폼(쓰레드·페이스북)은 이 규칙의 예외** — 위 `link_in_comment` 플래그 설명
  참고, 맨 아래 한 곳에만 고지문이 붙는다.

### 네이버 블로그·티스토리 — 이미지 삽입 위치 표시는 "지울 필요 없는" 형태로 (2026-08-01)

`rich_paste` 캡션(네이버 블로그·티스토리)은 문단 사이에 이미지 삽입 위치를 표시해야 하는데,
예전엔 `📷 [01_왜 이런 문제가 생길까요.jpg 삽입]`처럼 카메라 이모지+대괄호+파일명+"삽입"을
다 붙여서 "지워야 할 안내문"처럼 보였다 — 실제로 붙여넣을 때마다 매번 지워야 해서 번거롭다는
지적(2026-08-01). 대신 **번호 + 그 구간의 소제목만** 남긴 자연스러운 한 줄로 쓴다:

```
01 · 왜 이런 문제가 생길까요
```

- 카메라 이모지·대괄호·`.jpg`·"삽입" 전부 제거 — 번호만 있으면 `card_news/` 폴더에서 같은
  번호 접두어 파일(`01_*.jpg`)을 바로 찾을 수 있으므로 파일명까지 안 적어도 된다
- 소제목은 이미 `card_news_spec.json`의 카드 라벨과 동일한 문구를 그대로 쓴다 — 새로 짓지 않음
- 이 형태는 지우지 않고 그대로 둬도 자연스러운 소제목 줄로 읽히므로, 사진만 그 아래에 넣고
  발행해도 된다(기존처럼 붙여넣기 전에 안내문을 지우는 단계가 사라짐)
- 표지(00번)에도 동일하게 `00 · 표지`로 적용 — 표지는 복사 버튼이 자동으로 클립보드 맨 위에
  붙여주지만(대시보드 "표지 이미지는 자동 포함" 안내 참고), 본문에도 구간 표시가 남아있는
  게 자연스러워서 통일함
- **새 topic 캡션 작성 시 처음부터 이 형식으로 쓸 것** — 예전 topic 파일을 복사해서 시작하면
  옛 마커가 그대로 따라올 수 있으니 확인할 것
- **마커 바로 아래는 빈 줄 두 줄(`\n\n\n`)로 띄울 것**(2026-08-01) — 한 줄만 띄우면 에디터에
  붙여넣었을 때 마커와 다음 문단이 바로 붙어 있어서 그 사이에 이미지를 끼워 넣을 공간이
  부족하다는 지적. 마커 위쪽(이전 문단과의 사이)은 기존대로 빈 줄 한 줄만 유지 —
  아래쪽만 넉넉하게 띄운다.

### 네이버 블로그 캡션 — 최소 1000자 (2026-08-02)

저품질 블로그 판정 방지 목적("네이버 포스팅은 글자 수 기준으로 최소 1000자로 진행하자" —
사용자 확정). `tests/test_content_rules.py`의 `test_naver_blog_min_length`가 매번 자동
검사한다 — 새 topic 작성 후 이 테스트가 실패하면 내용을 억지로 늘리지 말고(반복·상투구
채우기 금지), 아래 "자료수집" 절 원칙대로 실제 연구·통계를 더 찾아서 깊이를 보강하는
방식으로 채울 것(돼지감자차_1이 899자로 미달이었을 때 이렇게 보강해서 해결한 사례 —
동물실험 결과를 실제 수치와 함께, 사람 대상 임상 근거는 아직 부족하다는 한계까지 정직하게
포함해서 늘림). 티스토리는 네이버 블로그와 캡션이 보통 동일한 문자열이라 같이 늘어나지만,
별도로 자동 검사하진 않음(네이버 블로그만 명시적으로 요청받음).

### `products` 필드 — 특정 요리로 좁히지 말고 식품 카테고리로 (2026-08-01)

"먹지 마세요" 회피형 topic에서 `products`에 넣을 대체재를 "저요오드 국물", "저나트륨
사골육수"처럼 특정 요리·조리형태로 좁히면, 이 값이 그대로 네이버 제휴 상품 검색
쿼리(`lib/dashboard.py`의 `_dock_products` → `affiliate/products/search?query=`)로 들어가는데
그런 이름의 실제 판매 상품이 없어서 소싱이 안 된다(2026-08-01 발견, 갑상선방해음식_1/
콩팥국물음식_1/탈모부르는음식_1에서 실제로 "저요오드 국물"/"저나트륨 사골육수"/"저염 국물"로
좁혀놨던 것 수정함). 대신 "저요오드 식품", "저나트륨 식품"처럼 실제로 팔리는 상품군이 걸리는
**식품 카테고리** 단위로 잡을 것 — 구체적인 단일 식재료(호두·블루베리·두부 등)는 그 자체가
검색 가능한 상품명이라 문제없고, "저X 국물/육수/찌개"처럼 형용사+특정 요리 조합만 피하면 된다.

⚠️ **"습관/타이밍" 항목도 `products`에 넣지 말 것** — 같은 날 2차 점검에서 추가로 발견(눈건강
해치는음식_1의 "20-20-20 눈 휴식 습관", 갑상선방해음식_1의 "두유 시간차 섭취")됨: 이런 항목은
좁은 게 아니라 **애초에 식품/상품이 아니라서** 검색해도 매칭되는 상품이 아예 없다.
- 스마트폰 사용법·복용 타이밍처럼 순수 행동 지침이면 → `products`에서 아예 뺀다(대체 식품이
  없으면 억지로 채우지 않는다, 눈건강해치는음식_1은 이렇게 2개만 남기고 정리함)
- 행동 지침이 특정 식품과 묶여 있으면(예: "두유를 약 먹고 2~4시간 뒤에") → 행동 설명은 빼고
  **식품명만** 남긴다("두유 시간차 섭취" → "두유"), "찐 케일·브로콜리"처럼 조리법 접두어가
  붙은 것도 접두어만 떼고 식재료명만 남긴다 — 단, 남는 게 아래 "·(가운뎃점) 금지" 규칙에
  걸리는 두 품목 나열(예: "케일·브로콜리")이면 그중 하나만 남길 것("케일")
- 판단 기준: **"이 문자열을 그대로 네이버/쿠팡에 검색했을 때 실제로 팔리는 상품이 나오는가"**
  하나로 통일 — 국물/육수처럼 요리로 좁힌 경우든, 습관/타이밍처럼 아예 식품이 아닌 경우든
  전부 이 기준에 걸리면 식품명/식품 카테고리로 정리한다.

⚠️ **"형용사+제조형태 상품" 전부 기본으로 "형용사 식품"으로 — 흔한 상품이라고 예외 두지
말 것** (2026-08-01, 3차 발견): "저지방 커피믹스"/"무가당 스무디"를 실제 대시보드에서
써보니 상품 검색이 안 됐다(사용자 확인) — "이 정도면 흔한 상품이라 검색되겠지"라는 판단이
이미 두 번 틀렸다(콩팥국물음식_1의 "저나트륨 사골육수", 이번 커피믹스·스무디). 그래서
**"이 정도는 괜찮지 않을까" 개별 판단을 하지 말고, [저/무/제로 등 건강·다이어트 형용사] +
[구체적 제조 형태 명사(커피믹스·스무디·그릭요거트·크래커·탄산음료·빵·조미료·김치 등)]
조합이면 무조건 "형용사 식품"으로 바꾼다**(예: 저나트륨 조미료 → 저나트륨 식품, 저염김치 →
저염 식품, 제로슈거 탄산음료 → 제로슈거 식품, 통곡물빵 → 통곡물 식품, 무가당 그릭요거트 →
무가당 식품). 유일한 예외는 **"형용사 식품"으로 바꾸면 말이 안 되는 경우**뿐 — 그 형용사가
그 품목에만 의미가 통할 때다: 디카페인(카페인 있는 음료에만 해당 → "디카페인 커피" 유지),
엑스트라버진(올리브오일 압착 등급에만 해당 → "엑스트라버진 올리브오일" 유지), 무아질산
(육가공품 보존료 이슈에만 해당 → "무아질산 햄" 유지, 아래 "·(가운뎃점) 금지" 규칙 때문에
"소시지"는 뺌). 반대로 채소·생선·곡물·베리류처럼
**이미 넓은 식품군 명사**(품목이 아니라 카테고리)에 붙은 형용사는 그대로 둬도 된다("루테인
풍부 녹황색 채소", "오메가-3 등푸른 생선", "저당 베리류") — 이미 카테고리 단위라 더 좁힐
필요가 없다.

⚠️ **`products` 값에 "·"(가운뎃점)로 두 품목을 이어붙이지 말 것**(2026-08-02, "이 기호
사람이 쓸 일 없으니까 배제해"): "케일·브로콜리"/"무아질산 햄·소시지"처럼 문장에서는
자연스러워 보여도, 실제 쿠팡 검색창에 저 문자열 그대로 쳐보는 사람은 없다 — 검색
쿼리로도, `output/product_links.json`(상품명 → 링크) 키로도 못 쓴다. **두 품목을 나열해야
하는 상황이면 쉼표가 아니라 그중 하나만 골라서 남긴다**("케일·브로콜리" → "케일",
"살코기·두부" → "두부") — 어느 걸 남길지는 `card_news_spec.json`에서 그 항목에 실제로
쓰인 `char_file`(대표 캐릭터)과 맞추면 자연스럽다. 기존 topic에서 이 패턴을 발견하면
(`grep -l "·" data/*/platform_captions.json` 등으로 점검) 바로 하나로 정리하고 대시보드
재생성할 것 — 2026-08-02에 갑상선_1/빈혈_1/새치_1/지방간_1/탈모_1/편두통_1 6개에서
실제로 발견·수정함.

⚠️ **카드뉴스(이미지)도 같이 확인할 것** — `products` 수정 시 `card_news_spec.json`의
`items[].name`(카드 제목, 이미지에 실제로 렌더링됨)이나 `body`에 같은 문구가 있는지
`grep`으로 확인한다. 카드 **제목**에 옛 문구가 그대로 있으면 `lib/card_news.py`로 그 카드만
재생성(영상은 재조립 오래 걸리니 카드뉴스 이미지만 먼저 — 영상은 나중 배치로 미뤄도 됨).
단, **본문 문장 속에 자연스러운 설명으로 들어간 경우는 건드리지 않는다** — 예를 들어
"넣은 저지방 커피믹스도 나와 있어요"나 "무가당 그릭요거트에 시나몬을 뿌리면" 같은 문장은
실제 조언 내용이라 `products` 검색 필드와는 성격이 다르다(2026-08-01 확인: 60대주의음식_1
등 6개 topic 점검했지만 카드 제목에 옛 문구가 남아있던 사례는 없었음 — 전부 본문 설명
문장이거나 narration.txt에만 있어서 이미지 재생성 불필요했음).

새 topic 작성 시에도 이 기준으로 `products` 항목을 잡을 것 — session 01(신규 생성)·02(기존
수정) 둘 다 적용.

## 대시보드 카드 — "열기" 버튼은 위/아래 둘 다 (2026-08-02)

각 플랫폼 카드의 "열기(캡션 자동복사) →" 버튼(`.btn-go`)이 원래 카드 상단(`head-actions`)
에만 있었는데, 캡션이 길면 다 읽고 수정한 뒤 다시 위로 스크롤해서 눌러야 했다("이거
링크는 하단에도 추가해도 되지않냐? 시작에 하나 마지막에 하나") — `CARD_TEMPLATE`의
`card-actions`(캡션 textarea 바로 아래, 복사 버튼 옆)에 똑같은 버튼을 하나 더 추가했다.
JS는 `document.querySelectorAll(".btn-go[data-copy-target]")`로 이미 전부 바인딩하므로
버튼을 추가만 하면 되고 별도 JS 수정은 필요 없음. **이후 카드 안에 "위에서만 하고
아래선 못 하는" 액션을 새로 발견하면 같은 패턴(HTML에 하나 더 추가, JS는 querySelectorAll이라
자동으로 바인딩됨)으로 처리할 것.**

## GitHub Pages — 정적 전용

`index.html`(GitHub Pages 루트)은 카드뉴스 스펙을 채우고 미리보기하는 정적 에디터일 뿐 —
실제 이미지 생성(PIL)·TTS·ffmpeg는 전부 로컬 Claude Code 세션에서 실행. Pages 자체에서 서버
로직을 돌릴 수 없다는 제약을 항상 전제로 설계할 것.

- 루트에서 각 topic 대시보드로 바로 갈 수 있게, `lib/dashboard.py`의 `generate()`가 매번
  `output/topics.json`(모든 `output/*/dashboard.html`을 스캔한 목록)을 자동 갱신하고,
  `index.html`이 페이지 로드 시 이 JSON을 fetch해서 상단에 링크 목록을 보여준다
  (2026-07-31, "매번 긴 URL 안 외우고 루트로 들어가면 안되나" 피드백) — 정적 fetch라
  Pages 제약 안에서 동작. 새 topic 추가 시 별도 작업 없이 dashboard 재생성만 하면 자동 반영됨.

## 카드뉴스 — 썸네일 품질

표지는 `lib/card_news.py`의 `make_cover(..., bg_photo_path=...)`로 실물 사진 풀블리드 배경 +
어두운 스크림 + 굵은 흰 타이틀 버전을 기본으로 쓴다(플랫 그라데이션 버전보다 우선). 팩트 카드
상단 라벨은 `card_news_spec.json`의 `"eyebrow"` 필드로 주제별 지정(기본값 `"HEALTH TIP"`) —
특정 주제 이름을 하드코딩하지 않는다.

## 자료 위치

| 종류 | 위치 |
|------|------|
| 재사용 에셋 (일러스트/실물/모션) | `assets_library/{illust,real,motion}/` |
| 주제별 작업 데이터 (대본·스펙·캡션) | `data/<주제>/`(단일 언어) 또는 `data/<주제>/<lang>/`(다국어 — 아래 "글로벌 확장 준비 — 폴더 구조" 절 참고) |
| 주제별 산출물 (카드뉴스·영상·대시보드) | `output/<주제>/`(단일 언어) 또는 `output/<주제>/<lang>/`(다국어) |
| 라이브러리 코드 | `lib/*.py` |
| 채널별 URL(정답 소스) | `data/social_accounts.json` — 새 topic 캡션은 여기서 복사할 것, 예전 topic JSON 복사 금지(오래된 URL 남아있을 수 있음) |
| 비밀키 | `.env` (커밋 안 함), `.env.example` 항상 최신 유지 |
| 실제 SNS 포스팅 기록 | `output/posting_log.csv` — 아래 "포스팅 기록" 절 참고 |

## 포스팅 기록 — CSV로 브라우저 ↔ git 왕복 (2026-08-02)

각 topic 대시보드의 플랫폼별 "완료" 체크는 브라우저 `localStorage`에만 저장돼서
Claude Code(파일시스템 기반)는 전혀 읽을 수 없고, 기기·브라우저가 바뀌면 동기화도
안 된다("브라우저에 기록되는걸 너가 알수가있어?" — 2026-08-02 확인된 한계). 이걸
git으로 추적되는 파일과 왕복시키는 구조를 추가했다:

- 체크할 때마다 `{topic, platform, postedAt}` 형태로 localStorage에 저장됨(예전엔
  단순히 `"1"`만 저장했음 — 그러면 topic/platform 정보가 없어서 못 내보냄)
- 루트 `index.html`의 **"📥 CSV 내보내기"** 버튼 — 브라우저에 쌓인 모든 완료 기록을
  `posting_log.csv`로 다운로드. **다운로드된 파일은 사용자가 직접 `output/posting_log.csv`
  경로로 옮겨서 git에 커밋해야 진짜 기록이 된다** — 정적 사이트라 브라우저가 파일
  시스템에 직접 못 씀
- **"📤 CSV 가져오기"** 버튼 — `posting_log.csv`를 다시 읽어서 브라우저 체크 상태로
  복원. 나중에 SNS API 연동이 되면 이 파일을 Claude가 직접 갱신하고, 사용자가
  가져오기로 브라우저에 반영하는 왕복도 가능해짐
- 새 topic 만들거나 `lib/dashboard.py`의 done-toggle 관련 로직을 고칠 땐 이
  `{topic, platform, postedAt}` JSON 저장 형식을 유지할 것 — CSV 내보내기가
  이 형식에 의존함

⚠️ **위는 플랫폼별 포스팅 기록(CSV)이고, topic 전체가 "완료"인지는 별개
파일이다** — 아래 절 참고, 헷갈리지 말 것.

## topic 완료 여부 — `output/completed_topics.json` (2026-08-02)

루트 `index.html`의 "완성된 콘텐츠" 목록에서 topic 전체(카드뉴스+영상+캡션 전부)가
끝났는지 표시하던 체크박스도 원래 localStorage(`hs_topic_done_*`)였는데, 위 포스팅
기록과 똑같은 한계("로컬스토리지로 하니까 좀 어렵다") — 사용자가 채팅으로 "이거
끝났어" 알려주면 Claude Code가 `output/completed_topics.json`(완료된 topic 폴더명
배열, 예: `["고령_1", "갑상선_1", ...]`)을 **직접 Edit/Write로 갱신**하는 방식으로
바꿨다.

- **브라우저에서는 더 이상 체크 상태를 바꿀 수 없다** — 정적 사이트라 JS가 이
  파일에 쓸 방법이 없어서, 체크박스를 배지(✅/⬜)로 바꿨다(읽기 전용). 완료 표시는
  오직 Claude Code가 이 JSON을 수정해야만 바뀐다.
- topic 폴더명은 `output/topics.json`의 `topic` 필드(= URL 인코딩 전 원본 폴더명)와
  정확히 같은 문자열이어야 매칭된다.
- 사용자가 옛날 topic 이름(예: "돼지감자차_1", "60대_1")으로 완료를 알려주는 경우가
  있는데, 카테고리+번호 리네임 이후엔 실제 폴더명이 다를 수 있다(예:
  돼지감자차_1→혈당_1, 60대_1→고령_1) — `data/<이름>` 폴더가 없으면 바로 실패
  처리하지 말고 `platform_captions.json`의 `title`로 매칭되는 실제 폴더를 찾아서
  반영할 것.
- 이 목록은 "업로드했는지/확인했는지" 등 다른 자동화의 기준으로도 쓸 수 있게
  단순 배열로 유지한다 — 나중에 더 많은 정보(완료 날짜 등)가 필요해지면 그때
  구조를 확장할지 사용자와 먼저 상의할 것(지금은 단순 배열이 목적에 충분하다는
  판단으로 시작).

## 상품 쿠팡 링크 — `output/product_links.json` (2026-08-02)

각 topic 대시보드의 상품 링크 입력칸(`.product-link-input`)도 원래 브라우저
localStorage(`hs_link_<topic>_coupang_<product>`)뿐이었다 — 같은 상품이 여러
topic에 반복 등장해도 매번 새로 입력해야 했고 "로컬스토리지로 하니까 좀
어렵다"는 한계도 동일했다("상품도 너한테 던져야겠다"). 이제 상품명 → 쿠팡
파트너스 링크 매핑을 `output/product_links.json`(git 추적)에 저장하고, `lib/
dashboard.py`의 `_dock_products()`가 대시보드 생성 시점에 이 파일에서 찾아서
`.product-link-input`의 `value`를 미리 채운다(`_load_product_links()`).

- **같은 상품명이면 링크도 그대로 재사용** — "똑같은 제품이면 링크 어차피
  똑같이 들어가면 되니까" — 상품명을 키로 쓰는 단순 `{상품명: 링크}` dict.
- 사용자가 채팅으로 상품+링크 목록을 알려주면 Claude Code가 이 파일에 직접
  추가한다(completed_topics.json/youtube_uploaded.json과 같은 패턴).
- ⚠️ **같은 상품명이 여러 번 들어오면 처음 것만 쓰고 나머지는 무시**("나도 막
  넣다보니 중복 있을수있는데 중복 무시하고 처음넣은거로하면돼 걍 똑같은
  링크에서 생성한거라 어차피 결과값 똑같음") — 사용자가 쿠팡 파트너스에서 같은
  상품으로 링크를 여러 번 생성해도 결국 같은 상품 페이지로 연결되니 구분할
  필요가 없다는 뜻. 새 목록을 추가할 때 파이썬 dict에 순서대로 넣으면 자연히
  이 규칙대로 처리된다(나중 값이 먼저 값을 덮어쓰지 않게, 이미 키가 있으면
  건너뛸 것).
- 대시보드 자체 UI(입력창 직접 타이핑 + localStorage)는 그대로 남아있다 —
  아직 이 JSON에 없는 새 상품을 대시보드에서 바로 입력하는 용도로는 여전히
  쓸 수 있지만, 다음에도 재사용하려면 채팅으로 알려줘서 이 JSON에 등록해야
  한다.
- `.dock-product-row`에 링크가 채워졌으면 `linked` 클래스도 서버 렌더링
  시점에 같이 붙는다(기존 JS가 localStorage 값 있을 때 붙이던 것과 동일한
  시각 표시 — 붙었는지/안 붙었는지 UI에서 바로 구분됨).

## 테스트 (2026-08-01)

`tests/`에 pytest 스위트가 있다 — `pip install -r requirements.txt`로 pytest까지 같이 깔린다.

```bash
source .venv/bin/activate
python3 -m pytest tests/ -v
```

- `test_session_lock.py`, `test_card_news.py`, `test_dashboard.py`, `test_video_assembler.py` —
  각 `lib/*.py` 모듈 단위 테스트. `tests/conftest.py`의 `make_solid_jpg`/`make_tiny_clip`/
  `make_silent_audio` 픽스처로 가벼운 합성 소스를 만들어 쓴다 — **Gemini/Kling/Typecast 같은
  실제 유료 API는 테스트에서 절대 호출하지 않는다.**
- `test_video_assembler.py`에 타이틀카드 fps 불일치(2026-07-31 실제 버그) 회귀 테스트 있음 —
  이 클래스의 버그가 재발하면 바로 잡힘.
- `test_content_rules.py`는 `data/*/`에 있는 **실제 topic 전체**를 매 실행마다 다시 스캔해서
  이 문서에 쌓인 규칙(해시태그 필수, "~대요" 금지, `network`/`rich_paste`/`comment_keyword`
  플래그, 장기명 걱정형 훅 금지, `char_file` 실존)을 위반하는
  topic이 있으면 그 topic명과 함께 바로 잡아낸다 — topic 이름을 하드코딩하지 않으므로 새 topic이
  추가돼도 그대로 커버된다. **새 topic 만들거나 기존 topic 수정한 뒤에는 이 테스트부터 돌려서
  확인하는 습관을 들일 것.**
- 세션 락과 마찬가지로 `session_lock` 테스트는 진짜 `.claude-locks/`를 절대 안 건드리게
  `monkeypatch`로 격리돼있다 — 새 테스트를 추가할 때도 이 원칙 유지할 것.

## 자료수집 — 원본 기사만으로 끝내지 않는다 (2026-07-31)

새 topic을 사용자가 준 기사 URL 하나로만 만들면 내용이 얕아진다("정보가 좀 더 디테일하게
나올 수 있으면 좋겠다, 조금 부족한 감" 피드백, 수면음식_1 사례). 원본 기사(WebFetch)로
소재를 잡은 뒤, 다뤄지는 각 항목(성분/음식 등)마다 **WebSearch로 추가 서칭**해서 구체적인
근거를 보강한다:

- 성분명(사포닌·멜라토닌 등)의 구체적 작용 메커니즘
- 실제 연구 결과(대상자 수, 기간, 수치, 저널명 등 — 있으면 출처까지)
- 재미있는 부가 정보(예: 대추차 끓일 때 생기는 거품의 정체)

이렇게 보강한 내용은 **카드뉴스/숏폼 나레이션에는 짧게 한 줄로 녹여서**(예: "실제 연구에서
평균 39분 더 잤어요" 같은 구체적 수치 한 줄이 "도움될 수 있어요" 같은 막연한 문장보다 훅도
더 세다) 넣고, **네이버 블로그·티스토리 캡션에는 출처·수치까지 풀어서** 담는다 — 카드뉴스는
짧게, 블로그는 디테일하게라는 기존 원칙(아래 "콘텐츠 톤" 섹션)은 그대로 유지하되, 그 안에
들어가는 정보의 밀도 자체를 원본 기사 요약보다 항상 한 단계 더 깊게 만든다는 뜻이다.

이후 모든 topic에 기본으로 적용한다 — 원본 URL 요약만으로 바로 스펙 작성 들어가지 말 것.

## 네이버 블로그·티스토리 — 실제 사례 + Before/After 구조 (2026-08-02)

AI 검색(네이버/구글 AI Report 등)이 정보성 글을 이미 요약해서 보여주다 보니 사실 나열만
하는 글은 클릭을 못 받는다는 문제의식("블로그는 이제 AI 검열을 너무 많이 하기도 하고...
정보성 글만 주면 사람들이 잘 안 볼 것 같아" — 사용자 지적). 차별화 포인트는 "경험"인데,
안 겪은 일을 1인칭으로 겪은 것처럼 쓰면 허위 체험담이 되어 건강 정보 표시광고법상 문제
될 수 있어서(사용자도 동의: "허위 정보를 주는 건 말이 안 되긴 하지") 아래 두 가지로
방향을 좁혔다 — **1인칭 가짜 경험담(예: "제가 직접 겪었는데 나았어요")은 절대 쓰지 않는다**:

1. **실제 사례를 문단 속에 묻지 말고 눈에 띄게 배치** — 지금도 실제 연구 참가자 사례를
   쓰고 있다(예: 갑상선방해음식_1의 "매일 생채소 많이 먹던 여성이 응급실行" NEJM 사례).
   이런 사례가 있으면 문장 하나로 지나가지 말고 별도 문단/눈에 띄는 배치로 강조한다.
2. **Before → After를 수치로 명확하게 구조화** — "수축기 혈압이 6.4% 상승했어요"처럼
   서술문에 녹이지 말고, "섭취 전 OOO → 섭취 30분 후 OOO"처럼 전후 비교가 한눈에
   보이게 짚는다. 반드시 실제 연구 수치 기반(자료수집 절 원칙과 동일 — 지어내지 않음).

**참고 사례**(2026-08-02 실제 적용): 돼지감자차_1 네이버 블로그 캡션에 "동물 실험에서는
실제로 확인된 변화도 있어요 — 당뇨를 유도한 쥐에게 돼지감자 복합물을 먹였더니, 대조군
보다 혈당이 45.6~50% 넘게 떨어진 결과가 나왔어요. 다만 이건 동물 실험 결과라 사람 대상
임상시험 근거는 아직 부족해요"를 추가함 — 실제 수치를 명확히 제시하면서 사람 대상 근거의
한계도 정직하게 함께 밝히는 패턴.

**적용 범위**: 네이버 블로그·티스토리(rich_paste 긴 캡션) 우선. 카드뉴스/짧은 캡션은
기존처럼 핵심 수치 한 줄로 압축(위 "자료수집" 절 원칙 유지) — 이 구조를 캡션 전체에
욱여넣지 않는다. 기존 topic 소급 적용은 필수 아님 — 새 topic부터 기본 적용하고, 기존
topic은 다른 이유로 캡션을 손볼 때(예: 1000자 미달 보강) 같이 반영하면 된다.

## 작성 순서 — 블로그(마스터) 먼저, 숏폼·카드뉴스는 거기서 압축 (2026-08-02)

지금까지는 나레이션·카드뉴스·블로그 캡션을 사실상 각자 따로 썼는데, 이제부터는 **작성
순서 자체를 고정**한다("내용 꽉꽉 채워서 만들어놓은 그 다음에 그걸 기반으로 유튜브에는
디테일 잡아서... 그렇게 가야될것같은데" — 사용자 확정):

1. **자료조사부터 다 끝낸다** — 위 "자료수집" 절 원칙대로 원본 기사 + WebSearch 보강,
   "실제 사례 + Before/After" 절 원칙대로 실제 연구·사례까지 전부 모은다
2. **네이버 블로그 캡션을 먼저 완성한다** — 1000자 이상, 모아둔 자료를 전부 풀어서 담는
   이 topic의 **마스터 콘텐츠**. 카드뉴스나 숏폼보다 먼저 쓴다
3. **narration.txt(숏폼)·card_news_spec.json(카드뉴스)은 이 마스터에서 뽑아 압축**해서
   만든다 — 별도로 다시 조사하거나 처음부터 새로 쓰지 않고, 블로그 원고에서 핵심 문장·
   수치를 발췌해서 "훅 중심, 효능 우선"(아래 "콘텐츠 톤" 절) 기준에 맞게 짧게 다듬는다
4. **티스토리는 네이버 블로그를 그대로 복붙하지 않는다** — 아래 "네이버 블로그·티스토리
   동일 원고 금지" 절 참고
5. **쓰레드는 마스터에서 짧게 뽑되 완전히 다른 형태로 재구성**한다 — 아래 "쓰레드 —
   별도 포맷" 절 참고

**이 순서를 지키면 좋은 점**: 플랫폼마다 따로 조사하다 정보가 미묘하게 어긋나는 걸
막을 수 있고, 정보 밀도 원칙(블로그는 깊게 / 숏폼·카드뉴스는 짧게)이 "따로 신경 써서
지키는 것"이 아니라 압축하는 과정에서 자연히 지켜진다.

**적용 범위**: 새 topic부터 — 기존 topic 재작업 시에도 참고할 수 있지만 소급 적용
필수는 아님.

### 네이버 블로그·티스토리 — 동일 원고 금지 (2026-08-02)

실제로 확인해보니 지금까지 두 플랫폼 캡션이 글자 하나 안 틀리고 100% 동일했다(예:
췌장_1 — 2158자 완전 일치). 네이버 통합검색이 티스토리 등 외부 블로그도 함께 노출하는
구조라 똑같은 글이 두 군데 있으면 "유사 문서"로 판정돼 한쪽(보통 후발/약한 도메인) 노출이
억제될 수 있다는 리스크가 확인됨.

- 같은 자료조사·같은 핵심 정보를 담더라도 **문장 표현·도입부·세부 설명을 서로 다르게
  재작성**한다 — 다시 조사할 필요는 없고 "다르게 쓰기"만 추가하면 됨(리서치 재사용,
  표현만 변경이라 비용이 크지 않음)
- ⚠️ **번호("NN · 소제목") 순서 자체는 바꾸지 말 것**(2026-08-02, 손발저림_1 작업 중
  발견): 처음엔 "문단 순서도 다르게"로 적었는데, 실제로 해보니 이 번호가 카드뉴스
  이미지 파일명(`<topic>_NN_...jpg`)과 그대로 대응돼서, 순서를 바꾸면 번호와 실제
  삽입해야 할 이미지가 어긋난다. 순서는 그대로 두고 **문장 자체를 처음부터 다시
  써서** 표현을 다르게 만들 것 — 도입부(첫 문단)와 각 항목 설명의 문장 구조·예시·
  디테일을 바꾸면 순서가 같아도 충분히 다른 문서가 된다
- 최소 기준: 첫 문단(도입부)은 반드시 다르게 쓰고, 각 항목 설명도 문장 구조·예시를
  바꿀 것 — 단순히 어미만 바꾸는 수준(예: "~해요"→"~합니다")은 부족함
- 네이버 블로그 최소 1000자 규칙(위 절)은 티스토리에도 동일하게 적용 — 재작성해도
  분량은 유지할 것
- **적용 범위**: 새 topic부터. 기존 topic은 급하지 않으면 소급 재작업 필수는 아님

### 쓰레드 — 별도 포맷 (2026-08-02)

지금까지 쓰레드 캡션이 카드뉴스/블로그 내용을 그냥 축약한 산문 문단이었는데, 쓰레드는
텍스트 중심 플랫폼 특성상 다른 구조가 더 반응이 좋다는 조사 결과를 반영한다:

- **이미지 1장 첨부**(전혀 안 붙이는 것보다 도달률이 유의미하게 높다는 근거 확인)
- **이모지 리스트형 구조**(✅📌 등)로 — 긴 줄글 문단보다 가독성 우위
- **완결된 결론보다 질문형/열린 마무리**로 끝내서 답글(참여) 유도 — "~일 수 있어요"로
  끝내지 말고 "여러분은 어떠셨나요?" 류 질문으로 마무리
- **분량은 50~200단어(한글 기준 대략 100~350자) 스윗스팟** — 지금처럼 짧은 건 유지하되
  구조 자체를 리스트+질문형으로 바꿀 것
- 카드뉴스/블로그를 요약하는 게 아니라 **마스터 콘텐츠에서 가장 흥미로운 포인트 1~2개만
  뽑아서** 새로 구성하는 방식에 가까움
- **적용 범위**: 새 topic부터, 기존 topic 소급 필수는 아님

## 콘텐츠 톤 — 훅 중심, 효능 우선 (2026-07-30)

숏폼 나레이션(`narration.txt`)은 백과사전식으로 다 나열하면 안 된다 — "이게 뭔지 → 효능 →
성분 → 사용법 → 주의사항" 순서로 늘어놓으면 훅도 없고 늘어져서(2026-07-30 돼지감자차_1이
63초까지 늘어졌던 사례) 실패다. 대신:

- 첫 문장부터 핵심 효능/훅으로 시작(정의·자기소개부터 깔지 않기)
- 전체 길이는 짧게(20~30초대 목표) — 디테일한 정보는 블로그용 캡션(`platform_captions.json`의
  네이버 블로그/티스토리 항목)에만 담고, 숏폼 나레이션과 카드뉴스는 핵심만
- ⚠️ **영어(및 다른 비한국어) topic — 최대 45초 하드 캡**(2026-08-03, "영어 영상 너무
  길게 만들길래 규칙 만들라했는데 규칙 안넣어놨나? 1분 30초는 너무길잖아... 45초정도로"):
  위 "20~30초대 목표"는 원래 한국어 기준으로 잡은 소프트 타깃이라 강제력이 없었다 — 실제로
  글로벌 확장 1차 배치에서 영어 topic 3개가 전부 80~96초(가슴쓰림_1/en 파일럿 포함하면 4개
  전부)로 나왔는데도 아무도 안 걸러냈다. 원인 추정: 영어는 같은 정보량이라도 음절·단어 수가
  더 많고, "번역 금지·독립 리서치" 원칙 때문에 원인 항목이 한국어판보다 늘어나는 경우가 있어서
  (예: 원인 3개 + 각 대안 설명까지) 문장이 계속 늘어난다. **새 topic(모든 언어) 작성 시
  narration.txt를 TTS 넘기기 전에 반드시 예상 길이를 확인하고 45초를 넘으면 항목 수를
  줄이거나(3개 원인 중 가장 약한 것 하나 빼기) 문장을 더 압축할 것** — "20~30초대"는 여전히
  목표치로 유지하되, 45초는 절대 넘기지 않는 상한선이다. narration.mp3 생성 후
  `ffprobe -v error -show_entries format=duration -of csv=p=0 <file>`로 실측 길이를 확인하고
  45초 초과면 그 자리에서 narration.txt를 다시 줄여 TTS를 재생성할 것 — "일단 만들고 나중에
  확인"이 아니라 생성 직후 바로 체크.
- 카드뉴스도 같은 원칙 — 효능/핵심 정보 카드를 앞쪽에, 사용법(끓이는 법 등) 같은 부차적 정보는
  뒤로 배치

### 문장 어미 — "~대요"(전언체) 금지 (2026-07-31)

"줄었대요", "쉬었대요"처럼 전해 들은 말투(~대요)는 출처가 있는 연구 결과를 말할 때도
신빙성이 없어 보인다는 지적("시험 결과가 있었다 차라리 이정도로 가야지"). 실제 연구·수치를
인용할 때는 **"~것으로 나타났어요" / "~결과가 있어요" / "~쉬었어요"**처럼 사실을 진술하는
어투로 쓴다. 새 topic 작성 후에는 "대요"로 끝나는 문장이 있는지 항상 검색해서 확인할 것:

```
grep -rn "대요" data/<topic>/
```

### 나레이션 — "무조건 다 끊을 필요는 없어" 류 헷지 문장 금지 (2026-08-02)

원인 설명 끝에서 마무리(실천 팁)로 넘어갈 때 "그래도/근데/무조건 다 끊을
필요는 없어"로 시작하는 헷지 문장을 거의 모든 topic이 습관처럼 넣고 있었다
("그 ~를 다 ~할 필요는 없어 하는거 필요없다고 너한테말했나? 이거 왜 계속
넣는거야? 그거없이 바로 결과로가라니까?" — 이미 하지 말라고 한 적 있는데
계속 반복됨). **이 헷지 문장 없이 원인 설명 바로 다음에 실천 팁(대체재)으로
곧장 넘어갈 것** — 예를 들어 "그래도 무조건 끊을 필요는 없어. 미역국은
하루 한두 번으로 줄이고..." 가 아니라 그냥 "미역국은 하루 한두 번으로
줄이고..."로 바로 시작한다. 새 topic 작성 후 아래로 검색해서 확인할 것:

```
grep -rn "필요는 없어\|필요없어" data/<topic>/narration.txt
```

⚠️ **적용 범위는 새 topic부터** — 이미 이 패턴이 들어간 기존 topic들은
사용자가 별도로 정리를 요청하기 전까지 소급 수정하지 않는다.

### 나레이션 — 항목 설명 전에 바로 "~~, ~~, ~~야." 로 먼저 정리 (2026-08-02)

원인이 여러 개(보통 3개)일 때, 각 항목을 하나씩 설명하기 전에 **항목 이름만
먼저 쭉 나열하는 문장이 무조건 있어야 한다** — "야 대신에 ~~ 때문일 수
있어 <-- 라고 보통 멘트 치잖아? 그럼 그 다음에 바로 ~~, ~~, ~~ 때문이야
이런식으로 간다음에 그 각각에 대한 설명을 넣는거로 하자. 아니면 인지가
잘안돼." → "아 ~~ 때문일 수 있어 다음에 ~~ 때문이야가 아니고 바로 ~~야.
로 하자 때문이 없애자" → "아 보니까 꼭 ~ 때문일수 있어로 시작을하는건
아니구나 어찌됐건 어떤 항목에 대해 설명하기 전에 정리는 무조건 필요해
바로 ~~, ~~, ~~야 이게 들어가야한다. 라는게 요지야".

**정리하면**: 원인을 암시하는 훅 문장이 "~~ 때문일 수 있어"로 시작하든
다른 형태든 상관없이, 각 항목을 하나씩 설명하기 시작하기 전에 반드시
항목 이름만 나열하는 문장이 들어가야 시청자가 뭘 설명할지 먼저 인지할 수
있다. 이 나열 문장은 **"~~, ~~, ~~야."로 끝내고 "때문이야"를 쓰지
않는다** — "때문"이 붙으면 이미 이유 설명처럼 들려서 뒤에 이어지는 각
항목별 상세 설명과 중복된 느낌을 준다. 예:

```
나쁜 예: 라면, 맥주, 흰쌀밥 때문이야. 먼저 라면은...
좋은 예: 라면, 맥주, 흰쌀밥이야. 먼저 라면은...
```

⚠️ 적용 범위 미정 — 이 규칙이 확정된 시점에는 새 topic 작성 기준으로
문서화하지만, 기존 topic 소급 여부는 아직 사용자가 별도 지시하지 않았으니
먼저 묻지 말고 사용자가 요청할 때 처리한다(위 헷지 문장 규칙과 달리 이번엔
소급 요청이 없었음).

### 나레이션 — 훅 문장 패턴 다양화 필수 (2026-08-03)

**WHY**: 실제로 여러 topic의 첫 문장을 나란히 뽑아보니 "[증상 나열]하다면,
이 세 가지부터 [확인/줄여/의심]해봐." 패턴이 토씨만 다르고 사실상 전부
동일했다 — "그러니까 그런거 패턴을 다양화하고 랜덤리하게 넣어야한다고
몇번이나 강조했었는데 안되눙.." (반복해서 지적했는데도 실제로 안 지켜지고
있던 상태였음, 2026-08-03). 짧은 영상은 "다음에 뭐가 나올지 궁금함"이
이탈 방지의 핵심 동력인데, 훅 구조가 매번 같으면 반복 시청자가 첫 문장만
듣고 뒤 내용을 예측해버려서 궁금증 자체가 안 생긴다 — 실제로 릴스 초반
2초 이탈률이 높은 문제와 직접 연결된다고 판단.

**새 topic 나레이션 작성 시, 직전에 쓴 topic 몇 개의 훅 구조를 확인하고
반드시 다른 패턴으로 쓸 것.** 아래 5가지를 예시 골격으로 로테이션한다
(그대로 빈칸 채우기 하지 말고, 이 구조적 아이디어만 참고해서 topic마다
자연스럽게 새로 쓸 것):

- **A. 증상 나열형**(기존 기본형 — 과다 사용 주의): "[증상 나열]하다면,
  이 세 가지부터 확인해봐." — 계속 이것만 쓰지 않는다.
- **B. 원인 선공개형**: "사실 [의외의 원인]이 [증상]을 부르고 있을 수
  있어." — 반전을 첫 문장에 바로 던짐.
- **C. 직접 질문형**: "혹시 매일 먹는 그거, [증상]이랑 관련 있는 거 알아?"
- **D. 구체적 시나리오형**: "어제도 [행동]했는데 오늘 [증상] 심해졌다면"
  — 훨씬 구체적인 장면으로 시작해서 몰입도를 높임.
- **E. 통계 선공개형**: "[증상] 겪는 사람 10명 중 O명이 모르는 게 있어."

⚠️ 적용 범위 — **새 topic부터** 의식적으로 로테이션. 기존 topic들의 훅을
전부 다시 쓰는 소급 작업은 오디오·영상 재생성까지 필요한 큰 작업이라
사용자가 별도로 요청하기 전까지 진행하지 않는다(2026-08-03, "일단 규칙
박아봐 더 수정할게 있나싶어서" — 규칙 확정이 우선, 소급 여부는 나중에
따로 결정).

## 콘텐츠 QA — LLM 리뷰 패스 (2026-08-03)

**WHY**: 손발저림_1 카드뉴스의 "맥주 대신 무알코올 맥주로 바꿔보세요" 같은
문장을 사용자가 직접 발견하고 지적함 — "이렇게 문맥에 맞지 않는 이상한 말들
얼마나 많겠어? 이런것들 주기적으로든 뭐든 전수 조사하면서 확인해서 수정할만한
그런게 필요하다고... 그래서 TC가 필요하다느니 뭐라느니 한건데.... 시험하는
방법론에 대해 고민해야해". 이런 문제는 문법·구조는 멀쩡해서(해시태그 누락,
헷지 문장, 중복 링크 같은) 기존 규칙 기반 검사로는 못 걸러진다 — "이 문장이
맥락상 말이 되는가"를 판단해야 하는 문제라서, `lib/content_review.py`가
Gemini API(`gemini-flash-latest`, `GEMINI_API_KEY` — `lib/gemini_illust.py`와
같은 키 재사용, 이미지 생성용 모델과는 별개)로 narration.txt + card_news_spec.json
(items[].name/body, closing.headline/tip/cta만 — char_file 등 비텍스트 필드는
제외)을 리뷰해서 다음을 찾는다:

1. 논리적으로 앞뒤가 안 맞는 문장
2. 문법은 멀쩡하지만 맥락상 김빠지거나 성의없는 대체/팁 제안(예: "맥주 대신
   무알코올 맥주로 바꿔보세요"처럼 동어반복적인 경우)
3. 과장되거나 근거 없어 보이는 의학적 주장
4. 앞 문장과 모순되는 내용

**사용법**:
```
python3 -m lib.content_review <topic> [lang]  # topic 하나만 리뷰(lang 생략 시 한국어)
python3 -m lib.content_review --all            # data/ 밑 모든 topic 배치 리뷰(한국어 전용, 일회성 전수 감사용)
```

⚠️ **글로벌 확장용 `lang` 파라미터(2026-08-03)** — "내가 한국인이다보니 문맥이랑
그런것들 스스로 검증이 안되잖아? ... 너 스스로 검증하는 시스템" 요청으로 추가.
사용자는 한국어 화자라 영어·스페인어 등 다른 언어 콘텐츠는 직접 못 읽지만, 이
리뷰는 LLM이 하는 거라 언어와 무관하게 똑같이 돌릴 수 있다는 점에 착안 —
`review_topic(topic, lang="영어")`처럼 언어명을 주면 그 언어 원어민 편집자
기준으로 검토하고, **비한국어일 때만 5번째 기준이 추가**된다: "언급된 해결책
재료·식품이 이 언어권/지역에서 실제로 흔히 구할 수 있는지"(위 "글로벌 확장
준비" 절의 "번역 금지, 지역별 독립 리서치" 원칙과 직결 — LLM이 초안 쓰는
과정에서 그 나라에 없는 재료가 슬쩍 들어갈 위험을 잡기 위함). `issue` 설명은
언어와 무관하게 항상 한국어로 반환하도록 프롬프트에 명시(quote는 원문 그대로) —
한국어 화자인 사용자가 결과를 읽을 수 있어야 하므로. 영어 합성 예시로 실제
검증함(과장된 의학적 주장 + 설명 없는 낯선 재료 둘 다 정확히 잡아냄).

⚠️ **새 topic은 narration.txt·card_news_spec.json을 다 쓴 뒤(콘텐츠 완성
시점) 이 리뷰를 무조건 통과시킨 다음에 완료로 간주한다**(2026-08-03, "1번
무조건 해야될거같고" — 사용자 확정) — 위 "완료 기준" 체크리스트에 다른 항목
(빈/로딩/에러 상태, 해시태그 등)과 동급으로 추가된 필수 단계. 플래그된 항목은
자동으로 고치지 않고 사람이 보고 판단할 내용으로 보고만 한다(오탐 가능성이
있는 LLM 판단이라 자동 수정은 위험) — 문제가 실제로 있으면 그 문장을 다시
써서 고친다.

⚠️ **기존 topic 전수 감사는 일회성으로 이미 진행함**(2026-08-03, "2번
진행해줘") — `--all`로 전체를 훑어서 발견된 문제는 별도로 사용자에게 보고.
이후 새로 감사가 필요하면 같은 명령을 다시 돌리면 됨(멱등, 데이터 변경 없이
읽기만 함).

### 해시태그 — 플랫폼별로 빠짐없이 (2026-07-31)

`platform_captions.json`의 각 플랫폼 캡션에는 전부 해시태그가 들어가야 한다 — 한 곳이라도
빠지면 발견하기 전까지 그 플랫폼만 계속 해시태그 없이 나간다(2026-07-31, 쓰레드 캡션에서
실제로 누락된 채 지나간 사례 발견). 새 topic 캡션 작성 후에는 매번 아래로 전체 플랫폼에
해시태그가 있는지 확인할 것:

```
python3 -c "
import json
spec = json.loads(open('data/<topic>/platform_captions.json').read())
for p in spec['platforms']:
    print(p['name'], '->', '#' in p['caption'])
"
```

### 제목/훅 카피 — "정보 서술형" 금지, "문제 겪는 사람 호출형"으로 (2026-07-31)

"이눌린 하나로 식후 혈당까지 잡을 수 있을까?" 같은 궁금증 유발형 질문 제목은 그저 그런
서술이라 후킹이 안 된다(2026-07-31 돼지감자차_1 재지적) — **이 주제로 문제를 겪고 있는
사람을 화면 앞에서 바로 호출하는 형태**로 써야 썸네일만 보고도 "내 얘기다" 싶어서 클릭한다.

- 패턴: "[증상/고민] 있는 분들 주목! - [주제] 이야기" (예: "혈당 관리에 어려움이 있는 분들
  주목! - 돼지감자차 이야기")
- 카드뉴스 제목뿐 아니라 영상 제목/배너, 각 플랫폼 캡션 첫 줄까지 전부 이 톤으로 통일한다
  (`title` 필드 + 각 플랫폼 `caption` 첫 줄)
- 본문 내용도 "이게 뭔지" 나열이 아니라, 그 문제를 겪는 사람 기준으로 실제 도움되는
  포인트(효능·주의사항)를 우선한다
- **이번 주제 한정이 아니라 이후 모든 주제에 기본으로 적용**할 것 — 매번 다시 지적받지 않게
  새 topic 작업 시작할 때 이 패턴부터 적용해서 초안 작성

### 훅 다음엔 "원인" 먼저, 그다음 "해결책" — 바로 해결책으로 넘어가지 말 것 (2026-08-01)

훅(증상 호출) 다음에 곧바로 해결책(추천 음식·효능/주의할 음식 목록)으로 넘어가면 "왜 이 증상이
생기는지" 설명이 빠져서 내용이 빈약하다는 지적(2026-08-01, 블루베리_1에서 처음 지적된 뒤 기존
전체 topic에 소급 적용함) — **훅 → 원인 → 해결책** 3단 구조를 항상 지킨다.

- 훅 카드/문단 바로 다음에 "왜 이런 문제가 생기는지" 일반적인 메커니즘을 설명하는 카드를 추가로
  넣는다(카드뉴스 기준 표지 다음, 두 번째 장). 이름은 "왜 [증상]이 생길까요" 식으로 짧게.
  각 음식 항목의 개별 효능/위험성 설명과는 별개로, 증상 자체의 원인(예: 정제 탄수화물→혈당
  스파이크→인슐린 저항성, 나트륨 과다→체내 수분 저류→부종/콩팥 여과 부담 등)을 한 번 정리해준다
- narration.txt에도 이 원인 설명을 훅 문장 바로 다음, 첫 음식/해결책 언급 전에 문단으로 추가한다
- platform_captions.json의 네이버 블로그·티스토리(이미지 삽입 지점 있는 긴 캡션)에도 표지 다음
  원인 문단을 넣고, 그 뒤 모든 이미지 번호를 하나씩 밀어준다. 다른 플랫폼(인스타·유튜브·틱톡 등
  짧은 캡션)에도 원인 한두 문장을 앞부분에 자연스럽게 녹인다
- **새 topic을 처음부터 만들 때도 이 구조로 초안을 짤 것** — 나중에 소급 적용하는 게 아니라
  처음부터 훅→원인→해결책 순서로 작성한다

#### ⚠️ "장기/증상 이름" 걱정형도 약하다 — 실제 증상으로 서칭해서 호출할 것 (2026-07-31)

"콩팥 건강 걱정되는 분들 주목!", "혈관 건강 걱정되는 분들 주목!"처럼 **장기 이름 + 걱정형**도
여전히 약한 훅이다 — "내가 콩팥이 문제가 있는지 없는지 걱정해서 들어올 사람은 없다"(사용자
피드백). 사람들은 장기 단위로 스스로를 걱정하지 않고, **구체적인 증상**을 겪을 때 그게 뭔지
찾아본다. 그러므로 새 topic 작업 시 주제가 특정 장기/질환과 관련 있다면:

1. WebSearch로 "그 장기/질환이 나빠지면 실제로 어떤 증상이 나타나는지" 먼저 서칭한다
   (예: 콩팥 → 부종·소변 거품·피로감, 혈관 → 손발 저림·차가움·어지러움)
2. 그 구체적 증상을 훅에 바로 쓴다: "[구체적 증상] 있다면 주목!" (예: "얼굴·발이 자주 붓고
   소변에 거품이 보인다면 주목!", "손발이 자주 저리거나 차갑고 잘 붓는다면 주목!")
3. 장기 이름이나 "건강 걱정되는"류 표현은 훅에서 뺀다 — 주제명(부제)에서만 장기/음식
   카테고리를 밝혀도 충분하다
4. 훅이 바뀌면 narration.txt 첫 문장, card_news_spec.json의 title, platform_captions.json의
   title과 모든 플랫폼 caption 첫 줄까지 전부 같이 바꿔야 한다 — 한 곳만 바꾸면 톤이
   섞인다(콩팥국물음식_1/60대주의음식_1에서 실제로 전체 동기화 작업 필요했음)
5. **영상 첫 장면(썸네일)·카드뉴스 표지도 예외 없이 같은 훅**을 써야 한다(2026-07-31,
   "숏츠 첫장면 썸네일이나 카드뉴스 첫번째거에도 동일하게 들어갈 수 있게" 요청) — 카드뉴스
   표지는 `card_news_spec.json`의 `title`을 그대로 쓰니 자동으로 맞지만, 영상 쪽
   `video_assembler.assemble()`의 `title_card_text`는 **별도 인자로 직접 넘기는 값**이라
   깜빡하고 옛날 훅이나 다른 문구를 넣기 쉽다 — 영상 조립 호출 시 `title_card_text`에
   card_news_spec.json의 title(마지막 줄 제외)과 동일한 문구를 넣을 것

**정리**: 이 규칙이 요구하는 훅의 정의는 "그 문제 때문에 실제로 나타나는 현상 — 그 현상을
겪는 사람이 원인을 몰라서 실제로 검색해볼 만한 것"이다. 장기/질환명이 아니라 이 기준으로
서칭·작성한다. 위 두 topic(콩팥국물음식_1, 60대주의음식_1)은 이미 이 규칙 적용해서 올려둔
상태이므로 재작업 불필요 — **이 규칙은 지금 이 시점 이후 새로 시작하는 topic부터 적용**한다.

#### ⚠️ 훅 어미를 "주목!"으로만 고정하지 말 것 — 문장 형태를 topic마다 다르게 (2026-08-01)

"[구체적 증상] 있다면 주목!"이 훅의 **내용 기준**(구체적 증상 호출)으로는 여전히 맞지만,
문장이 매번 "~주목!"으로 똑같이 끝나면 채널 전체가 판박이로 보여서 "이런 느낌 게시물은
패턴 다 아니까 무시해야겠다"는 학습 반응이 생길 위험이 있다("패턴이 너무 똑같으니까...
무조건 나겠구나 라고 생각하지 못하게" — 사용자 지적). **증상 호출이라는 원칙은 유지하되,
문장의 어미/형태를 아래처럼 topic마다 바꿔가며 쓴다**:

1. **호출형(기존)**: "~있다면 주목!" — 예: "손발이 자주 저리거나 차갑고 잘 붓는다면 주목!"
2. **질문형**: "~이신가요?" — 예: "자꾸 속이 더부룩하고 신물이 올라오시나요?"
3. **원인 예고형**: "~고 있다면, 이유가 있어요" — 예: "자도 자도 피곤하고 있다면, 이유가
   있어요"
4. **경고/중단 유도형**: "~라면 이제 그만" / "~하고 계셨다면 잠깐" — 예: "자기 전 이 습관,
   하고 계셨다면 잠깐"
5. **저장 유도형**: "~라면 저장부터 하세요" — 예: "이 증상 있다면 저장부터 하세요"
6. **반전형**: "~그거, 사실 [의외의 원인] 때문이에요" — 예: "자꾸 붓는 얼굴, 사실 이 음식
   때문일 수 있어요"
7. **체크리스트형**: "아래 중 하나라도 해당되면" — 예: "손발 저림·부종·피로감, 하나라도
   해당되면"
8. **긴급/시급성형**: "~라면 지금 확인하세요" — 예: "요즘 부쩍 붓는다면 지금 확인하세요"
9. **혼잣말/공감형**: "나만 그런가 싶었다면" — 예: "요즘 유독 피곤한 게 나만 그런가
   싶었다면"
10. **비교/대조형**: "다른 게 아니라 ~ 때문일 수 있어요" — 예: "나이 탓이 아니라 이 음식
    때문일 수 있어요"
11. **직접 화법(대화체) 질문형**: "혹시 ~하지 않나요?" — 예: "혹시 자고 일어나도 개운하지
    않나요?"
12. **숫자/통계 제시형**: "[N명 중 1명]이 겪는다는 ~, 혹시 나도?" — 예: "성인 3명 중 1명이
    겪는다는 이 증상, 혹시 나도?" (수치는 실제 리서치로 뒷받침된 것만 쓸 것 — 자료수집
    절 "원본 기사만으로 끝내지 않는다" 규칙과 연결)

- 매 새 topic 시작할 때 **직전에 쓴 topic 2~3개와 다른 형태**를 고른다 — 어떤 걸 고를지
  코드에 고정하지 않고, `data/` 최근 topic 몇 개의 `title` 어미를 먼저 확인하고 다른 걸
  선택할 것. 패턴이 12개나 있으니 매번 같은 1~2개(특히 1번 호출형)로 되돌아가지 않도록
  의식적으로 다른 번호를 골라볼 것
- 어떤 형태를 쓰든 위 "장기/증상 이름 걱정형" 금지, "구체적 증상으로 호출" 원칙은 그대로
  적용된다 — 바뀌는 건 문장 끝맺음뿐, 증상을 구체적으로 짚어야 한다는 내용 기준은 동일
- 위 "훅이 바뀌면 narration/spec/캡션 전부 동기화" 규칙도 그대로 — 문장 형태를 topic
  하나 안에서 섞으라는 게 아니라, **topic 간에** 다양하게 가져가라는 뜻이다

## 엔딩 CTA — 구독/좋아요/팔로우 (2026-08-02)

카드뉴스 마무리 카드와 숏폼 영상 맨 끝에 구독·좋아요·팔로우 요청 문구를 기본으로
넣는다("카드뉴스랑 숏폼 영상 마지막에 구독, 좋아요, 팔로우 요청하는 글도 추가하자").

- **카드뉴스**: `card_news_spec.json`의 `closing.cta`를 `"더 많은 건강정보가 궁금하다면
  구독·좋아요·팔로우 해주세요"`로 통일(기존 "팔로우"만 있던 문구에서 확장). 새 topic도
  이 문구를 기본으로 쓸 것 — 굳이 topic마다 다르게 쓸 필요 없음(2026-08-01 정한 "훅
  어미는 다양화" 규칙과는 별개 — 이건 마무리 CTA라 고정 문구로 통일하는 쪽이 낫다는
  판단).
- **숏폼 영상**: `lib/video_assembler.py`의 `assemble()`이 `end_card_duration`(기본
  2.0초, 항상 켜짐)만큼 영상 맨 끝에 제목 카드와 같은 스타일의 CTA 카드를 자동으로
  붙인다 — 호출부에서 별도 인자를 안 넘겨도 `DEFAULT_END_CARD_TEXT` 상수가 자동 적용됨.
  끄고 싶으면 `end_card_duration=0`. `--title-card-char`를 넘겼으면 엔딩 카드도 같은
  캐릭터를 자동 재사용한다(`end_card_char_path` 따로 안 넘겨도 됨).

⚠️ **기존 영상(.mp4)도 소급 재작업 대상(2026-08-02 정정)** — 처음엔 매니페스트 없이
원래 조립 인자(캐릭터 1명/여러 명 motion_schedule 여부, bg_style, bg_color 등)를
재구성하는 위험 때문에 소급 적용을 보류했으나, 사용자가 "저거 잘못들어간거야"로
정정 — **기존에 이미 만들어진 topic들의 .mp4도 칠판 배경·배너 실사진·엔딩 CTA를
전부 반영해서 다시 조립한다.** 조립 인자는 `data/<topic>/card_news_spec.json`
(품목별 char_file → `assets_library/motion/<char>_motion.mp4` 매핑, narration.srt
문장 시작 시각으로 motion_schedule 구간 계산), `platform_captions.json`(자연어
title), `assets_library/real/`(대표 품목 실사진 → `--title-banner-photo`)에서
전부 유도 가능 — 새 topic 표준 호출 예시와 동일한 방식으로 재구성할 것.

표지는 실사진 배경(블러 처리 포함) 버전까지 거쳤다가, 최종적으로 **숏폼 영상 맨 앞
제목 카드와 완전히 동일한 스타일**로 확정됐다("카드뉴스 첫장도 숏폼 영상 썸네일이랑
똑같이 그냥 가져가자" 피드백) — 사진도 캐릭터도 주제 태그도 없이, 단색 ACCENT 배경 +
문제 제기 훅 카피만 큼직하게. `card_news.py`의 `make_cover_titlecard(hook_text, out_path)`
사용 — `video_assembler.py`의 `_make_title_card_png`와 같은 로직(단색 배경, 굵은 흰 글자,
단어 단위 줄바꿈)을 카드뉴스 캔버스(1080x1350)에 맞게 재사용한 것. 새 주제는 표지 생성 시
이 함수를 기본으로 쓰고, `_make_cover_photo`(사진 블러 버전)는 더 이상 기본이 아님.
카드뉴스 2번째 장부터(팩트카드·마무리)는 기존 그라디언트 배경 스타일 그대로 유지.

⚠️ **표지 배경 캐릭터가 topic 간에 겹치지 않게 확인할 것**(2026-08-01, "썸네일 자체가
아예 똑같은 애들도 좀 있잖아" 지적): `make_cover_titlecard`의 흐린 배경은 `items[0]`
캐릭터를 기본으로 쓰는데, 카페인·탄산음료처럼 여러 topic이 공통으로 다루는 소재라
`items[0]`이 우연히 같은 캐릭터로 겹치면(실제로 커피 5개, 탄산음료 5개 topic이 전부
같은 표지로 겹쳤던 사례) 표지가 통째로 동일해 보인다. **새 topic 작성 시 `data/*/
card_news_spec.json`의 기존 `items[0].char_file`들을 먼저 훑어서 겹치는 게 있으면**,
`card_news_spec.json`에 `"cover_char_file": "<파일명>.jpg"`를 추가해서 그 topic이 이미
가진 다른 item의 캐릭터로 표지를 바꾼다(없으면 기존처럼 `items[0]`으로 자동 폴백) —
새 캐릭터를 만들 필요 없이 그 topic 안의 다른 아이템 캐릭터를 재활용하면 된다. 이미
있는 topic들의 중복은 2026-08-01에 일괄 정리했음(`f15b8c0`, `9dfe023`).

⚠️ **`cover_char_file`로도 못 피하면(그 topic이 쓸 수 있는 다른 아이템도 이미 다
다른 topic에 쓰이고 있는 경우) 배경 색상도 바꿀 것**(2026-08-01, "그렇게 하더라도
언젠가 겹치게 될 확률이 높은데 그 때는 배경 색상 변경도 고려해줘" — topic 수가
계속 늘어나는 한 캐릭터 풀이 유한해서 재충돌은 시간문제라는 전제로 확정된 방침):
`card_news_spec.json`에 `"cover_scrim_color": "#RRGGBB"`를 추가하면 같은 캐릭터를
배경으로 써도 스크림 톤이 달라져서 표지가 구분된다(기본값은 브랜드 ACCENT 로즈핑크
`#C84A62`). 새 topic의 표지가 기존 topic과 캐릭터까지 겹치는데 대체할 다른 아이템도
없다면, 새 색을 하나 정해서(브랜드 톤에서 크게 벗어나지 않는 선에서 — 예: 앰버·틸·
바이올렛 계열) 이 필드에 지정할 것. `cover_char_file`(1차)과 `cover_scrim_color`
(2차) 둘 다 필요하면 같이 써도 된다.

### 팩트카드 배경 — 실사진 블러(2026-08-02, 저품질 방지)

"블로그 저품질 판정이 유사 이미지를 신호로 잡는다"는 우려(사용자 확인) 대응 —
지금까지 팩트카드(2번째 장부터)는 전부 고정 그라디언트(`BG_TOP`~`BG_BOTTOM`) 배경이라
30개 topic 수백 장이 똑같은 틀로 보였다. `lib/card_news.py`의 `generate()`가 이제
item의 `char_file`(예: `커피_illust.jpg`)에서 품목명을 뽑아 `assets_library/real/`에서
같은 품목 실사진(`커피.jpg` 또는 `커피_real_01.jpg`)을 **자동으로** 찾아 블러 배경으로
쓴다(`_photo_backdrop`) — spec에 따로 필드를 추가할 필요 없음, 실사진이 없는 품목은
기존 그라디언트로 자연 폴백. 캐릭터 배지·제목·본문 위치는 항상 그대로, 바뀌는 건
배경뿐이다.

⚠️ **같은 실사진이 다른 topic에서 재사용될 때는 자동으로 크롭이 달라진다** — "한
게시물 안에서 같은 사진 반복은 무관하지만 게시물 간 유사 이미지는 저품질 신호가
된다"고 확인됨(사용자). `_photo_backdrop`이 topic+파일명을 시드로 확대율·크롭
위치·좌우반전을 결정적으로 바꾸므로 별도 조치 없이 topic마다 실제 픽셀이 달라진다
(같은 topic을 재생성하면 항상 같은 결과 — 재현 가능, 우연이 아님). 새 실사진을
`assets_library/real/`에 추가할 때는 `<품목>_real_NN.jpg` 명명 규칙만 지키면
자동으로 매칭된다.

### 폰트 크기 — "이 정도면 크겠지"로 판단하지 말 것 (2026-07-31)

카드뉴스 글자 크기 지적이 세션 내내 여러 번 반복됐다(2026-07-30, 2026-07-31) — 한 단계
키우고 넘어가면 다시 작다는 피드백이 돌아오는 패턴이 계속됐다. 새 카드뉴스를 만들 때는
`make_fact_card`/`make_closing`/`_make_cover_photo`의 현재 폰트 크기(2026-07-31 기준 —
표지 훅 84, 팩트카드 제목 92/본문 58, 마무리 헤드라인 56/팁 42)를 그대로 기준값으로 삼고,
임의로 더 작게 잡지 않는다.

⚠️ **위 규칙은 "임의로 작게 잡지 말라"는 것이지, 긴 텍스트가 패널 밖으로 잘리는 것까지
그대로 두라는 뜻은 아니다**(2026-08-01): `make_fact_card`의 title(`item["name"]`)은
title/closing headline과 달리 작성자가 미리 줄바꿈해서 넘기는 게 아니라 단일 문자열
그대로 92px로 렌더링되는데, "왜 이런 문제가 생길까요"류 짧은 문구 기준이라 그보다 훨씬
긴 문장(예: 가슴쓰림유발음식_1의 "왜 가슴이 쓰리고 신물이 올라올까요", 17자)이 들어오면
패널 오른쪽 밖으로 잘려나갔다(사용자가 실제 스크린샷으로 발견). `_fit_single_line_size()`
헬퍼가 기준 크기(92)에서 시작해 패널 폭에 맞을 때까지만 자동으로 줄이도록
`make_fact_card`에 이미 반영해뒀다(짧은 이름은 여전히 92px 그대로 유지) — 이 자동 축소는
"기본값을 낮추는 것"이 아니라 "안 잘리게 하는 안전장치"이므로 위 규칙과 충돌하지 않는다.
새 topic의 item name은 그래도 형제 카드들처럼 짧게 쓰는 게 기본(예: "왜 이런 문제가
생길까요" 패턴, 12~13자 내외) — 자동 축소는 예외적으로 길어진 경우의 안전망이지, 길게
써도 된다는 뜻은 아니다.

⚠️ **"칸 밖으로 넘어가는 텍스트" 자체를 일반 원칙으로 취급할 것**(2026-08-01, 사용자
확정: "내용도 그렇고 토픽도 그렇고 타이틀이든 뭐든 칸 넘어갈정도로 넣지는 않도록 하자").
item name 하나만 고치고 끝내지 않고, 같은 유형(작성자가 매 topic마다 새로 쓰는 단일
문자열, 미리 줄바꿈 안 함, 고정 폭 안에 렌더링)인 `eyebrow`/closing `cta_text`까지 같이
`_fit_single_line_size`로 보호해뒀다(2026-08-01, 커밋 98932a2). 점검해보니
`video_assembler.py`의 타이틀 카드·상단 배너·자막(`_make_title_card_png`/`_make_title_png`/
`_make_caption_png`)은 이미 전부 단어 단위 줄바꿈 로직이 있어서 원래부터 안전했다 — 문제는
항상 "줄바꿈도 안 되고 자동 축소도 없이 고정 크기로 박아넣는" 렌더링 함수였다.

**앞으로 카드뉴스·영상에 새 텍스트 렌더링 함수를 추가할 때 기준**: 그 텍스트가 (a) 매
topic마다 작성자가 새로 쓰는 자유 문자열이고 (b) 호출부가 미리 줄바꿈해서 넘기지 않는
단일 문자열이라면, 반드시 아래 중 하나를 적용한다 — 안 하면 이번과 같은 잘림 버그가
똑같이 재발한다:
1. 단어 단위 줄바꿈(여러 줄로 흘려보내기) — `make_cover_titlecard`/`_make_title_png`/
   `_make_caption_png` 패턴
2. 폭에 맞을 때까지 폰트 크기 자동 축소 — `_fit_single_line_size` 패턴
3. 텍스트 길이에 맞춰 컨테이너(배지·칩) 자체를 늘리기 — `_char_medallion`의 이름 칩,
   `_make_ad_tag_png` 패턴

## 글로벌 확장 준비 (2026-08-03)

**글로벌 topic은 카드뉴스 없이 숏츠만 만든다**("글로벌은 카드뉴스 필요없겠다
숏츠만 있으면 됨" — 2026-08-03 확정). 인스타그램 카드뉴스는 한국어 topic도
이미 제외 대상이라(위 "플랫폼 로테이션" 절) 실질적으로 카드뉴스가 쓰이는
곳은 네이버 블로그·티스토리 이미지 삽입 정도인데, 글로벌 topic은 초기
단계에서 유튜브 쇼츠 채널 위주로 붙이기로 한 만큼 카드뉴스 제작(Gemini
일러스트 배치·PIL 렌더링)에 드는 시간을 아낀다. `en_heartburn_1` 파일럿에서
이미 만든 카드뉴스는 소급 삭제하지 않고 그대로 둠 — **새 글로벌 topic부터
`lib/card_news.py generate()` 호출을 생략**하고 `lib/video_assembler.py`로
바로 넘어갈 것.

### 글로벌 topic 플랫폼 범위 — 유튜브 + 인스타그램 릴스만 (2026-08-04)

"글로벌은 유튜브랑 인스타만 올릴거거든? 근데 어차피 유튜브는 너가 다
관리하는거니까 탭 따로 둘 필요없이 인스타에 올릴 플랫폼만 구축해주면
될거같은데" — 글로벌 topic은 틱톡·페이스북·쓰레드·인스타그램 카드뉴셀 계정을
아예 운영하지 않으므로, `platform_captions.json`의 `platforms` 배열에
**"YouTube Shorts"·"Instagram Reels" 두 항목만** 넣는다. 유튜브는
`lib/youtube_upload.py`로 완전 자동 업로드라 대시보드 UI에 카드 자체가 안
뜨고(`_UI_EXCLUDED_PLATFORMS`), 인스타그램만 사람이 수동 업로드하므로 실제
UI에는 인스타그램 릴스 카드 하나만 남는 게 정상이다.

- **새 글로벌 topic**: 처음부터 이 두 플랫폼만 캡션을 작성한다 — 틱톡·
  페이스북·쓰레드·Instagram Carousel 캡션 자체를 만들지 않는다(리서치·작성
  비용을 아예 아낌).
- **소급 적용 완료(2026-08-04)** — 기존 글로벌 topic 58개
  `platform_captions.json`에 이미 있던 틱톡·페이스북·쓰레드·Instagram
  Carousel 항목은 전부 제거하고 두 플랫폼만 남기는 일괄 스크립트를 돌려
  반영함. 완성된 영상이 있어 `dashboard.html`을 이미 생성해둔 topic/lang은
  `lib/dashboard.generate()`로 재생성해서 정적 카드도 같이 갱신했고,
  `_update_topics_index()`로 통합 허브 페이지(`output/<topic>/dashboard.html`)도
  다시 만들어서 라이트카드 폴백까지 반영됨.
- ⚠️ 작업 시점에 세션 락이 걸려 있던 `통풍_1/ja`, `피부_1/ja`, `다리쥐_1/es`
  3개는 다른 세션이 편집 중이라 건드리지 않고 건너뜀 — 해당 세션이 완료
  시점에 이 규칙에 맞춰 직접 정리할 것(또는 다음에 이 topic/lang을 만지는
  세션이 처리).
- 한국어(`ko`) topic은 이 규칙과 무관 — 기존 플랫폼 로테이션(네이버 블로그·
  티스토리·인스타그램 릴스·유튜브 등) 그대로 유지.

한국 외 시장(미국·유럽·일본·동남아·남미 등)으로 채널을 늘리는 준비 단계.
콘텐츠 리서치 방식·TTS 보이스·폰트는 먼저 끝남(아래 절). **채널 인프라(Google
Cloud/OAuth)·수익화(아마존·알리 등 제휴)도 이어서 이 세션이 담당** — "다른
세션이 진행 중"이라던 위 노트는 실제로는 같은 세션 연속 작업이었음이 확인돼
정정함.

### 폴더 구조 — `<주제>/<lang>/` 중첩 (2026-08-03)

다국어 topic은 `en_heartburn_1`처럼 언어를 폴더명 자체에 넣어 완전히 별도
topic처럼 만들지 않는다 — 같은 주제(예: 가슴쓰림)의 언어별 콘텐츠를 한
폴더 아래 모아서 관리하기로 확정("기존처럼 관절_1 이런 한국어 폴더 안에
ko/en/fr 이런식으로 폴더 만들고 그안에 들어오도록 하자" — 2026-08-03).
파일럿으로 만들었던 `en_heartburn_1`(독립 topic 폴더)은 기존 `가슴쓰림_1`과
병합해서 이 구조로 재정리함:

```
data/가슴쓰림_1/ko/{card_news_spec.json, narration.txt, platform_captions.json}
data/가슴쓰림_1/en/{card_news_spec.json, narration.txt, platform_captions.json}
output/가슴쓰림_1/ko/{card_news/, dashboard.html, *_narration.mp3, *_narration.srt, *_shorts.mp4}
output/가슴쓰림_1/en/{...동일 구조...}
```

- **기존 한국어 topic(30여 개)은 소급 이전 대상 아님** — 새로 다국어를 붙이는
  topic만 이 구조로 전환한다. 한국어 단일 언어인 topic은 계속 기존 flat
  구조(`data/<주제>/`) 그대로 둔다.
- **`platform_captions.json`의 `"topic"` 필드는 언어별로 다르게** —
  `"가슴쓰림_1_ko"` / `"가슴쓰림_1_en"`처럼 언어 접미사를 붙인다. 이 값이
  대시보드의 localStorage 키(완료 체크·상품 링크) 접두어로 쓰이는데, ko/en이
  같은 값을 쓰면 두 언어 대시보드가 완료 체크 상태를 서로 덮어쓴다.
- **`lib/session_lock.py` 락은 언어 세분화 없이 `<주제>` 단위**로 건다(예:
  `가슴쓰림_1`) — 락 파일명이 `{topic}.lock`이라 topic 문자열에 "/"가 들어가면
  깨진다. 같은 주제의 ko/en을 동시에 다른 세션이 작업하는 걸 막는 효과도
  있어서(구조 전환 중 레이스 위험) 이 세분화 없는 락이 오히려 안전하다.
- **`lib/card_news.py`의 `generate()`는 `topic_prefix` 파라미터로 파일명
  접두어를 명시할 것** — 기존엔 `out_dir.parent.name`(예:
  `output/가슴쓰림_1/card_news` → "가슴쓰림_1")으로 자동 추론했는데, 언어별
  하위 폴더가 생기면서 `output/가슴쓰림_1/en/card_news`의
  `out_dir.parent.name`은 "en"이 되어 추론이 깨진다. 새 다국어 topic은
  `generate(spec_path, char_dir, out_dir, topic_prefix="가슴쓰림_1_en")`처럼
  명시로 호출할 것(빈 문자열 `""`을 주면 접두어 없이 저장 — 폴더 자체가 이미
  topic+언어를 구분해주므로 접두어가 굳이 필요 없다면 이 방식도 가능).
- **`lib/typecast_tts.py`의 `synthesize(topic, ...)`는 코드 수정 없이 그대로
  동작** — `topic="가슴쓰림_1/en"`처럼 슬래시 포함 문자열을 넘기면
  `Path("output") / topic`이 자동으로 중첩 경로(`output/가슴쓰림_1/en/`)를
  만든다. 이 폴더 안에서는 `narration.mp3`/`narration.srt`가 이미
  유일하므로(폴더 자체가 topic+언어를 구분) topic 접두어 없이 그대로 둬도 된다.
- **`lib/dashboard.py`의 `_update_topics_index`는 1단계(`output/<주제>/
  dashboard.html`)와 2단계(`output/<주제>/<lang>/dashboard.html`) glob을 둘 다
  스캔**해서 `output/topics.json`에 중첩 topic은 `"topic": "가슴쓰림_1/en"`
  형태로 등록한다. 루트 `index.html`은 `completed_topics.json`/
  `youtube_uploaded.json`을 조회할 때 `t.topic`(예: "가슴쓰림_1/en") 뿐 아니라
  `t.topic.split("/")[0]`(예: "가슴쓰림_1")로도 한 번 더 확인한다 — 이 두
  트래킹 파일은 여전히 언어 구분 없는 topic 단위로 기록되므로, 이미 완료
  표시된 기존 topic이 구조 전환 후에도 완료 배지를 그대로 유지하게 하기 위함.
- **`tests/test_content_rules.py`의 `_discover_topics()`도 2단계까지
  스캔** — `data/<주제>/platform_captions.json`이 없으면(중첩 구조로 전환된
  topic) 하위 폴더를 훑어서 `<주제>/<lang>` 형태로 각각 독립 topic처럼 검사
  대상에 넣는다. 새 콘텐츠 규칙 테스트를 추가할 때 이 이중 구조를 전제로
  작성할 필요는 없다 — `DATA_DIR / topic / ...` 패턴은 topic 문자열에 "/"가
  있어도 `Path`가 알아서 중첩 경로로 처리하므로 기존 테스트 코드는 그대로
  재사용된다.
- ⚠️ **아직 안 한 것**: `output/completed_topics.json`/
  `output/youtube_uploaded.json`/`output/product_links.json`을 직접 갱신하는
  스크립트나 `lib/youtube_upload.py`의 topic 선택 로직은 여전히 flat 구조
  전제 — 다국어 topic의 유튜브 업로드 오케스트레이션은 실제 언어별 채널
  인프라가 붙을 때 별도로 설계할 것(위 "아직 안 한 것" 노트와 동일 맥락).

### 채널 인프라 진행 상태 — `data/global_channels.json`

16채널(한국어 + `typecast_voices_global.json`의 15개 언어) 각각의 GCP
프로젝트/OAuth/refresh_token 발급 상태를 이 파일에 트래킹한다
(`output/completed_topics.json`과 같은 패턴 — 대화가 아니라 파일에 직접
진행 상태를 반영).

- 채널당 GCP 프로젝트를 따로 둔다 — YouTube Data API 쿼터가 프로젝트당이라
  (기본 1만 유닛/일, 업로드 1건≈1600유닛), 프로젝트 하나를 16채널이 같이
  쓰면 업로드 개수가 서로 발목 잡힘.
- 계정 구조는 **구글 계정 1개 + 브랜드 계정(채널) 16개**로 확정 — 계정을
  16개로 쪼개는 것도 검토했으나(정지 시 블라스트 반경 축소 목적) 전화번호
  인증 제약·대량 계정 생성 탐지·관리 부담 대비 이득이 크지 않다고 판단.
- **OAuth 프로덕션 전환(미검증) 리스크를 인지하고 감수하기로 결정함**
  (2026-08-03, 사용자 확정): `youtube.upload`는 Google이 민감 스코프로
  분류해서 개인 Gmail 계정(Workspace 아님)은 Production 전환 시 정식
  verification 없이는 "확인되지 않은 앱" 경고가 뜨고, 이 상태로 계속 쓰면
  계정 전체가 정지될 이론적 리스크가 있다 — 실제로는 콘텐츠 스팸 신고·결제
  사기 같은 다른 신호가 더 흔한 정지 트리거이고, 순수 OAuth 미검증 사용
  자체로 정지되는 경우는 드물다고 판단해 진행하기로 함. 나중에 여유 생기면
  Google Workspace(도메인 소유 + 유료) 계정으로 옮겨서 OAuth User Type을
  Internal로 바꾸면 이 리스크 자체가 사라짐 — 지금 결정이 나중 전환을
  막지 않음.
- **`scripts/gcp_bulk_create_channels.sh`**: GCP 프로젝트 16개 생성 + YouTube
  Data API 활성화까지 자동화(gcloud CLI 필요, 실행 전 `gcloud auth login` +
  결제 계정 연결 필요). OAuth 동의화면 설정·프로덕션 전환·클라이언트 ID
  발급은 Google이 공개 API를 제공하지 않아 Cloud Console에서 채널마다 수동
  — 스크립트 실행 후 출력되는 순서대로 진행.
- **`lib/youtube_auth_setup.py --channel <code>`**: 채널별 refresh_token
  발급(예: `--channel en`은 `YOUTUBE_EN_CLIENT_ID`/`_SECRET`을 읽어서
  `YOUTUBE_EN_REFRESH_TOKEN`을 발급). `--channel` 없이 실행하면 기존
  한국어 채널의 무접두사 변수(`YOUTUBE_CLIENT_ID` 등)를 그대로 씀 — 기존
  단일 채널 운영 코드는 안 건드림.
- ✅ **완료(2026-08-03)** — 위 "아직 안 한 것"은 en/ja 콘텐츠가 실제로 생기면서
  해소됨: `lib/youtube_upload.py`가 언어별 캡션 포맷(`CAPTION_MARKERS`)·재생목록
  이름(`PLAYLIST_TITLE_PREFIX`)까지 다국어 오케스트레이션을 갖췄고, 발행 페이스·
  시간대까지 아래 "유튜브 정책 리스크 대응 — 계정 구조·발행 페이스" 절에서
  확정함.

### 유튜브 정책 리스크 대응 — 계정 구조·발행 페이스 (2026-08-03)

글로벌 확장(16개 채널) 진행 중 유튜브의 "비진정성 콘텐츠"/스팸 정책과 계정
구조 리스크를 실제로 조사(공식 문서 원문 확인 + 복수 독립 소스 교차검증)한
끝에 확정한 대응 방침. 이 조사와 결정 과정 전체는 이 세션 대화 로그에 있고,
여기엔 **최종 결론과 왜 그렇게 정했는지의 핵심만** 남긴다 — 다음에 이 주제
다시 나오면 처음부터 재조사하지 말고 이 절부터 확인할 것.

**리스크는 서로 다른 3개 층위가 있었다** (섞어서 생각하면 안 됨):
1. **수익화 정책("비진정성 콘텐츠", 2026-07-16 정책 명확화)** — AI 페르소나가
   건강·금융·법률 등 민감 주제를 다루면 애드센스/YPP 자격만 제거됨. 리치·삭제와
   무관 — 이 프로젝트는 애초에 애드센스가 아니라 제휴 수익 모델이라 사실상 무관.
2. **커뮤니티 가이드라인 스팸 정책("자동화된 대량생산")** — 수익화 여부와
   무관하게 전체 채널에 적용, 경고→(90일 내 재위반 시)스트라이크→3스트라이크/
   90일=채널 영구 삭제. 공식 위반 예시가 "같은 배경음악+반복 AI 이미지+AI
   대본을 쓰는 채널"로, 이 파이프라인 구조와 유사도가 높음 — 게시 빈도·시각적
   변주로 대응(아래).
3. **계정 캐스케이드** — 한 구글 계정 아래 여러 브랜드 채널이 있으면, 그 중
   하나가 정책 위반으로 걸릴 때 나머지도 같이 정지/삭제되는 사례가 실제로
   보고됨(1월 16개 채널 동시 삭제 사례와 일치). 공식 문서엔 "기존 채널이 전부
   자동 연쇄 삭제된다"고 명시돼 있진 않지만(재범 방지 목적의 "신규 채널 생성
   금지" 조항만 명시), 실제 관찰된 패턴은 이보다 넓음. **게시 빈도·콘텐츠
   변주로는 전혀 완화가 안 되는, 계정 아키텍처 레벨의 별개 문제.**

**확정한 대응**:
- **계정 분리**: 한국 채널은 사용자의 메인 구글 계정에 그대로 둠(이미 실적
  있어서 안 건드림). 나머지 15개 언어는 **신규 구글 계정 4개**를 만들어 3~4개
  언어씩 나눠 담는다(예: 4/4/4/3) — 하나가 걸려도 최악의 경우 그 계정 안
  채널들만(최대 4개) 영향받고, 한국 채널과 다른 그룹들은 무사하다. 완전히
  0으로 만들려면 채널마다 계정을 따로 파야 하지만(운영 부담 커짐), 4개 그룹은
  안전성·운영 편의성 사이 타협점으로 확정. **계정 생성 자체는 사용자가 직접
  해야 함**(Claude Code가 구글 계정을 만들 수 없음) — 만들어지면 그 계정 기준
  OAuth 연동을 이어서 진행할 것.
- **발행 페이스**: 채널(언어)당 **하루 2개, 그 나라 현지 시간 오전 10시·오후
  6시**로 확정("일 1개가 나을거같아 2개가 나을거간아" 고민 끝에 최종 "2개"로
  결정). 여러 소스에서 "3개/일 이상"이 위험 신호로 언급된 것보다 충분히
  낮은 속도. 구현: `lib/youtube_upload.py`의 `LANGUAGE_TIMEZONES`/
  `upload_daily_per_channel`(`--daily-per-channel` CLI) — 언어별 1차 타겟
  국가(`data/global_research_rules.md`) 현지 시간대 기준으로 스케줄 계산.
- **콘텐츠·비주얼 변주**: `lib/video_assembler.py`에 이미 반영 — 썸네일
  배경색 8종·텍스트 세로 위치 3곳·캐릭터 좌우 위치/가끔 생략(6개 중 1개)·
  칠판 색상+나무틀 8종, 전부 topic의 title 문자열 기반 결정적 해시로 선택
  (재생성해도 값 안 바뀜). 콘텐츠 자체도 번역이 아니라 시장별 독립 리서치라
  원인/해결책이 topic마다 실제로 다름.
- ⚠️ **완전한 안전 보장은 없음** — 위 조치들은 리스크를 낮추는 것이지 0으로
  만드는 게 아니다(계정 분리만 유일하게 "차단"에 가까움). 정책은 계속 바뀔 수
  있어서 이 절의 내용도 주기적으로 재확인 필요.

### 채널 초기 세팅 — `lib/youtube_channel_setup.py` (2026-08-03)

브랜드 계정으로 채널만 만들면 설명·키워드가 전부 빈 값(실측: en/ja 둘 다
`description=""`, keywords 없음)이라 검색 노출에 불리하다 —
`channels().update()`(part=brandingSettings)로 언어별 채널 설명·키워드·
국가(country)·기본 언어(defaultLanguage)를 반영한다. `CHANNEL_META` 딕셔너리에
언어별로 완전히 새로 쓴 카피(번역 아님, 한국어 채널 설명의 어투·이모지 스타일만
참고)를 담아둠. 실행: `python3 -m lib.youtube_channel_setup <code>`.

- en/ja 이미 적용 완료(`data/global_channels.json`의 `channel_meta_configured`)
- ⚠️ **배너 이미지·외부 링크는 이 스크립트가 안 다룬다** — 배너는 이미지 업로드가
  필요한 별도 디자인 작업, 외부 링크는 YouTube가 Studio 전용으로 옮기고 공개
  API에서 뺀 지 오래라 API로 설정하는 방법이 없음(Studio에서 채널마다 직접).
- 나머지 13개 채널은 quota 승인 → OAuth 발급 끝나는 대로 `CHANNEL_META`에
  그 언어 항목 추가하고 같은 스크립트로 반영할 것.

### 원칙 — 언어마다 독립적으로 리서치 (번역 금지)

같은 topic(예: 당뇨·관절)이어도 **원인 설명·해결책 음식·거기 쓰이는 실사진/일러스트까지
언어권마다 그 지역 원문 자료로 새로 리서치**한다 — 한국어로 만든 걸 번역해서 뿌리면
안 됨. 이유 두 가지:
1. 해결책으로 제시하는 음식이 그 나라에 없거나 생소할 수 있음(한국 소싱 기준으로
   짠 콘텐츠라)
2. 유튜브의 "반복적/파생적 콘텐츠"(repetitious content) 정책 — 번역 클론은 여러
   채널이 사실상 같은 콘텐츠라는 게 드러나 수익화 심사에서 불리함

topic(다룰 증상/주제) 자체는 언어권 공통으로 재사용 가능 — 독립 리서치가 필요한 건
"원인 설명·해결책·이미지" 실질 콘텐츠 쪽.

#### ⚠️ 자동 검사 — `--lang-check`로 "진짜 독립적인지" 확인할 것 (2026-08-03)

**WHY**: 이 원칙을 문서에 적어놔도 실제로 안 지켜지는 topic이 나왔다("그게
어쨌든 한국이랑 글로벌의 유튜브 숏츠 영상 제목이 다르게들어가고있다는거지?
해석했을 떄 완전히 동일한지가 궁금한거야" → "독립적으로 되어있지 않을 떄
각각의 세션이 판단해서 제목을 바꾸도록 유도해야할거같아. 저품질이 큰
이슈라서"). 실제 title 텍스트가 언어별로 다른 문자열이라는 것만으로는 "진짜
독립 리서치"인지 보장이 안 된다 — 2026-08-03 실제 감사에서 가슴쓰림_1/ja·
갑상선_1/en·고령_1/en·골다공증_1(en/ja)·구내염_1(en/ja)·다리쥐_1/en·당뇨_1/en·
부종_1/en은 실제로 다른 상황·예시·타깃으로 재구성됐지만, **가슴쓰림_1/en,
갑상선_1/ja, 고령_1/ja, 공복_1(en/ja), 관절_1(en/ja), 눈_1/en, 눈_2/en,
눈_3/en은 문장 구조·예시·강조점이 한국어 원문과 1:1로 대응하는 사실상의
직역**이었다(전체 21개 언어쌍 중 10개) — 사람이 한국어·다른 언어 제목을
나란히 놓고 훑어봐서는 "다른 언어니까 다른 것 같다"고 착각하기 쉬운데,
실제로는 얼마든지 번역일 수 있다는 뜻.

`lib/content_review.py`의 `check_language_independence(base_topic)` /
`check_language_independence_all()`이 Gemini로 ko 제목과 각 비-ko 언어
제목을 비교해서 구조·예시·프레이밍까지 진짜 다른지(`is_translation: false`)
아니면 사실상 번역인지(`is_translation: true`) 판단한다:

```
python3 -m lib.content_review --lang-check <base_topic>   # topic 하나
python3 -m lib.content_review --lang-check --all           # 다국어 topic 전체
```

**새 언어 콘텐츠를 작성한 뒤(narration.txt·platform_captions.json 완성 시점),
위 "콘텐츠 QA" 절의 `content_review.py` 리뷰와 함께 이 검사도 무조건
돌린다** — ko가 아닌 언어를 하나라도 추가했다면 완료 기준에 포함. **`is_translation: true`로
플래그된 언어가 있으면, 그 세션이 스스로 판단해서 그 언어의 title/narration.txt
/card_news_spec.json을 그 언어권 상황에 맞는 진짜 다른 각도로 다시 써야 한다**
— 자동으로 고치지 않는다(오탐 가능성이 있는 LLM 판단이라 사람/세션이 최종
판단, `content_review.py`의 다른 검사들과 같은 원칙). 다시 쓸 때는 위
"언어권별 리서치 룰"(`data/global_research_rules.md`)과 "훅 문장 패턴
다양화" 절의 12가지 골격을 참고해서, ko와 겹치지 않는 상황·예시·타깃을
새로 고른다(가슴쓰림_1/ja의 "회식 후" 프레이밍, 부종_1/en의 "점심 반지,
저녁 발목" 예시처럼 — 위 감사 결과에서 실제로 통과한 사례를 참고할 것).
제목을 바꾸면 narration.txt 첫 문장·card_news_spec.json title·모든 플랫폼
caption 첫 줄까지 동기화해야 하는 건 기존 "훅이 바뀌면 전부 동기화" 규칙과
동일 — 그 언어의 TTS·영상도 재생성 대상이 된다.

#### ⚠️ 미해결 백로그 — 번역 의심 언어쌍 재작성 (세션이 알아서 집어갈 것, 2026-08-03)

**WHY 이 절이 따로 있는지**: "기존꺼도 수정이 필요할거같긴한데... 니들 세션들끼리
서로 알수있도록 문서에다가 반영해라 이런식으로 진행할수없어? 내가 따로
말하지않더라도?" — 사용자가 매번 다시 요청 안 해도, CLAUDE.md를 읽는 모든 세션이
이 백로그를 자동으로 알고 처리하게 만드는 게 목적. 이 프로젝트는 무인 cron이
없어서(“요청 시점에만 실행” 원칙) 세션이 스스로 깨어나 실행하진 않지만, **사용자가
무슨 요청을 하든 세션은 시작 시 이 CLAUDE.md를 먼저 읽으므로**(이 문서 맨 위
"개발 시작 전 필수 사전 파악" 절), 아래 목록은 항상 세션의 컨텍스트 안에 있다.

**처리 규칙**: 다른 급한 요청 중이 아니고, 아래 topic 중 `python3
lib/session_lock.py check <topic>`가 `UNLOCKED`인 게 있으면 — **사용자에게
확인받지 않고 완전 자동화 모드로 바로 재작성 진행**(위 "완전 자동화 모드" 절과
동일 원칙: 정의된 콘텐츠 파이프라인 작업이라 승인 불필요). 절차:

1. `lib/session_lock.py acquire <topic> "번역 의심 언어쌍 재작성"`
2. 해당 언어의 `data/global_research_rules.md` 절 + "훅 문장 패턴 다양화" 12종
   참고해서, ko와 겹치지 않는 새 상황/예시/타깃으로 title·narration.txt·
   card_news_spec.json·platform_captions.json을 그 언어권 기준으로 다시 쓴다
   (번역이 아니라 새로 리서치 — 위 "원칙" 절)
3. `python3 -m lib.content_review <topic>/<lang> <언어명>`로 일반 콘텐츠
   리뷰, `python3 -m lib.content_review --lang-check <topic>`로 재검사해서
   `is_translation: false`로 바뀌는지 확인
4. TTS(`typecast_tts.py`) 재생성 → 영상 재조립(`video_assembler.py`) →
   대시보드 재생성 → 이 topic 항목을 아래 목록에서 제거
5. commit·push, `session_lock.py release <topic>`

**대상 목록** (2026-08-03 감사 기준, ✅ 처리 완료되는 대로 이 목록에서 지울 것):

| topic/lang | 문제 |
|---|---|

✅ **완료(2026-08-04)**: 관절_1/en(BBQ+맥주 후 엄지발가락 통풍 발작 프레이밍 —
퓨린·요산 결정 메커니즘, 맥주/붉은고기/가당음료 원인에 체리·두부·무가당 차로
대응), 관절_1/ja(更年期 관절통 프레이밍 — 에스트로겐 감소로 관절 보호 기능이
약해지는 시기라는 원인, 술·단 과자·짠 가공식품은 줄이고 두유·낫토·두부 등 대두
이소플라본 식품을 늘리는 대응)는 ko의 "계단 통증·아침 뻣뻣함" 구조와 완전히
다른 각도로 재작성해서 `--lang-check` 통과(`is_translation: false`) 후 이
목록에서 제거함. content_review에서 en의 체리 효과 과장·과당→요산 메커니즘
오류, ja의 "술 대신 두유"류 부자연스러운 1:1 대체 제안(대체 관계가 성립하지
않는 이종 식품군 간 치환)을 잡아내서 각각 정정 — 재작성 중 새 콘텐츠 품질
이슈가 나오면 그 자리에서 같이 고칠 것.

✅ **완료(2026-08-03)**: 가슴쓰림_1/en(새벽 3시 야간 역류 프레이밍), 눈_1/en(야간
운전 전조등 빛번짐 프레이밍), 눈_2/en(카페인 스태킹/에너지드링크 프레이밍),
눈_3/en(계절 알레르기 프레이밍)은 락 해제 확인 후 재작성·`--lang-check` 통과·
TTS 재생성·영상 재조립·대시보드 재생성까지 마쳐서 이 목록에서 제거함 — 재작성
중 narration.txt가 45초 하드 캡을 넘긴 것도 같이 발견해서 압축했고(가슴쓰림_1/en
91초→36초), content_review에서 새로 걸린 논리 오류(매운맛 재료를 "안 맵게"라고
잘못 제안한 것, 우유가 위산 역류를 오히려 악화시킨다는 의학적 오류 등)도 같이
고쳤다 — 재작성 중 새 콘텐츠 품질 이슈가 나오면 그 자리에서 같이 고칠 것.

✅ **완료(2026-08-04)**: 공복_1/en(모닝커피 습관·코르티솔/혈당 프레이밍 — 계란·
코티지치즈·커피 타이밍 조절), 공복_1/ja(출근길 배변 리듬·위결장반사 프레이밍 —
백탕·요거트·키위)는 ko의 "다이어트해도 체지방 안 빠짐→공복 음식 5가지" 구조와
완전히 다른 각도로 재작성해서 `--lang-check` 통과(`is_translation: false`) 후
이 목록에서 제거함. 재작성 중 content_review가 en 나레이션의 코르티솔 타이밍
모순(기상 후 30~45분은 코르티솔이 정점을 향해 계속 오르는 구간인데 "그때
이미 진정되기 시작"이라고 쓴 것)을 잡아내서 "45~60분 뒤로 미루기"로 정정 —
재작성 중 새 콘텐츠 품질 이슈가 나오면 그 자리에서 같이 고칠 것.

✅ **완료(2026-08-04)**: 갑상선_1/ja는 ko의 "미역국→생채소 스무디→두유" 구조를
버리고 昆布だし・こんぶ茶(北海道 연안부 갑상선종 역사, 웰프-차이코프 효과)·
イソジンなどのヨード系うがい薬(습관적 가글로 요오드 과다 섭취)·納豆や豆乳
(갑상선약 흡수 방해)로 완전히 다른 3축 프레이밍으로 재작성해서 `--lang-check`
통과(`is_translation: false`) 후 이 목록에서 제거함. content_review에서 콩
제품 항목이 "이미 갑상선 약을 복용 중"이라는 전제 없이 갑상선 기능을 직접
저하시키는 것처럼 읽히는 논리 오류를 잡아내서 조건문을 추가해 정정 — 또한
narration.txt의 "항목 나열" 문장(3개 원인 이름이 한 문단에 동시 등장)이
`build_motion_schedule`의 이름 매칭 휴리스틱을 오작동시켜 캐릭터가 실제
설명 시점보다 훨씬 이르게(12초 vs 실제 30초) 조기 전환되는 버그를 발견 —
SRT 문단 경계 기준 수동 motion_schedule로 우회함(코드 자체는 안 고침, 이후
비슷한 "항목 나열형" 다국어 topic 재작성 시 같은 증상이 재발할 수 있다는
점 참고).

✅ **완료(2026-08-04)**: 고령_1/ja는 ko의 "손발저림·차가움·부종(포화지방/
나트륨)" 구조를 버리고 일본 고령자의 실제 이슈인 "かくれ脱水(숨은 탈수)"로
완전히 다른 프레임으로 재작성함 — 원인은 加齢로 인한 갈증 감각 저하+신장
농축력 저하, 항목은 濃い緑茶・コーヒー(카페인 이뇨작용)·ビール・日本酒
(알코올 이뇨작용)·水だけの水分補給(물만 마셔 전해질이 희석되는 습관)로
`--lang-check` 통과(`is_translation: false`) 후 이 목록에서 제거함.
content_review에서 초안의 "そば・うどんの汁を残さず飲む"(소바·우동 국물을
탈수 예방책으로 권장) 항목이 고령자 나트륨 제한 권고와 정면 충돌하는
의학적 오류로 플래그되어, "물만 마시는 습관" 항목으로 교체 — 또한 갑상선_1/ja와
동일한 `build_motion_schedule` 버그(항목 나열 문장에서 항목명이 그대로
매치되며 캐릭터 전환 순서가 꼬임, 특히 항목 이름이 나레이션 문장에 그대로
등장할수록 심함)를 다시 발견해서 같은 방식(SRT 문단 경계 기준 수동
motion_schedule)으로 우회함 — 이 패턴이 두 topic에서 연달아 재현된 걸 보면
"항목 나열형" 다국어 topic 재작성 시 거의 항상 나타나는 문제로 보임(코드 자체
수정은 아직 안 함).

이 목록이 비면 이 절 자체를 지워도 된다 — 남아있는 한 "미해결 작업"으로 취급할 것.

### 언어권별 리서치 룰 — `data/global_research_rules.md` (2026-08-03)

"이런것들 엄청 고도화시키는게 중요해" 요청으로, 위 원칙을 실제로 어떻게 지킬지
15개 언어 각각에 대해 정리한 체크리스트를 별도 파일로 만듦 — 언어별 1차 타겟
국가, 표현 규제 주의사항(광고법·건강강조표시 규정 등), **종교·문화적 금기
식품**(할랄 원칙 적용 언어권: 아랍어·터키어·인도네시아어·벵골어, 힌디어는
소고기 금기), 신뢰할 만한 로컬 보건 기관 목록을 담는다.

⚠️ **벵골어·스페인어는 시장부터 먼저 정할 것** — 벵골어는 방글라데시(이슬람)
vs 인도 서벵골(힌두)로 금기 식품이 정반대, 스페인어는 스페인(EU 규제) vs
라틴아메리카(각국 개별 규제)로 적용 법규가 다르다. 문서에 기본값(방글라데시,
라틴아메리카)을 정해뒀지만 실제 콘텐츠 제작 전 재확인할 것.

`lib/content_review.py`의 `lang` 파라미터(LLM 사후 리뷰 — "이 지역에서 실제로
구할 수 있는 재료인지" 자동 체크)와는 역할이 다르다 — `global_research_rules.md`는
**작성 전** 참고하는 사전 가이드, content_review는 **작성 후** 안전망. 새 언어권
topic을 쓸 때는 이 문서를 먼저 확인 → 작성 → content_review로 마무리하는 순서.

### TTS 보이스 — `data/typecast_voices_global.json`

기존 `data/typecast_voices.json`(한국어 전용, 건드리지 않음)과 별도 파일. 언어(시장)
키마다 `api_language`(Typecast API `language` 파라미터 값)와 `voices`(name+actor_id)
목록을 담는다:

```json
{ "영어": { "api_language": "eng", "voices": [{"name": "Silas", "actor_id": "tc_..."}] } }
```

- 현재 15개 언어: 영어·일본어·대만어·스페인어·포르투갈어·프랑스어·독일어·러시아어·
  베트남어·아랍어·벵골어·터키어·태국어·인도네시아어·힌디어
- **중국(본토)·광동어·스웨덴어는 의도적으로 제외**
  - 중국 본토는 유튜브 자체가 차단돼 있어 배포 채널로 의미가 없음(대만어는 대만에서
    유튜브가 정상 작동하므로 포함 유지). 중국 시장은 나중에 별도 채널(샤오홍슈·도우인
    등)로 접근할 경우 다시 검토 — 단, 그 플랫폼들은 API 자동화 자체가 중국 사업자
    인증 없이는 사실상 막혀있음.
  - 스웨덴어는 인구(~1000만)가 작아 "LLM 콘텐츠 리뷰 신뢰도가 학습 데이터 적은
    언어일수록 떨어진다"는 리스크 대비 시장 규모가 안 맞는다고 판단해 제외(2026-08-03).
    벵골어·힌디어·아랍어·태국어·터키어는 같은 리스크가 있어도 인구 규모(수억 명 단위)가
    커서 제외하지 않고, 대신 스케일 키우기 전 원어민 스팟체크를 거치기로 함 — 아래
    "콘텐츠 QA" 절 참고.
- **대만어·중국어는 Typecast API 언어 코드가 둘 다 "zho"로 동일** — 구분은 어떤
  보이스(악센트)를 쓰느냐로만 갈림, API 파라미터로는 구분 안 됨.
- 사람이 실제로 들어보고 어색한 보이스는 제외함(Claude는 오디오를 들을 수 없어서
  판단 불가 — 텍스트로 언어별 짧은 문장 생성해서 사람이 검수하는 절차로 진행함).
  제외 이력: Dean(벵골어), Antonio(스페인어·인도네시아어 전체), Takuya Murakami
  (대만어·일본어 전체), Rin Ishida(태국어만 — 일본어는 유지), **Silas(영어,
  2026-08-03 — 글로벌 확장 1차 배치 중 고령_1/en에서 실제로 들어보고 제외,
  `data/typecast_voices_global.json`에서 이미 삭제 완료)**.

### 폰트 — 라틴 문자권은 Noto Sans, 스크립트별로 별도 소싱 필요

기존 `FONT_PATH`(card_news.py/video_assembler.py 공통, `/System/Library/Fonts/
AppleSDGothicNeo.ttc` macOS 시스템 폰트)는 한국어+기본 라틴만 지원 — 스페인어·
포르투갈어·프랑스어·베트남어·터키어의 악센트 문자(á·ã·ç·đ·ş 등)가 누락되는 걸
`fontTools`로 실제 글리프 커버리지 확인해서 발견함(영어·독일어·스웨덴어·인도네시아어는
우연히 커버됨).

- **`assets_library/fonts/NotoSans-Bold.ttf`** 추가(SIL OFL 1.1, `OFL-NotoSans.txt`,
  Google Fonts `google/fonts` 저장소의 가변 폰트에서 `wght=700, wdth=100`
  인스턴스를 `fontTools.varLib.instancer`로 정적 추출) — 영어·스페인어·포르투갈어·
  프랑스어·독일어·스웨덴어·베트남어(까다로운 결합 성조 부호 포함)·인도네시아어·
  터키어 **라틴 문자권 9개 언어 전부 글리프 커버리지 확인 완료**.
- **비라틴 스크립트 폰트도 완료(2026-08-03)** — 스크립트별 Noto Sans 계열을 같은
  방식(가변 폰트 → `wght=700` 정적 Bold 추출)으로 소싱:
  - `NotoSansArabic-Bold.ttf`(아랍어), `NotoSansBengali-Bold.ttf`(벵골어),
    `NotoSansDevanagari-Bold.ttf`(힌디어), `NotoSansThai-Bold.ttf`(태국어),
    `NotoSansJP-Bold.ttf`(일본어), `NotoSansTC-Bold.ttf`(대만어) — 전부 각
    `OFL-NotoSans<Script>.txt` 라이선스 동봉
  - 러시아어(키릴 문자)는 이미 `NotoSans-Bold.ttf`가 커버함("latin-greek-cyrillic"
    통합 폰트라 별도 폰트 불필요 — `fontTools`로 확인)
  - ⚠️ **아랍어는 RTL인데 시스템에 raqm이 없다**(`PIL.features.check("raqm")` →
    `False`) — 그런데도 실제 렌더링 테스트(글자 이어짐꼴, 아랍어+라틴 숫자 혼합
    문장의 양방향 배치)가 육안으로는 정상으로 보였다. 원인은 확인 안 했고(freetype
    자체가 일부 GSUB을 처리하는 것으로 추정), **완벽하다고 단정하지 말고 실제
    아랍어 원어민 스팟체크 때 이 부분도 같이 확인할 것** — 필요하면
    `arabic-reshaper` + `python-bidi`를 나중에 추가로 넣을 수 있음.
  - 벵골어·힌디어(데바나가리)·태국어는 결합 문자(conjunct)·성조 부호 위치까지
    포함해서 렌더링 테스트 육안 확인함(자소 분리 없이 정상 결합됨).
- ⚠️ **연결 완료(2026-08-03 버그 수정)** — 위 항목은 원래 "아직 연결 안 됨"으로
  남아있었는데, 실제로 `en_heartburn_1`을 재생성해서 프레임을 뽑아보니 우상단
  아이템 라벨·"떠든 사람"류 명패가 전부 한글 그대로 나오는 게 실제로 확인됐다
  (`_place_chalk_doodle`의 `lang`/`item_label_overrides`/`topic_word` 파라미터
  자체는 이미 만들어져 있었지만, 실제 재생성에 쓰는 `lib/rebuild_video.py`가 그
  파라미터들을 계산해서 넘기질 않아서 "만들어져 있지만 연결 안 된" 상태였음).
  이번에 두 가지를 고쳤다:
  1. `lib/rebuild_video.py`의 `derive()`가 topic 폴더명 프리픽스(`en_`, `ja_`
     등, `data/global_channels.json`의 `code` 기준)로 언어를 감지해서 `lang`/
     `item_label_overrides`(캐릭터 파일명 stem → topic 언어의 짧은 표시 이름,
     같은 캐릭터를 원인/대안 항목이 같이 쓸 땐 더 짧은 이름 우선)/`topic_word`
     (언어 프리픽스와 번호 뗀 급훈용 한 단어)를 계산해서 `assemble()`에 넘기게
     함 — `assemble()`에도 없던 `topic_word` 파라미터를 새로 추가하고
     `_build_chalkboard_bg`까지 관통시킴.
  2. **폰트도 같은 문제였다** — `CHALK_FONT_PATH`(칠판 손글씨 자막)와
     `FONT_PATH`(제목 카드·상단 배너·구식 캡션, 둘 다 한국어 전용 폰트)가
     `lang`과 무관하게 항상 고정이었다. 영어는 Gaegu/AppleSDGothicNeo에 라틴
     글리프가 우연히 있어서 안 깨져 보였지만, 아랍어·벵골어·힌디어·태국어·
     일본어·대만어는 해당 스크립트 글리프가 아예 없어서 전부 깨졌을 것 —
     `_chalk_font_for_lang(lang)`/`_title_font_for_lang(lang)` 두 헬퍼를 새로
     만들어 위 6개 함수(`_make_chalk_caption_png`/`_make_item_label_png`/
     `_doodle_text_box`/`_make_title_png`/`_make_title_card_png`/
     `_make_caption_png`)가 전부 이걸 통해 폰트를 고르게 했다. `FONT_PATH`는
     `.ttc`(폰트 컬렉션)라 `index=6`을 쓰는데 대체 폰트는 단일 `.ttf`라
     `index=0`이어야 해서 (경로, index) 쌍으로 반환하는 구조로 처리.
     아랍어·일본어로 실제 캡션 PNG를 렌더링해서 육안으로 글자가 안 깨지는 것까지
     확인함. `_make_ad_tag_png`("광고" 고정 텍스트)는 손 안 댐 — 텍스트 자체가
     한국 표시광고 규정 문구라 다국어 대응은 별개 검토 필요.
  3. ✅ **수정 완료(2026-08-03)** — `build_motion_schedule()`(`lib/rebuild_video.py`,
     캐릭터 여러 명 topic의 화면 전환 타이밍을 나레이션 키워드 겹침으로 추정)이
     한국어 단어 매칭 기준으로 짜여있어서 영어 나레이션에서 0.1~0.2초짜리
     스퓨리어스 세그먼트가 튀어나오던 문제, 디버깅해서 원인 세 가지 다 수정함:
     ① `_WORD_RE`가 한글+영문만 매치해서 아랍어·벵골어·힌디어·태국어·일본어·
     중국어·러시아어는 키워드가 아예 안 뽑힘 → `\w+`(유니코드 전체)로 확대.
     ② 대안 항목("~ 대신") 판별이 한국어 문자열 검사라 다른 언어는 안 걸러짐 →
     항목 목록 내 다른 이름이 접두어인지로 언어 무관 판단(`_is_alt_item`).
     ③ (실질적 주범) "it/of/the/and" 같은 영어 기능어가 독립 토큰으로 뽑혀
     무관한 문단에서 우연히 겹치며 컷오프를 넘겨 엉뚱한 항목이 조기 배정됨
     (한국어는 조사가 명사에 붙는 구조라 이 문제 자체가 없었음) → 언어별
     불용어 목록(`_STOPWORDS_BY_LANG`, en/es/pt/fr/de/ru/tr/id/vi) 추가.
     검증 못 한 언어(ar/bn/hi/th/ja/zh-TW)는 불용어 필터 없이 기존과 동일하게
     동작 — 실제 해당 언어 topic 만들 때 결과물 보고 스퓨리어스 세그먼트
     보이면 그 언어 불용어 목록 추가할 것. `가슴쓰림_1/en`으로 검증 완료(스퓨리어스
     세그먼트 사라짐), 기존 한국어 다중 캐릭터 topic 3개(손발저림_1/관절_1/
     다리쥐_1) 회귀 없음 확인.
     - ⚠️ **다음 할 일**: 이 수정은 코드만 반영됐고, `가슴쓰림_1/en`의 실제
       영상(`output/가슴쓰림_1/en/*_shorts.mp4`)은 아직 수정 전 버전 그대로다 —
       재생성 안 함(당시 다른 세션이 `가슴쓰림_1` 락을 잡고 `/ja` 작업 중이라
       충돌 방지 차 보류). **`가슴쓰림_1` 락이 풀린 뒤** 아래를 실행해서
       재생성할 것:
       ```
       python3 -c "import lib.rebuild_video as rv; rv.rebuild('가슴쓰림_1/en')"
       python3 lib/dashboard.py data/가슴쓰림_1/en/platform_captions.json output/가슴쓰림_1/en/card_news output/가슴쓰림_1/en/*_shorts.mp4 output/가슴쓰림_1/en/dashboard.html
       ```
       (video_path는 실제 글롭된 mp4 파일명으로 바꿔서 실행 — `ls output/가슴쓰림_1/en/*_shorts.mp4`로 확인)
       재생성 전엔 `lib/session_lock.py`로 `가슴쓰림_1` 락 확인/획득할 것.

### 칠판 명패 풀 — 14개 언어 추가 (2026-08-03)

"한국 로컬 감성 파츠는 아예 빼. 글로벌 각각에서 문화별로 칠판에 뭔가를 적는
문화가 있는지... 서브에이전트 구축해서 병렬로" 요청으로, 서브에이전트 4개를
병렬로 돌려 언어별 실제 교실 당번/직책 문화를 웹 검색으로 리서치한 뒤
`_NAMEPLATE_POOL_XX`/`_FIRST_NAMES_XX`로 코드에 반영함(일본어·대만어·
베트남어·태국어·스페인어·포르투갈어·프랑스어·독일어·러시아어·터키어·
인도네시아어·아랍어·벵골어·힌디어, 총 14개 — 한국어/영어는 이미 있었음).
번역이 아니라 각 문화의 실제 관행(예: 일본 日直/係活動, 독일 Tafeldienst/
Klassensprecher, 인도 कक्षा मॉनिटर)을 새로 조사해서 구성 — `_NAMEPLATE_POOL_BY_LANG`/
`_FIRST_NAMES_BY_LANG` 딕셔너리로 lang 코드 하나만 보고 바로 찾게 함(매핑에
없는 코드는 영어로 폴백). 아랍어·벵골어·힌디어는 "떠든 사람"류 부정적
콜아웃 항목을 리서치 근거 부족으로 의도적으로 빼고 전부 긍정적 직책만 사용.
⚠️ 각 풀의 정확한 근거(출처 URL)는 이 기능을 만든 세션 대화 로그에만 있고
파일엔 요약만 남김 — 나중에 원어민 스팟체크 시 재조사 필요할 수 있음(특히
터키어 세부 "sorumlu" 역할 일부, 태국어 별명 목록).

⚠️ **`_NAMEPLATE_POOL_EN`에서도 실제 영어권 교실 문화가 아닌 항목 발견 →
"Birthday: {}"/"Door Holder: {}" 제거함**(2026-08-03, "Birth day, Door
Holder 이거 없애자 이런문화 없잖어 영어권에는?" — 사용자 확인): 미국
교실 "job chart"(줄반장·게시판 담당·화분 담당 등)는 실제 존재하는 관행이지만,
저 두 항목은 그 형식의 "직책 명패"로 흔히 쓰이는 문화가 아니다(생일은
별도 "Birthday Board"로 다루는 경우가 많고, 문 잡아주기는 특정 role로
명패화하는 관행이 확인 안 됨) — 14개 언어 리서치 때 정작 먼저 있던 영어
풀은 재검증을 안 거쳐서 놓친 gap. **새 언어 풀을 추가하거나 기존 풀을
검토할 때, "그 문화권에 실제로 있는 관행인가"를 검증 안 된 상태로 그냥
그럴듯해 보인다고 넣지 말 것** — 애매하면 빼는 쪽으로(부정 콜아웃 항목을
근거 부족으로 뺀 아랍어·벵골어·힌디어와 같은 기준).

### 다국어 렌더링 버그 3종 — 가슴쓰림_1/ja 실제 제작 중 발견·수정 (2026-08-03)

`가슴쓰림_1/ja`를 처음부터 끝까지 직접 만들어보면서(리서치→나레이션→TTS→
영상 조립) 코드 레벨 버그 3개를 발견해서 고쳤다 — 전부 일본어 하나만이
아니라 앞으로 만들 모든 비한국어 topic에 영향 있는 것들이라 여기 모아 기록:

1. **`lib/typecast_tts.py`의 `SENTENCE_END` 정규식이 전각 문장부호를 못
   읽음** — `[.!?]`만 봐서 한국어(반각 문장부호만 씀)는 문제없었지만, 일본어
   ·중국어(전각 `。！？`)나 힌디어·벵골어(단다 `।`)는 문장 하나도 안 걸려서
   나레이션 전체가 SRT 자막 한 덩어리로 나올 뻔했다 — `[.!?。！？।؟]`로
   확장(아랍어 `؟`도 선제적으로 추가, 아직 실사용 검증은 안 함).
   ⚠️ **잔여 한계**: 이 정규식을 고쳐도 Typecast API 자체가 일본어 "단어" 단위
   타임스탬프를 문장 경계 없이 통째로 반환하는 경우가 있다(직접 재현 확인) —
   아래 3번 MAX_CAPTION_LINES가 이 잔여 문제의 최종 안전망 역할을 한다.
2. **`lib/video_assembler.py`의 모든 줄바꿈 로직이 `text.split()`(공백
   기준)만 씀** — 한국어·영어처럼 띄어쓰기하는 언어는 문제없지만, 일본어·
   중국어·태국어(scriptio continua, 띄어쓰기 자체가 없는 언어)는 문장 전체가
   공백 하나 없는 "단어 하나"로 인식돼서 줄바꿈이 아예 안 되고 캔버스 폭을
   넘겨 양옆으로 잘려나갔다(타이틀 카드 실제 스크린샷으로 발견). `_wrap_text_
   for_lang(draw, text, font, max_width, lang)` 헬퍼를 추가해서 `ja`/`zh-TW`/
   `th`는 글자 단위로, 나머지는 기존 공백 단위로 줄바꿈한다 — `_make_title_png`/
   `_make_title_card_png`/`_make_caption_png`/`_make_chalk_caption_png` 4곳
   전부 이걸로 교체.
3. **칠판 자막 최대 4줄 강제(`MAX_CAPTION_LINES`, `_split_long_caption_entries`)**
   — 1번 잔여 한계로 문장 여러 개가 자막 한 구간에 뭉쳐서 칠판을 거의 다
   채우는 걸 실제 스크린샷으로 확인, "이런 현상이 언어 늘어날수록 어디서든
   생길거같은데... 4줄정도를 최대로 해야" 지적(사용자, 원인이 API든 그냥 원래
   긴 문장이든 결과가 나쁜 건 똑같다는 취지) — SRT 각 구간을 실제 폰트로
   줄 수를 재서(`_count_wrapped_lines`, 추측이 아니라 `_wrap_text_for_lang`
   그대로 재사용한 정확한 측정) 4줄을 넘으면 문장부호 기준으로 쪼개고, 원래
   시간 구간을 글자 수 비례로 나눠 배분한다. 언어·topic 무관하게 항상 적용됨.
4. **`lib/rebuild_video.py`의 `derive()`가 재생성 시 위 수정을 무효화할
   뻔함**(별도 발견) — `item_label_overrides`가 "같은 char_file을 쓰는 항목 중
   최단 이름" 휴리스틱만 썼는데, 그 char_file을 알대 항목 하나만 쓰면(예:
   바질_illust.jpg가 대안 항목 하나에만 쓰이는 경우) 비교 대상이 없어서 긴
   문장이 그대로 라벨에 남았다 — `card_news_spec.json`의 `char_display_names`
   (카드뉴스 배지용 짧은 이름)가 있으면 최우선으로 쓰도록 수정. `end_card_text`도
   `DEFAULT_END_CARD_TEXT`(한국어 고정)만 쓰던 걸 `spec["closing"]["cta"]`
   (비한국어면)로 교체 — 안 그러면 재생성할 때마다 엔드카드만 한국어로
   돌아갈 뻔했다.

⚠️ **적용 범위**: 전부 코드 레벨 수정이라 새로 만드는 topic은 자동 적용되고,
`rebuild_video.py`로 재생성하는 기존 topic도 자동 반영된다 — 별도 조치 필요
없음. 다만 이미 만들어진 `가슴쓰림_1/en`·`/ja`는 이 수정들이 반영된 뒤에
재조립했지만, 아래 "45초 하드 캡" 규칙은 나중에(다른 세션이) 추가한 거라
반영 안 된 채로 남아있음 — 그 절 참고.

## 영상 광고 표시 의무 (2026-08-05 리서치)

네이버 클립 캡션에 네이버 쇼핑 커넥트 수수료 고지문구를 넣으면서, "이 문구가
본문/댓글 어디에 들어가야 맞는지"부터 확인하다가 더 큰 문제를 발견했다.

**경위**: 처음엔 웹서치로 "클립은 본문에 [광고] + 첫 번째 고정 댓글에 고지문구"
라는 정보를 찾았는데(개인 블로그/쓰레드 후기 기반, 네이버 공식 문서는 못 찾음),
사용자가 "그 숏폼 동영상이 클립에서 직접 커넥트 버튼으로 눌러서 넣을 때를
말하는거 아니냐, 그러면 링크 버튼 활성화되면서 광고 표시가 자동으로 뜬다"고
지적 — 실제로 이 프로젝트는 클립 앱의 네이티브 커넥트 버튼을 안 쓰고
캡션에 텍스트로 링크만 넣는 방식이라(`_buildLinkBlock`), 그 "네이티브 버튼"
기준의 가이드가 그대로 적용되는지 자체가 불확실했다.

**공식 확인**: 법제처 "찾기쉬운 생활법령정보"(easylaw.go.kr, 정부 공식)에서
공정거래위원회의 "추천·보증 등에 관한 표시·광고 심사지침" 기반 안내를 확인:

- "광고"(또는 "유료광고"·"상업광고" 등 동등 표현) 텍스트 표시가 **필수**
- 위치: 제목이나 영상 시작 부분처럼 **소비자가 쉽게 찾을 수 있는 곳** — 본문
  중간·작은 텍스트는 부적절 판정
- 노출: 영상 전체가 광고 성격이면 시작·종료부에 표시하고 **영상 중에
  반복적으로 표시**해야 함
- **댓글로 표시하는 건 명시적으로 "부적절한 광고 표시" 사례**로 제시됨 —
  이전에 찾았던 "첫 댓글에 넣으면 된다"는 비공식 정보와 정면으로 배치
- 미준수 시 시정명령, 과징금(매출액 2% 이내), 징역·벌금까지 — 권고사항이
  아니라 실제 법적 의무

**결정**: 영상 자체에 "[광고]" 오버레이를 넣는 기능이 아직 없어서, 구현 전까지는
한국어 topic 전부(신규 포함) 커미션 관련 요소(링크·고지문구)를 캡션에서 완전히
빼고 순수 정보성 콘텐츠로만 운영하기로 함 — "지금 토픽으로 들어가고 있는 애들
까지는 전부 광고를 안달아야겠다... 문제가 생길수있으니 캡션같은것도 다
수정해줘. 아예 다 링크없는거로." 향후 영상에 "[광고]" 오버레이 구현되면, 그
플래그가 붙은 topic만 캡션에 링크+광고 코멘트를 다시 붙이는 조건부 로직으로
설계할 예정(아직 미착수 — 메인 CLAUDE.md "플랫폼 캡션" 절 참고).

## 글로벌 제휴(아마존/알리익스프레스) 준비 리서치 (2026-08-05, 아직 미착수)

"나중에 아마존이나 알리 붙일거거든" — 글로벌 5개 언어(en/ja/es/pt/ru)에 향후
아마존 어소시에이트·알리익스프레스 제휴 링크를 붙일 계획이라 미리 조사해둔
내용. **아직 코드/데이터에 반영 안 됨** — 실제 착수 시 이 절 참고해서 시작할 것.

### 지역별 정부 표시광고 규정(요약, 상세 근거는 리서치 세션 로그 참고)

| 언어 | 지역 | 법적 의무 | 표시 문구 | 영상 내 위치 |
|---|---|---|---|---|
| en | 미국(FTC) | 의무 | `#ad` / `Paid partnership` | 영상 시작, 눈에 띄게 |
| ja | 일본(景品表示法, 2023.10~) | 의무 | `広告` / `PR` | **영상 내내 지속 노출**(시작만으론 불충분, 댓글 표시 불인정) |
| es | 멕시코(PROFECO) | 불확실(가이드는 있으나 법적 구속력 있는지 법률자문 의견 갈림) | `#Publicidad` / `#Patrocinado` | 명확한 기준 없음(권장 수준) |
| pt | 브라질(CONAR, 자율규제) | 법 아님, 업계 자율규제 | `#publicidade` / `#publi` | 영상 시작 또는 광고 등장 순간 |
| ru | 러시아(광고법, 2022~/과태료 2023~) | 의무 | `Реклама` + erid 코드(로스콤나드조르 ЕРИР 등록 필요) | 화면 텍스트 필수, 빠르게 봐도 읽힐 크기 |

### 아마존 어소시에이트·알리익스프레스 지역별 가용성

- **러시아 — 아마존 완전히 불가**: 2022년 러시아 배송·신규 판매자 가입 전면
  중단, 2026년까지도 복구 안 됨(2025년 러시아 자체 인터넷 규제로 아마존이
  의존하는 미국 클라우드/CDN 인프라까지 차단된 정황도 있음). 러시아는
  **AliExpress.ru(CIS 전용 별도 법인, Admitad 등 통해 가입)만** 가능 — 일반
  글로벌 AliExpress 프로그램(`portals.aliexpress.com`)은 러시아를 커미션
  제외 국가로 명시함.
- 미국·일본·브라질: 아마존 어소시에이트 + 알리익스프레스 둘 다 공식 확인됨.
- 멕시코: 아마존 어소시에이트(`afiliados.amazon.com.mx`)는 확인됨. 알리익스프레스는
  공식 지원국 목록에서 직접 확인 못 함(Admitad 등 제3자 네트워크로만 정황
  확인 — 확신도 낮음, 실제 착수 시 재확인 필요).

### 최종 언어별 문구(정부 규정 + 업체 자체 필수 문구 결합, 실제 착수 시 그대로 사용 가능)

- **en**: "As an Amazon Associate I earn from qualifying purchases. This video contains affiliate links (#ad)."
- **ja**: 「Amazonのアソシエイトとして、[채널명]は適格販売により収入を得ています。【PR】」
- **es**: "Como Afiliado de Amazon, percibo dinero con las compras elegibles. Contenido publicitario."
- **pt**: "Como participante do Programa de Associados da Amazon, sou remunerado pelas compras qualificadas efetuadas. #publi"
- **ru**: "Реклама" + erid 코드만 — 아마존 자체가 없어서 업체 필수 문구도 없음(정부
  규정만 적용).

### 결정 — 러시아는 채널 유지하되 모네타이즈는 맨 마지막 순서로

러시아어 콘텐츠 채널(topic·TTS·유튜브 계정) 자체는 계속 유지 — 지금처럼 순수
정보성으로 운영. 다만 제휴 수익화는 나머지 4개 언어(아마존 어소시에이트로
비교적 간단)를 먼저 붙이고, 러시아는 Admitad 별도 가입 등 설정이 더 번거로워
맨 마지막 순서로 미루기로 함("니말대로하자" — 완전 배제 대신 우선순위만 뒤로).

## 주의사항

- `bin/`(Rhubarb 바이너리)은 gitignored — 지금은 기본 경로에서 안 쓰지만 남겨둠
- `.venv/`는 gitignored — 클론 후 `python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`
- 커밋 시 `.env` 포함 여부 항상 재확인
- 로컬에서 결과물 바로 확인하려면 `open output/<주제>/dashboard.html` — GitHub Pages 푸시는
  모바일에서 SNS 앱으로 업로드할 때만 필요, 매번 기다릴 필요 없음

---

# 영상 트랙 절 (2026-08-31 CLAUDE.md 본문에서 이관)

영상 제작이 2026-08-25부로 전면 중단돼 아래 절들은 실행되지 않는다. 본문이
1,600줄까지 불어나 새 세션이 지금 지켜야 할 규칙을 찾기 어려워져서 통째로
옮겼다 — 내용은 손대지 않았으니 영상 트랙을 재개하면 그대로 되살릴 것.

## 캐릭터 모션 — 생성 중단 (2026-08-05)

- **새 캐릭터는 모션을 만들지 않는다** — Kling도 `build_static_motion_loop`
  정지 루프 mp4도 호출하지 않는다. 캐릭터는 일러스트(jpg) 생성까지만 —
  `lib/video_assembler.py`의 `_build_character_loop`/`_build_character_segment`가
  확장자로 자동 분기해서 정지 이미지로 처리한다(`--motion`에 `*_illust.jpg`
  경로를 그대로 넘기면 됨, `rebuild_video.py`의 `_char_media_path()`도 동일).
- 과거 Kling 모션 mp4(77개)는 자산으로 남아있고 재사용 가능 — 새로 안 만들면 됨.


## 영상 조립 (`lib/video_assembler.py`)

- **배경 기본값은 칠판 스타일**(`--bg-style chalkboard`, 기본값이라 생략 가능,
  `--images` 불필요) — 실물 칠판 사진 + 분필체 자막 + 랜덤 낙서(별·하트 등
  93종, seed 고정이라 topic마다 재현 가능)+ 명패(26종, 0~2개 랜덤)+칠판 색상
  변형(26종). `--bg-style photo`는 과거 topic 재조립 등 특수한 경우에만.
- **표준 호출 예시**:
  ```
  python3 lib/video_assembler.py \
    --motion "assets_library/motion/<캐릭터>_motion.mp4" \
    --audio "output/<topic>/<topic>_narration.mp3" \
    --srt "output/<topic>/<topic>_narration.srt" \
    --out "output/<topic>/<topic>_shorts.mp4" \
    --title "<훅> <주제명, 자연어>" \
    --title-card-text "<훅만>" \
    --title-card-char "assets_library/illust/<캐릭터>_illust.jpg" \
    --title-banner-photo "assets_library/real/<대표품목>_real_01.jpg" \
    --bg-color 0x00FF00
  ```
  `--title`에 topic 폴더명(슬러그)을 그대로 넣지 말 것 — 자연어 문구만.
- 캐릭터 여러 명(품목 3개 이상)이면 `--motion` 대신 `--motion-schedule
  "시작-끝:경로,..."`(나레이션 기준 0초부터 빈틈없이).
- 실사진 없는 topic은 `--images`에 캐릭터 일러스트를 넣지 말고
  `make_gradient_bg()`로 그라디언트 배경 사용.
- ⚠️ **인스타그램 릴스용 안전 여백 영상 — 포맷 무관 전부 필수**: 칠판 포맷만의
  문제가 아니라 **데스크탑에서 인스타그램 업로드 자체가 이 비율이어야 바로
  되는 호환성 문제**다 — 3개 템플릿(판서형/`before_after_transition`/
  `checklist`) 전부 빠짐없이 만들어야 한다(2026-08-21부로 영상 트랙은
  한국어 단일, 아래 "글로벌 확장" 절 참고).
  `build_instagram_safe_video(source_path, out_path)`(기본 상하좌우 20% 여백)로
  `<원본파일명>_instagram.mp4`를 같은 폴더에 만들어두면 `dashboard.py`가
  자동으로 찾아서 인스타그램 릴스 카드에만 연결한다(원본 `_shorts.mp4`는
  유튜브 자동 업로드용으로 그대로 둠). **영상을 새로 만들거나 재생성할 때마다
  포맷 상관없이 항상 같이 만들 것** — "칠판 포맷일 때만"으로 오해해서 신규
  포맷 영상 다수(47개)에서 누락됐던 적이 있음. ⚠️ **2026-08-17 훨씬 큰
  규모(156개)로 재발 확인·백필 완료** — 원인 특정은 못 했으나(재조립
  파이프라인의 특정 단계가 조용히 이 스텝을 건너뛴 것으로 추정) `shorts.mp4`는
  있는데 짝이 되는 `_instagram.mp4`가 없는 영상이 전체의 1/3 넘게 쌓여있었음.
  기존 `shorts.mp4`를 재렌더링할 필요 없이 `build_instagram_safe_video()`만
  단독 재호출해서 누락분을 채우면 된다(입력이 완성된 최종 영상 하나뿐이라
  narration/spec 의존성이 없음) — 새 topic 작업 후 다음 커맨드로 주기적으로
  스캔·백필할 것:
  ```python
  from pathlib import Path
  from lib.video_assembler import build_instagram_safe_video
  for f in Path("output").glob("*/**/*shorts.mp4"):
      if "_instagram" in f.name:
          continue
      sibling = f.with_name(f.stem + "_instagram" + f.suffix)
      if not sibling.exists():
          build_instagram_safe_video(str(f), sibling)
  ```
- output 폴더 안 파일명은 전부 `<topic>_` 접두어 붙일 것(`card_news.py`/
  `--out`은 직접 지정, `fish_tts.py` 결과는 필요시 rename).


## 배경음악(BGM) (`lib/bgm.py`)

- **소스는 유튜브 오디오 보관함**(studio.youtube.com > 오디오 보관함)에서
  다운로드한 무보컬 트랙만 `assets_library/music/`에 둔다(2026-08-05) —
  저작권 걱정 없는 무료 소스로 확정(Pixabay Audio API는 403으로 막혀있고
  Suno는 무료 플랜이 비상업 전용이라 둘 다 제외 — 상세는 archive "BGM 소스
  선정" 절). `assets_library/music/`는 대용량이라 git엔 안 올라간다.
- **선택**: `lib.bgm.pick_track(seed)`가 seed(topic 폴더명 등) 기준 결정론적
  시드로 트랙 하나를 고른다 — 트랙이 없으면 `None`, 호출부는 BGM 없이 폴백.
- **믹싱**: `BGM_VOLUME_DB=-24dB` + `FADE_SEC=1.5초` 페이드로 "내레이션 절대
  방해 안 하는 밑바탕"용으로만(사용자 확정, 값 조정은 이 두 상수만 바꾸면
  전체 파이프라인에 일괄 반영). `amix`엔 항상 `normalize=0` 명시할 것 —
  기본값이면 입력 개수만큼 자동으로 볼륨을 나눠서 내레이션까지 작아진다.
- 단순 케이스(내레이션 길이만큼만 깔면 되는 3개 신규 템플릿)는
  `lib.bgm.mix_bgm(narration_path, out_path, duration, seed)` 하나로 끝 —
  리턴값을 기존 최종 mux 단계의 `audio_path` 자리에 그대로 넣으면 되고,
  비디오 코덱/트림 로직은 안 건드려도 된다.
- 복합 케이스(`video_assembler.py`의 `assemble()` — 제목 카드·엔딩 카드
  무음 구간까지 포함한 전체 영상 길이 동안 계속 깔아야 해서 기존
  `adelay`/`apad` 필터와 한 filter_complex 안에서 조립해야 함)는
  `lib.bgm.bgm_filter_segment(seed, duration, in_label, out_label)`로 필터
  조각만 받아서 직접 조립할 것.


## 영상 포맷 다각화 (`lib/templates/`)

- **로스터 3개**: 판서형(기존, `video_assembler.py`) + `before_after_transition`/
  `checklist`(`lib/templates/proto_*.py`). `timeline`/`ranking_countdown` 둘 다
  2026-08-05 제외 — `timeline`은 진행 라인/스톱 애니메이션 타이밍이 나레이션과
  안 맞아서, `ranking_countdown`은 실사진 없이 텍스트·배지만으로 구성되고
  item 개수를 spec 그대로(최대 7개) 다 렌더링해서 화면이 과밀해 보여서. 코드는
  각각 `lib/templates/proto_timeline.py`/`proto_ranking_countdown.py`에
  남아있고 `rebuild_video.py`의 `FORMAT_ROSTER`에서만 빠짐, 문제 고치면
  재편입 가능.
- **선택**: `lib/rebuild_video.py`의 `select_format(topic)`이 topic 문자열
  기준 결정론적 시드(`sum(ord(c)*(i*k+c0)...)  % len(options)`, 축마다 다른
  (k,c0))로 하나를 고른다 — 진짜 랜덤 아님(재생성해도 같은 topic은 같은
  포맷), 다른 topic·언어 참고 안 함(전역 상태 없음). `rebuild(topic)`이 그
  포맷대로 자동 분기 렌더링 — `python3 -m lib.rebuild_video <topic>`만
  실행하면 됨.
- ⚠️ **현재 로스터: chalkboard + before_after_transition 1:1(2026-08-11
  최종)** — `_FORMAT_WEIGHTED_POOL = ["chalkboard", "before_after_transition"]`.
  경위: 2026-08-08 3:1:1 가중치 도입 → 2026-08-10 "신규 포맷 퀄 너무 안
  좋다"는 판단으로 `["chalkboard"]`만 남김 → 2026-08-11 `before_after_transition`
  실제 렌더 프레임을 육안 확인해서 두 결함을 고친 뒤(본문·클로징 화면
  top-anchor라 하단 40~50%가 비던 문제 → 세로 중앙 정렬, `_remove_chroma`
  하드컷뿐이라 `_hero_card`에서 크로마키 잔상 보이던 문제 → feather+디스필)
  사용자가 리뷰하고 재승인, 다시 풀에 포함. **`checklist`는 같은 고도화를
  거쳤지만("새 포맷 고도화" 요청으로 render_why/render_closing 세로 중앙
  정렬 + get_icon 배지색 통일까지 완료) 이번엔 로스터에서 제외**(사용자
  명시적 결정, "체크리스트는 빼고 칠판이랑 before after 이 두개") — 코드는
  그대로 남겨둠, 나중에 다시 넣을 수 있음. **프로스펙티브만** 적용(신규/
  재조립하는 것부터, 기존 영상은 소급 재조립 안 함).
  ⚠️ **`before_after_transition`은 클로징 화면이 오래 떠 있으려면 마무리
  멘트 자체를 충분히 길게 쓸 것**(사용자 확인, 테스트 렌더 리뷰 중) — 이
  포맷은 나레이션 길이로 각 화면 노출 시간이 정해지므로, 마지막 화면
  (headline+tip)을 다 읽을 시간을 주려면 그 구간 대본이 다른 화면만큼
  (혹은 그 이상) 길어야 한다 — 짧게 쓰면 화면이 휙 지나가버림.
  ⚠️ **`before_after_transition` 프레임 0(썸네일)·beat 정지 구간 수정
  (2026-08-13)** — "썸네일만 나오고 그다음 첫 이미지가 그대로 이어지는
  느낌" 사용자 지적으로 실측한 결과 훅 화면이 나레이션 길이(수 초)만큼
  프레임 0 역할까지 겸했고, 기전/원인 각 beat도 나레이션 길이(최대 13초)
  동안 완전 정지였다. 판서형과 동일하게 나레이션과 무관한 1.3초 무음
  타이틀 카드를 앞에 별도로 붙이고(`TITLE_CARD_DURATION`), 모든 beat
  세그먼트(`_build_segment`)에 zoompan 기반 미세 줌(최대 1.045배, beat
  길이 역산이라 길이 무관하게 처음부터 끝까지 서서히 확대)을 추가해 정지
  구간 자체를 없앴다. **프로스펙티브만 적용** — 기존 렌더링된 영상은 소급
  재조립 안 함(위 "프로스펙티브만" 원칙 그대로), 재조립하면 자동 반영됨.
- ⚠️ **재조립 필요(2026-08-06)**: `checklist`/`before_after_transition` 둘 다
  이 날짜에 실측 버그 다수 수정 — **커밋 `21288ffd`~`88251692` 이전에
  렌더링된 기존 영상은 아래 문제들을 그대로 갖고 있어 재조립 필요**:
  - `checklist`: 항목별 원인→해결책, 헤더 문구 전환 시 텍스트 겹침(순차
    페이드로 수정), 원인→해결책 타이밍이 실제 나레이션과 최대 8.7초 어긋남.
  - `before_after_transition`: 오프닝 캐릭터 누락(cover_char_file 없는
    topic), 원인 구간이 실제 나레이션보다 최대 12초+ 일찍 뜨는 어긋남,
    **"해결책 항목별 화면(N개)" 구조를 없애고 "원인 화면들 → 요약(클로징)
    화면 하나"로 변경**(해결책을 원인만큼 자세히 나레이션하지 않는 topic이
    많아 항목별 화면 하나당 줄 수 있는 시간이 구조적으로 부족했음 — 코골이_1
    실측: 항목별일 때 최대 4초 vs 요약 화면 하나로 합치니 8.5초).
  - 둘 다: 훅 화면 장식 스타일 4종(plain/banner/boxed/underline) +
    `before_after_transition`은 위치 지터도 추가(`checklist`는 이미 있었음).
  재조립은 `python3 -m lib.rebuild_video <topic>` 재실행(같은 topic은
  `select_format`이 결정론적이라 포맷 그대로 유지됨). mp4 재생성 후
  인스타그램 크롭(`build_instagram_safe_video`)·`dashboard.html`도 함께
  갱신할 것(위 "영상 조립" 절 참고).
- 신규 4개 템플릿 공통 시그니처: `render(topic_dir, lang, audio_path,
  srt_path, spec_path, out_path)`. `card_news_spec.json`의 `items` 개수를
  그대로 읽어서 3개 고정 아님. 폰트는 `video_assembler.py`의
  `_title_font_for_lang`/`_wrap_text_for_lang` 재사용(16개 언어 자동 지원).
- **안전영역**: `_YT_SAFE_RIGHT=150`/`_YT_SAFE_BOTTOM=320`(유튜브 Shorts 앱
  UI가 가리는 영역) — 4개 템플릿 전부 이 값을 로컬 상수로 복제해서 모든
  텍스트 블록이 `(0,0,W-150,H-320)` 안에 들어오도록 강제 체크한다. 새
  템플릿 추가 시 반드시 넣을 것 — 빠뜨리면 실기기에서 텍스트가 잘려 보인다.
- **topic-seeded 다양화를 신규 포맷에도 반드시 적용할 것** — 판서형이
  이미 하던 방식(색상 변형 26종·낙서 93종·명패 0~2개 랜덤 배치 등, 전부
  topic 문자열 기반 결정론적 seed)과 동일한 원칙을 `before_after_transition`/
  `checklist`/`ranking_countdown`에도 확인·적용한다. 각 템플릿이 이미 갖고
  있는 축(테마 색상, 배지 모양, wipe 스타일, 진행 표시 스타일 등)을
  `python3 -m lib.check_video_staleness` 같은 감사 없이도 주기적으로
  points-of-repetition 관점에서 재점검할 것 — 저품질/반복 콘텐츠 정책
  리스크와 직결됨(YouTube "inauthentic content" 정책, 2026-08-05 리서치).
- 산출물이 코드보다 오래됐는지는 `python3 -m lib.check_video_staleness`로
  확인(미게시 topic만 대상, 재생성은 자동으로 안 함).
- ⚠️ 이 영상 포맷 시스템(코드+문서) 전체가 2026-08-04에 한 번 커밋 안 된 채
  통째로 유실됐다가 재구축됨 — **이 종류 작업(대규모 신규 코드)은 완성되는
  즉시 커밋할 것**, 리뷰용이라고 미루지 말 것.
- **오프닝(프레임 0) = 사실상 피드 썸네일**(커스텀 썸네일 업로드 없음) —
  `before_after_transition`/`checklist`/`ranking_countdown` 전부 프레임 0
  전용 훅 화면 렌더러를 따로 둔다. 새 템플릿도 아이템 화면을 프레임 0에
  그대로 쓰지 말 것.
  - 훅 화면 노출 시간은 SRT 첫 구간(`entries[0]`) 길이 그대로 쓸 것 — 실제
    훅 나레이션 문장이라 임의 비율로 잡으면 나레이션과 화면이 어긋난다.
  - 중앙정렬은 안전영역 중심이 아니라 캔버스 진짜 중앙(`VISUAL_CX = W/2`)
    기준으로: 안전박스 자체가 좌우 비대칭(왼쪽 50 vs 오른쪽 150)이라 안전
    영역 중심에 맞추면 화면상 왼쪽으로 쏠려 보인다. `VISUAL_CENTER_MAX_WIDTH
    = 2*min(VISUAL_CX-SAFE_LEFT, SAFE_RIGHT-VISUAL_CX)`로 폭을 캡핑하고
    `VISUAL_CX`에 정렬하면 두 조건(안전영역 준수 + 시각적 중앙) 동시 만족.
  - 오프닝 프레임 리뷰 이미지는 `output/_thumbnail_review/`에 모아둠(git 추적).
- **텍스트 전환은 항상 순차 페이드, 동시-알파 크로스페이드 금지**: 서로 다른
  문단/문구를 동시에 반투명 겹쳐 그리면(체크박스·아이콘처럼 모양이 비슷한
  게 아니라 텍스트라서) 글자 단위로 뒤섞여 읽을 수 없다 — 전환 구간 앞
  절반엔 이전 텍스트만 페이드아웃, 뒤 절반엔 새 텍스트만 페이드인할 것.
  `checklist`에서 항목별 fact/fix, 헤더 문구 두 곳 모두 이 문제였음
  (2026-08-05 수정). 새 텍스트 전환 로직 추가 시 이 패턴 적용.


## TTS

⚠️ **한국어는 Voicebox로 전환(2026-08-17)** — `lib/voicebox_tts.py`,
Fish Audio 한국어 보이스 품질 불만족으로 로컬 Voicebox.app(Qwen3-TTS MLX,
클론 보이스 "한국어1")과 실측 비교(같은 대본으로 생성해서 직접 청취) 후
교체 확정. **en/ja는 대상 밖 — 계속 `lib/fish_tts.py`**(아래 그대로).
새 한국어 topic은 반드시 `python3 lib/voicebox_tts.py <topic> <text>` 사용.

- ⚠️ **Voicebox.app이 이 컴퓨터에서 항상 켜져 있어야 함** — 클라우드 API가
  아니라 로컬 추론(기본 포트 17493). 꺼져 있으면 `synthesize()`가 바로
  에러(연결 실패 메시지로 원인 명확히 표시, 조용히 실패하지 않음).
- ⚠️ **생성이 느림** — 문장 단위로 API를 호출해서 이어붙이는 구조라(아래
  WHY), 실측 700자 나레이션 기준 총 100초 안팎. 여러 topic 한 번에 돌릴
  땐 이 배속을 감안할 것.
- ⚠️ **word-level alignment가 없어서 문장 단위로 쪼개 호출**(fish_tts.py의
  `_MAX_SENTENCES_PER_CALL=1`과 발상은 같으나 원인은 다름 — Fish는 응답
  안정성 때문, Voicebox는 애초에 정렬 타임스탬프 자체를 안 줌): 문장 하나당
  호출 하나 → 결과 오디오 길이(ffprobe 실측)를 그 문장 구간으로 그대로 씀 →
  narration.srt는 문장 단위 엔트리(fish_tts.py의 `_build_srt` 결과와 동일한
  granularity라 하류 파이프라인엔 차이 없음).
- **보이스는 지금 "한국어1" 프로필 하나로 고정**(`PROFILE_ID_KOR` 상수) —
  fish_tts.py처럼 topic마다 랜덤 선택하지 않음. 보이스를 더 늘리기로
  하면 풀 방식으로 바꿀 것.
- `synthesize(topic, text, voice_name=None, lang="kor")` 반환 모양은
  fish_tts.py와 동일(`audio_path`/`srt_path`/`duration`/`word_count`/
  `words`) — 호출부는 어느 TTS를 쓰는지 신경 쓸 필요 없음. `lang="kor"`
  외 값을 넘기면 즉시 에러(en/ja를 실수로 여기로 보내는 걸 막음).

### 나레이션 TTS 검증 도구 2종 (2026-08-18 도입 — 필수)

⚠️ jp-review-shorts 콜라겐씨젤리_JP_1 한국어 나레이션 "미용 성분이
7종에서 10종으로"가 TTS에서 "칠곱종"/"셥종"처럼 한자어(칠·십)와
순우리말(일곱·열) 숫자 읽기가 뒤섞여 깨지는 사고를 사용자가 직접
청취로 발견 — health-shorts 나레이션 412개를 같은 패턴으로 전수
스캔했더니 **25개 topic이 동일 위험군**으로 확인됨(2026-08-18,
`data/*/narration.txt`/`data/*/ko/narration.txt` 대상). 사용자가
"한국어는 내가 들을 수 있지만 en/ja는 뭐가 문제인지도 모르겠다"고
지적 — 이 두 도구로 대응한다:

- **`lib/narration_qa.py`** — "작은 수+순우리말 카운터"(개/명/종/가지/
  번/살/마리 등) 패턴을 정규식으로 미리 잡아내는 텍스트 레벨 체크.
  **한국어 narration.txt를 다 쓴 직후, TTS 호출 전에 항상
  `python3 -m lib.narration_qa data/<topic>/[ko/]narration.txt` 실행할
  것.** 연도·만/천 단위 큰 수·정수 퍼센트는 검사 대상 아님(이미 자연스럽게
  읽힘) — `COUNTERS` 목록 참고.
- **`lib/tts_audio_qa.py`** — TTS 오디오를 Voicebox 내장 Whisper로
  재전사해 원문과 비교하는 **언어 무관** 검증 도구. 세션은 오디오를
  못 듣지만 전사된 "텍스트"는 en/ja든 뭐든 읽고 원문과 비교할 수 있다는
  게 핵심 — jp-review-shorts en 나레이션에서 이 방법으로 "MS 1500"이
  "Mississippi 1500"으로 읽히는 실제 오독을 찾아낸 전례 있음. 사용법:
  `python3 -m lib.tts_audio_qa <audio_path> <narration.txt 경로>
  <lang(ko/en/ja)>`. ⚠️ 자동 pass/fail 게이트 아님 — Whisper 자체가
  숫자 표기를 정규화하므로(예: "일곱 종"을 다시 "7종"으로 씀) 완전
  일치를 기대하면 안 되고, 원문에 없는 엉뚱한 단어·뜻 없이 뭉개진 구간
  (특히 브랜드명·고유명사·약어)만 의심 신호로 볼 것.
- ⚠️ **25개 영향 topic 잔여 처리는 아직 안 함** — 텍스트 수정 자체는
  안전하지만(narration.txt만 고치면 됨), 이미 발행된 영상까지 재렌더링·
  재업로드할지는 규모(412개 중 25개, 다수가 이미 유튜브에 라이브)를
  감안해 사용자와 범위를 먼저 정한 뒤 진행할 것 — 무단으로 대량
  재업로드하지 말 것.

### Fish Audio (`lib/fish_tts.py`, en/ja 전용)

⚠️ **Fish Audio로 전환(2026-08-14, Typecast 완전 폐기)** — Typecast 음성
품질 불만으로 교체 결정. `TYPECAST_API_KEY`는 `.env`에서 제거됐고
`lib/typecast_tts.py`/`data/typecast_voices*.json`도 삭제됨(2026-08-14) —
2026-08-14 이전에 이미 Typecast로 생성된 오디오는 sunk cost로 그대로 둔다
(재생성 안 함).

- `synthesize(topic, text, voice_name=None, lang="kor")` — 반환 모양은
  `audio_path`/`srt_path`/`duration`/`word_count`/`words`이고, 호출 관례도
  호출 관례도 그대로: `topic` 인자에 언어 세그먼트를 포함시켜서 넘긴다
  (`"눈_8"`→ko, `"눈_8/en"`→en, `"눈_8/ja"`→ja — output 경로가 topic 문자열
  그대로 `output/<topic>/narration.*`가 되므로). CLI:
  `python3 lib/fish_tts.py <topic> <text> [voice_name]`.
- 보이스 미지정 시 언어별 풀(`data/fish_audio_voices.json`(kor)/
  `_en.json`/`_ja.json`)에서 랜덤 선택. **캐릭터 여러 명 topic도 항상 단일
  보이스** — 멀티보이스 세그먼트 방식은 안 씀.
- ⚠️ **보이스 풀 큐레이션 기준(중요)** — Fish Audio `/model` 목록은 자체
  큐레이션이 아니라 사용자 업로드 보이스 클로닝 마켓플레이스라 실존 인물
  (연예인·정치인 등)·저작권 캐릭터 클론이 섞여 있다. 세 JSON 파일 모두
  제목에 실존 인물·캐릭터 이름이 전혀 없는(순수 설명형 제목만) 항목만
  사람이 확인 후 큐레이션한 목록 — 새 보이스 추가 시 항상 이 기준으로 확인.
- **배속 없이 원 속도** — `AUDIO_TEMPO = 1.0`(health-shorts 기존 원칙 그대로
  유지, 변경 없음).
- ⚠️ **나레이션 텍스트의 소수점 퍼센트는 말로 풀어 쓸 것** — Fish Audio는
  "0.09%"처럼 숫자 사이 마침표가 있는 토큰에서 문장 경계 정렬(alignment)이
  깨진다(jp-review-shorts 실측). "0.09%" 대신 "0.09퍼센트"/"영점 영구
  퍼센트"처럼 TTS가 읽을 문장에서만 풀어 쓸 것 — 화면 표시용(카드뉴스 등)
  텍스트는 대상 아님.
- ⚠️ **긴 나레이션은 문장 단위로 API를 여러 번 호출한다(`_MAX_SENTENCES_PER_CALL
  = 1`, 자동 처리됨)** — Fish Audio 스트리밍 응답이 청크 여러 개로 나뉘는
  긴 텍스트에서 마지막 청크가 잘리는 재현 버그가 있어서, 이 파일이 내부적으로
  문장 하나당 API 호출 하나로 쪼개고 WAV로 재조립한다. 호출부는 신경 쓸 필요
  없음 — `synthesize()`만 호출하면 됨.


## 유튜브 쇼츠 자동 업로드 (`lib/youtube_upload.py`)

⚠️ **한국어 채널 신규 업로드 완전 중단(2026-08-21, "트래픽이 전혀 나오지않아")**
— `python3 lib/youtube_upload.py`(단일/`--backlog` 등 전부)를 ko에 더 이상
쓰지 않는다. "유튜브에 올려줘"/"업로드해" 같은 트리거 문구가 와도 실행하지
말고, 이미 트래픽 이유로 안 올리기로 했다고 안내할 것. 이미 라이브된 기존
ko 영상은 삭제 요청 없었으니 그대로 둔다(내려달라는 별도 지시가 없는 한
손대지 말 것). 영상 자체(숏츠 mp4)는 계속 만들어서 인스타그램 릴스·틱톡·
네이버 클립 등 다른 영상 플랫폼엔 그대로 씀 — 유튜브 업로드 단계만 스킵.
아래는 이 결정 이전에 쓰던 세부 동작 기록(참고용, 재개 시에만 유효).

- ⚠️ **ko topic 경로는 flat이 원칙이지만 코드가 flat/`ko/` 서브폴더 둘 다
  지원함(2026-08-17)** — "topic 폴더명" 절 원칙대로 ko는 언어 하위 폴더 없이
  `output/<topic>/`·`data/<topic>/`에 바로 있는 게 정상이다. 그런데 실측
  발견: 귀_7/소화_15/순환_9/어지럼증_8/여성_6/여성_9/피로_2 7개 topic이 한
  세션의 실수로 `output/<topic>/ko/`·`data/<topic>/ko/`에도 콘텐츠가 통째로
  재생성된 채 남아있었다(구버전 flat 산출물은 시각적으로도 색상 등이 달라
  발견됨 — 구버전은 삭제, 새 버전인 `ko/`는 그대로 둠). 업로드 코드
  (`_has_video`/`_upload_short_inner`)는 원래 flat만 봐서 이 7개 topic이
  "영상 없음"으로 판정돼 업로드 후보에서 조용히 빠지고 있었다 — `_content_dir`/
  `_content_data_dir` 헬퍼를 추가해 flat을 먼저 보고 없으면 `ko/`로 폴백하게
  고쳤다. **새 topic 작업 시 ko 콘텐츠를 실수로 `ko/` 하위 폴더에 만들지 말
  것**(다른 언어와 헷갈리기 쉬움) — 이미 만들어져 있어도 위 폴백 덕에
  업로드는 되지만, `data/`·`output/` 둘 다 flat이 원칙이라는 걸 잊지 말 것.
- 단일 topic: `python3 lib/youtube_upload.py <topic> [private|unlisted|public] [예약시각]`
- ⚠️ **일괄 업로드 표준은 `--backlog`(2026-08-07, `--daily-per-channel`
  대체) — "유튜브에 올려줘"/"업로드해" 등 트리거 문구 하나로 요청하면 이걸로
  진행할 것**: `python3 lib/youtube_upload.py --backlog [privacy]`
  (`upload_backlog()`). 채널(언어)마다 아직 안 올라간 topic 전체를 한 번에
  큐잉해서, `uploadLimitExceeded`(계정당 하루 실제 업로드 개수 제한 — API
  쿼터와 무관하고 `publishAt` 예약으로도 못 피함, 2026-08-07 실측: pt
  17개·en 31개·es 34개에서 채널마다 다른 상한에 걸림)에 부딪힐 때까지
  자동으로 밀어붙인다 — 채널별로 몇 개가 안전한지 미리 추측할 필요 없음.
  한도 도달 시 그 채널만 즉시 포기하고(`_is_upload_limit_error`) 다음 채널로
  넘어가며, 스케줄은 `_last_scheduled_publish_at`으로 그 채널에 이미 예약된
  것 중 가장 늦은 시각 다음부터 이어붙인다(겹침 방지). topic이 다 소진되면
  새로 준비되는 만큼만 올라가서 자연히 하루 2개 안팎으로 수렴함.
  - ⚠️ **`--daily-per-channel`/`--daily-batch`는 레거시 — 새로 쓰지 말 것.**
    전자는 채널당 하루 2개로 고정해 backlog가 쌓이면 못 따라가고, 후자는
    언어 확장 전 한국어 단일 채널 시절 로직이라 6개 채널 체제에 안 맞는다.
- 업로드 성공 시 자동으로: 카테고리 재생목록에 추가(중복 삽입 방지 확인 후),
  Supabase `youtube_uploaded` 테이블에 기록. 커스텀 썸네일 자동 설정은 하지 않음
  (2026-08-08 재확인: `thumbnails().set()` API 자체는 정상 작동하고 채널
  그리드·검색·구독피드엔 반영되지만, 조회수 대부분이 나오는 스와이프 쇼츠
  재생 화면엔 유튜브 플랫폼 자체 한계로 절대 반영 안 됨 — 자동화 가치 없어
  보류) — 유튜브 자동 제안 또는 Studio 수동.
- ⚠️ **업로드 추적은 Supabase `youtube_uploaded`가 유일한 근거 — 로컬
  `output/youtube_uploaded.json`은 폐기(2026-08-15)** — 예전엔 이 git 추적
  파일이 "이미 올렸는지"의 유일한 판단 근거였는데, 실제로 두 번 사고가 났다:
  ①무관한 커밋(대시보드 재생성 버그)이 실수로 기록 67건을 날려서, 그 사이
  `--backlog`가 이미 올라간 topic을 재업로드(vernhaven ko 채널에서 10개+
  topic 중복 실측). ②동시 세션 경쟁 — 두 세션이 비슷한 시각에 `--backlog`를
  돌리면 로컬 파일 읽기-쓰기 사이에 락이 없어 같은 topic을 동시에 집을 수
  있음. Supabase `youtube_uploaded`는 `topic`이 PRIMARY KEY라 DB 자체가
  유일성을 보장하고, `upload_short()`가 **실제 YouTube API를 부르기 전에**
  `_sb_reserve_upload(topic, lang)`으로 그 topic을 `status='pending'`으로
  원자적 INSERT 시도한다 — 이미 누가 예약/확정해뒀으면(PK 충돌) 조용히
  실패하고 `upload_short()`가 그 즉시 RuntimeError로 끝난다(API 호출 자체를
  안 함, 경쟁 조건이 물리적으로 닫히는 지점). 성공하면 업로드 진행 후
  `_sb_finalize_upload()`로 `status='confirmed'`+실제 `video_id` 기록,
  실패하면 `_sb_release_upload()`로 예약 행을 지워서 나중에 재시도 가능하게
  한다. `select_daily_topics*()`의 후보 조회도 매번 Supabase를 새로
  읽는다(로컬 캐시 없음 — 다른 세션이 방금 올린 것까지 즉시 반영돼야 함).
  `lib/youtube_organize_playlists.py`(채널 스캔으로 소급 정리)는 예약 단계가
  필요 없어 `_sb_record_existing_upload()`(즉시 upsert)를 쓴다. `index.html`은
  원래도(2026-08-08 스키마 도입 시점부터) Supabase를 직접 읽었으므로 이번
  변경 대상 아님 — Python 스크립트 쪽만 로컬 파일에서 Supabase로 옮겼다.
- OAuth 1회 설정은 archive의 "유튜브 쇼츠 자동 업로드" 절 참고(사용자가 직접
  해야 하는 단계 포함).
- ⚠️ **리프레시 토큰은 ~7일마다 만료됨(OAuth 앱이 "테스트" 상태라 구글 정책상
  자동 만료, 2026-08-11 실측: ko `invalid_grant: Token has been expired or
  revoked`)** — 업로드 시도 시 이 에러가 뜨면 토큰 만료다. 재발급 절차:
  `python3 lib/youtube_auth_setup.py [--channel <코드>]`(코드 없으면 ko) →
  브라우저가 자동으로 열리고 해당 채널 소유 구글 계정으로 로그인+동의만
  하면 스크립트가 새 refresh_token을 바로 출력함 → `.env`의 `YOUTUBE_
  REFRESH_TOKEN`(또는 `YOUTUBE_<코드>_REFRESH_TOKEN`)에 그 값으로 교체 →
  `_get_credentials(lang)` 재호출로 정상 작동 확인. 브라우저 로그인은
  사용자가 직접 해야 하는 단계라, 만료 감지 시 이 절차 그대로 안내하고
  진행할 것(프로덕션 전환은 유튜브 전체관리가 "민감 스코프"라 구글 앱
  검수가 필요해서 개인 단일 채널 규모에선 배보다 배꼽 — 그냥 만료될 때마다
  재발급하는 쪽을 유지하기로 함).
- ⚠️ **캡션 구분자(`CAPTION_MARKERS`) 언어별로 여러 후보 필요** — 같은
  언어라도 topic마다 "タイトル:/説明:"·"Title:/Description:" 등 다른
  표기가 섞여 있을 수 있음(2026-08-07 ja에서 "概要欄:" 변형 발견, 10개
  topic 업로드 실패로 드러남). 새 SKIP/실패가 구분자 불일치면 해당 언어의
  `CAPTION_MARKERS` 후보 리스트에 실제 발견된 표기를 추가할 것.
- 제목 100자 초과 시 `upload_short`가 단어 경계에서 자동으로 잘라 올림
  (YouTube `invalidTitle` 거부 예방, 2026-08-07) — 새 topic 캡션 작성 시
  제목을 100자 이내로 쓰는 게 우선이고, 이건 최후 안전장치일 뿐.


---

# 2026-09-24 정리 직전 CLAUDE.md 전문

본문을 활성 규칙만으로 다시 쓰기 직전 상태를 그대로 보존한다(커밋 e96b280b7 시점). 본문에서 빠진 경위·수치·인용문은 여기서 찾는다.

# health-shorts

건강 상식 정보를 카드뉴스 + 숏폼(캐릭터 나레이션) + 플랫폼별 캡션으로 만드는 파이프라인.
`shopping-shorts-video`와는 완전히 별도 프로젝트.

## 🚨 영상 포맷은 `XRAY_FORMAT.md` 하나만 본다

반투명 인체 포맷의 화면 구성·시간표 규칙·조립 전후 검사는 전부 `XRAY_FORMAT.md`에 있다
(2026-09-24 견본 `소화_14` 확정). 육아 건강 채널·댕냥사전도 같은 문서를 따른다.

🚨 **완료 = 완성본을 재고 눈으로 본 뒤다.** `preflight_xray.py`(조립 전 스펙) 0건 → `xray_build.py`
(조립·카드·deploy·대시보드·미션컨트롤까지 한 번에) → `verify_output.py`(완성된 mp4) 0건 → 도입부·칸 구간·
칠판 구간 프레임을 직접 확인. 2026-09-24 하루 동안 "0건 통과 = 완료"로 보고했다가 사용자가 결함을
다섯 번 먼저 찾았다 — 검사 통과는 영상이 맞다는 뜻이 아니다.

## 배포 채널 — 네이버 블로그(카드뉴스) + 네이버 클립(영상) (2026-09-11 갱신)

🚨 **영상 트랙 재개, 배포처는 네이버 클립 하나뿐이다 — 유튜브에는 올리지 않는다
(2026-09-11 사용자 확정).** 2026-09-09에 "영상 트랙 다시 중단"으로 적어뒀던 방침을
대체한다. 상품 링크는 네이버 커넥트만 쓴다(쿠팡 파트너스 링크 기능은 제거된 상태 유지).

- **유튜브 업로드는 하지 않는다** — `lib/youtube_upload.py`는 재개 대비로 남겨두되
  호출하지 않는다. Supabase `youtube_uploaded` 테이블은 2026-09-11에 전량 비웠다
  (325건 전부 실물 없는 유령 기록이었음 — ko 채널 영상 0개, ja 계정 삭제됨 실측
  확인. 백업: `~/.claude/backups/health-shorts/youtube_uploaded_20260911_1732.json`).
- `_ALLOWED_PLATFORMS`(`lib/dashboard.py`·`lib/mission_control_sync.py`)는
  `{"네이버 블로그", "네이버 클립"}` — 이 둘만 대시보드·mission-control에 뜬다.

- 페이스북·인스타그램 릴스·틱톡·쓰레드·유튜브 쇼츠는 대시보드·mission-control
  양쪽에서 숨는다(캡션 원본은 안 지웠으니 되살리려면 `_ALLOWED_PLATFORMS`만 넓히면 됨).
- 새 topic `platform_captions.json`에는 **네이버 블로그 + 네이버 클립** 둘을 넣는다.
- 상품 링크 UI에서 쿠팡 검색·입력·검증·고지문은 제거된 상태 — 네이버 브랜드커넥트
  링크만 남는다.
- 영상 트랙 상세 규칙(렌더 명령어·TTS·모션 등)은 `CLAUDE_ARCHIVE.md` "영상 트랙 절"
  참고. 포맷 규칙은 바로 아래 절이 최신이다.

## 🚨 나레이션 보이스 — 채널 통일 (2026-09-12 확정)

**AI 영상 콘텐츠(ai-video-network / 1biteinfo)와 같은 보이스로 통일한다** — 사용자 지시
("헬스숏츠도 이후 목소리는 전부 AI 동영상 콘텐츠 만드는 그 TTS로 통일하자").

- 기본 보이스: **`30대 남자 인터뷰어`**(차분하고 친근함). `lib/fish_tts.py`의
  `DEFAULT_VOICE_BY_LANG`에 상수로 고정돼 있다 — en `Sleepless historian`,
  ja `落ち着いた男性`도 1biteinfo와 동일하다.
- 🚫 **topic마다 보이스를 난수로 고르던 `_random_voice_name()`은 폐기했다.**
  "채널 목소리가 단조로워지지 않게" 한다는 의도였으나, 실제로는 같은 채널에
  성별·연령대가 제각각인 영상이 쌓였다(18편 표본에서 남성 11/여성 7, F0 91~258Hz).
- 🚨 **한국어는 이 보이스 하나만 쓴다(2026-09-19 사용자 확정)** — `synthesize()`가 다른 이름을 받으면
  예외를 낸다. 반투명 인체 포맷 시험(소화_9)에서 이 보이스로 전체를 다시 뽑고 확정했다.
- ⚠️ **두 채널군의 톤을 맞추는 게 목적**이므로 여기만 바꾸지 말고
  `ai-video-network/1biteinfo/CLAUDE.md`의 기본 보이스와 항상 같이 볼 것.
- 기존 영상은 재생성하지 않는다(TTS 재생성 금지 원칙) — **이후 만드는 것부터** 적용.

## 🚨 영상 포맷 — 칠판(chalkboard) 단독 고정 (2026-09-11 확정, 재협상 금지)

**아래 3가지는 여러 날에 걸쳐 반복 지적된 사항이다. 영상 코드를 건드릴 땐 먼저 이
절을 확인하고, 셋 중 하나라도 어긋나면 그 자리에서 고칠 것.**

1. **포맷은 칠판 하나뿐** — `lib/rebuild_video.py`의 `_FORMAT_WEIGHTED_POOL =
   ["chalkboard"]`. before_after_transition·checklist 등 신규 포맷 코드는 남아
   있지만 풀에 다시 넣지 않는다.
2. 🚨 **썸네일 카드 문구는 병명 한 덩어리만**(2026-09-21 사용자 "썸네일은 이제부터 딱 그 병명만 박아") —
   "위염", "중이염", "녹내장". 문장을 넣으면 0.2초 카드에 작게 깔려 안 읽힌다. 설명은 제목(캡션)이 한다.
   `rebuild_video`가 `spec["thumb_word"]`, 없으면 title 첫 줄의 쉼표 앞부분을 뽑아 `title_card_text`로 넘긴다.
   ⚠️ 반투명 인체 포맷은 도입부 Flow 클립이 0초부터 덮으면 이 카드가 사라진다 — `xray_splice`가 0.2초부터
   얹는다(2026-09-21 실측으로 발견, 썸네일이 Flow 첫 프레임으로 바뀌어 있었다).

2-1. **맨 앞 썸네일 카드는 0.2초** — `assemble()`의 `title_card_duration` 기본값과
   `proto_before_after_transition.TITLE_CARD_DURATION` 둘 다 0.2. 썸네일 프레임만
   잡고 바로 본론으로 넘어간다.
3. **훅 직후 무음 요약 카드는 폐기됨** — 2026-08-16에 "결론을 먼저 던지자"로 넣었던
   summary_card는 나레이션 없이 2.4~5초를 잡아먹어 도입을 늘어뜨렸다. 코드에서
   완전히 제거했으니(인자·CLI 옵션·테스트 포함) 되살리지 말 것.
4. **상단 후킹 배너도 폐기됨(2026-09-17)** — `rebuild_video`가 `show_title_banner=False`를
   넘긴다. 배너는 제목 전체(훅+주제 라벨)를 화면 위 4줄까지 깔아두는데, 도입부엔
   칠판 자막이 같은 훅 문장을 그대로 다시 말한다 — **0~5초에 같은 내용이 9줄로 겹쳐
   떠서** 이탈을 가르는 구간이 텍스트 벽이 돼 있었다(눈_25 프레임 실측). `title`
   인자 자체는 색상·캐릭터 배치 시드로 계속 쓰이므로 빈 문자열로 넘기지 말 것.
5. **도입부 조건절 대사도 잘라냈다(2026-09-17)** — "[증상 나열]~라면 주목하세요"식
   첫 문장은 정보를 하나도 안 주면서 평균 5.6초(최장 8.7초)를 먹었다.
   `scripts/trim_opening_summary.py`가 오디오·자막·원고에서 그 문장만 잘라낸다 —
   **TTS 재호출이 없다.** 30편에 적용해 총 168초를 제거했다. 컷 지점은 srt
   타임스탬프(TTS 추정값이라 몇십 ms 어긋난다)가 아니라 `silencedetect`로 찾은
   실제 무음 한가운데다 — 그래야 앞 문장 꼬리가 안 남고 다음 문장 첫 음절도 안 날아간다.
   원본 백업: `~/.claude/backups/health-shorts/narration_pretrim_20260917/`.

6. **네이버 클립 안전영역(2026-09-18)** — 네이버 클립은 폰 화면을 채우려고 **양옆을 약 48px씩
   잘라내고**, 오른쪽 x>900(y≈990~1720)에 좋아요·댓글·공유 버튼, y<100에 닫기·분석 버튼,
   y>1455에 채널명·캡션·광고 칩을 얹는다(실사용 스크린샷에서 환산). 그래서 칠판과 그 위 장식을
   가운데 세로 띠(`CHALKBOARD_STRIP_X`/`_W`, 110px = 네이버가 자르는 96px + 여유)를 잘라내 **폭만** 좁혀 테두리가 안 잘리게 하고(균일 축소는 위아래까지 줄어 금지), 자막 폭은 720px, `광고` 표시와
   품목 라벨은 `NAVER_SAFE_RIGHT`/`NAVER_SAFE_TOP` 안쪽에 둔다(`광고` 표시는 공정위 의무라 가려지면
   안 된다). 칠판 위에 얹는 요소(자막·CTA)는 `_board_xy()`로 같은 축소 변환을 거친다 — 절대좌표로
   얹으면 칠판과 어긋난다. 레이아웃을 바꾼 뒤엔 `scripts/rerender_all_videos.py`로 전량 재조립할 것.

7. **반투명 인체 포맷(`data/<topic>/xray.json`이 있는 topic)** — 규칙은 전부 `XRAY_FORMAT.md`. 여기 적혀 있던
   2026-09-19 시험 단계 서술(도입부 8초 고정·훅 한 문장 6초·"~부터 확인하세요" 훅 등)은 그 뒤 뒤집혔으므로
   지웠다 — 두 곳에 적어두면 옛 쪽을 읽고 틀린다. 클립 색인은 `assets_library/xray/clip_index.json`
   (`python3 -m lib.clip_index --show <이름>`), 요청 자리는 `assets_library/xray/작업/`(이름 고정).

기존 영상 109편은 이 규칙으로 전부 재생성 완료(2026-09-11) — 전량 미게시 상태였다.

## 🚨 제작 방법론은 `PIPELINE_METHOD.md` 하나로 (2026-09-24 확정)

검색어에서 출발하는 원고 작성, 공공데이터 수치, 클립 색인, 행위 클립 디테일 기준, 부위 클립 region —
**실제로 돌려보고 확정한 것들을 한 문서에 모았다.** 원고·영상 작업 전에 그걸 먼저 읽는다.

**다른 채널(댕냥사전 등)도 그대로 쓸 수 있게 뺀 문서다** — 경로만 자기 프로젝트에 맞게 바꾸면 된다.
이식 가능한 도구: `lib/naver_keywords.py` · `lib/naver_searchad.py` · `lib/clip_index.py` ·
`lib/topic_search_rank.py` · `scripts/make_part_clip.py`. 공공데이터는
`~/Desktop/project/public-data/`에 이미 받아져 있어 프로젝트를 안 가린다.

## 🚨 채널명은 「건강만사전」 (2026-09-23 확정)

사람이 보는 모든 표기는 이 이름을 쓴다 — 영상 엔딩 카드(`video_assembler.BRAND_NAME`), 카드뉴스
마무리 CTA, 캡션 해시태그 `#건강만사전`(태그 줄 맨 앞), 대시보드 제목.

⚠️ **내부 식별자는 안 바꾼다** — 저장소 폴더명 `health-shorts`, R2 키 프리픽스
`health-shorts/card_news/`, Supabase 테이블·`source_project` 값. 다른 프로젝트
(`ai-video-network/deploy/health-shorts/`, vernhaven `ingest_health_shorts.py`)가 이 문자열로
경로를 맞추고 있어 바꾸면 그쪽이 조용히 깨진다.

## 🚨 제목은 검색어부터 — 감이 아니라 월간 검색량으로 (2026-09-23 확정)

사용자 지시: "네이버 클립은 사람들이 검색했던 검색어 기준으로 영상을 띄워주는 경향이 있다. 제목도
검색어 트렌드에 맞춰 짓는 게 핵심." 기존 "훅은 핵심 키워드로 시작" 규칙은 그 키워드가 **실제로
검색되는 말인지**를 한 번도 확인하지 않았다.

```
.venv/bin/python3 -m lib.topic_search_rank --report              # 저장된 순위표
.venv/bin/python3 -m lib.topic_search_rank --topics <topic>      # 특정 topic 재측정
```

- **의미는 자동완성이, 숫자는 검색광고 API가 준다.** keywordstool의 연관 키워드를 그대로 믿지 말 것 —
  광고주 그룹 기준이라 "족저근막염"을 던지면 "대상포진"·"독감예방접종"이 딸려온다. `topic_search_rank`는
  자동완성으로 후보를 만든 뒤 그 후보들의 볼륨만 조회한다. 진료과·병원 키워드는 `_CLINIC`으로 걸러진다
  (검색량은 크지만 **병원을 찾는 검색**이라 건강정보 영상으로는 안 잡힌다).
- 고른 검색어는 `card_news_spec.json`의 **`search_keyword`**에 적고, 제목 첫 줄과 네이버 캡션 첫 줄을
  그 말로 시작한다. `content_review.check_search_keyword()`가 검사한다(필드가 없는 옛 topic은 통과).
- ⚠️ **`check_title_closing()`은 2026-09-23까지 title 배열의 마지막 줄만 봤다** — 그래서 "…계속된다면 /
  **저장부터 하세요** / 후유증 막는 대처법 3가지"처럼 **중간 줄**에 박힌 폐기 CTA가 116개 topic에서
  린터를 통과한 채 살아남아 있었다. 지금은 전 줄을 검사한다. 그 줄은 지우기만 하면 앞 훅이
  "~난다면"으로 끊기므로 **훅까지 같이 다시 쓸 것.**
- **썸네일 단어(`thumb_word`)와 검색어는 다른 필드다.** 썸네일은 화면을 채울 짧은 병명 한 덩어리
  (8자 이하여야 `_make_title_card_png`가 화면 폭 88%까지 자동 확대한다), 검색어는 길어도 된다.
- 🚨 **`~부작용` 복합어는 검색량이 항상 0으로 나온다** — 광고 집행이 불가능한 키워드라 API가 데이터를
  안 주는 것이지 수요가 없다는 뜻이 아니다(위고비 86,800 / 위고비부작용 0). 이 숫자로 폐기 판정을
  하지 말고 자동완성으로 수요를 확인할 것.
- **검색량이 낮으면 먼저 표기를 의심할 것**(2026-09-23 심사 실측) — 저볼륨 원인은 대개 소재가 아니라
  말이었다: 오표기(반월판연골파열 290 → **반월상**연골파열 6,390), 학술어(헬리코박터파일로리 900 →
  헬리코박터**균** 65,200), 신조어(사회적시차 50 → 월요병 1,620).

## 🚨 원고 고도화 — 6가지를 다 넣어야 완성 (2026-09-20 확정)

사용자 지적: "지금까지의 내용들이 사람들에게 크게 도움이 되지 않는 느낌, 놀랄 만큼 도움될 내용이 많이
들어가면 좋겠다". 기존 원고는 "커피·진통제·짠 음식이 위에 나쁘다"처럼 **다 아는 말**만 하고, 실행할 수 있는
숫자도 병원에 가야 할 신호도 없었다. **새 topic은 아래 6가지를 전부 넣는다.**

1. **실행 가능한 수치 3개 이상** — 단위까지("하루 400mg 이하", "식후 30분", "2주"). "줄이세요"는 실행 불가.
2. **통념 반박 1개** — "~가 좋다고 알려졌지만 오히려 …". 시청자가 이미 아는 말만 하면 볼 이유가 없다.
3. **기전 한 줄** — 왜 그렇게 되는지(반투명 인체 포맷의 기전 클립과 짝을 이룬다).
4. **해결책은 "얼마나·언제·어떻게" 중 2개 이상** 구체화.
5. **병원에 가야 할 신호 1줄** — 구체적 증상으로("체중이 줄거나 검은 변을 보면 바로 진료").
6. **기관명을 문장 안에** — 기존 `check_unsourced_claims` 규칙 그대로.

- **분량은 62~85초**(공백 제외 380~521자, 6.13자/초). 48초로는 위 6가지가 안 들어간다. 시청층이 중장년이라
  속도를 올리는 것보다 길이를 늘리는 쪽이 맞다.
- **검사**: `content_review.check_content_depth()`가 1·2·5번과 분량을 자동으로 잡는다.
  **`card_news_spec.json`에 `"content_v2": true`를 넣은 topic만 검사한다** — 기존 434개는 TTS 재생성
  비용 때문에 손대지 않기로 했으므로(2026-09-19) 마커 없는 옛 topic은 통과시킨다.

## 🚨 나간 건 지운다 — 로컬은 사본일 뿐 (2026-09-21 확정)

사용자 지시: "카드뉴스를 블로그에 이미 올린 토픽들은 전부 폴더랑 미션콘트롤에서까지 제거하고 새롭게 올려야 할
것들만 남기도록 하자. 용량도 좀 줄이고."

- **영상은 카드뉴스가 네이버 블로그에 아직 안 나간 topic만 만든다**(2026-09-21 기준 277개).
- 카드 이미지는 인입 시점에 Cloudflare R2로 올라가고 블로그·대시보드는 그 URL로 서비스한다 —
  **로컬 파일은 그때부터 사본**이다. `python3 scripts/prune_published.py --commit`이 ①vernhaven
  `blog_posts`에 있는 (topic, lang)의 `output/<topic>/<lang>/` ②블로그에 나간 topic의 한국어 카드·
  mp4·mp3·dashboard ③`deploy/health-shorts/<topic>/` 사본 ④미션컨트롤 `topics` 행을 지운다.
- ⚠️ **`posting_log`는 절대 지우지 않는다** — 지우면 "안 올린 topic"으로 되살아나 다시 만들게 된다.
- ⚠️ `data/<topic>/` 스펙 JSON은 남긴다(전부 18MB, 재업로드·번역에 필요).
- 2026-09-21 1차 정리 결과: `output/` 2.43GB → 812MB, `deploy/health-shorts` 977MB → 508MB,
  미션컨트롤 385행 → 256행. 백업: `~/.claude/backups/health-shorts/data_specs_20260921.tgz`.

## 🚨 없는 클립은 요청한다 — 비슷한 걸로 때우지 않는다 (2026-09-21 확정)

사용자 지시: "클립이 더 필요하다고 판단되면 막 연관없는 거 아무거나 넣지 말고 나한테 더 요청을 해.
더 추가로 작업해도 문제없으니까."

- 라이브러리(`assets_library/xray/output/`: 행위 9 · 기전 15 · 부위 21)에 그 topic의 기전과 맞는 클립이
  없으면 **남는 클립을 끼워 넣지 말 것** — 화면과 나레이션이 어긋나는 사고가 이 채널에서 반복됐다.
- 대신 `data/<topic>/clip_requests.json`에 `{"requests": [{"name": "m_...", "why": "...", "midjourney": "...", "flow": "..."}]}`
  형식으로 적고, 사용자에게 렌더를 요청한 뒤 **그 topic은 조립을 멈춘다**(다른 topic은 계속 진행).
- `content_review.check_xray_clips()`가 ①xray.json이 가리키는 클립 부재 ②안 받은 요청을 실패로 잡는다.

## 육아 트랙 (2026-09-24 착수, 시험 배치 10편)

건강 파이프라인·포맷을 그대로 쓰되 **자리를 가른다** — `data/육아/<topic>/` · `output/육아/<topic>/` ·
`assets_library/xray/작업_육아/` · `stills/baby/`. 🚨 **topic 이름은 `육아_1`로 평평하다(경로만 접힌다)** —
이 저장소에서 topic 안의 `/`는 이미 언어를 뜻해서 `육아/육아_1`로 부르면 조용히 깨진다. 경로 해석은
`lib/tracks.py`가 전담하고 `tests/test_tracks.py`가 막는다. 시청자층이 20~30대 부모로 갈리므로
**10편 올려보고 조회수로 확장 여부를 판단**한다.

| topic | 주제 | 제목 검색어(월) |
|---|---|---|
| 육아_1 | 해열제 교차복용 | 해열제교차복용 17,750 |
| 육아_2 | 수족구 | 수족구 83,900 (🚫 "수족구병" 5,910) |
| 육아_3 | 이유식 시작 시기 | 이유식시작시기 7,650 |
| 육아_4 | 신생아 황달 | 신생아황달 7,440 |
| 육아_5 | 돌발진 | 돌발진 9,780 + 열꽃 5,970 |
| 육아_6 | 기저귀 발진 | 기저귀발진 4,580 |
| 육아_7 | 모세기관지염 | **RSV 바이러스 29,570**(모세기관지염 2,570은 부제로만) |
| 육아_8 | 아기 설사·탈수 | 아기설사 2,830 + 아기탈수증상 1,630 |
| 육아_9 | 영아산통 | **신생아 배앓이 2,470**(영아산통 1,840) |
| 육아_10 | 아기 변비 | **신생아 변비 3,230**(아기변비 1,920) |

조사 원본은 `data/_audit/baby_track_keywords.md`.

**현황(2026-09-24)**: 10편 전부 원고·카드뉴스(90장, R2 업로드)·TTS·대시보드·Supabase 등록까지 끝났다. **영상만 클립 대기** — `작업_육아/0_작업지시.md`의 35종(01~17 미드저니만, 18~35 미드저니→Flow)을 받으면 `collect_clips.py --commit` → 부위 클립 생성(`make_part_clip.py`, region은 스틸 보고 실측) → `xray_build.py`로 조립한다.

- **영유아 정보는 틀리면 위험도가 다르다** — 해열제 용량·이유식 시작 시기·수면 자세 같은 건 대한소아청소년과학회·
  질병관리청 기준을 문장 안에 명시하고, 병원 신호(5번)를 더 구체적으로 쓴다. 🚨 **WebSearch/WebFetch 요약이
  원문에 없는 문장을 지어낸다** — 수치는 `curl`로 원문을 받아 grep으로 대조할 것(육아_3에서 실제로 걸렸다).
  같은 기관 문서 안에서 지침이 엇갈리기도 한다(땅콩: 알레르기 절은 "미루지 마라", 도입순서 절은 "돌 무렵" —
  질식 위험 때문이다. 이런 건 단정하지 말고 이유를 갈라 쓸 것).
- **상품**: 브랜드커넥트 계정은 health와 같은 걸 쓴다(댕냥사전과 달리 발급 가능). `sweep --baby`,
  카탈로그 `data/_audit/brandconnect_catalog_baby.json`. 유아용품 제외 규칙(`_KID_WORDS`)은 health
  도메인에만 적용되고, 도메인은 `tracks.domain_of(topic)`이 정한다 — 이걸 안 넘기면 육아 품목이
  전부 거짓 "없음"으로 찍혀 추천 금지 목록에 굳는다.
- **이미지**: 🚨 **아기가 보이는 장면은 Flow(Veo)에 안 보낸다** — 미드저니 정지 스틸 + `make_part_clip.py`.
  행위 칸은 보호자(비접촉은 Flow, 접촉은 스틸+코드). 상세는 `XRAY_FORMAT.md` 6절.

## 🚨 해결책 품목은 브랜드커넥트에 있는 것만 — 조사 단계에서 확인 (2026-09-19 신설)

**`products`에 넣는 품목은 네이버 브랜드커넥트에 제휴 가능한 상품이 있을 때만 쓴다.** 없는 품목(예: 일반의약품
"위장약" — 온라인 판매 불가라 검색 결과가 강아지 밥그릇 하나뿐이었다)은 링크를 달 수가 없다. 조사 때마다:

```
.venv/bin/python3 -m lib.brandconnect login          # 전용 Chrome(로그인 1회, 세션 유지)
.venv/bin/python3 -m lib.brandconnect check <topic>  # products 전부 검사 → data/<topic>/brandconnect.json
.venv/bin/python3 -m lib.brandconnect sweep [품목…] # 전 품목 카탈로그 + 빈 링크 발급·저장
```

- `content_review`의 `check_products_in_brandconnect()`가 **확인 기록이 없거나 "없음"인 품목을 실패로 잡는다.**
- 링크 발급도 자동이다 — 리뷰 많고 가격이 합리적인(후보 중간값 0.3~1.5배) 관련 상품을 골라 "링크 발급"을
  누르고, 응답 상품 ID가 고른 상품과 같을 때만 `global_product_links`(market=naver)에 넣는다. mission-control
  "미등록 링크"·대시보드가 이 테이블을 읽는다. **사용자가 직접 넣은 링크는 덮어쓰지 않는다.**
- 공개 API가 없어 로그인된 **실제 Chrome**(전용 프로필 `~/.config/health-shorts/brandconnect-chrome`, 포트
  9333, 127.0.0.1 전용)에 붙어 페이지가 받는 응답을 읽는다. Playwright Chromium으로는 네이버 로그인이 안 됐다.
  세션을 서버·저장소에 두지 말 것(계정 탈취 위험). 검색 간격은 2.5~5초 — 사람 속도를 넘기지 말 것.
- 카탈로그: `data/_audit/brandconnect_catalog.json`(품목 → 있음/없음·고른 상품·링크).
- "없음"은 **검색 결과가 0건일 때만** 찍는다(표기 차이 낫토↔낫또·보디워시↔바디워시로 거짓 "없음"이 났었다) — 추천 금지 목록
  (`data/brandconnect_unavailable.json`)이 된다. 상품은 있는데 기준에 안 걸린 건 "확인필요"(금지 아님).
  거짓 "없음"이 거짓 "있음"보다 해롭다(쓸 수 있는 품목을 영영 못 쓰게 된다).
- 🚨 **대체품 말고 도움 되는 품목을(2026-09-19 사용자)** — 원인 습관의 비슷한 대체품(술→무알코올 맥주,
  담배→전자담배, 커피→디카페인)은 해결책으로 약하다. **몸에 실제로 도움이 되는 영양제·도구를 추천한다.**
  `content_review`가 경고로 잡는다(`WEAK_SUBSTITUTES`).
- 댕냥사전(반려동물, 신규)은 **브랜드커넥트 계정이 다르다** — 이 계정으로 링크를 발급하면 안 된다.
  `sweep --pet`은 있는지만 확인하고 발급하지 않는다(`data/_audit/brandconnect_catalog_pet.json`).

## 🚨 광고 배너 클릭 유도 CTA (2026-09-14 신설)

**영상 하단에 "제품 보러 가기" + 아래 화살표를 얹어, 플랫폼이 붙이는 제휴 광고
배너로 시선을 내린다.** 네이버 클립 등이 영상 하단에 광고 배너를 자동으로 띄우는데
시청자가 그냥 지나쳐서, 배너 바로 위에 클릭 유도 장치를 둔다.

- 구현: `lib/ad_cta.py` 하나가 정본이다. `video_assembler.assemble()`이 오디오 mux와
  **같은 ffmpeg 호출**에서 합친다(따로 돌리면 완성본을 두 번 인코딩하게 된다).
- 가로 위치는 `CTA_CX` 상수 — 좌측 x<260은 "급식 당번" 명패, 우측 x>760은 원형
  사진이 이미 쓰고 있어 그 사이만 비어 있다.
- ⚠️ **세로 위치는 절대 좌표가 아니라 칠판 상단 기준이다(`CTA_CY_FROM_BOARD_TOP`,
  2026-09-17)** — 옛 `CTA_CY=1545`는 상단 배너가 있던 시절(칠판이 y≈255부터 시작)의
  실측값이라, 배너를 끄자 그 좌표가 **나무 선반 한가운데를 뚫고 앉고 화살표가 광고
  배너가 아니라 사진 배경을 가리켰다**(실측 확인). `assemble()`이
  `CTA_CY_FROM_BOARD_TOP + effective_top_pad`로 계산해서 넘긴다. `CTA_CY`는
  `apply_to_video()`(완성본 덧입히기 경로) 폴백으로만 남아 있다.
- ⚠️ **CTA는 `CTA_START_SEC=5.0`부터 뜬다(2026-09-17)** — t=0부터 깔면 맨 앞
  제목 카드(=썸네일) 글자를 배지가 덮고, 네이버 클립은 업로더가 영상 프레임에서
  썸네일을 고르는 방식이라 **깨끗한 후보 프레임이 하나도 없어진다.** 광고 유도가
  박힌 썸네일은 클릭률을 떨어뜨려서 배너로 시선을 내리려다 유입을 깎는 역효과다.
  등장 지연은 fade가 아니라 overlay의 `enable`로 건다 — 오버레이 입력이 PNG
  한 장이라 자체 타임스탬프가 0에 멈춰 있어서 fade는 st에 영영 도달하지 못한다.
- 정지해 있으면 칠판에 묻히므로 **재생 내내 위아래로 까딱이며(±13px) 살짝 기울어진다
  (±5°)**. 두 주기를 다르게 둬야(1.6s / 2.3s) 기계적으로 안 보인다.
- 문구는 언어별로 `_LABEL_BY_LANG`에 있다(영상은 현재 ko만 만들지만 그대로 둔다).
- ⚠️ **CTA가 붙으면 비디오를 재인코딩해야 한다**(`-c:v copy` 불가). 조립이 그만큼
  느려지는 건 정상이다.
- ⚠️ **`<name>.pre_cta.mp4`(2026-09-14 소급 적용 때 남긴 CTA 없는 원본)는 이제
  쓰지 말 것** — 상단 배너가 박혀 있던 시절의 렌더라, 거기서 CTA를 다시 입히면
  폐기한 배너가 같이 되살아난다. 디자인을 손볼 땐 `rebuild_video`로 다시 조립한다.

## ⚠️ 일러스트 생성 전면 중단 — 실사진으로 대체 (2026-08-25)

**새 캐릭터 일러스트를 만들지 않는다.** AI 일러스트 썸네일이 매 topic 같은
패턴으로 보여서 독자가 알아보고 재접근을 안 하는 부작용이 확인됐다 —
`lib/gemini_illust.py` 신규 호출 중단(파일 자체는 재개 대비용으로 보존, 상단
주석 참고). 표지 배경·팩트카드 배지·마무리 카드 배지 등 예전에 일러스트가
들어가던 모든 자리는 이제 **실사진**(공용 풀 `~/Desktop/project/assets-shared/`)으로 채운다 —
레이아웃·구도·블러 처리는 기존과 동일, 이미지 소스만 바뀜(`lib/card_news.py`의
`_photo_medallion`이 이전 `_char_medallion`을 대체).

- 실사진은 이제 **프로젝트 밖 공용 풀**에서 꺼내 쓴다 —
  `~/Desktop/project/assets-shared/`(sqlite 카탈로그 + `files/`). health-shorts·
  sparelow·babbleroot·furrowly에 흩어져 있던 사진을 전부 여기로 모았고, 새 사진도
  전부 여기로 들어온다. 저장소 밖에 둔 이유는 `git clean -x`·worktree 삭제로 날아가는
  걸 막기 위해서다.
- **품목당 사진은 여러 장 확보한다** — 품목 하나에 사진 한 장이면 커피(273곳)·맥주(166곳)
  같은 품목이 수십 개 topic에 같은 이미지로 나가서, 일러스트를 버린 이유("독자가
  알아보고 재접근을 안 한다")가 그대로 재현된다. 재사용 상한 5회 기준으로 품목별
  필요량을 계산해 소싱한다.
- 어느 topic·슬롯에 어느 사진이 나갈지는 `assets-shared/assign.py`가 만든 **배정표**가
  정한다(sqlite `assignments`). 한 번 정해진 배정은 고정 — 재렌더할 때마다 이미지가
  바뀌면 이미 게시된 글과 안 맞는다. 배정을 바꾸려면 그 행만 고칠 것.
- 소싱은 `assets-shared/sourcer.py --queries queries/items.json` — 이미 목표치를 채운
  품목은 건너뛰므로 중단 후 재실행해도 안전하다. 품목↔영어 검색어 매핑은
  `assets-shared/queries/items_ko_en.py`(295품목 전량, 인물 품목엔 `asian` 자동 부착).
- ⚠️ **Pexels가 간헐적으로 `x-ratelimit-remaining: -1`을 뱉는다(2026-08-25 실측)** —
  그대로 믿으면 잔여 2만 회를 두고도 소진으로 오판해 소싱이 멈춘다. `sourcer.py`의
  Budget이 음수·급락값을 무시하게 돼 있으니 이 방어를 걷어내지 말 것.
- ⚠️ **기존 topic도 전부 실사진으로 소급 전환한다(2026-08-25, 이전의 "소급 적용 안 함
  프로스펙티브 원칙"은 폐기)** — spec 583개(ko 396 + 언어 변형), 서로 다른 품목 295개,
  이미지 슬롯 4,487개가 대상. API 사용량이 모자라면 시간을 들여서라도 전량 전환한다.

⚠️ **국가별 SEO 블로그 확장(2026-08-13 착수, 2026-08-13 범위 확정)** — 사이트
자체는 별도 프로젝트 `../seo-blog/CLAUDE.md`에서 진행(health-shorts 하위
아님, 여러 콘텐츠 소스가 공유하는 독립 프로젝트). **health-shorts는 그
사이트가 쓸 9개 언어(ko/en/ja/de/fr/it/es/nl/sv) 블로그 콘텐츠(카드뉴스
이미지+`blog_seo` 본문) 저작을 담당** — 기존 ko/en/ja 영상 트랙과는 별도
서브트랙, 상세는 "글로벌 확장" 절의 "블로그 SEO 서브트랙" 참고.

⚠️ **이 프로젝트 Supabase에 `ori` 스키마가 얹혀 있음(2026-08-13, 임시)** —
`../ori` 프로젝트가 무료 티어 2프로젝트 한계로 이 프로젝트의 Supabase에
자기 데이터를 얹어둔 상태(`public` 아니라 별도 `ori` 스키마, 완전히
분리돼 있어 health-shorts 자체 테이블엔 영향 없음). Supabase 대시보드에서
낯선 `ori` 스키마·테이블이 보여도 실수로 지우지 말 것 — 상세는
`../ori/CLAUDE.md` 참고. 새 Supabase 프로젝트 슬롯이 생기면 분리 예정.

**이 파일은 "지금 지켜야 할 활성 규칙"만 담는다.** 결정 배경(WHY)·과거 버그 상세·
사용자 인용문·뒤집힌 결정의 히스토리는 전부 `CLAUDE_ARCHIVE.md`(이 파일의 이전
전체 버전, 2026-08-04 분리)에 그대로 남아있다 — "왜 이렇게 했더라" 싶으면 거기서
같은 제목으로 찾을 것. 새 결정을 추가할 땐 여기엔 결론만 한 줄로, 자세한 경위가
필요하면 archive 쪽에 이어서 적을 것(둘 다 git 추적됨).

## 새 topic 완료 기준 — 두 트랙을 다 채워야 끝난다 (2026-08-30 확정)

건강 topic 하나는 **나가는 곳이 둘**이고, 둘 다 카드뉴스가 필요하다. 한쪽만 있으면
완료가 아니다 — 실제로 blog_seo 8개 언어를 가진 48개 topic 중 **언어별 카드뉴스까지
갖춘 게 21개뿐**이었다(나머지 27개는 스펙만 써놓고 렌더를 안 해서 vernhaven hero가
비어 있었다).

| | 네이버 블로그 트랙 | SEO 블로그 트랙(vernhaven) |
|---|---|---|
| 원고 | `네이버 블로그` 항목 — **위치가 topic마다 다르다**, 아래 "한국어 캡션 파일은 topic마다 위치가 다르다" 절 참고 | `data/<topic>/<lang>/platform_captions.json` (`blog_seo` 항목, 8개 언어) |
| 카드 스펙 | `data/<topic>/ko/card_news_spec.json` | `data/<topic>/<lang>/card_news_spec.json` (언어마다 따로) |
| 카드 이미지 | `output/<topic>/ko/card_news/` | `output/<topic>/<lang>/card_news/` |
| 반영 | `python3 -m lib.mission_control_sync --commit` | vernhaven `ingest_health_shorts.py --commit` |

- **언어별 카드뉴스를 빠뜨리지 말 것** — 스펙만 써두고 렌더를 안 하면 vernhaven의
  `hero_image_url`이 영영 안 채워진다(인입 스크립트가 찾는 커버 파일이 없어서).
  실측으로 144편이 hero 없이 게시 대기 중이었다.
- 렌더: `python3 lib/card_news.py data/<topic>/<lang>/card_news_spec.json assets_library/illust output/<topic>/<lang>/card_news <topic> <lang>`
- ⚠️ **한국어 카드는 lang 인자가 `ko`가 아니라 `kor`다.** `ko`로 넘기면 폰트 매핑에
  없어서 라틴 폰트로 떨어지고 한글이 전부 글리프 오류로 죽는다.
- ⚠️ **topic을 리네임하면 옛 접두어 카드가 같은 폴더에 그대로 남는다**(손톱_1 →
  손발_1에서 9장 잔존 실측). 게시할 때 잘못 집어가기 쉬우니 리네임 직후 확인할 것.

### 카드와 기사가 서로 다른 말을 하는지 반드시 대조할 것

네이버 게시물은 **카드 이미지와 그 아래 글이 한 화면에 같이 나간다.** 그런데 카드
스펙은 영상 트랙 시절에 먼저 쓰였고 `blog_seo` 기사는 나중에 사실검증을 거쳐 쓰여서,
같은 주제인데 숫자와 기전이 어긋난 채 남은 곳이 있다. 캡션을 기사에서 뽑으면
**이미지와 본문이 서로 다른 숫자를 말하는 게시물**이 된다.

```
python3 -m lib.card_article_diff [topic ...]
```

카드에만 있고 기사엔 없는 수치를 뽑아준다. **숫자만 본다** — 기전이 뒤집힌 경우
(카드 "카페인 탓" vs 기사 "카페인이 아니라 폴리페놀")는 못 잡으니 카드 문구와 기사를
직접 읽어 대조할 것. 통과/실패 게이트가 아니다(카드에만 있는 게 정상인 수치도 섞인다).

⚠️ **어느 쪽이 맞는지는 일정하지 않다 — 매번 원문을 봐야 한다.** 실측 사례:

- 계절질환_1 — **카드가 맞았다.** 기사의 "온도차 8도·필터 한 달~세 달"은 프랑스
  ADEME 권고에서 온 값이고, 한국은 서울대 국민건강지식센터 "5~6℃를 넘지 않도록",
  서울대학교병원 "필터는 최소한 2주에 한 번"이다
- 손발_1 — **기사가 맞았다.** 카드의 "카페인이 철분 흡수 방해"는 틀렸다. 실제로는
  클로로겐산·탄닌 같은 폴리페놀이고 디카페인 커피도 같은 저해를 일으킨다
- 피부_1 — **둘 다 손봐야 했다.** 카드의 "성장호르몬 75%"는 기사가 뒷받침하지 않는
  수치였고, "도넛 최대 2.7g"은 카드만 보면 그게 보통인 것처럼 읽혔다(기사: 국내 평균 0.2g)

### 이미지 배정을 기계적으로 바꿀 땐 alt도 같이 바꿀 것

topic 간 이미지 중복을 없애려고 `PENDING:` 마커만 재배정하면 **alt 텍스트가 옛 사진을
설명한 채 남는다.** 66개 슬롯이 통째로 어긋난 적이 있다(alt는 "블리스터 포장"인데
사진은 남색 배경 위 알약 두 알). 마커를 건드리는 스크립트는 alt도 함께 다시 쓰고,
바꾼 사진을 **직접 열어보고** alt를 작성할 것 — 파일명만 보고 쓰면 또 어긋난다.

## 세션 운영

- **역할 고정 없음** — 세션 하나가 신규 topic·기존 topic 수정·설정 변경 전부 처리.
- **완전 자동화 모드** — "다음 topic 해줘"류 요청은 소재 선정→리서치→콘텐츠 작성→
  이미지/TTS/영상 조립→대시보드→**Supabase `topics` upsert**→git commit·push까지
  중간 승인 없이 끝까지 진행. 단, 유튜브 외 플랫폼 업로드는 API 연동이 없어
  사람이 대시보드에서 수동으로 함.
  - ⚠️ **Supabase `topics` upsert는 절대 빠뜨리지 말 것(2026-08-11 실측 사고)**
    — `lib/dashboard.py`의 `generate()`/`_update_topics_index()`는 로컬
    `output/topics.json`만 갱신하고 Supabase는 **전혀 건드리지 않는다**.
    index.html의 메인 목록은 Supabase `topics` 테이블만 읽으므로, 이 단계를
    빠뜨리면 "커밋·푸시는 됐는데 사이트 목록엔 안 보이는" 상태가 된다(실제
    발생 — 새 topic 3개를 만들고 push까지 했는데 Supabase만 안 넣어서
    목록에서 안 보였음). 새 topic(또는 리네임) 작업 마지막에 항상:
    ```python
    import os, json, requests
    from dotenv import load_dotenv
    load_dotenv()
    url, key = os.environ['SUPABASE_URL'], os.environ['SUPABASE_SERVICE_ROLE_KEY']
    headers = {'apikey': key, 'Authorization': f'Bearer {key}', 'Content-Type': 'application/json',
               'Prefer': 'resolution=merge-duplicates,return=minimal'}
    topics = json.load(open('output/topics.json'))
    rows = [{'topic': t['topic'], 'title': t.get('title'), 'url': t.get('url'),
             'thumbnail': t.get('thumbnail'), 'ad_tag': bool(t.get('ad_tag', False)),
             'tracks': t.get('tracks', [])} for t in topics if t['topic'] in {"<새 topic 이름들>"}]
    requests.post(f'{url}/rest/v1/topics', headers=headers, json=rows, timeout=30)
    ```
    작업 끝나면 로컬 `output/topics.json`과 Supabase `topics` 테이블 topic
    목록이 정확히 일치하는지 한 번 diff로 확인할 것(위 스크립트의 `missing`
    계산 패턴 재사용 가능).
  - ⚠️ **topic 리네임 시 `topic` 컬럼만 PATCH하면 안 됨(2026-08-11 실측
    사고 — "메인 페이지에 썸네일 왜 아작나있는게 많지?")** — `url`/`thumbnail`
    컬럼에도 옛 폴더명이 URL 인코딩된 채로 박혀 있어서(`output/%EB%91%90%ED%94%BC_1/...`
    처럼), `topic` 값만 새 이름으로 바꾸면 그 행이 가리키는 경로는 여전히
    존재하지 않는 옛 폴더를 가리켜 썸네일·링크가 깨진다. 리네임 후에는 위
    upsert 코드를 **필터(`if t['topic'] in {...}`) 없이 `output/topics.json`
    전체**로 돌려서 `url`/`thumbnail`까지 통째로 덮어쓸 것 — `Prefer:
    resolution=merge-duplicates`라 기존 행 그대로 안전하게 갱신된다.
  - ⚠️ **위 upsert 요청 body에 `updated_at`을 절대 포함하지 말 것(2026-08-11)**
    — `index.html`의 메인 목록 정렬이 `posting_log`(사람이 플랫폼 포스팅
    체크박스를 눌러야만 생기는 수동 기록이라 방금 만든 topic엔 아직 없을 수
    있음)가 비어있을 때 `topics.updated_at`을 "이 topic이 DB에 처음
    upsert된 시각" 폴백으로 쓴다(위 upsert 코드가 `updated_at`을 안 보내는
    한, `topic` 컬럼 PK 최초 삽입 시점의 `default now()`가 그대로 남아있고
    이후 PATCH·upsert로는 절대 안 바뀜 — 실측 확인됨, 2026-08-08 스키마
    도입 때 백필된 173개만 그 시각을 공유하고 그 뒤로 만들어진 topic은 전부
    고유한 실제 생성 시각을 가짐). `updated_at`을 payload에 넣어서 매번
    "지금"으로 새로 찍으면 이 신호가 깨져서 오래된 topic이 새 topic처럼
    정렬 맨 위로 튀는 버그가 생긴다.
  - ⚠️ **"끝까지" = 한국어 하나(2026-08-21부로 en/ja 폐기, 아래 "글로벌
    확장" 절 참고)** — 예전엔 en/ja까지 만들어야 완료였으나 이제 영상
    트랙은 ko 대본·TTS·영상까지가 완료 기준. en/ja 신규 작업 금지.
- ⚠️ **소재 선정(2026-08-10)** — "다음 topic 해줘"처럼 소재가 정해지지 않은
  요청이면, 원인/음식 위주로 즉흥 선정하지 말고 먼저 확인:
  1. **비-건강 일반 트렌드부터 WebSearch로 훑고, "품목 분해→성분 매칭"
     순서로 근거를 만든다(2026-08-10 방법론 확정)** — "오늘 실시간 이슈"/
     "이번 주 화제" 등으로 지금 뜨는 일반 트렌드(음식·상품·이슈, 건강
     카테고리 무관)를 확인한 뒤:
     a. **품목 자체를 서칭** — 그게 정확히 뭔지, 무엇으로 구성되는지 확인
     b. **재료·성분·숨겨진 특성을 분해** — 그 품목 안에 실제로 들어있는
        재료·성분·조리법·부작용 소지 등을 하나씩 나열
     c. **각 성분을 건강 정보와 매칭** — 이미 알려진 건강 지식(기전·수치)에
        분해한 요소들을 하나씩 대응시켜서, 실제로 근거가 있는 요소만 남김
     d. 이렇게 재구성된 내용을 대본(narration.txt)의 원재료로 쓴다 — 트렌드
        자체를 억지로 "건강이랑 관련 있다 치고" 쓰는 게 아니라, 품목을 분해한
        뒤 그 구성요소 중 실제로 건강과 맞물리는 부분만 골라서 쓰는 것
     e. **억지 연결 금지 원칙은 그대로** — b~c 단계에서 근거 있는 요소가
        하나도 안 나오면 그냥 폐기하고 다음 단계로(자동 판정기를 따로 만들지
        않고 세션이 매번 직접 판단하는 이유는 `data/seasonal_topics.md`
        상단 절 참고)
     f. ⚠️ **"화제/유행"이라는 검색 결과를 그대로 믿지 말고 최신성을
        재확인할 것(2026-08-11 실측 사고)** — 두바이초콜릿을 "요즘 SNS에서
        난리 난"으로 썼는데, 다시 찾아보니 "2026 상반기를 흔든 음식"류
        기사는 회고성 콘텐츠였고 실제로는 국내 검색량이 1년 만에 90% 이상
        줄어든 이미 식은 트렌드였다("짧은 유행 주기... 열풍이 급격히
        식었다" — 실제 기사 표현). 헤드라인만 보고 "요즘 뜬다"로 단정하지
        말고, ①기사 자체가 회고형("올해를 흔든", "~상반기") 어투인지
        ②검색량 추이·"식었다/줄었다"류 후속 기사가 있는지 반드시 한 번 더
        검색해서 교차 확인할 것.
     g. ⚠️ **topic 폴더명은 원인 품목이 아니라 증상/신체부위/카테고리로
        지을 것(2026-08-11 재확인)** — 위 a~d 방법론으로 원인 품목을
        분해했더라도, 그 결과로 만드는 topic 폴더명은 기존 관례(아래
        "topic 폴더명" 절) 그대로 증상·신체부위·카테고리 기준이다.
        "두바이초콜릿_1"처럼 품목명을 그대로 폴더명에 쓴 사고가 실제
        발생함(올바른 예: 혈당 스파이크가 핵심이면 `혈당_N`처럼 기존
        카테고리에 편입). 여러 신체 부위에 걸치면(예: 미세먼지처럼 피부·
        눈·기관지 전부) 그 원인 자체가 이미 "냉증"·"체취"급 독립 카테고리로
        볼 수 있는지 먼저 판단하고, 애매하면 폴더 만들기 전에 먼저 물어볼 것.
  2. 위에서 못 찾았으면 `python3 -m lib.trend_check --show`(오늘자 스냅샷
     없으면 `--daily`)로 `data/seasonal_topics.md` 후보들의 구글 트렌드
     모멘텀 확인, 후보를 `--related <키워드>`로 구체적 각도까지 좁힘
  3. 이번 달 계절 절 + 모멘텀(▲) 겹치는 카테고리 우선, 기존 topic과
     `ls data/ | grep "^<카테고리>_"`로 중복 확인
  4. 네이버 쪽 트렌드(데이터랩·뉴스 API)는 보류 중(사용자가 해외 체류로
     네이버클라우드플랫폼 휴대폰 인증 불가 — 한국 복귀 후 이어감) —
     그때까지 WebSearch로 정성적 보완만
  4-1. ⚠️ **`python3 -m lib.trend_check --hidoc`(2026-08-12 추가)** — 하이닥
     뉴스 메인 "전체 인기뉴스" top5를 가져온다. **이건 네이버/구글 검색
     트렌드가 아니라 하이닥 자체 사이트 조회수 기준 랭킹**(건강 매체 하나의
     편집/독자 반응 신호) — 국민 전체 관심사로 오해하지 말 것. signal.bz
     같은 일반 실시간 검색어 API는 정치/연예 위주라 건강 소재 발굴에 안
     맞아서 시도했다가 대체로 찾은 것("이거 추가하면 좋겠다" 확인받고
     `lib/trend_check.py`에 정식 추가). bs4 없이 정규식으로 파싱해서 하이닥이
     페이지 구조를 바꾸면 깨질 수 있음 — 그 경우 경고만 뜨고 조용히 빈
     리스트를 반환(파이프라인 안 막음), WebSearch로 수동 보완할 것.
  5. 급성 유행 이슈(감염병 등)는 계절 캘린더보다 우선하되, 반드시 공식
     출처(질병관리청 등)로 교차 확인 후 진행 — 상세는 `data/seasonal_topics.md`
- **topic 폴더명** = 핵심 카테고리 단어 + `_N`(예: `가슴쓰림_1`). 서술형 접미사
  금지. 새 topic 시작 전 `ls data/ | grep "^<카테고리>_"`로 기존 번호 확인.
  트랙(숏츠/카드뉴스) 무관 동일 규칙 — 별도 네임스페이스 없음.
  - ⚠️ **카테고리 단어는 실제 신체 부위·증상을 가리키는 가장 평이한 단어를
    쓴다(2026-08-11 대량 재편)** — "구강"·"호흡기"·"모발" 같은 조어 대신
    이미 쓰는 쉬운 말(입/코/머리)을 우선한다. 현재 확정된 카테고리:
    눈·입·귀·코·손발·머리(두피·새치·탈모뿐 아니라 기억력·편두통 같은 뇌/인지
    증상도 여기로 통합됨, 2026-08-11 — "뇌" 카테고리는 안 씀)·냄새·피부·소화·
    순환·근골격·수면·비뇨기·어지럼증·혈당·대사·여성. 새 topic이 이 중 하나에
    해당하면 새 카테고리를 만들지 말고
    여기 번호를 이어서 붙일 것. 서로 다른 장기라 하나의 신체부위명으로 못
    묶는 주제(소화/순환/근골격/비뇨기/대사 등)만 예외적으로 계통명을 쓴다.
  - ⚠️ **계절질환(2026-08-12 신설)** — 원인이 계절/환경 자체인 topic(냉방병·
    온열질환 등, 체온 조절 관련) 전용 카테고리. **땀띠·자외선·식중독처럼
    여름에 흔해도 원인이 계절 자체는 아닌 topic은 여기 안 넣고 몸 부위
    카테고리(피부·소화 등) 그대로 쓸 것**("땀띠 자외선 식중독 이런건
    계절은 아니지" 확정). 냉방병_1→계절질환_1, 온열질환_1→계절질환_2로
    리네임됨 — 미세먼지_1은 체온 조절이 아니라 대기질 문제라 이 카테고리에
    안 넣고 기존 단독 유지.
  - **새 topic 카테고리 판단 순서(2026-08-11)** — 아래 순서대로 검토해서
    가장 먼저 맞는 걸로 정한다:
    1. **느끼는 부위가 명확하면 그 부위 카테고리** — 원인 기전이 아니라
       사람이 실제 겪는 위치 기준. 예: 다래끼(원인은 세균감염이지만 눈에서
       느낌)→눈, 코골이(원인은 기도협착이지만 코/코골이 소리)→코, 통풍(원인은
       요산 대사질환이지만 관절이 아픔)→근골격. "무슨 과에서 진료하는지"가
       아니라 "어디가 아프다고 말하는지"가 기준.
    2. **여러 장기가 걸쳐서 부위 하나로 못 묶으면 계통명** — 소화(위+장+
       식도 등), 순환(혈류 관련 전신 증상), 근골격(뼈+관절+근육), 비뇨기
       (신장+방광+전립선), 대사(혈당·호르몬·요산처럼 특정 부위 통증 없이
       수치로만 확인되는 전신 대사질환 — 갑상선·지방간·췌장이 여기).
    3. **위 1·2 어디에도 안 맞으면(전신 증상, 환경 노출, 나이 관련 등)
       단독 유지** — 억지로 카테고리를 만들지 않는다. 예: 피로_1(원래
       만성피로_1이었으나 서술형 접미사 제거), 고령_1, 미세먼지_1(여러
       부위에 걸친 환경 노출이라 부위 하나로 못 묶음).
    4. 카테고리를 정했으면 `ls data/ | grep "^<카테고리>_"`로 기존 번호
       확인 후 다음 번호를 붙인다. 완전히 새 카테고리 단어를 만들기 전에는
       반드시 위 목록에 이미 있는 카테고리에 안 맞는지부터 확인할 것.
  - 장_2는 변비_1과 콘텐츠가 완전 중복이라 로컬(`data/`, `output/`)에서만
    삭제했다 — 이미 6개 언어로 라이브된 실제 유튜브 영상과 그 Supabase
    `youtube_uploaded`/`posting_log` 기록은 그대로 둔 상태다. 나중에
    "장_2"를 찾다가 로컬에 없다고 당황하지 말 것.
- ⚠️ **두 트랙(2026-08-07) → 카드뉴스 단일 트랙(2026-08-25)** — 영상 제작
  전면 중단(맨 위 절 참고)으로 "요청 문구로 구분"하던 옛 방식은 끝났다.
  **이제 "topic 해줘"든 "카드뉴스만"이든 항상 카드뉴스만 진행**: `card_news.py`만
  실행(TTS·영상 조립 생략). `platform_captions.json`엔 네이버 블로그·
  네이버 블로그만 포함 — 유튜브쇼츠·틱톡·인스타릴스·네이버클립(영상
  필요 플랫폼)과 쓰레드는 애초에 안 넣음. ⚠️ **`narration.txt`
  자체를 안 만든다(2026-08-12 명확화)** — TTS를 안 호출하니 나레이션
  파일이 최종 산출물이 아님, `data/<topic>/card_news_spec.json` +
  `platform_captions.json` 두 개만 작성하면 됨(아래 "콘텐츠 QA" 절의
  `content_review`도 narration.txt 없이 이 두 파일만으로 정상 동작함).
  - 트랙 표시는 수동 플래그 없이 자동 파생됨 — `platform_captions.json`의
    `platforms[]`에 `type: "video"`인 항목이 하나라도 있으면 "숏츠", 없으면
    "카드뉴스"(`lib/dashboard.py`의 `_update_topics_index()`가 매번 재계산해서
    `output/topics.json`에 기록, `index.html`이 이 값으로 "🎬 숏츠"/"🗞 카드뉴스"
    두 섹션으로 나눠 보여줌). 새 필드를 손으로 추가·관리할 필요 없음.
  - ⚠️ **`dashboard.py generate()`의 `spec_path`는 반드시 `platform_captions.json`
    (2026-08-17, "카드뉴스만" topic 50개 "업로드 플랫폼" 섹션 통째로 누락 사고)** —
    `card_news_spec.json`(카드뉴스 레이아웃 스펙, `platforms` 키 없음)을 실수로
    넘기면 `spec.get("platforms", [])`가 조용히 빈 리스트가 되어 네이버 블로그
    "열기(캡션 자동복사)" 버튼을 포함한 "업로드 플랫폼" 섹션 전체가 대시보드에서
    사라진다 — 에러도 경고도 없어서 대시보드를 직접 열어보기 전까진 못 알아챈다.
    50개 topic이 이 상태로 방치됐다가 사용자가 직접 발견(`눈_9`). 지금은
    `generate()`가 `"platforms"` 키 자체가 없으면 바로 크래시하도록 가드가
    걸려있어 같은 실수를 하면 즉시 드러난다 — 그래도 CLI 호출 시
    `platform_captions.json` 경로를 넘기는 게 기본, `card_news_spec.json`은
    `card_news.py`(이미지 렌더링)에만 쓴다는 걸 헷갈리지 말 것.
- ⚠️ **카드뉴스 표지 배경색은 매 topic 다르게(2026-08-12)** — `card_news_spec.json`에
  `cover_scrim_color`(hex 문자열)를 안 넣으면 기본값인 브랜드 ACCENT(로즈핑크)로
  전부 동일하게 나온다("왜 죄다 빨간바탕만 해놨냐" 지적, 실측 14개 topic 전부
  같은 핑크였음). 새 topic마다 다른 hex를 지정할 것 — 이미 쓰인 값은
  `grep -rh "cover_scrim_color" data/*/ko/card_news_spec.json`으로 확인 후 안 겹치는
  톤으로 고른다.

## 동시 세션 안전장치 — 매번 필수

1. **세션 락**: 작업 전 `python3 lib/session_lock.py check <topic>` →
   `UNLOCKED` 아니면 사용자에게 확인. 진행 시 `acquire`, 끝나면 `release`.
   락 파일명 제약상 다국어 topic도 언어 세분화 없이 `<주제>` 단위로 건다.
2. **git worktree** — 새 작업 시작할 때마다:
   ```
   git worktree add ../health-shorts-worktrees/<짧은-설명> -b session/<짧은-설명> main
   ```
   그 안에서 작업·커밋까지 끝내고, 메인 워크트리로 돌아가
   `git pull --ff-only && git merge session/<설명> && git push` → 정리
   (`git worktree remove` + `git branch -d`). 다른 세션의 `git add`/`commit`과
   완전히 격리되므로 인덱스 레이스가 안 생긴다.
3. worktree를 못 쓰는 상황이면 커밋 직후 `git show --stat HEAD`로 의도한 파일이
   맞는지 항상 확인 — 다른 세션 파일이 섞여 들어갔으면 데이터 손실은 아니니
   되돌리지 말고 새 커밋으로 바로잡기만 하면 됨. **미커밋 상태로 오래 두지
   말 것** — 다른 세션의 동시 git 작업(merge/rebase/stash 등)에 밀려 유실될
   수 있음. 2026-08-05 실제로 CLAUDE.md 편집이 두 번 유실됐다(한 번은 미커밋
   상태에서, 한 번은 커밋까지 했는데도 다른 세션의 히스토리 재작성으로) —
   커밋했다고 완전히 안전한 것도 아니니 중요한 변경은 완료 직후
   `git log --oneline -5 -- <파일>`로 실제로 히스토리에 남아있는지 재확인할 것.
4. `assets_library/`(공용 자산) 새로 만들기 전엔 `session_lock.py list`로 활성
   락 확인.

## 배포

- 카드뉴스·캡션·영상 중 뭐든 먼저 끝나는 대로 바로 commit·push — 셋 다 끝날 때까지
  안 묶는다. `dashboard.py`가 영상 없으면 자동으로 "준비 중" placeholder 보여줌.
- **서브에이전트로 여러 topic 병렬 생성 시**: 영상 조립(`video_assembler.py`)·
  대시보드 생성은 별도 2단계로 미룬다 — 1단계(리서치~TTS~캐릭터 일러스트/모션)
  전부 끝나면 커밋·push, 그다음 2단계로 영상 일괄 조립.

### 업로드용 완성본 모음 — `ai-video-network/deploy/health-shorts/` (2026-09-19 신설)

🚨 **네이버 클립(영상)·네이버 블로그(카드뉴스) 수동 업로드 때 `output/<topic>/`
400여 개를 매번 뒤지지 말고, `../ai-video-network/deploy/health-shorts/<topic>/`
하나만 볼 것** — `shorts.mp4` + `card_news/*.jpg`가 topic별로 모여 있다. 이
저장소와는 다른 프로젝트(`ai-video-network`)의 `deploy/` 트리를 같이 쓰는
것뿐(코드는 완전 독립) — `ko`/`en`/`ja`/`zh`/`inbox`와 나란한 최상위
폴더로, 그쪽 topic과 섞이지 않는다.

- `python3 scripts/stage_for_deploy.py` — `output/<topic>/shorts.mp4`가
  있는 topic만 복사/갱신(mtime 비교). `--dry-run`으로 미리보기.
- `python3 scripts/cleanup_deploy.py --commit` — mission-control에서 네이버
  블로그+네이버 클립 둘 다 체크된(완료) topic만 복사본을 지움(원본은
  안 건드림). 기본은 `--dry-run` 없이 미리보기, `--commit`으로 실제 삭제.
- 새 topic의 shorts.mp4를 만들 때마다 `stage_for_deploy.py`를 다시 돌릴
  것 — 자동 트리거 없음, 수동 재실행 필요.

## 에셋 라이브러리

⚠️ **`illust/`는 2026-08-25부로 신규 생성 안 함** — 위 "일러스트 생성 전면
중단" 절 참고, 새 캐릭터는 `real/`에 실사진으로만 추가한다. 아래 재사용
관례(파일명 한국어 통일·형태별 통합 등)는 `real/`에도 동일하게 적용할 것.

⚠️ **`assets_library/{illust,real,motion,music}/`는 로컬 유일본, 백업 없음 —
이 경로 대상 `rm`류 명령은 항상 절대경로 + 실행 직전 `pwd` 확인 필수
(2026-08-16 사고 확정)** — 다른 작업(워크트리 간 비교) 도중 `cd`로 이동한 채
잊고 상대경로로 `rm -rf assets_library`를 실행해 메인 디렉토리의 실제
자산(726MB)이 삭제됐다. git엔 원래도 안 올라가는 경로라(대용량 미디어 정책)
git으로 복구 불가, Time Machine·iCloud 동기화도 이 기기엔 없어서 대부분
유실 확정.

⚠️ **일러스트 저해상도 복구분 전량 폐기(2026-08-18)** — 사고 직후
`output/**/card_news/*.jpg`의 fact card 배지를 크롭해 231/255개를
복구했었지만(`data/illust_recovery_manifest.json`), 실제 화질이 원본
640px 대비 약 130px 수준으로 확대 시 심하게 깨져 재사용 가치가 없다고
판단 — 231개 전부 삭제, 매니페스트 파일도 삭제(재생성 우선순위 판단
근거로서의 용도가 없어짐). **앞으로는 새 topic 작업 때마다 필요한
캐릭터를 그때그때 다시 생성해서 자연스럽게 다시 쌓는다** — 일괄 복구
재시도는 안 함. `real`/`motion`/`music`은 복구 안 됨 — `real`은
`real_photo_sourcing.py`로 재소싱 가능(무료 API라 손실 크지 않음),
`motion`은 8/5부로 이미 신규 생성 중단 정책이라 실질 영향 적음, `music`은
유튜브 오디오 보관함에서 수동 재다운로드 필요.

**앞으로 규칙**: 이 4개 폴더(또는 그 상위 `assets_library/`) 대상
삭제·덮어쓰기 명령은 (1) 상대경로 대신 항상 절대경로 사용, (2) 명령
직전에 `pwd`로 위치 재확인, (3) 정말 필요한 삭제가 아니면 삭제 대신
이름 바꿔서 보관 후 나중에 지우는 방식을 우선한다.

⚠️ **재발 방지 — `assets_library/illust/`는 이제 git에 커밋한다
(2026-08-18, 정책 전환)** — 예전엔 "대용량 미디어는 git에 안 올림" 원칙으로
4개 폴더 전부 gitignore했지만, 실제 사고를 겪어보니 일러스트만큼은 파일당
수십 KB 수준(실측 232개/7.7MB, jpg 캐릭터 아트)이라 용량 부담이 사실상
없고 git이 유일한 백업 경로가 된다 — `.gitignore`에서 `assets_library/illust/`
제거 완료, `real`/`motion`/`music`은 그대로 gitignore 유지(real도 작지만
지금은 illust만 범위로 좁힘, motion/music은 여전히 대용량). 새 캐릭터
일러스트를 만들 때마다 다른 산출물과 마찬가지로 그 즉시 커밋할 것 —
로컬에만 두고 넘어가지 말 것.

- `assets_library/{illust,real,motion}/<품목>_*` — 캐릭터 일러스트/실사진/모션
  루프. 새 캐릭터 만들기 전 `ls assets_library/illust/`로 비슷한 시각 카테고리
  기존 파일 있는지 먼저 `Read`로 확인 — 있으면 그대로 재사용, 새로 만들지 말 것.
  - ⚠️ 파일명은 topic 언어와 무관하게 **항상 한국어**로 짓는다(번역하지 말고
    한글로 새로 이름 짓기, 예: `nopal`→`백년초`, `avena`→`귀리`) — 언어별로
    같은 물건이 중복 생성되는 사고를 막기 위함(상세 경위는 archive "파일명
    한국어 통일" 절).
  - ⚠️ 약/보충제류는 "품목명"이 아니라 "물리적 형태"로 먼저 재사용 확인
    (정제·캡슐·병은 이름이 달라도 그림으로 구분이 안 됨) — 아래 형태면
    반드시 먼저 재사용, 진짜 시각적으로 다를 때만 예외(상세 경위는 archive
    "약/보충제 형태 통합" 절):
    - 정제(원형 알약) → `아스피린_illust.jpg`
    - 캡슐(알약) → `알약_illust.jpg`(색만 다른 변형 금지)
    - 보충제/영양제 병 → `영양제_illust.jpg`
- **실사진 소싱**: `lib/real_photo_sourcing.py <영어검색어> <후보수> <pexels|unsplash|both>`
  → Read로 골라서 `assets_library/real/<품목>_real_NN.jpg`로 저장.
- **크로마키 배경색**: 기본 초록(`#00FF00`)이되, 캐릭터 자체가 초록 계열(오이·
  상추 등)이면 겹치므로 `lib/gemini_illust.py`의 `pick_bg_color(avoid=[...])`로
  파란/마젠타/시안/보라 중 하나 고르고 `video_assembler.py --bg-color`도 동일하게 맞출 것.

## 플랫폼 캡션 (`platform_captions.json`)

- **로테이션(2026-08-25 개편)**: 브런치·핀터레스트는 항상 제외. 한국어 topic은
  **네이버 블로그**만(카드뉴스 단일 트랙 — 맨 위 절 참고, 인스타/페북 카드뉴스는
  2026-08-26 폐지).
  아래 유튜브 쇼츠·틱톡·인스타그램 릴스·네이버 클립 관련 서술은 영상 트랙
  재개 전까지 비활성.
  ⚠️ **영상 트랙은 2026-08-21부로 한국어 단일 — 글로벌(en/ja) 로테이션은
  더 이상 없음**(아래 "글로벌 확장" 절 참고, blog_seo 서브트랙은 별개로
  계속 en/ja 포함 8개 언어).
- ⚠️ **유튜브 쇼츠 로테이션 제외(2026-08-21, "트래픽이 전혀 나오지않아")** —
  숏츠 영상 자체(mp4)는 계속 만들어서 인스타그램 릴스·틱톡·네이버 클립엔
  그대로 쓰지만, 유튜브 업로드용 캡션·자동 업로드는 더 이상 안 한다.
  `data/social_accounts.json`에서 제거 완료 — 새 topic은 이 파일을 그대로
  복사하므로 자동으로 빠진다. 상세는 `CLAUDE_ARCHIVE.md`의 "유튜브 쇼츠 자동 업로드" 절(2026-08-31 이관).
- ⚠️ **티스토리 완전 중단(2026-08-13, 소급 적용)** — 유입이 너무 저조하고
  Open API도 2024-02에 영구 폐지돼 자동화 여지가 없어서 플랫폼 로테이션에서
  뺐다. 국내는 네이버 블로그, 해외는 `seo-blog`(8개 언어 블로그 서브트랙, 위
  "블로그 SEO 서브트랙" 절)로 간다. **JSON 데이터·대시보드 UI 둘 다 소급
  정리 완료** — 기존 97개 topic의 `platform_captions.json`에서도 티스토리
  항목을 전부 제거하고 대시보드를 재생성했다(단순 배열 원소 삭제라 재생성
  비용이 저렴해서 프로스펙티브만 원칙의 예외로 소급까지 처리, 실제 게시된
  과거 포스팅 자체가 사라지는 건 아니고 대시보드에서 다시 열람·수정할 카드만
  없어짐). `data/social_accounts.json`(새 topic이 복사하는 소스)에서도
  제거해서 앞으로도 다시 안 들어간다.
- ⚠️ **글로벌(비한국어) topic은 제휴/상품 관련 필드 자체를 안 쓴다** —
  `products`·`suppress_product_block`은 **한국어 topic 전용**, 글로벌은 이
  필드들을 아예 안 넣고 순수 건강정보 콘텐츠로만 작성(채널이 커지면 그때
  아마존/알리익스프레스 제휴로 전환 예정, 아직 아님).
- ⚠️ **"댓글에 키워드 남기면 DM으로 알려드려요" 류 자동화 CTA — 한국어·글로벌
  전부 완전히 폐기**(2026-08-05, 이전에도 한 번 지시했는데 반영이 안 돼서
  재확인 및 전체 정리함 — 상세는 archive "댓글DM CTA 폐기" 절).
  `comment_keyword`·`comment_dm_automation` 필드는 이제 어떤 topic·플랫폼에도
  쓰지 않는다 — 새 topic 캡션에 이런 CTA 문구(어느 언어든) 자체를 쓰지 말 것.
  마무리 CTA는 그냥 "더 유용한 건강정보 궁금하면 팔로우/구독"류로 통일.
- ⚠️ **"[광고]" 표시 의무 — 화면 오버레이 부분은 영상 중단으로 비활성, 캡션
  내 고지 규칙은 카드뉴스에도 그대로 적용(2026-08-25)**: 아래 서술 중
  `video_assembler.py`/`ad_tag` 오버레이 구현 부분은 영상을 안 만드는 지금
  해당 없음 — 하지만 플랫폼별 "링크를 본문에 넣을지 댓글에 넣을지"
  (`suppress_product_block`/`link_in_comment`)와 "[광고]+고지문을 캡션 맨
  아래 한 덩어리로" 규칙은 여전히 유효하다. 카드뉴스가 나가는 네이버 블로그는
  본문에 링크를 넣는 쪽이라 캡션 맨 아래 고지 한 덩어리로 처리한다.
- ⚠️ **영상 우상단 "[광고]" 오버레이 — 구현 완료, 2026-08-06부터 신규 topic
  전부 기본 적용**(공정거래위원회 공식 지침 대응 — 상세는 archive "영상 광고
  표시 의무" 절). **적용 시점은 프로스펙티브(이 날짜 이후 새로 만들거나
  재조립하는 것부터)일 뿐 — 이 날짜 이전에 이미 만들어둔 기존 영상까지
  일부러 찾아서 소급 재조립하지 말 것**("2026-08-06부터 신규 topic 전부
  기본 적용"을 "기존 것도 다 소급해야 하나"로 오해해 200개 넘는 기존 영상을
  건드릴 뻔했던 사고 이후 명문화, 사용자 확인: "새로 만드는것부터 진행할것").
  다른 이유(콘텐츠 수정, 템플릿 버그 수정 등)로 어차피 재조립하는 영상은
  최신 코드를 쓰므로 자연히 태그가 붙는다 — 그건 문제 없음.
  - 커미션(네이버 쇼핑 커넥트·쿠팡 파트너스 등)이 걸린 콘텐츠는 **영상 화면에
    "[광고]"(또는 "유료광고" 등 동등 표현) 텍스트가 필요** — 제목/시작 부분처럼
    눈에 잘 띄는 위치, 영상 전체가 광고 성격이면 시작·종료부에 표시하고
    **영상 중에 반복적으로 표시**해야 함. **댓글로 표시하는 건 공식적으로
    인정 안 됨**(공정위 지침에 "부적절한 표시" 사례로 명시). 미준수 시
    시정명령·과징금(매출액 2% 이내)·징역/벌금 대상 — 가벼운 권고사항 아님.
  - **구현 위치**: `lib/video_assembler.py`의 `assemble(..., ad_tag=True)`
    (판서형, ffmpeg overlay 필터) + `draw_ad_tag_overlay()`(checklist·
    before_after_transition, PIL 프레임/화면 단위 직접 합성) — 3개 템플릿
    전부 지원. `rebuild_video.py`의 `derive()`가 `ad_tag=True`를 기본값으로
    넘기므로 **`python3 -m lib.rebuild_video <topic>`만 실행하면 실제 커미션
    링크 여부와 무관하게 항상 우상단에 붙는다** — 링크 확정 전이라도 태그
    자체는 미리 켜둔다는 뜻(예전 "링크 확정된 topic에만" 원칙에서 변경됨).
  - **언어별 문구**: `AD_TAG_TEXT_BY_LANG`(한국어=광고, en=AD, ja=広告,
    es=Publicidad, pt=Anúncio, ru=Реклама) — 언어별로 이미 mp4를 따로
    렌더링하는 구조라 문구만 바꿔 넘기면 되고 별도 영상을 더 만들 필요는
    없다. `_title_font_for_lang`을 재사용해 스크립트별 폰트 깨짐도 방지.
  - ⚠️ **캐릭터 여러 명 topic(item_schedule로 우상단에 캐릭터 이름 라벨이도
    같이 뜨는 경우) 자리 겹침 주의** — 광고 태그를 위, 캐릭터 라벨을 그
    아래로 세로 스택하도록 이미 처리해뒀음(`video_assembler.py`), 신규
    오버레이 요소를 우상단에 추가할 땐 이 두 개와도 자리가 안 겹치는지 확인.
  - **적용 여부 육안 확인**: `platform_captions.json`에 `"ad_tag": true`
    필드를 넣어두면(신규 topic은 기본으로 넣을 것) `dashboard.py`가 개별
    대시보드 헤더에 배지(🏷️ 광고표시 적용)로, `index.html` 메인 목록에도
    같은 아이콘으로 표시한다.
  - **플랫폼별 대응(2026-08-06 확정)** — 태그가 화면에 붙는다고 전부 일괄로
    링크를 되살리는 게 아니라 플랫폼마다 다르게 간다:
    - **유튜브 쇼츠·인스타그램 릴스·틱톡(한국어+글로벌 전부)**:
      태그가 떠도 **계속 보류** — 채널 구독자·팔로워 수가 아직 적어서 판매
      유도보다 성장이 우선이라는 별개의 비즈니스 판단(공정위 이슈와
      무관). "나중에 세팅 완료되고 나면" 다시 켤 예정, 그 세팅이 뭔지는
      아직 미정 — 다시 켜기 전에 반드시 사용자 확인. 이 3개 플랫폼은
      `suppress_product_block: true` 유지.
    - **페이스북은 재개(2026-08-07)**: `suppress_product_block: false`(또는
      필드 생략). `link_in_comment: true`라 캡션 본문이 아니라 게시 후
      댓글에 구매 링크+고지문 추가하는 기존 플로우 그대로.
    - **네이버 클립도 재개(2026-08-06)**: `suppress_product_block: false`로
      바꾸고 이미 만들어둔 클립 앱 네이티브 "상품" 버튼 플로우를 그대로 씀
      (위 `network` 절 참고).
      - ⚠️ **"[광고] + 고지 문장"은 캡션 본문 맨 아래, 한 덩어리로(2026-08-10
        재확정)** — "[광고]"만 첫머리·고지 문장은 댓글이라는 이전 초안
        (2026-08-06, 출처 불명확한 3차 가이드 기준)에서, 실제로는 캡션
        본문에 둘 다 넣고 위치만 맨 아래로 최종 확정됨("클립이나 숏츠나
        맨 아래에 이렇게 들어오게 하자... 어차피 바로 눈에 들어올만큼
        짧은 글인데" — 네이버 클립 캡션은 200~300자라 스크롤 없이 전체가
        한눈에 보이므로 공정위 "추가 행동 없이 보이는 위치" 요건은 위/
        아래 어느 쪽이든 충족됨). `dashboard.py`의 `applyProductLinks()`가
        `network:"naver"`+`link_in_comment:true` 조합일 때 자동으로
        "[광고] " + 고지문을 캡션 맨 끝에 붙인다 — 원고 작성 시 별도로
        넣을 필요 없음.
  - 텍스트/이미지만 올라가는 플랫폼(네이버 블로그·쓰레드·인스타그램
    카드뉴스)은 이 문제와 무관 — 절대 `suppress_product_block`을 걸지 말 것,
    쓰레드는 영상 대신 이미지 1장만 첨부하는 게 설계상 확정된 방식이라 여기
    포함 안 됨.
- **필수 플래그**(한국어 topic, 빠짐없이):
  - `no_caption_link: true` — 인스타 릴스·틱톡(URL 클릭 안 되는 플랫폼)
  - `network: "naver"` — **네이버 블로그만**(2026-08-05 수정: 클립은 캡션에
    링크를 안 씀 — 사용자가 클립 앱 네이티브 "쇼핑 커넥트" 버튼으로 영상에
    직접 상품을 붙이는 방식이라, 캡션에 naver.me 링크가 끼어들면 안 됨)
  - `rich_paste: true` — 네이버 블로그만(클립보드 이미지 붙여넣기)
  - `suppress_product_block: true` — 유튜브 쇼츠·인스타그램 릴스·틱톡엔
    계속 건다(위 "[광고]" 항목 참고). **페이스북(2026-08-07)·네이버 클립
    (2026-08-06)은 안 건다**(`false` 또는 필드 자체를 생략) — 재개. 네이버
    블로그·쓰레드·인스타 카드뉴스처럼 영상이 안 붙는 플랫폼엔
    애초에 걸지 말 것.
  - `link_in_comment: true` — 쓰레드·페이스북(본문 링크 대신 댓글에), 항상
    `no_caption_link`와 같이 씀. 제휴 고지문구는 본문 맨 아래 한 곳만.
- 해시태그 전 플랫폼 필수 — 새 topic 후 `test_content_rules.py`로 자동 검사됨.
- **`products` 필드**: 특정 요리·조리형태(예: "저나트륨 사골육수")가 아니라
  실제 쿠팡/네이버에서 검색되는 **상품 카테고리**로("저나트륨 식품"). ⚠️ **설명형
  수식어를 붙인 문장 조각도 금지(2026-08-14 재확인 — 4개 topic에서 동시 발견해서
  일괄 수정: "충분한 수분 섭취용 텀블러"❌→"텀블러"⭕, "쿠션 워킹화"❌→"워킹화"⭕,
  "위 진정 소화제"❌→"소화제"⭕, "편한 신발"❌→"신발"⭕)** — "이런 용도에 좋은"류
  수식어를 앞에 붙이면 실제 검색창에 그대로 치는 사람이 없어 검색이 안
  된다. ⚠️ **"가정용"·"고농도" 금지(2026-08-15 정정 — 예전엔 "가정용
  혈압계"를 정상 예시로 들었으나 뒤집힘)**: 실측 확인 결과 쿠팡 상품
  카테고리는 "휴대용"·"의료용"·"반려동물용"처럼 나뉘지 "가정용"·"고농도"
  단위로는 안 갈린다 — "가정용 혈압계"❌→"혈압계"⭕, "가정용
  혈당측정기"❌→"혈당 측정기"⭕, "고농도 산소캔"❌→"산소캔"⭕.
  `test_content_rules.py`의 `test_products_no_unsearchable_prefix`가
  기계적으로 차단(휴대용처럼 실제 쿠팡 카테고리에 있는 수식어는 계속
  허용). 그 topic 특유의 서술("~섭취용"·"~케어용")을 조어처럼 덧붙이는
  것도 안 된다 — 헷갈리면 실제 쿠팡/네이버 검색창에 그 문구를 그대로 쳐서
  결과가 정상적으로 나오는지 확인할 것. ⚠️ **식품에
  한정 안 됨(2026-08-12 명확화)** — "식품 카테고리로"라고만 적혀 있어서 오해하기
  쉬운데, 실제로는 인공눈물·온찜질팩·혀클리너·치실·치간칫솔·루테인 영양제·
  자외선차단 선글라스·미백치약처럼 건강기능식품·위생용품·기기류도 topic 성격에
  맞으면 그대로 쓴다("식품"은 대표 예시일 뿐). "·"(가운뎃점)로 두 품목
  이어붙이지 말고 하나만 남길 것. 습관/타이밍처럼 상품이 아닌 항목은 아예
  빼거나 관련 상품명만 남길 것.
- 네이버 블로그 캡션은 최소 1000자(`test_content_rules.py`가 자동 검사).
- 이미지 삽입 위치 표시는 `01 · 소제목` 형식(카메라 이모지·대괄호·"삽입" 금지),
  마커 아래는 빈 줄 두 줄.
- 쓰레드는 별도 포맷 — 이미지 1장, 이모지 리스트형, 질문형 마무리, 50~200단어.

## 콘텐츠 톤

- ⚠️ **한국어 대본은 존댓말(2026-08-05)** — "배속 주지않고 원배속으로 가고,
  존댓말로 스크립트 추출하기로 했어" 확정. 기존 반말("~해봐", "~거든",
  "~좋아")로 만든 영상·narration.txt는 그대로 두고 손대지 않는다 — **새로
  추출하는 것부터만** 해요체/합니다체(예: "~해보세요", "~때문이에요",
  "~좋아요")로 작성. 다른 5개 언어는 이미 정중한 어조라 문체 자체를 바꿀
  필요는 없고, 원어 표현이 지나치게 캐주얼하면 같이 다듬을 것.
- ⚠️ **구조: 훅은 핵심 키워드로 시작(2026-08-10, 기존 "장기/질환명 걱정형
  훅 금지" 뒤집음)** — "검색어 노출 자체가 키워드 중심이어야 한다"는 판단으로,
  이제는 반대로 장기/질환명(대상포진·위·장 등 topic 핵심 키워드)을 훅 맨
  앞에 놓고, 그 뒤에 사람들이 실제 검색할 만한 구체적 걱정 포인트로 자연스럽게
  이어 붙인다. 예: "대상포진, 물집 잡히기 전 이 증상부터 왔다면"⭕ — 키워드만
  던지고 안 이어지거나("대상포진 주의"류) 막연한 "~걱정되는 분들"로 흐리는
  건 여전히 금지, 키워드 뒤에도 구체적 증상·맥락이 와야 함. 원인 설명 →
  해결책 순서는 그대로. 제목/캡션 첫 줄까지 전부 동일 톤 통일.
- ⚠️ **제목 마지막 라벨(`title` 배열 마지막 줄·블로그 제목의 "-" 뒤쪽)은
  해결책을 미리보기하는 명사구, 행동유도(CTA)형 문구 금지(2026-08-10,
  "저장부터 하세요 이딴건 너무 구닥다리식 의미도 없는 문구")** — "저장부터
  하세요"/"주목하세요"/"확인하세요"류는 무엇에 대한 해결책인지 전혀 안
  드러나고 CTA는 이미 캡션 마무리에서 따로 쓰므로 제목에서 또 쓰면 자리만
  차지하고 클릭 유도력이 없다. 대신 "OO 줄이는 습관 3가지"/"OO 낮추는 음식
  몇 가지"처럼 실제 콘텐츠 내용을 요약하는 명사구로 마무리할 것. 예: "다래끼,
  자꾸 나거나 낫는 데 오래 걸린다면 - 재발 줄이는 습관 3가지"⭕. `test_no_weak_organ_worry_hook`처럼
  `tests/test_content_rules.py`에 알려진 CTA 문구 목록을 자동 검사하지만
  ("걱정되는 분들 주목"만 잡음), 이건 완전 자동 판별이 어려워 최종 판단은
  작성 시점에 직접 할 것.
- ⚠️ **블로그 제목(`platform_captions.json`의 `title` 필드)은 25~50자
  (2026-08-10 최초 25~40자 확정 → 2026-08-12 완화)** — `lib/content_review.py`의
  `check_blog_title_length()`가 `review_topic()` 경유로 검사(2026-08-12 정정:
  이전에 "tests/test_content_rules.py의 test_blog_title_length"라고 잘못
  적혀있었음 — 실제로는 이 파일이 아니라 content_review.py에 있고, 함수명도
  `check_`로 시작함). `test_content_rules.py`처럼 전체 topic을 매번 훑는
  회귀 테스트가 아니라 **새 topic 작성 시 `content_review` 호출로만 걸리는
  검사**라 기존 topic엔 소급 적용 안 됨(실측: 86개 중 25~40자 기준으로는
  58개, 25~50자로 완화한 지금 기준으로도 39개가 범위 밖 — 전부 이 규칙
  확정 이전에 쓰인 옛 topic들, 소급 수정 대상 아님).
- ⚠️ **"제목"(카드뉴스 표지·영상 타이틀 카드에 쓰이는 `card_news_spec.json`의
  `title` 배열, 유튜브 등 업로드 제목 필드)엔 가운뎃점(·)·이모지류 특수문자
  금지(2026-08-10, "AI 느낌이나 베낀 느낌 나 보인다")** — 하이픈(-)·물음표(?)·
  느낌표(!)는 그대로 허용. **본문(나레이션·캡션 본문)은 대상 아님** — 거기선
  가운뎃점·이모지 계속 써도 됨, 오직 제목에서만 빼면 됨. **새로 쓰는 제목부터**
  적용 — 기존 topic 제목은 소급 수정 안 함.
- ⚠️ **훅 문장 형태는 반드시 `python3 -m lib.content_review --hook-pattern <topic>`으로
  먼저 확인하고 그 패턴대로 쓸 것(2026-08-10)** — 예전엔 "직전 2~3개와 다른 걸
  의식적으로 고르라"는 지침만 있었는데, 실측 확인 결과 57개 topic 중 34개(60%)가
  "~라면 이 N가지부터 확인해봐"류 한 패턴으로 수렴해있었다("규칙은 있는데 실제로
  거의 안 지켜지고 있었다" — 사용자 지적, "매번 난수 돌리면 되는거아닐까" 제안으로
  select_format()과 동일한 topic-시드 방식 채택). `lib/content_review.py`의
  `select_hook_pattern(topic)`이 10종(반전형·결론선치형·통념반박형·숫자통계
  제시형·효과수치형·장면제시형·비교대조형·질문형·직접화법질문형·혼잣말공감형)
  중 결정론적으로 하나를 정해준다.
- 🚨 **도입부 문형(2026-09-19 확정)** — "[상황]에서 [증상]이 나타나면 이걸 확인하세요" 한 문장으로 열고
  **곧바로 첫 항목**으로 넘어간다(예: "위염, 매운 음식 먹고 속이 쓰리고 아프다면 이 세 가지부터 확인하세요.
  먼저 커피 같은 카페인 음료예요."). **"저장하세요"·"주목하세요"류 유도 꼬리는 금지** — `check_opening_hook()`
  이 잡는다. ⚠️ 2026-09-17에 "~라면 확인하세요"까지 금지로 묶었던 건 과잉이었다(당시 사용자가 지적한
  "Summary"는 이 문형이 아니라 영상 상단 배너였다). 사이에 요약·설명 문장을 끼워 도입을 늘리지 말 것.
- 항목 여러 개 설명 전엔 "~~, ~~, ~~야."로 먼저 이름만 나열(“때문이야”로 끝내지
  않기), 그다음 각각 설명 — ⚠️ **캡션뿐 아니라 narration.txt(영상 나레이션)에도
  똑같이 적용할 것(2026-08-09 재확인)**: 규칙 자체는 2026-08-04부터 있었지만
  narration.txt에는 실제로 안 지켜진 topic이 다수 발견됨(여성_1 등, 규칙 확정
  이후 작성분도 포함) — 캡션에만 있고 영상엔 없는 사례가 반복되지 않게 원고
  작성 시 둘 다 확인할 것.
- ⚠️ **항목 사이 연결어 필수(2026-08-09, "뚝뚝 끊기고" 지적)** — 원인 항목을
  나열식으로 이어붙이면(연결어 없이 문장만 바뀌는 구조) 나레이션이 뚝뚝
  끊겨 들린다. "먼저", "두 번째로는", "그리고 마지막으로" 같은 최소한의
  브릿지 표현을 각 항목 시작에 넣을 것.
- ⚠️ **마무리 문장은 특히 확신형으로(2026-08-09)** — 클로징이 "~수 있어요"류로
  흐리게 끝나면 전체 영상이 "그래서 어쩌라는 거지" 식으로 힘없이 마무리된다.
  아래 헷지 어미 금지 규칙을 마무리 문장에 가장 엄격하게 적용할 것.
- 금지 표현: "~대요"(전언체, "~것으로 나타났어요"로), "무조건 다 끊을 필요는
  없어"류 헷지 문장(원인 설명 뒤 바로 해결책으로).
- ⚠️ **"~수 있어요/~수도 있어요"류 헷지 어미 남발 금지(2026-08-09)** —
  "확신에 찬 말투로 해달라고 분명히 말했었는데 반영이 안 됐다" 재지적,
  57개 topic 중 16개에서 61회 발견. 매 문장 끝을 가능성 표현으로 흐리지
  말고 단정형으로 끝낼 것("혈류가 줄어들 수 있어요"❌ → "혈류가
  줄어들어요"⭕, "부담을 줄 수 있어요"❌ → "부담을 줘요"⭕). 실제로
  개인차·불확실성이 있어서 단정하면 안 되는 의학적 진술(진단·치료 관련)에서만
  예외적으로 허용 — 생활습관·음식 설명 같은 일반 서술문에는 절대 쓰지 말 것.
  기존 topic 재생성은 안 함(TTS 재생성 금지 규칙과 동일 원칙, 톤 문제로
  재호출 안 함) — **새로 쓰는 원고부터** 적용.
- ⚠️ **나레이션 목표 길이 60~75초, 원인/이유 항목 최대 3개(2026-08-16)** —
  실측 결과(대사_13/12·근골격_15·순환_9/11) 최근 topic 평균이 740~840자
  (공백 제외)로 2분~2분45초까지 늘어나 있었음("정보량 부족하면 늘려도
  됨" 원칙이 상한선 없이 누적된 결과). 이 프로젝트 TTS 실제 발화 속도는
  초당 약 5.5자(공백 제외, 위 5개 topic 실측 평균)이므로, 60~75초 목표는
  대략 **330~410자**에 해당 — narration.txt 작성 후 공백 제외 글자수를
  세서 이 범위 안인지 확인할 것(TTS 호출 **전에**, 재생성 금지 원칙과
  동일한 이유). 분량을 실제로 줄이는 방법은 글자를 줄줄이 쳐내는 게
  아니라 **원인/이유 항목 자체를 최대 3개로 제한**하는 것 — 현재 5~6개
  항목을 나열하던 게 길이 누적의 핵심 원인이었음. 항목당 설명도 기전
  부연 없이 핵심 한 문장으로("왜 그런지"는 남기되 배경 설명은 생략).
  해결책·마무리 문장은 그대로 확신형 유지(위 헷지 어미 금지 규칙과
  무관). **기존 topic 재생성은 안 함** — 새로 쓰는 원고부터 적용.
- ⚠️ **TTS 재생성 기준**(2026-08-05 재확정 — 상세 경위는 archive "TTS 재생성
  금지 확정" 절):
  - **금지**: 길이가 마음에 안 들어서(45초 초과, 1분 넘음 등) 다시 뽑는 것.
    언어 상관없이(한국어 포함) topic당 TTS 1회 원칙 — TTS는 글자수 과금이라
    "뽑고→길면 재작성→다시 뽑고" 반복이 API 호출 배수로 이어짐. 대신
    `estimate_duration(text, lang)`으로 **작성 전에** 예상 길이를 가늠해서
    애초에 적당한 분량으로 쓴다 — `data/tts_pacing.json`은 `synthesize()`
    호출마다 자동 갱신되는 경험 기반 데이터라 손대지 말 것.
  - **허용(권장)**: 작성 세션이 직접 재검토(아래 "콘텐츠 QA" 절)해서 실제
    사실관계·논리 오류·과장을 발견했으면 narration.txt를 고치고 TTS를 다시
    호출한다 — 오디오와 텍스트가 어긋난 채(=녹음에 실제 오류가 남은 채)
    두지 말 것. 가능하면 **TTS 호출 전에** 이 재검토를 먼저 끝내서 애초에
    사후 재생성이 필요 없게 만드는 게 최선(길이 때문이 아니라 "틀려서"
    고치는 것이므로 위 금지 규칙과 무관).
- 콘텐츠 작성 순서: 자료조사 → 네이버 블로그(마스터, 1000자+) 완성 →
  narration.txt/카드뉴스는 거기서 압축 → 쓰레드는 별도 재구성.
- 원본 기사 요약만으로 끝내지 말고 WebSearch로 메커니즘·연구 수치까지 보강.
- 엔딩 CTA(구독/좋아요/팔로우)는 카드뉴스·영상 둘 다 기본 자동 삽입
  (`end_card_duration` 기본 2.0초, 끄려면 0).

## ⚠️ `platform_captions.json`의 최상위 `title` 필드를 함부로 바꾸지 말 것

이 필드는 표시용 제목이 아니라 **유튜브 재생목록 정리의 매칭 키**다.
`lib/youtube_organize_playlists.py`가 `title == video["title"]`로 **실제 업로드된 영상
제목과 문자열 완전 일치**를 걸어 topic을 찾는다(`_topic_titles()`). 여기를 고치면
이미 올라간 영상과 매칭이 끊겨 재생목록 정리가 조용히 깨진다.

- 네이버 블로그 제목(캡션 첫 줄)과 **다른 게 정상**이다. 2026-08-30 기준 342건 중
  150건이 불일치인데 오류가 아니다 — 유튜브 제목과 블로그 제목은 원래 별개다.
- 캡션 제목을 다시 쓰는 작업(제목 다양화 등)에서는 **캡션 첫 줄만 건드리고 이 필드는
  그대로 둘 것.**
- 정말 바꿔야 하면 유튜브 쪽 영상 제목도 함께 바꾸거나, 매칭이 깨져도 되는지 먼저 확인할 것.

## 사실 검증 사이클 (2026-08-28 신설 — 사람이 매번 못 보니 기계가 잡는다)

**전제: 언어별 내용을 사람이 디버깅할 수 없다.** 8개 언어 콘텐츠의 사실관계를 매번
눈으로 확인하는 건 불가능하므로, 판정 결과를 데이터로 남기고 테스트가 재발을 막는다.

```bash
.venv/bin/python3 -m lib.claim_audit                    # 등록된 topic 감사
.venv/bin/python3 -m lib.claim_audit --all --warnings   # 전체 + 출처 없는 수치 경고
.venv/bin/python3 -m lib.claim_audit 머리_2 소화_2       # topic 지정
.venv/bin/python3 -m pytest tests/test_content_rules.py -k known_false   # 게이트
```

🚨 **`pytest -k`에 한글 topic명을 쓰면 조용히 0건이 수집된다(2026-08-28 실측).**
pytest가 테스트 ID의 한글을 `\uc5ec\uc131` 형태로 이스케이프해서 `-k "여성"`이 매칭되지
않는다. **exit 5(수집 0건)인데 실패가 아니라 성공처럼 보이므로 검증한 줄 알고 넘어가게
된다** — 실제로 이 프로젝트에서 그렇게 지나간 적이 있다.
- ❌ `pytest -k "여성"` → 0건
- ✅ `pytest -k "uc5ec"` → 매칭됨(지저분하지만 동작)
- ✅ **권장: `python3 -m lib.claim_audit <topic>`** — topic 인자를 그대로 받는다
- `-k blog`, `-k known_false`처럼 **영문 키워드는 정상**

### ⚠️ 한국어 캡션 파일은 topic마다 위치가 다르다 (2026-08-31 정정)

**한국어 캡션은 topic당 한 곳에만 있다. 그런데 그 한 곳이 topic마다 다르다.**

- 언어 폴더가 없는 옛 topic → `data/<topic>/platform_captions.json` (342개)
- 언어 폴더가 생긴 topic → `data/<topic>/ko/platform_captions.json` (17개)
- 양쪽 다 있는 topic은 **0개**다. `tests/test_content_rules.py`의
  `test_korean_caption_lives_in_one_place`가 이 불변조건을 지킨다.

⚠️ **flat만 보고 "네이버 캡션이 없다"고 판단하지 말 것.** 실제로 한 세션이 그렇게
오진해서 이미 `ko/`에 있는 캡션 26개를 flat에 중복 생성했고, 같은 topic에 서로 다른
네이버 원고가 두 벌 생겨 어느 쪽이 나갈지 알 수 없는 상태가 됐다.

`ko/` 폴더 파일은 **네이버 블로그 항목과 `blog_seo` 항목을 같이 담는다** — 파일
하나가 두 곳으로 나간다. 어느 항목을 고쳤는지에 따라 반영 경로가 갈린다.

| 고친 항목 | 나가는 곳 | 반영 방법 |
|---|---|---|
| `네이버 블로그` (flat 또는 ko/) | 네이버 블로그(사람이 대시보드에서 복사해 수동 게시) | `python3 -m lib.mission_control_sync --commit` |
| `blog_seo` (`<lang>/`) | vernhaven `blog_posts` | vernhaven `ingest_health_shorts.py --commit` |

flat ko의 네이버 항목을 고친 뒤 vernhaven ingest를 돌려도 **한 줄도 안 들어간다** —
정상이다(그 스크립트는 `blog_seo`만 본다). 2026-08-28 ko 오류 수정 때 이걸 확인하느라
한 번 헛돌았다.

### 돌아가는 방식

1. **다국어 확장이 곧 감사다.** 새 언어를 쓸 때 각 언어권 공식기관 원문으로 재검증하므로
   ko의 오류가 자연히 드러난다 — 실제로 21개 topic 작업에서 38건을 찾았다.
2. **판정 결과를 `data/_audit/ko_known_issues.json`에 적는다.** topic·유형·틀린 주장·
   확인된 사실 네 가지를 남긴다.
3. **`test_no_known_false_claims`가 자동으로 감시한다.** 그 수치·어구가 캡션에 남아 있으면
   pytest가 실패한다. 새 항목을 추가하면 별도 작업 없이 감시 대상이 된다.
4. **고치면 테스트가 초록이 된다.** 사람이 확인할 일이 없다.

### 확인된 실패 유형 8가지 — 새 콘텐츠 작성 시 전부 의심할 것

| # | 유형 | 실제 사례 |
|---|---|---|
| 1 | 출처를 못 찾는 수치 | "칼슘 600mg이 철 흡수 58% 감소" |
| 2 | 존재를 확인 못 한 논문 | "Diabetes Care 2008, 169 vs 141mg/dL" |
| 3 | 실존 논문 수치 부풀리기 | PREDIMED 실제 30% → ko "39%" |
| 4 | 무관한 연구 갖다 붙이기 | IARC 대장암 수치를 새치 근거로 |
| 5 | 맥락 오용 | 피트산 수치는 원문과 일치하나 밀빵 철흡수 실험 |
| 6 | 전제 자체가 반박됨 | "보충제가 백발을 늦춘다" ← MedlinePlus가 부정 |
| 7 | 보도자료 수치를 논문 수치처럼 | Zuraikat 2024의 14.8%는 논문 본문에 없음 |
| 8 | 원문 오독 | "150에서 250으로"를 "150~250 증가"로 |
| 9 | 다른 나라 기준값을 한국 콘텐츠에 그대로 씀 | 프랑스 ADEME의 "온도차 6~8도"를 한국 글에 |

⚠️ **3·4·5·7번은 검색하면 논문이 나오므로 가장 잡기 어렵다.** 실존 논문을 인용할 땐
**(a) 원문 본문에 그 수치가 있는지**(보도자료 말고) **(b) 그 연구가 이 주제·이 인구집단에
대한 것인지** 둘 다 확인할 것.

### 국가별로 다른 것 — 하나를 옮기면 나머지가 전부 틀린다

진단 컷오프·권장량·판정 방식은 나라마다 다르다. 대사증후군은 **판정 방식 자체**가
갈렸다(3 of 5 / 허리 전제조건 / 4 중 2 / 인슐린저항성 중심), 일본은 한국과 **남녀가
반전**된다(男85/女90). 자세한 목록은 메모리 `feedback_blog_seo_country_specific_thresholds`.

### 공식 출처끼리 충돌하면 섞지 말고 하나만

평균내거나 병기하면 어느 쪽도 아닌 값이 된다. 최신·기관 위상·문서 자체의 신뢰도로 고르고
**근거를 커밋 메시지에 남길 것**(스페인 SEGO 문서는 "흡연이 근종을 줄인다"는 문장이
같이 있어 신뢰도가 낮다고 판정한 전례가 있다).

### WebSearch/WebFetch 요약을 믿지 말 것

정상 페이지에서도 원문에 없는 내용을 "있다"고 답한 사례가 3건 나왔다(ISS·1177·
Voedingscentrum). **핵심 주장은 `curl`로 원문을 받아 문자열로 대조**할 것. 404면 사이트맵
재탐색, 403/캡차/JS-SPA면 그 출처를 포기하고 다른 공신력 기관으로.
자주 막히는 곳: Ameli·Mayo 뉴스룸·CDC·FEC·Drugs.com. 대안: VIDAL/Inserm/ANSES/SNFGE/PubMed.
⚠️ `e-healthnet.mhlw.go.jp`는 NXDOMAIN이 됐다 → `dmic.ncgm.go.jp`, 후생노동성 PDF 직접 링크.

### 안전 관점도 함께 볼 것

사실 오류가 아니어도 위험한 조언이 있다. ko의 "저염 제품으로 바꾸세요"는 소금대체제가
대개 염화칼륨이라 **신장질환자·이뇨제 복용자에게 하이퍼칼레미아 위험**이었다. 권고를
쓸 때 특정 질환자에게 위험하지 않은지 각국 공식 경고를 확인할 것.

### "기관명 없는 귀속"은 그 자체가 신호다

"냉방 가이드는", "Cooling and health guides commonly recommend", "한 의학 자료에
따르면" — 기관을 안 밝히는 문장은 대개 **다른 나라 기준을 옮겨왔거나 출처를 못 찾은
자리**다. 실측: ko의 "냉방 가이드는 온도차 8도"는 프랑스 ADEME 값이었고 한국 권고
(5~6℃)와 달랐다. 같은 문장이 en에도 "commonly recommend"로 들어가 있었다.

- 수치를 쓸 땐 **어느 기관인지 문장 안에 적는다.** 못 적겠으면 그 수치를 뺀다.
- 같은 topic의 다른 언어가 실명으로 인용하고 있으면(fr은 ADEME·WHO를 밝힘) 그건
  그대로 두고, 뭉뚱그린 쪽만 고친다 — 나라마다 기준이 다른 게 정상이다.

## blog_seo 발행 품질 게이트 (2026-08-31 신설)

**분량·제목 길이는 인입 스크립트가 "경고"로만 찍어서 아무도 안 고쳤다** — 실측 결과
84편이 최소 분량 미달(최저 257단어), 49편이 제목 길이 초과인 채로 이미 인입돼 있었다.
콘텐츠를 만드는 이 저장소에서 막는다.

```bash
.venv/bin/python3 -m pytest tests/test_blog_seo_quality.py -q
```

- 기준: 라틴문자권 본문 500단어 / 일본어 1000자, 제목 65자(ja·ko는 35자) —
  `scripts/ingest_health_shorts.py`의 동명 상수와 **값 동기화 필수**(저장소가 달라 공유 불가)
- 이미 인입된 미달분은 `data/_audit/blog_seo_quality_debt.json`에 등록돼 예외로 통과한다.
  **새 위반은 즉시 실패**한다
- ⚠️ **고친 항목은 채무 목록에서 지워야 한다** — 안 지우면 "고쳤는데 목록에 남아있다"로
  실패한다(목록이 줄어들기만 하게 만드는 장치)
- 증량은 **물타기 금지** — 있는 문장 늘려쓰기·같은 말 반복·면책 문구 증량이 아니라
  그 언어권 공식기관 자료로 H2 섹션을 더한다
- 제목을 줄일 땐 **`slug`를 절대 바꾸지 말 것**(이미 인입된 글의 URL이 끊긴다)

## 같은 틀로 수렴하지 않게 — 매 topic 시작 전 확인 (2026-08-31 신설)

사용자가 "제목이나 내용이 비슷비슷해서 저품질 되는 느낌"이라고 먼저 알아챘고,
실측해보니 사실이었다. **제목은 385개 중 완전 중복 0건이라 눈에 안 띄는데 구조가
판박이였다** — 이런 쏠림은 글을 몇 편 읽어봐서는 잘 안 드러나고 전수로 세야 보인다.

측정 당시(수정 전):

| 축 | 쏠림 |
|---|---|
| 카드 아이템 7개 고정 | 385개 중 320개(83%) |
| 원인/해결 배열이 단 두 가지 | 307개(80%) |
| 네이버 본문 "먼저 ~"로 시작 | 77% |
| 네이버 본문 "마지막으로"로 마무리 | 64% |
| ko blog_seo H2 "정리하면" | 91% |

```
python3 -m lib.repetition_audit          # 지금 어디가 쏠려 있는지 계기판
```

게이트가 아니다 — 몇 %부터 위험한지 객관적 기준이 없어 임계값을 안 뒀다. 큰 작업
전후로 돌려 수치가 내려가는지 보는 용도다.

**새 topic 작성 전에 아래를 전부 확인하고 그대로 쓸 것.** 하나라도 건너뛰면 그 축이
바로 다시 수렴한다(실제로 `select_title_archetype`은 문서에만 있고 구현이 없던
기간이 있었고, 그동안 94%가 같은 H2로 수렴했다).

```
python3 -m lib.content_review --hook-pattern <topic>                  # 훅 어미
python3 -m lib.content_review --card-structure <topic>                # 카드 짜임 6종
python3 -m lib.content_review --connectives <topic>                   # 네이버 접속어 5종
python3 -m lib.content_review --title-archetype <topic> <lang>        # blog_seo 제목
python3 -m lib.content_review --closing-archetype <topic> <lang>      # blog_seo 클로징
# H2 섹션 헤더는 CLI 없이 직접 import
python3 -c "import lib.content_review as c; print(c.select_section_header_archetype('<topic>','<lang>','summary'))"
python3 -c "import lib.content_review as c; print(c.select_section_header_archetype('<topic>','<lang>','actual_fix'))"
```

- **아키타입은 문구가 아니라 스타일이다.** 같은 아키타입이 뽑힌 topic끼리도 문구는
  달라야 한다 — 그 글의 결론을 이름 붙이는 헤더를 쓸 것. "요점 한 줄"은 여전히
  상투구고, "결국 문제는 온도차였어요"가 그 글의 헤더다.
- **아이템 개수를 7개로 고정하지 말 것.** 소재에 따라 5~9개로 달라지는 게 정상이다.
- **"왜 이런 문제가 생길까요"로 첫 장을 여는 습관을 경계할 것**(22회 반복됨).
- 의료 안전 섹션 라벨(`받아야 할 때`·`받으세요` 류)은 다양화 대상이 아니다 —
  반복돼도 그대로 두는 게 맞다.
- 소재상 뽑힌 아키타입이 도저히 안 맞으면 다른 걸 써도 되지만, 그때도 직전 몇 개
  topic과 같은 짜임은 피할 것.

## ⚠️ `.env` 빈 값 때문에 조용히 안 돌던 동기화 (2026-08-31)

**`.env`에 키 이름은 있는데 값이 빈 줄이 21~25개 있었다.** 그중 둘이 실제로 쓰이는
값이라 아래 두 동기화가 **에러 없이 아무것도 안 하고 끝나 있었다** — 대시보드가
낡은 채로 남아 있어도 아무도 모른다.

- `python3 -m lib.card_news_hub --commit` — `BLOG_NETWORK_SUPABASE_*`가 비어
  RuntimeError로 죽는데, 출력이 길어 마지막 줄을 안 보면 성공처럼 보인다.
  값은 vernhaven-blog `.env.local`의 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`와
  같다(공유 blog-network 프로젝트)
- `lib/mission_control_log.py`의 `report_issue()` — `MISSION_CONTROL_INGEST_*`가
  비면 **조용히 `False`를 반환**하고 끝난다(fire-and-forget 설계라 호출부가 반환값을
  안 본다). 값은 jp-review-shorts `.env`와 동일, URL은 `http://localhost:3011`이라
  **mission-control dev 서버가 떠 있을 때만 실제로 기록된다**(배포본은 Vercel SSO
  뒤라 API 접근 불가)

- 동기화가 안 먹는 것 같으면 **먼저 `.env` 값이 비었는지 확인**할 것:
  `python3 -c "from dotenv import dotenv_values; print([k for k,v in dotenv_values('.env').items() if not v])"`
- 나머지 빈 키(YouTube TW/ES/PT/RU, FISH_AUDIO, PIXABAY, META_HANIP, THREADS,
  SUPABASE_ACCESS_TOKEN/DB_PASSWORD/PROJECT_REF)는 **코드에서 참조하지 않아 무해**하다.
  `SUPABASE_ANON_KEY`도 파이썬은 안 쓴다 — 브라우저는 `supabase_client.js`의 값을 쓴다

## blog_seo 발행 상태 (2026-09-02 기준)

vernhaven `blog_posts`의 health-shorts 분 **447편 서빙 중** — 아래 넷 다 0이다.
`scripts/ingest_health_shorts.py --commit`으로 재인입한 뒤 이 수치가 늘면 그게 회귀다.

| 항목 | 값 |
|---|---|
| 중복 (topic, lang) | 0 |
| hero 이미지 플레이스홀더 | 0 |
| 500단어 미만 | 0 (한때 84편) |
| 제목 길이 초과 | 0 (한때 49편) |

- 진행 중 상태는 `data/_audit/blog_seo_quality_debt.json`(분량·제목)과
  `blog_seo_wip.json`(8개 언어 미완)에 등록한다. **둘 다 지금 비어 있다** —
  항목이 생기면 그게 남은 일이고, 다 채우면 지워야 테스트가 통과한다
- ⚠️ **인입은 절전에 매우 취약하다.** 통짜로 돌리면 소켓이 죽어 프로세스가 영원히
  멈춘다(34분에 CPU 1.4초 실측, 다섯 번 반복). topic 몇 개씩 인자로 넘겨 쪼개 돌리고,
  성공한 것만 기록해 재실행 시 건너뛰게 할 것

## 🚨 네이버 블로그 원고 품질 — 나레이션의 확장판이 아니다 (2026-09-17 신설)

사용자 지적: "네이버블로그도그렇고 클립도 그렇고 내용이 좀 애매해 그렇게까지
사람들한테 도움이 되지 않는 느낌이야." 전수 실측으로 원인 둘을 특정했다.

| 실측 | 결과 |
|---|---|
| 수치를 쓰면서 출처 기관을 본문에 안 밝힘 | 385편 중 **199편(52%)** |
| "연구 결과가 있어요"식 무기명 귀속 | 67편(17%) |
| 나레이션 문장을 40% 이상 그대로 재사용 | 152편 중 **62편**(최고 87%) |

- **수치에는 기관명을 그 문장 안에 쓴다. 못 쓰겠으면 그 수치를 뺀다.**
  "식후 혈당 최고치가 30% 가까이 낮아진다는 연구 결과가 있어요"는 독자가 확인할
  방법이 없어서 카더라와 구별이 안 된다 — 이게 "애매하다"의 실체다. 이 규칙은
  예전부터 blog_seo에만 기계 검사가 걸려 있었고 네이버 원고는 무방비였다.
- **네이버 원고는 나레이션을 늘려 쓴 게 아니라 별도 장르다.** 60~75초 나레이션엔
  350~410자밖에 없는데 네이버 원고는 1000자 이상이라, 재사용하면 나머지를 같은 말
  반복으로 채우게 된다. 영상에 **못 담은 것**으로 채울 것 — 분량·빈도·타이밍,
  예외 상황(외식·바쁜 날·이미 진단받은 경우), 흔한 오해, 언제 병원에 가야 하는지.
- 도입 문단과 첫 본문 섹션이 같은 문장을 되풀이하는 패턴이 흔하다 — 도입은
  본문에 없는 말만 한다.
- 기계 검사(`review_topic()`에 연결, `python3 -m lib.content_review <topic>`):
  `check_naver_blog_quality()`. pytest 게이트는
  `tests/test_content_rules.py::test_naver_blog_quality_no_new_violations`.
- 실측 시점 위반 227편은 `data/_audit/naver_blog_quality_debt.json`에 채무로
  등록돼 예외 통과한다(전량 재작성은 게시 완료분 수정이라 별개 결정). **새 topic의
  위반은 즉시 실패한다.** ⚠️ **고친 topic은 채무 목록에서 지울 것** — 안 지우면
  "고쳤는데 목록에 남아있다"로 실패한다(목록이 줄어들기만 하게 만드는 장치).

## 콘텐츠 QA — 완료 전 필수

⚠️ **Gemini API는 일러스트 생성(`lib/gemini_illust.py`)에만 쓴다(2026-08-15
확정)** — `content_review.py`가 예전엔 논리 오류·과장·번역독립성까지 Gemini로
자동 판단했지만 전부 제거했다. 판단형 검사는 이제 **작성한 세션/에이전트가
직접 비판적으로 재검토**해서 판단한다 — 외부 API를 대신 부르지 않는다.

```
python3 -m lib.content_review <topic> [lang]   # 기계적 검사만(제목 잘림·CTA 문구·글자수)
python3 -m lib.content_review --hook-pattern <topic>   # 제목 작성 전에 먼저 — 10종 훅 패턴 중 이번 topic이 뭔지 확인
```

⚠️ **`content_review <topic>`은 flat과 `ko/` 두 캡션 파일을 모두 검사한다(2026-08-30
수정)** — 예전엔 `ko/` 폴더가 있으면 그쪽만 읽어서 **네이버 캡션이 어떤 검사도 안 받고
지나갔다.** 네이버 캡션 26개를 새로 쓰는 동안 서브에이전트 셋이 각각 "내 파일을 안
본다"고 따로 보고해서 드러났다. 두 파일의 `title`은 원래 다른 게 정상이므로(유튜브
매칭 키 절 참고) 경고가 어느 쪽 파일 것인지 보고 판단할 것.

- **새 topic은 (전체 기준: narration.txt+card_news_spec.json / 카드뉴스만:
  card_news_spec.json만) 완성 시점에 아래 두 가지가 완료 조건**:
  1. `content_review` 통과(기계적 검사 — 제목 잘림·CTA 문구·블로그 제목 글자수)
  2. **세션이 직접** `lib/content_review.py` 상단 `MANUAL_REVIEW_CHECKLIST`
     항목(논리 오류·과장·모순·기전 모호함·지역 재료 적합성·번역투·번역독립성 등)을
     읽고 콘텐츠를 재검토 — 문제 있으면 직접 판단해서 고칠 것. 자동 수정 안 됨.
- ⚠️ **`--hook-pattern`은 title을 쓰기 전에 먼저 돌릴 것(2026-08-12 재확인)** —
  "훅은 핵심 키워드로 시작" 규칙(아래 "콘텐츠 톤" 절)만 적용하고 이 명령은
  건너뛴 채 4개 topic 전부 "[키워드], ~다면 - OO 습관" 한 패턴으로 제목이
  획일화된 사고 발생. `content_review`와 나란히 여기 QA 체크리스트에 있어야
  실제로 매번 돌아간다 — "콘텐츠 톤" 절에만 적혀 있으면 다른 톤 규칙에 묻혀
  누락되기 쉬움.
- 권장 순서: 위 두 QA를 TTS 호출 **전에** 끝내면 사후 재생성 자체가 필요
  없어진다 — 다만 TTS 이후에 실제 오류가 발견되면 재생성한다(위 "콘텐츠 톤"
  절 TTS 규칙 참고, 절대 규칙은 아니고 권장 순서).
- 다국어 topic은 언어 간 제목/훅이 사실상 번역인지(번역 금지 원칙, 아래
  "글로벌 확장" 절) 세션이 직접 비교해서 판단할 것 — 문장 구조·예시·강조점이
  거의 1:1로 대응되면 번역으로 간주하고 그 언어권 독자에게 맞춘 진짜 다른
  각도로 다시 쓸 것.
- **blog_seo도 위 두 QA 대상**(2026-08-13 도입) — `platform_captions.json`에
  `blog_seo` 항목이 있으면 title/meta_description/body_html도 세션이 직접
  재검토(번역투 표현·그 언어권 광고규제 위험 표현·제목의 검색 키워드
  전진배치 여부·출처 없는 정밀 수치 포함, `MANUAL_REVIEW_CHECKLIST` 7번 항목).
  blog_seo가 2개 언어 이상 있으면 제목 간 번역 여부도 세션이 직접 비교.
- ⚠️ **blog_seo 기계적 검사는 `test_content_rules.py`가 `pytest` 실행마다
  자동으로 돈다(2026-08-13, API 호출 없음)** — 필수 필드 누락·
  meta_description 글자수(120~160, seo-blog `ingest_health_shorts.py`의
  `META_DESCRIPTION_RANGE`와 값 동기화 필수)·본문에 `<img>` 최소 1장·HTML
  태그 밸런스·slug 형식·title=meta_description 완전 동일 여부. 위
  `content_review`(기계적) + 세션 직접 재검토(판단형)와 역할이 다르다 —
  이쪽은 새 topic 작성 시 수동 호출 없이도 매번 자동으로 걸린다.
- ⚠️ **`card_news_spec.json`의 `title` 배열 마지막 줄 = 카드뉴스 표지·영상
  오프닝 타이틀 화면에서 자동으로 떼어내는 "독립 라벨"이다**(2026-08-08,
  "썸네일 글 이상하게 나오는 현상... 짤려서 만들어지는애들이 많아" — 실측
  52개 topic/언어 조합에서 발견, `data/title_truncation_backlog.md` 참고).
  `lib/card_news.py`/`lib/rebuild_video.py`
  둘 다 `" ".join(title[:-1])`로 마지막 줄을 뺀 나머지만 훅으로 쓴다 —
  `title`을 그냥 "훅 문장 하나를 여러 줄로 나눈 것"으로만 쓰면 마지막 줄이
  통째로 잘려나가 미완성 문장으로 노출된다. 올바른 예:
  `["혈당 관리에 어려움이 있는", "분들 주목!", "돼지감자차 이야기"]`(마지막 줄이
  진짜 독립 라벨). `content_review.py`의 `check_title_truncation()`이 훅이
  문장부호로 안 끝나면 자동으로 잡아주지만(review_topic 호출 시 항상 실행),
  title을 작성하는 시점에 이미 "마지막 줄 = 짧은 독립 라벨"인지 스스로
  확인할 것.

## 글로벌 확장

⚠️ **언어 범위 축소(2026-08-05)**: 진행 언어는 한국어·영어·일본어·스페인어·
포르투갈어·러시아어 6개로 확정했었음. 그 외 언어(대만어·프랑스어·독일어·
베트남어·아랍어·벵골어·터키어·태국어·인도네시아어·힌디어)는 data/output
콘텐츠 전부 삭제, 관련 문서도 정리함.

⚠️ **2차 언어 범위 축소(2026-08-09, "트래픽 보니까 사이즈가 안나와서... 건강쪽은
축소해야겠다 볼륨자체를")**: 스페인어·포르투갈어·러시아어도 추가로 제외 —
**진행 언어는 한국어·영어·일본어 3개로 확정**. es/pt/ru의 data/output 콘텐츠
전부 삭제, `data/global_research_rules.md`·`typecast_voices_global.json`·
`global_channels.json`도 3개 언어(영/일, 한국어는 별도 트래킹)만 남기고 정리함.
이미 YouTube에 업로드된 es(50)/pt(56)/ru(59) 영상은 그대로 두고 신규 제작만
중단하기로 했었으나, ⚠️ **es/pt/ru 계정 자체가 이후 전부 삭제됨(2026-08-11
확인)** — YouTube·Instagram 모두 사용자가 직접 삭제, 콘텐츠 신규 제작뿐
아니라 그 3개 채널로의 배포 자체가 완전히 종료됨. 실측 증상: 해당 3개
언어의 YouTube `_get_credentials()`가 `invalid_grant: Account has been
deleted`로 실패(리프레시 토큰 만료의 `Token has been expired or revoked`와는
다른 에러라 구분됨) — 이 에러가 뜨면 재인증이 아니라 애초에 그 언어가
더 이상 운영 대상이 아니라는 뜻이니 재발급 시도하지 말 것.

⚠️ **3차 언어 범위 축소 — 영상/숏츠 트랙 en/ja 완전 폐기(2026-08-21,
"유튜브 채널도 다르게 바꿀테니 걍 콘텐츠 다내려")**: **영상 트랙은 이제
한국어 하나만 — en/ja 신규 제작도, 기존 유지보수도 전부 종료.**
- `data/*/en`·`data/*/ja`·`output/*/en`·`output/*/ja` 로컬 콘텐츠 전량
  삭제 완료(158개 topic씩, blog_seo 서브트랙이 쓰는 de/fr/it/es/nl/sv는
  영상과 무관한 별도 트랙이라 대상에서 제외 — 아래 "블로그 SEO 서브트랙"
  절 참고).
- ⚠️ **en 채널이 jp-review-shorts와 공유 중인 "Vernhaven" 채널로 확인됨**
  (`YOUTUBE_EN_CLIENT_ID` 해시 일치) — 채널 통째로 삭제하지 않고 Supabase
  `youtube_uploaded` 기록을 실제 영상 제목/본문과 매칭해서 health-shorts
  소유 영상만 골라 삭제(81개 중 60개 삭제, 21개는 업로드 후 캡션이 많이
  바뀌어 안전하게 매칭 안 돼 미처리로 남음). **다른 프로젝트와 채널을
  공유 중이면 "채널 영상 전체 삭제" 같은 일괄 작업을 절대 하지 말 것** —
  항상 이 프로젝트가 실제로 올린 영상인지 개별 확인 후 삭제.
- ja(`@vernhaven_ja`)는 jp-review-shorts와 공유하지 않는 전용 채널로 확인됨
  (jp-review-shorts `.env`에 JA 접두사 자격증명 자체가 없음) — 그래도 en과
  동일하게 제목/본문 매칭 방식으로 89개 중 62개 삭제, 27개는 매칭 안 돼
  미처리. ⚠️ **`.env`의 `YOUTUBE_JA_CLIENT_ID`/`_SECRET`이 통째로 비어있던
  상태였음(2026-08-21 발견, refresh_token 만료가 아니라 원천 OAuth 클라이언트
  자체가 없었음)** — Google Cloud Console `worthitshopping-ja` 프로젝트에서
  클라이언트를 새로 발급받아 해결. 이후 다른 언어 채널에서도 같은 증상(만료가
  아니라 client_id/secret 자체가 빈 값)이 나오면 재인증이 아니라 Console에서
  클라이언트 재발급부터 해야 한다는 뜻.
- 미처리 48개(en 21 + ja 27)는 이 커밋 히스토리에 topic 목록이 남아있음 —
  필요하면 YouTube Studio에서 채널별로 직접 찾아 지울 것, 자동 재매칭
  재시도는 안 함(잘못 지울 위험이 이득보다 큼).
- ⚠️ **정정(2026-09-12): "Fish Audio는 en/ja 전용, 사실상 미사용"은 사실이 아니었다.**
  한국어 나레이션도 Fish Audio로 생성돼 왔고, `_random_voice_name()`이 **topic마다
  보이스를 난수로 골라서** 채널 목소리가 제각각이 됐다 — 기존 영상 18편 표본 실측에서
  남성 11 / 여성 7, F0가 91~258Hz로 흩어져 있었다("애새끼 목소리도 있고 난장판"). 
- 새 topic·언어는 한국어 하나로만 진행할 것 — 확장이 다시 필요해지면
  git 히스토리에서 복원.

- **번역 금지 — 언어마다 독립 리서치**(원인·해결책·이미지 전부 그 지역
  원문 자료 기준). topic(다룰 주제) 자체는 공통, 실질 콘텐츠만 독립.
- ⚠️ **아마존/알리익스프레스 제휴 리서치는 es/pt/ru 포함해서 이미 해뒀었음**
  (archive "글로벌 제휴 준비 리서치" 절) — es/pt/ru가 제외됐으니 실제 착수는
  한국어·영어·일본어만 해당.
- **글로벌 topic은 카드뉴스 없이 숏츠만** — `card_news.py generate()` 생략.
- **폴더 구조**: `data/<주제>/<lang>/`, `output/<주제>/<lang>/` 중첩(기존
  한국어 단일 topic은 flat 구조 그대로 유지, 소급 이전 안 함).
  `platform_captions.json`의 `"topic"` 필드는 `<주제>_<lang>` 형식으로 언어
  접미사 필수(localStorage 키 충돌 방지).
- 언어별 리서치 체크리스트는 `data/global_research_rules.md`(1차 타겟 국가,
  광고법·종교적 금기 식품 등) — 작성 전 필독. TTS 보이스는
  `data/fish_audio_voices_en.json`/`_ja.json`, 채널 인프라 상태는
  `data/global_channels.json`(언어별 `instagram_url` 포함 — Instagram Reels
  platform_captions.json의 `url` 필드는 항상 이 값으로 채울 것, 한국어 계정
  URL을 재사용하지 말 것).
- 폰트: 라틴 문자권은 `NotoSans-Bold.ttf`, 스크립트별 별도 폰트
  (`NotoSansArabic/Bengali/Devanagari/Thai/JP/TC-Bold.ttf`) 이미 연결 완료 —
  `_chalk_font_for_lang(lang)`/`_title_font_for_lang(lang)` 사용.
- 칠판 명패 풀은 14개 언어 문화별로 리서치 반영됨(`_NAMEPLATE_POOL_BY_LANG`).
- 유튜브: 채널당 별도 GCP 프로젝트, 계정은 한국+신규 4개 그룹으로 분리.
  발행 페이스는 `CLAUDE_ARCHIVE.md`의 "유튜브 쇼츠 자동 업로드" 절 참고(2026-08-31 이관). 상세 근거는
  archive의 "유튜브 정책 리스크 대응" 절.

### 블로그 SEO 서브트랙 (9개 언어 — 한국어 포함, 2026-09-03 정정)

위 규칙(6개→3개 언어 축소, "글로벌 topic은 카드뉴스 없이 숏츠만" 등)은
**영상/숏츠 트랙 얘기다 — 블로그 서브트랙은 정반대**: 영상·TTS 없이
카드뉴스 이미지 + 블로그 본문만, 대상 언어는 **ko/en/ja/de/fr/it/es/nl/sv
9개**. 위 언어 축소 결정(트래픽 부족)은 유튜브·인스타 같은 소셜 채널
얘기라 이 서브트랙엔 적용 안 됨 — 구글 오가닉 서치는 계정·팔로워 없이도
인덱싱되면 트래픽이 생기는 별개 채널이라서다.

⚠️ **한국어를 빼고 "8개 언어"라고 적어둔 게 오래 남아 여러 세션이 반복해서
"ko는 대상이 아니다"라고 잘못 판단했다(2026-09-03 정정).** 실제 지시는 처음부터
"한국도 같이 넣되 네이버 블로그와 내용이 겹치지 않게 구성"이었다. 그 결과 ko만
글이 절반이고(96편 vs 다른 언어 212~253편) 분량도 절반이라(1,086자 vs 일본어
1,876자) /ko 페이지에 "2분 읽기"짜리 얇은 글만 몇 개 뜨는 상태가 됐다.

**ko는 네이버 캡션과 같은 글이면 안 된다 — 같은 topic을 다른 각도로 쓴다.**
같은 사람이 두 곳을 다 봤을 때 중복으로 느끼지 않아야 하고, 검색엔진 입장에서도
서로 다른 페이지여야 한다. 겹침은 `python3 -m lib.ko_overlap_audit`로 잰다
(8자 단위 shingle). 실측 중앙값은 9%로 대체로 무난했지만 피부_1이 76%로 사실상
같은 글이었다 — 20%를 넘으면 다시 쓸 것.

- **산출물**: `data/<topic>/<lang>/card_news_spec.json` +
  `platform_captions.json`(`blog_seo` 플랫폼 항목, 필드는 seo-blog
  `CLAUDE.md` "Track B 데이터 계약" 절 그대로) — narration.txt·TTS·영상
  없음("카드뉴스만" 트랙 규칙 그대로 적용).
- **리서치**: 토픽 자체는 기존 로스터(`data/<카테고리>_<N>`) 재사용,
  콘텐츠는 언어마다 독립 리서치(번역 금지 원칙 동일 적용) — 언어별 세부
  가이드는 `data/global_research_rules.md`(de/fr/it/es/nl/sv 6개 섹션 추가됨).
  - ⚠️ **"독립 리서치"는 "매번 백지에서 리서치"를 뜻하지 않는다(2026-08-15
    명확화, 토큰 소모 절감 목적)** — 같은 topic의 기존 언어(대부분 ko)
    `platform_captions.json` 캡션엔 이미 검증된 원인/기전 주장(예: "실내외
    온도차 5도 초과 시 자율신경 부담")이 들어있다. 새 언어 착수 전에 그
    캡션을 먼저 읽고 **"이미 나온 주장 후보 목록"**으로 삼아, WebSearch를
    "이 주제 전체를 처음부터 탐색"이 아니라 "이 구체적 주장을 뒷받침하는
    그 언어권 원문 자료가 있는지" 타겟 검색으로 좁힐 것 — 그래도 그 언어
    소스로 검증은 반드시 하고(못 찾으면 그 주장은 빼거나 다른 걸로 교체),
    `body_html` 문장 자체는 여전히 새로 쓴다(원문 캡션 문장을 그대로
    옮기거나 기계번역하지 않음 — 이 부분이 "번역 금지" 원칙이 실제로
    막는 지점). 순수 현지 전용 파트(H2 "실제로 도움되는 것" — 현지에서
    구할 수 있는 해결책)는 원래부터 공통 소재가 없으므로 이 절이
    적용되지 않고 계속 처음부터 리서치한다.
- **본문(`body_html`) 형식**: `narration.txt` 재활용 아님 — 별도 장르.
  도입 1문단 → H2 "원인/기전" 1~2개 → H2 "실제로 도움되는 것"(현지에서
  구할 수 있는 해결책) → 선택적 FAQ/요약, 전부 시맨틱 `<h2>`/`<p>`/`<ul>`
  (seo-blog가 body_html을 마크다운 변환 없이 그대로 렌더링하므로).
  본문 안에 `<img>` 최소 1장 직접 삽입할 것 — seo-blog 페이지가
  `hero_image_url`을 본문에 시각적으로 렌더링하지 않기 때문(OG/JSON-LD
  전용).
- ⚠️ **핵심 문구는 `<strong>`으로 감쌀 것(2026-08-19, "너무 줄줄줄 적혀있어서
  잘 읽히지가 않아... 중요한 부분에는 강조를" 피드백)** — seo-blog 쪽
  CSS가 `<strong>`에 색상 대비까지 주도록 바뀌어서, 이제 본문에 강조
  태그가 얼마나 있는지가 실제 가독성에 직결된다. 문단마다 핵심
  수치·결론·행동지침(예: "하루 2리터", "금연이 유일한 방법입니다") 중
  1개 안팎을 `<strong>`으로 감쌀 것 — 문장 전체나 접속사까지 감싸지 말고
  진짜 핵심 어구만, 한 문단에 여러 개 몰아넣지 말 것(전부 강조되면
  아무것도 강조 안 되는 것과 같음).
- ⚠️ **수치에는 출처와 "조건"을 함께 쓸 것(2026-08-28, YMYL 전수감사)** —
  건강 콘텐츠는 구글 품질 평가상 YMYL이라 가장 엄격한 잣대가 적용된다.
  실측 사례: AREDS2 시험 결과를 "루테인·제아잔틴이 AMD 진행 위험을 약 26%
  낮췄다"로 썼는데, 실제 18%는 **베타카로틴 포함 제형 대비**이고 25%는
  **식이 섭취가 낮았던 하위집단**에서만 나온 수치였다 — 조건을 떼면 일반
  효과처럼 읽히고 독자는 그걸 보고 보충제를 산다. 비교 대상·하위집단·기간
  중 그 수치가 성립하는 조건을 반드시 함께 쓰고, 못 쓰겠으면 그 수치를
  빼는 쪽이 맞다.
  - `python3 -m lib.content_review <topic> <lang>`의 `check_unsourced_claims`가
    "수치는 있는데 그 문장·앞 문장에 출처 신호가 없는" 자리를 기계적으로
    뽑아준다(틀렸다는 뜻이 아니라 확인이 필요한 자리라는 뜻). **조건 누락은
    기계가 못 잡으니** `MANUAL_REVIEW_CHECKLIST` 7-1을 직접 확인할 것.
  - 관찰연구(코호트·단면)는 "~와 연관됐다", 무작위 개입시험만 "~를 낮췄다"로
    쓴다(체크리스트 7-2).
  - ⚠️ 검증 시 `pubmed.ncbi.nlm.nih.gov`는 WebFetch가 쿠키 벽에 막혀 본문을
    못 읽는다 — `europepmc.org`·저널 사이트·각국 공식기관 페이지를 쓸 것.
    **막힌 페이지를 받았을 때 내용을 지어내지 말 것**(이 워크스페이스에서
    실제로 있었던 사고).
- **제목·메타디스크립션**: 같은 topic·언어라도 유튜브 제목/카드뉴스 표지
  문구와 다른 문패턴을 쓸 것 — `lib/content_review.py`의
  `select_title_archetype(topic, lang)`이 `select_hook_pattern`(영상 훅
  패턴)과 동일한 원리로 title 아키타입(질문형/숫자·리스트형/원인지목형/
  통념반박형/비교형) 중 하나를 topic+lang 합산 시드로 고정 선택한다.
  `meta_description`은 title 요약이 아니라 별도 작성.
  - ⚠️ **본문 작성 전 반드시 확인할 것(2026-08-19 신설)** — 제목 쓰기 전
    `python3 -m lib.content_review --title-archetype <topic> <lang>`, 클로징
    쓰기 전 `python3 -m lib.content_review --closing-archetype <topic> <lang>`
    로 나온 아키타입/전략대로 작성할 것. H2 "실제 해결책" 섹션 헤더도
    `select_section_header_archetype(topic, lang, "actual_fix")`(CLI 없음,
    직접 import해서 호출)로 확인할 것. 2026-08-19 감사에서 16개 topic 중
    94%가 동일 H2 헤더("What Actually Helps"), 75%가 동일 클로징 오프너
    ("None of this means...")로 수렴한 게 발견돼 이 장치를 새로 도입했다 —
    "title 아키타입을 시드로 고정 선택한다"는 문장은 예전부터 이 문서에
    있었지만 실제로는 `select_title_archetype` 자체가 구현돼 있지 않았다
    (문서만 있고 코드가 없던 상태).
- ⚠️ **완료 기준 = 8개 언어 `blog_seo` 전부** — "ko만 만들고 끝났다고
  보고하지 말 것" 원칙(위 "세션 운영" 절)을 8개 언어로 확장 적용, 일부
  언어만 만들고 완료 보고하는 사고 방지.
- **이미지 호스팅(2026-08-13 해결)**: `output/`는 health-shorts 자체 Vercel
  배포에서 Basic Auth로 잠겨있어 크롤러가 못 읽지만, seo-blog 인입 스크립트
  (`scripts/ingest_health_shorts.py`)가 `--commit` 시 카드뉴스 커버를
  Supabase Storage 퍼블릭 버킷(`blog-images`)에 자동 업로드하고
  `hero_image_url`·본문 `<img src>`의 `PENDING_SUPABASE_STORAGE_UPLOAD`
  플레이스홀더를 실제 URL로 치환해준다 — health-shorts 쪽에서는 계속
  플레이스홀더 그대로 작성만 하면 됨, 추가 조치 불필요.

## 대시보드 (`lib/dashboard.py`)

- `generate(spec_path, card_news_dir, video_path, out_path)` — 플랫폼별 카드
  HTML 생성, `output/topics.json` 자동 갱신. 유튜브 쇼츠/틱톡은
  `_UI_EXCLUDED_PLATFORMS`로 카드 자체가 안 뜬다(업로드 자동화됨/제외 결정).
- ⚠️ **플랫폼 카드는 단일 그리드, type별 섹션·"🎬 숏츠"/"🗞 카드뉴스" 탭
  분리 없음(2026-08-13)** — 예전엔 영상/카드뉴스/텍스트 type마다 별도
  `<section>`으로 나누고, 영상 그룹과 텍스트 그룹이 둘 다 있으면 탭 버튼으로도
  나눴었다. 티스토리 제거로 텍스트 그룹이 네이버 블로그 하나만 남으면서
  분리할 실익이 없어져 전부 하나의 "업로드 플랫폼" 그리드로 합쳤다(카드별
  type 배지는 유지, `TYPE_ORDER`로 video→cards→text 순서만 보장). 새 플랫폼
  type을 추가해도 섹션을 따로 만들 필요 없음 — `platforms_by_type`에 담아
  같은 루프에서 이어붙이면 된다.
- 다국어 topic은 `output/<topic>/dashboard.html`(통합 탭 페이지)이 언어별
  `output/<topic>/<lang>/dashboard.html`을 iframe으로 보여준다 — 영상 없는
  언어는 경량 폴백 카드(`_light_platform_card`).
- 완료 기록: `output/completed_topics.json`(topic 전체), 유튜브는
  `output/youtube_uploaded.json`, 상품 링크는 `output/product_links.json`
  (쿠팡)·`output/naver_product_links.json`(네이버 커넥트), 포스팅 기록은
  `output/posting_log.csv`(브라우저 CSV 내보내기 → 커밋).
  - ⚠️ **입력값 "-"는 링크 없음과 동일 취급(2026-08-17)** — 캡션에
    "상품명 구매: -"가 그대로 노출된 사고. `lib/dashboard.py`의
    `_isRealLink()`가 저장/로드/캡션삽입 전 구간에서 빈 문자열과 "-"를
    똑같이 "링크 없음"으로 처리한다 — 기존에 이미 "-"로 잘못 저장된
    값도 DB 수정 없이 다음 로드부터 자동으로 정상 처리됨.
  - ⚠️ **상품 링크는 Supabase 직접 연동(2026-08-08, CSV 왕복 폐기)**:
    개별 topic 대시보드의 `.product-link-input`은 이제 Supabase
    `product_links`(topic·market·product·url) 테이블에 직접 upsert/delete —
    브라우저 localStorage·CSV 내보내기 안 씀. **상품명 하나로 여러 topic에
    공용 재사용되는 캐시**는 별도 `global_product_links`(market·product·url,
    topic 없음) 테이블 — `index.html`의 "🔗 미등록 상품 링크" 섹션이
    `output/all_products.json`(전체 topic이 실제 쓰는 상품명 목록,
    `lib/dashboard.py`의 `_write_all_products()`가 매 대시보드 생성마다
    자동 갱신)과 이 테이블을 대조해서 링크 없는 상품만 보여주고, 입력하면
    바로 이 테이블에 저장된다. `output/product_links.json`/
    `naver_product_links.json`(git 추적, `_load_product_links()`가 대시보드
    생성 시 읽어 입력창을 미리 채움)은 이 테이블과 별개인 로컬 스냅샷 —
    `scripts/supabase_backfill.py`는 로컬 json → Supabase 1회 백필용(반대
    방향, Supabase에 새로 채운 값이 이 로컬 json에 자동으로 안 내려옴 —
    두 값이 어긋날 수 있다는 뜻, 필요 시 수동 확인).
- 이슈는 `lib/mission_control_log.py`의 `report_issue()`로 mission-control
  (`../mission-control`)에도 보고됨(2026-08-15, `check_video_staleness.py`/
  `youtube_upload.py` 연동 — `MISSION_CONTROL_INGEST_URL`/`_SECRET` 미설정
  시 조용히 스킵).

## 🚨 카드뉴스 이미지 저장소 — Cloudflare R2 (2026-09-13 전환)

**카드뉴스 jpg는 R2(`https://img.vernhaven.com/health-shorts/card_news/`)에서 서빙한다.**
7,222장 1.14GB가 git·Vercel 배포에 그대로 실려 있었고(2026-08-08엔 609장 79.6MB였던 게
14배가 됨), R2는 **egress가 무료**라 CDN에서 받는 편이 싸고 배포도 가벼워진다.
블로그 4개 사이트가 쓰는 버킷과 같은 버킷이다(`verticals/.env.r2`, 프리픽스로 분리).

| 키 규칙 | 로컬 경로 |
|---|---|
| `health-shorts/card_news/<topic>/<파일명>` | `output/<topic>/card_news/` 및 `output/<topic>/ko/card_news/` |
| `health-shorts/card_news/<topic>/<lang>/<파일명>` | `output/<topic>/<lang>/card_news/` (ko 제외) |

> ko와 루트를 같은 키로 합치는 이유: 둘을 동시에 가진 topic이 0개라 충돌하지 않고,
> 대시보드가 topic만 알면 URL을 만들 수 있다.

- **업로드는 자동이다** — `lib/card_news.py`가 렌더 직후 `_sync_to_r2()`로 올린다.
  따로 실행할 명령이 없다. 실패하면 stderr에 경고만 나오고 렌더는 성공 처리되므로,
  **경고가 보이면 그 topic은 배포본에서 이미지가 안 뜬다** — 다시 렌더하거나 수동 업로드할 것.
- URL을 만드는 정본은 `lib/dashboard.py._card_news_r2_base()` 하나다. 키 규칙을 바꾸려면
  이 함수와 `card_news.py._sync_to_r2()`를 **같이** 고쳐야 한다.
- 대시보드의 "카드 이미지 전체 다운로드"는 `<a download>`가 아니라 **fetch+blob**이다 —
  R2가 다른 오리진이라 download 속성이 무시되기 때문(버킷에 GET CORS를 열어뒀다).
- ⚠️ `_sync_to_r2()`는 **저장소 `output/` 밖으로 렌더하면 업로드를 건너뛴다.** 안 그러면
  pytest가 tmp_path로 `generate()`를 부르는 것만으로 테스트 픽스처가 실제 버킷에
  올라간다(2026-09-13 실측, 13장 업로드됨 — 삭제 완료). **외부 저장소에 쓰는 코드를
  렌더 함수 안에 붙일 땐 테스트 경로에서도 실행된다는 걸 먼저 확인할 것.**

## 카드뉴스 허브 (`lib/card_news_hub.py`)

⚠️ **UI 진입점 제거(2026-08-21, AI영상 채널 피벗)** — `index.html`의
육아·반려동물/경제 탭 버튼을 뺐다(SEO 블로그 9개 언어 파이프라인은
완전히 별개라 무관, 그쪽은 그대로 운영). 이 워크플로우(babbleroot/
furrowly/sparelow 네이버 블로그 포스팅)를 더 이상 안 하기로 한 결정 —
아래 스크립트·`output/card_news_hub.json`·vernhaven-blog `/admin?tab=naver`
연동 자체는 코드 그대로 남아있다(재개 가능성 대비 완전 삭제 아님), 그냥
새로 실행/커밋할 이유가 없어졌을 뿐.

네이버 블로그 운영용 — 이 프로젝트가 못 다루는 3개 신규 버티컬(육아=
babbleroot/반려동물=furrowly/경제=sparelow, `VERTICAL_REPOS` 상수)의
콘텐츠 저장소(`<이름>-content`)를 스캔해 `output/card_news_hub.json`으로
합친다(건강 자신과 일본상품리뷰는 각자 완전한 자체 대시보드가 있어 제외 —
파일 상단 주석 참고). `index.html`의 "카드뉴스 허브" 탭이 로컬에서 이
JSON을 읽어 캡션 복사·네이버 계정 열기 UI를 보여준다.

```
python3 -m lib.card_news_hub          # output/card_news_hub.json만 갱신(로컬)
python3 -m lib.card_news_hub --commit # 위에 더해 공유 Supabase에도 push
```

⚠️ **`--commit`은 vernhaven-blog 등 6개 형제 블로그가 쓰는 별도 공유
Supabase 프로젝트(`BLOG_NETWORK_SUPABASE_URL`/`_SERVICE_ROLE_KEY`, `.env` —
이 프로젝트 자신의 `SUPABASE_*`와는 다른 프로젝트)에 `naver_card_news`
테이블로 upsert한다(2026-08-16, "카드뉴스 허브를 vernhaven admin 페이지
하나에 탭으로 진짜 통합하고 싶다" 요청)** — vernhaven-blog `/admin?tab=naver`가
이 테이블을 읽어서 배포 환경에서도 같은 데이터를 캡션 복사·업로드완료
토글과 함께 보여준다(스키마·상세는 `vernhaven-blog/CLAUDE.md` "관리자
대시보드 `/admin`" 절 "네이버 카드뉴스 탭" 참고, 원본은 그쪽 문서). 이
프로젝트 쪽에서 신경 쓸 건 하나뿐 — **`posted`(업로드 완료 여부) 컬럼은
`push_to_supabase()`가 의도적으로 payload에서 뺀다**, 그래야 이 스크립트를
재실행해도 admin에서 사람이 이미 체크해둔 업로드 완료 상태가 안 지워진다.
새 topic을 만들거나 캡션·네이버 URL을 고친 뒤엔 `--commit`을 다시 돌려야
admin 쪽에 반영된다(자동 트리거 없음, 수동 재실행 필요).

## 배포 플랫폼 — Vercel (⚠️ 2026-08-14 정정, 예전 "GitHub Pages/서버 로직
불가" 서술은 낡은 정보)

`index.html`은 정적 에디터이고 실제 콘텐츠 생성(PIL/TTS/ffmpeg)은 전부 로컬
세션에서 하는 게 맞지만, **배포 자체는 GitHub Pages가 아니라 Vercel**이고
`middleware.js`(Edge Middleware, Basic Auth 게이트)가 이미 실제 서버 로직으로
돌아가고 있다 — "서버 로직 불가"는 틀린 전제였다. `/api/*.js`(Node 서버리스
함수, service_role 키로 Supabase 쓰기 전담 — 아래 "Supabase 쓰기 경로" 절)도
같은 이유로 추가 가능했다. `middleware.js`의 matcher가 `/(.*)`(전체 경로)라
`/api/*`도 자동으로 같은 Basic Auth 게이트 안에 들어간다 — 새 API 함수에
인증을 따로 구현할 필요 없음.

### Supabase 쓰기 경로 — anon 직접 쓰기 폐기(2026-08-14)

⚠️ **`index.html`/`lib/dashboard.py`의 JS가 `sb.from(...).upsert()/delete()`로
anon key를 써서 직접 쓰던 걸 전부 `/api/*.js` 서버 함수(service_role 키) 경유로
바꿨다** — 이유: 이 프로젝트 Supabase를 vernhaven-blog 계열(공유 Supabase,
퍼블릭 사이트)과 나눠 쓰게 되면서 anon key가 그쪽 브라우저 번들에도 그대로
노출된다. RLS가 "anon 전체 허용"이면 그 노출된 키로 아무나 이 관리 테이블
(topics/completed_topics/posting_log/product_links/global_product_links/
youtube_uploaded)을 읽고 쓰고 지울 수 있었다 — Vercel Basic Auth는
`index.html` 페이지 자체만 가리지 Supabase REST API를 직접 두드리는 건 못
막는다.

- **읽기(select)는 그대로 anon key + `sb.from()`** — 안전(RLS `anon read`
  정책, `supabase/schema.sql`).
- **쓰기(insert/upsert/delete)는 `/api/posting-log.js`,
  `/api/completed-topics.js`, `/api/global-product-links.js`,
  `/api/product-links.js`로 이동** — `api/_supabase.js`가 공용 헬퍼(service_role
  키로 PostgREST 직접 호출, SDK 의존성 추가 안 함 — 이 프로젝트는 빌드 스텝이
  없는 정적 배포라 package.json/node_modules 자체가 없어서 fetch만 씀).
- **`topics` 테이블 쓰기는 원래도 안전했음** — Python 스크립트가 서버 측에서
  service_role 키로 upsert하는 구조라(위 "Supabase `topics` upsert" 절)
  클라이언트 anon 쓰기가 애초에 없었다, 이번 변경 대상 아님.
- Vercel 프로젝트 env var에 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` 추가
  필요(로컬 스크립트용 `.env`에는 이미 있었지만 Vercel엔 `HS_ADMIN_PASSWORD`만
  있었음) — 이 함수들이 실행되는 서버 환경에서 읽는다.

### GitHub Pages 비활성화(2026-08-15) — 절대 다시 켜지 말 것

⚠️ 이 저장소는 GitHub Pages도 동시에 켜져 있었다(`main` 브랜치 루트를
그대로 정적 서빙, `choijaaaaaa.github.io/health/`) — Vercel과 완전히
별개의 무료 배포 경로라 존재를 잊기 쉽다. 두 가지 문제가 겹쳐서
비활성화했다:

1. **인증 완전 우회**: `middleware.js`(Basic Auth)는 Vercel Edge
   Middleware라 Vercel 배포에만 적용된다 — GitHub Pages는 순수 정적
   호스팅이라 이 게이트가 아예 안 걸린다. 비밀번호 없이 미공개 topic
   목록·상품 링크가 그대로 보이는 상태로 몇 시간~며칠 방치됐을 수 있다.
2. **위 "Supabase 쓰기 경로" 변경과 충돌**: 체크박스 등 쓰기 동작이
   `/api/*.js` 서버 함수를 호출하는데, GitHub Pages는 서버 함수 자체를
   지원하지 않아 그 요청이 전부 404로 조용히 실패 — 사용자 입장에선
   "체크해도 저장이 안 됨"으로 보였다.

`gh api repos/choijaaaaaa/health/pages -X DELETE`로 비활성화 완료.
**앞으로 대시보드는 항상 Vercel URL(`health-shorts.vercel.app`)로만
접속할 것** — 북마크·자동화 스크립트에 github.io 링크가 남아있으면
갱신할 것. Pages를 다시 켜려면(Settings → Pages) 반드시 이 두 문제를
먼저 해결한 뒤에만 — 지금 상태로 재활성화하면 두 문제 다 즉시 재발한다.

## 테스트

```bash
source .venv/bin/activate
python3 -m pytest tests/ -v
```

- `test_content_rules.py`는 `data/*/`의 모든 topic을 스캔해서 이 문서 규칙
  위반을 자동으로 잡는다(해시태그, 필수 플래그, 금지 표현, `char_file` 실존
  등) — **topic 작업 후 항상 이 테스트부터 돌릴 것.**
- Gemini/Kling/Typecast 등 유료 API는 테스트에서 절대 호출 안 함
  (`tests/conftest.py`의 합성 픽스처만 사용).

## 자료 위치

| 종류 | 위치 |
|------|------|
| 재사용 에셋 | `assets_library/{illust,real,motion,music}/` |
| 주제별 데이터 | `data/<주제>/` 또는 `data/<주제>/<lang>/` |
| 주제별 산출물 | `output/<주제>/` 또는 `output/<주제>/<lang>/` |
| 라이브러리 코드 | `lib/*.py` |
| 채널별 URL | `data/social_accounts.json` — 새 topic은 여기서 복사(예전 topic 복사 금지) |
| 비밀키 | `.env`(커밋 안 함), `.env.example` 최신 유지 |

## 대용량 미디어는 git에 안 올림 (2026-08-05)

레포 용량 문제로 아래는 **로컬에만 유지하고 git엔 커밋하지 않는다**
(`.gitignore`에 이미 반영 — `git add`해도 자동으로 무시됨):

- `assets_library/{real,motion,music}/` 전부
- `output/**/*.mp4`(원본·인스타그램 안전여백 버전 둘 다)

⚠️ **`assets_library/illust/`는 예외 — 2026-08-18부로 git 추적**(위 "에셋
라이브러리" 절 재발 방지 대책 참고) — 파일당 수십 KB급이라 용량 부담이
없고, 삭제 사고를 실제로 겪어본 뒤 git을 유일한 백업 경로로 삼기로 함.
새 캐릭터 일러스트는 만드는 즉시 커밋할 것.

⚠️ **카드뉴스 이미지는 2026-09-13부로 Cloudflare R2에서 서빙한다** — git에는
아직 추적되지만(히스토리에 이미 있어 빼도 용량이 안 줄고, 워크트리 병합 시 로컬
파일이 삭제되는 사고 이력이 있어 추적 해제는 보류) **Vercel 배포에서는 제외**된다
(`.vercelignore`의 `output/**/card_news/`). 대시보드가 이미지를 R2 절대 URL로
가리키므로 배포본에 파일이 없어도 정상이다. 자세한 건 아래 "카드뉴스 이미지 저장소" 절.

영상·이미지 생성 코드는 그대로다 — 파일은 여전히 로컬에 정상적으로 만들어지고,
git이 추적만 안 할 뿐이다. `assets_library/{backgrounds,fonts,
channel_banners}/`는 코드가 직접 참조하는 공용 리소스라 계속 추적한다.

⚠️ **여러 워크트리를 쓸 때 이 파일들을 다른 워크트리로 병합하지 말 것** —
`git rm --cached`(추적만 해제, 로컬 파일 보존)로 만든 커밋을 다른 워크트리에
`git merge`로 반영하면, 그 워크트리 디스크에 있던 실제 파일이 통째로
삭제된다(2026-08-05 실제 발생 — 상세는 archive "대용량 미디어 워크트리 병합
사고" 절). 이런 종류(파일은 남기고 추적만 빼는) 변경은 항상 메인 워크트리에서
직접 `git rm --cached` + commit + push로 끝낼 것 — 워크트리에서 만들어서
병합하지 말 것.

⚠️ 이미 커밋된 과거 버전은 git 히스토리에 그대로 남아있어서, 위 조치만으로는
레포의 실제 저장 용량이 안 줄어든다 — 앞으로의 증가만 막는다. 히스토리 자체를
줄이려면 `git filter-repo` + force-push가 필요한데, 여러 세션이 동시에 이
레포를 쓰고 있어서 그건 각 세션의 로컬 클론을 깨뜨리는 파괴적 작업이다 —
사용자 확인 없이 진행하지 말 것.

## 주의사항

- `bin/`(Rhubarb), `.venv/`는 gitignored.
- `.venv/` 새로 만들 때: `python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`
- 커밋 시 `.env` 포함 여부 항상 재확인.
- 로컬 확인은 `open output/<주제>/dashboard.html` — Pages 푸시는 모바일 업로드
  때만 필요, 매번 기다릴 필요 없음.
