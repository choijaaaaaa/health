# 숏폼 제작 방법론 — 확정본 (2026-09-24)

health-shorts에서 실제로 돌려보고 확정한 것들. **다른 채널(댕냥사전 등)이 그대로 가져다 쓰라고
따로 뺀 문서다.** 경로는 health-shorts 기준이니 자기 프로젝트에 맞게 바꾸되, **원리와 순서는
그대로 따르는 게 맞다** — 아래는 전부 "안 해서 사고가 났던 것"을 고친 결과다.

**14개 절 전부 확정이다. 미확정 항목은 없다.** 앞서 보류로 남겨뒀던 둘(8번 좌우 분할, 10번
카드 동기화)은 2026-09-24에 검증을 마치고 본문으로 옮겼다 — 8번은 ffmpeg 합성 결과를 프레임으로
확인했고, 10번은 자동 검사를 붙여 실제 사고 5건을 잡았다.


## 🚨 먼저 볼 것 — `XRAY_FORMAT.md`

**영상 포맷(화면 구성·시간표·검사)은 이제 `XRAY_FORMAT.md` 한 곳에 모았다.** 2026-09-24에
사용자가 `소화_14`를 보고 확정한 견본 기준이고, 육아 건강 채널·댕냥사전도 그 문서를 그대로
따른다. 이 문서(PIPELINE_METHOD)는 **원고를 어떻게 쓰는가**(검색어·공공데이터·구조)를 다룬다.

포맷 관련해서 아래 8·12·13·14절과 `XRAY_FORMAT.md`가 어긋나면 **`XRAY_FORMAT.md`가 이긴다** —
그쪽이 견본과 함께 갱신된다.

---

---

## 1. 원고는 검색어에서 출발한다 — 요소 채우기로는 안 된다

### 왜

"실행 가능한 수치 3개 · 통념 반박 · 기전 · 해결책 · 병원 신호 · 기관명" 6요소를 다 채운 원고
16편을 만들었는데 **"내용이 허전하다, 수준이 낮다"**는 평가를 받았다. 요소는 채웠지만 **사람들이
실제로 궁금해하는 걸 안 다뤘기** 때문이다.

실측 사례: 대상포진 원고를 썼는데 자동완성 **2·4위가 "전염성·감염"**인데도 전염 이야기가 한 줄도
없었다. 위고비 원고는 사람들이 **가격·부작용·나눠맞기**를 찾는데 근손실·요요만 말했다.

### 순서

```bash
# ① 사람들이 뭘 검색하는지 먼저 본다 (키 불필요)
python3 -m lib.naver_keywords "<검색어>"
python3 -m lib.naver_keywords "<검색어> 부작용"    # 갈래를 더 판다

# ② 그 표현들의 월간 검색량 (네이버 검색광고 API 키 필요)
python3 -m lib.naver_searchad "후보1" "후보2" ...
```

- **자동완성 상위가 곧 원고의 항목**이어야 한다. 지금 원고가 그걸 안 다루면 **각도를 통째로 바꾼다.**
- ⚠️ 검색광고 API는 **`~부작용` 복합어를 전부 0으로 돌려준다**(광고 집행 불가 키워드라 데이터를
  안 준다). 위고비 86,800인데 위고비부작용 0이다. **수요가 없다는 뜻이 아니다** — 자동완성으로 판단한다.
- ⚠️ 연관 키워드를 keywordstool에서 그대로 받지 말 것. **광고주 그룹 기준**이라 "족저근막염"을 던지면
  "대상포진"·"독감예방접종"이 딸려온다. **의미는 자동완성이, 숫자는 검색광고 API가** 준다.
- 검색량이 낮으면 **소재가 아니라 말을 의심**한다: 오표기(반월판→**반월상**연골파열 22배),
  학술어(헬리코박터파일로리→헬리코박터**균** 72배), 신조어(사회적시차→월요병 32배).

---

## 2. 수치는 공공데이터에서 캔다

`~/Desktop/project/public-data/`에 공공데이터포털 카탈로그 **8,766건**과 실제 CSV **5,894개(5.1GB)**가
받아져 있다. 건강보험심사평가원 진료 통계, 질병관리청 감염병 발생현황 같은 것들이다.

```bash
cd ~/Desktop/project/public-data
python3 find.py 실신 진료          # 여러 단어면 많이 맞을수록 상위
python3 find.py --head 15055567    # 그 데이터 앞 5줄
```

- 목록의 **`○`는 이미 받아둔 것**, `×`는 카탈로그에만 있는 것(링크 만료로 대개 실패). `○`부터 본다.
- **`건강보험심사평가원_병원급이상 진료과목별 상병 통계`(pk 15055567)** 가 가장 쓸모 있다 —
  상병코드별 환자수·진료비가 들어 있어 **직접 집계**할 수 있다.
  실제 사용례: 상병 R55(실신) → "응급실 5만 명", N18(만성 신장질환) → "내과 31만 명".
- 🚨 **행위코드만 있는 데이터는 쓰지 말 것.** `15089624`는 행위코드가 이름 없는 5자리 숫자
  9,907종뿐이라 위내시경을 특정할 수 없다. 코드 마스터 없이 쓰면 추측이 된다.
- 공공데이터에 없으면 **기관 원문을 `curl`로 받아 대조**한다. 🚨 WebFetch 요약은 원문에 없는 내용을
  지어낸 전례가 있다. 식약처 허가사항·질병관리청 국가건강정보포털·학회 진료지침이 1순위다.
  (실제 사용례: 위고비 허가사항에서 오심 43.9%·구토 24.5%·변비 지속 47일을 직접 읽었다.)
- ⚠️ 학회 지침은 **개정판을 확인**할 것. 헬리코박터 "표준 3제요법 14일"은 2025 개정에서 1차 권고가
  아니게 됐다(내성 33.3%, 제균률 66.9%).

---

## 3. 원고 구조 — 본보기 하나를 정해두고 그걸 따른다

16편 중 **`data/소화_14/narration.txt`(위경련) 하나만** "그나마 괜찮다"는 평가를 받았다. 차이는 셋뿐이었다.

| | 좋은 원고 | 허전한 원고 |
|---|---|---|
| 훅 | 증상을 **구체적으로 묘사**하고 질문으로 끝냄<br>"명치가 쥐어짜듯 아프고 식은땀까지 나신 적 있으신가요?" | 정보를 던짐<br>"물집보다 통증이 먼저 옵니다" |
| 통념 반박 | 훅 **직후 독립 문장**, 독자가 **실제로 하는 행동**을 뒤집음<br>"탄산음료로 트림 내는 분 많은데, 오히려 가스가 더 차서" | 항목 설명 속에 묻힘 |
| 항목 예고 | 이름을 **먼저 깐다**<br>"방아쇠는 스트레스, 빈속 커피, 술이에요" | 없음 — "이 세 가지" 하고 바로 "먼저" |
| 깊이 | 증상 **하나를 층위별로** 판다(위→뇌→언제 몰리나) — 임상은 "언제·얼마나·누구에게"까지 | 칸 채우려 **상관없는 항목**을 셋으로 맞춤, 통계 숫자만 던짐 |
| 숫자 | **계산까지** 해줌<br>"400mg인데 한 잔이 132mg이라 4잔이면 넘어감" | 기준치만 나열 |

핵심은 시작 10초 안에 **"어, 나 그거 하는데"** 하는 순간을 만드는 것이다. 그게 없으면 정보 나열로 읽힌다.

- 분량은 **62~85초**. 글자수 환산은 채널마다 다르니 9번을 볼 것(상수를 그대로 베끼면 안 된다).
- 훅은 검색어로 시작하고 **40자 가까이 채운다**(짧으면 도입 영상이 순식간에 지나간다).
- 🚫 유도 문구("저장하세요"), 헷지 어미 남발("~수 있어요"), **대체품 추천**(술→무알코올) 금지.

---

## 4. 클립은 색인으로 찾는다 — 이름을 믿지 않는다

### 왜

topic마다 에이전트가 프레임을 뽑아 보고 판단했는데, **같은 클립을 매번 다시 보면서 매번 다르게
판단하고 결과가 어디에도 안 남았다.** 그리고 **이름이 실제와 다른 클립이 30개**나 나왔다.

| 클립 이름 | 실제 |
|---|---|
| `act_check_skin`(피부) | **무릎**을 짚고 무릎 관절 점등 |
| `act_rub_leg`(다리 문지름) | **무릎**을 짚는 자세 |
| `part_elbow`(팔꿈치) | **무릎**이다(원본 스틸이 `cu_knee.jpg`) |
| `m_airway_blockage`(기도) | **식도**에 음식이 낌 |
| `act_hold_one_breast`(유방) | **기관지·폐** 점등, 게다가 3.8초에 다른 컷으로 하드컷 |
| `part_liver`(간) | 간은 안 켜지고 **담낭·담관**만 |
| `act_sleep_paralysis`(가위눌림) | **기도** 점등(수면무호흡용) |

### 구조

`lib/clip_index.py` + `assets_library/xray/clip_index.json`. 클립 하나하나를 프레임으로 직접 보고 적는다.

```json
"m_osmotic_water_pull": {
  "kind": "기전",
  "seen": "관 안쪽 벽을 따라 금색 알갱이가 줄지어 붙어 흐르고, 관은 열린 채 비어 있다",
  "fits": ["설사","묽은 변","장에 물이 몰림","당알코올 불편","탈수","다뇨"],
  "avoid": "이름 때문에 오해하기 쉬운 용도나 쓰면 안 되는 곳"
}
```

```bash
python3 -m lib.clip_index --for 설사 복통     # 증상어로 검색
python3 -m lib.clip_index --show <클립명>
python3 -m lib.clip_index --missing          # 아직 기술 안 된 것
```

- **`fits`가 핵심이다.** 한 클립은 여러 주제에 매핑된다 — `m_osmotic_water_pull`은 설사·부종·다뇨에
  다 쓰인다. 나중에 그 말로 검색해서 나와야 하니 **사람들이 쓸 법한 말**로 적는다.
- 검색 가중치는 **`fits` 3점 · `seen` 2점 · 이름 1점.** 이름이 함정인 적이 반복돼서 일부러 낮췄다.
- `seen`은 **해석이 아니라 묘사**다. 호박색으로 켜진 곳이 어디인지 반드시 적는다.
- 색인을 만들 땐 **4초 클립의 앞뒤가 많이 다르므로 여러 시점**(0.5/2.0/3.6초)을 뽑아 본다.
  앞뒤가 다른 장면인 클립이 실제로 여럿 있었다(`m_mineral_erosion`은 앞 2초와 뒤 2초가 완전히 다름).

---

## 5. 행위 클립은 동작이 디테일해야 한다

뭉뚱그린 동작은 어느 병에 붙여도 같아 보인다. `act_grab_belly`(배 움켜쥐기)는 위경련·장염·생리통·
과민성대장 어디에나 붙고, **그래서 어디에도 안 맞는다.**

요청 프롬프트에 반드시 적을 것:

| | 나쁜 예 | 좋은 예 |
|---|---|---|
| 어디를 어떻게 | "배를 만진다" | "명치를 주먹 관절로 누르고 몸을 앞으로 접는다" |
| **피하는 동작** | — | "옷깃이 닿지 않게 팔을 들어 올린 채 옆구리를 비운다" |
| 반복의 리듬 | "긁는다" | "세 번 빠르게 긁고 멈췄다가 다시 두 번" |
| 무게중심 | "서 있다" | "한쪽 다리에 체중을 싣고 반대쪽 골반이 내려간다" |

- 얼굴이 featureless라 **표정을 못 쓴다.** 통증·불편은 전부 몸으로 — 어깨가 올라가고, 숨이 멈추고,
  손이 가다 말고 멈칫하는 식.
- **동작 안에 증상이 보여야 한다.** 대상포진이면 띠 물집, 하지정맥류면 도드라진 핏줄.
- 증상이 없는 병(헬리코박터)은 **그 병 고유의 장면**을 잡는다 — 공용 냄비에 각자 숟가락을 담그는 동작.

### 없으면 요청하고 멈춘다

🚨 **비슷한 클립으로 때우지 않는다.** 화면과 나레이션이 어긋나는 사고가 반복됐다.
`data/<topic>/clip_requests.json`에 적고 그 topic은 조립을 멈춘다(검사가 미수령 요청을 실패로 잡는다).

---

## 6. 부위 클립은 `--region` 없이 만들지 않는다

스틸에서 코드로 만드는 부위 점등 클립은 **region을 안 주면 피사체 전체가 호박색**이 된다.
신장 이야기를 하는데 온몸이 노랗고, 자궁 이야기에 간·신장까지 켜진다.

실측으로 4종을 다시 만들었다: `part_kidney` `0.32,0.26,0.36,0.14` · `part_uterus_ovary`
`0.26,0.40,0.48,0.22` · `part_bladder` `0.30,0.50,0.40,0.16` · `part_thyroid` `0.36,0.56,0.28,0.12`.

전신·몸통 스틸은 region 필수, 장기 클로즈업 스틸(손톱·입술·눈꺼풀)은 생략 가능.

---

## 7. 검수는 라벨이 아니라 프레임으로

조립이 끝나면 **여러 시점의 프레임을 뽑아 컨택트시트로 붙여 직접 본다.** 파일명과 로그만 보면
"신장 이야기인데 온몸이 노란" 영상이 그대로 나간다(실제로 deploy까지 올라갔다).

```bash
for t in 0.1 3 15 35 50 65; do
  ffmpeg -y -v error -ss $t -i <영상> -frames:v 1 -vf scale=270:-1 /tmp/f$t.png
done
ffmpeg -y -v error -i /tmp/f0.1.png ... -filter_complex "...hstack=3..." /tmp/sheet.png
```

---

## 8. 위쪽 영상 칸은 좌우로 나눈다 — 왼쪽 행위 · 오른쪽 기전

### 왜

기전만 크게 띄우면 "몸 안에서 무슨 일이 벌어지는지"는 보이는데 **그게 어떤 행동 때문인지**가
안 보인다. 둘을 나란히 놓아야 "이 행동을 하면 → 몸이 이렇게 된다"가 한 화면에서 읽힌다.

### 구조

칸 하나가 960×680(가로)이다. **구분선 8px을 가운데 두고 좌우 476씩** 쓴다.

```
[ 이 행동이 ]            │            [ 몸 안에선 ]
   행위 클립 476         │8            기전 클립 476
```

🚨 **클립을 자르지 않는다.** 세로 클립(720×1280)을 칸 높이 680에 맞추면 폭이 382라 반칸에 넉넉히
들어가고, 남는 좌우는 **클립 배경색으로 메운다**(클립 네 귀퉁이를 찍어 재면 된다). 폭을 먼저 맞추고
넘치는 높이를 잘라내면 전신 클립에서 **발이 잘려 동작의 무게중심이 안 보인다.**

구분선이 8px인 이유: **반칸 폭이 짝수로 떨어져야 한다.** 6px이면 477이 홀수라 libx264가 폭을 짝수로
맞추면서 결과가 958px로 2px 모자랐다(실측).

구분선과 라벨을 두는 이유: 배경색이 같은 두 장면이 맞닿으면 한 화면으로 읽혀 **좌우가 다른 이야기라는
게 안 보인다.** 구분선만으로는 "다른 장면"까지만 전달되므로, 어느 쪽이 원인이고 어느 쪽이 몸 안인지
라벨로 못 박는다.

### 구간 길이는 반복이 아니라 재생 속도로 맞춘다

구간은 13~30초인데 클립은 4~6초다. **되풀이하면 같은 동작이 여러 번 끊겨 돌아가는 게 그대로 보인다.**
한 번을 구간 길이에 맞춰 늘리면 동작이 끊기지 않는다.

- 늘리는 배율 = 구간 길이 ÷ 클립 길이. **4배까지 연다**(시청층이 중장년이라 느린 편이 오히려 낫다).
  🚨 상한을 2.5로 뒀더니 18.6초 구간(6초 클립, 3.1배 필요)이 상한에 걸려 **결국 반복으로 넘어갔다** —
  "반복 말고 속도"가 실제로는 안 지켜지고 있었다.
- **4배로도 못 채우면 반복하지 말고 멈춘다.** 그건 그 구간에 보여줄 장면이 모자라다는 뜻이고, 같은
  4초를 다섯 번 돌리면 "같은 그림에 색만 바뀌는" 화면이 된다(실측 지적). 클립을 하나 더 요청한다.
- **1.15배 이상 늘릴 땐 프레임 보간**(`minterpolate=fps=30:mi_mode=mci:mc_mode=aobmc`). 그냥
  `setpts`만 늘리면 같은 프레임이 반복돼 뚝뚝 끊긴다.
- 두 클립은 길이가 달라 각자의 배율로 같은 구간을 채운다.

### 🚨 데이터가 안 뚫려 있으면 코드가 있어도 안 켜진다

이 기능은 코드가 다 있는데도 한동안 안 켜졌다. **시간표를 푸는 함수가 `act` 필드를 떼고
돌려줬기 때문**이다(`lib/xray_timeline.py`의 `resolve()`). 그래서 topic이 `act`를 적어도
조립기에 전달이 안 됐고, 원고 작업 세션들이 "행위는 도입부에만 들어간다"는 잘못된 전제로
작업을 계속했다. 자기 프로젝트에 이식할 때 **시간표 → 조립기 경로에서 `act`가 살아서 가는지
한 번 찍어보고 시작할 것.**

```
timeline 행: {"from": "<자막 구절>", "item": "<칠판 품목>", "mech": "<기전 클립>", "act": "<행위 클립>"}
```

### 검수도 행위 클립까지 봐야 한다

없는 클립을 잡는 검사가 `mech`만 보고 있으면, 없는 행위 클립을 적어둬도 **검수는 통과하고
조립할 때서야 터진다.** `mech`·`act` 둘 다 검사 대상에 넣을 것.

---

## 9. 나레이션 길이는 상수로 추정하지 말고 음성을 재라

### 왜

발화 속도 상수 하나로 글자수를 초로 환산하면 **틀린다.** health-shorts에서 실제로 난 사고:

- 상수가 6.7자/초로 박혀 있었는데, 그건 **배속 1.1배이던 시절 값**이었다. 그 뒤 "배속 없이
  원 속도"로 1.0에 돌아왔는데 상수만 안 따라왔다(6.7÷1.1 = 6.09, 실측 6.13과 일치).
- 이 9% 과대평가 탓에 **재작성 7편이 전부 "목표 안"으로 통과했는데 실제 음성은 83~92초**였다.

게다가 같은 보이스라도 topic마다 **5.83~6.40자/초로 흔들린다**(문장 길이·숫자 읽기·문장 사이
쉼의 양이 달라서다). 상수 환산은 ±4초가 그냥 난다 — 멀쩡한 원고를 줄이게 만드는 오탐이 나온다.

### 어떻게

- **보이스나 배속을 바꾸면 발화 속도를 다시 잰다.** 채널마다 다르다(댕냥사전은 5.57자/초).
- **mp3가 이미 있으면 상수를 쓰지 말고 `ffprobe`로 직접 잰다.** 상수는 TTS 전 사전 계산에만 쓴다.
- 목표 길이는 **실제로 만들어져 통과한 길이**에 맞춘다. 지킬 수 없는 숫자를 걸어두면 경고가
  상시 켜져 있어 아무도 안 본다(health-shorts는 "80초"가 틀린 상수로 환산한 값이라 한 번도
  지켜진 적이 없었다 — 발행본 67편 중 48%가 초과였다).

---

## 10. 원고를 고치면 카드·캡션·제목이 같이 따라와야 한다

### 왜

원고 각도를 바꾸면 카드 `items`·제목·블로그 캡션이 **옛 내용으로 남는다.** 실측 사고 두 건:

- 고령_15: 원고를 "전염·치료시기·백신"으로 갈았는데 카드는 "담으로 오인·병원 미루기"에 머물러
  **전염이 통째로 빠졌다.**
- 대사_14: `items`만 고치고 제목이 "근육 지키고 요요 줄이는"으로 남아, 구토·탈모·췌장을 말하는
  영상과 제목이 따로 놀았다.

둘 다 사람이 눈으로 훑어야만 보이는 종류라 그때까지 아무 검사에도 안 걸렸다.

### 기계로 잡는 법

두 가지를 검사한다.

1. **항목 누락(확정적)** — 나레이션이 "먼저 전염이에요"처럼 선언한 항목이 카드 어디에도
   없으면 실패. 정규식으로 항목 선언부를 뽑아 spec 전문에 있는지 본다.
2. **제목 각도(휴리스틱)** — 제목이 나레이션·항목과 거의 안 겹치면 경고.

🚨 **제목 검사는 낱말로 재면 안 된다.** 한국어는 조사·어미가 붙어 표면형이 달라져서
"있다면"·"쓰는"·"것과"가 전부 "원고에 없는 말"로 잡힌다 — 실측 오탐률이 9건 중 9건이었다.
**줄 단위 글자 2-gram 겹침**으로 재고, 줄 하나씩이 아니라 **제목 전체에서 가장 잘 맞는 한 줄**을
본다(마무리 줄은 원래 원고에 없는 말이라 혼자 보면 11%로 뜬다). 각도가 살아있는 제목은 최소
한 줄이 60~100% 겹치고, 어긋난 제목만 모든 줄이 낮게 나온다(실측 최대 23%).

---

## 11. 미렌더 클립 요청은 시트 하나로 모은다

요청은 topic마다 `clip_requests.json`에 흩어져 쌓이는데 **렌더는 사람이 한 자리에서 한다.**
손으로 옮겨 적으면 (1) 새 요청이 시트에 못 올라가 묻히고 (2) 이미 뽑은 것이 남아 두 번 뽑는다.
실제로 요청 5건이 시트에 못 올라간 채 묻혀 있었다.

**시트를 스크립트로 다시 만든다** — 각 topic의 요청을 모으되 `output/`에 파일이 있으면 받은
것으로 보고 뺀다. 그러면 시트엔 항상 "지금 뽑아야 할 것"만 남는다. 시트는 직접 고치지 말고
고칠 내용은 해당 topic의 `clip_requests.json`에 넣는다(`scripts/build_clip_request_sheet.py`).

---

## 12. 제목·상단 띠·썸네일은 자리마다 하는 일이 다르다

🚨 **세 자리를 같은 말로 채우면 안 된다.** 실측 사고(2026-09-24): 크레아틴 편이 업로드 제목·영상
상단 띠·썸네일 전부 "크레아틴"이었다. **크레아틴은 원인 하나**일 뿐이고 시청자가 겪는 건 탈모·배탈·
신장 수치인데 화면 어디에도 그게 없었다.

| 자리 | 넣는 것 | 왜 |
|---|---|---|
| 업로드 제목 | **검색어** | 검색으로 노출되는 자리다 |
| 영상 상단 띠 | **증상** | 이미 들어온 사람에게 "네 얘기다"를 말하는 자리 |
| 썸네일 | **증상** 한 덩어리 | 누르게 만드는 건 물질 이름이 아니라 자기 증상이다 |

- 검색어가 **이미 증상·질환**이면(이명·대상포진) 세 자리가 같아도 된다.
- 검색어가 **물질·제품·음식**이면(크레아틴·위고비·콜라겐) 상단 띠와 썸네일은 **반드시 증상으로.**
- 자기 프로젝트에 이식할 때: 검색어 목록을 훑어 **물질·제품·음식인 것만 골라** 그 topic들의 썸네일·
  상단 띠를 점검하면 된다. 전체를 다 볼 필요 없다(실측 87개 중 실제 문제는 13개였다).

---

## 13. 한 항목은 "행동 → 원인 → 아이템" 세 마디다

공공데이터를 쓰기 시작한 뒤 원고에 **기관명과 전문용어만 덧붙는** 사고가 났다. 지적 원문:
*"행동이랑 그 행동에 대한 원인 그걸 개선할 수 있는 아이템, 이거로 단순하게 가야 사람들이 이해를
할텐데 넣어야 된다고 생각되는거만 덕지덕지 넣어놨다."*

| | 말할 것 | 예 |
|---|---|---|
| 행동 | 시청자가 **실제로 하는 것** | "검사 전날 땀을 많이 흘렸다" |
| 원인 | 그래서 **몸에서 무슨 일이** | "물이 모자라면 노폐물이 더 진하게 찍힌다" |
| 아이템 | **뭘 하면 되는지**(칠판 품목과 같은 것) | "전날 물 두 컵 더" |

### 기관명·수치는 이럴 때만

- 🚫 문장마다 "○○은 ~라고 해요"를 붙이지 마라. 한 원고에 질병관리청이 다섯 번 나왔다 —
  *"뭐만하면 질병관리청 이지랄하네"*.
- 🚫 **수치를 위해 구간을 따로 만들지 마라.** 통계만 말하는 구간을 끼우면 위쪽 영상이 그 수치와
  무관해져 *"헷갈리기만 하고"* 끝난다.
- ✅ 쓸 자리: 퍼센트·비중처럼 **"얼마나 흔한지"를 정확히 말해야 할 때.**
- 검사로 거는 법: **수치가 없는 문장에 붙은 기관명만** 센다. 전부 세면 멀쩡한 인용까지 걸려
  18개 topic이 실패로 뜨는데 실제 문제는 4개였다(실측).

---

## 14. 오늘 실측으로 확정한 자잘한 규칙들

이식할 때 그대로 가져가면 같은 사고를 피한다. 전부 **"조용히 잘못 도는"** 종류였다.

- **부위 라벨은 기전 쪽(오른쪽 아래)에.** 칸이 좌우로 갈릴 때 왼쪽에 두면 행위 클립 이름처럼
  읽힌다 — *"기전에 해당하는게 그 부위라서, 행동이랑은 별개로"*.
- **도입부 컷 시각은 나레이션 시각으로 적고, 빌더가 제목카드 길이를 더한다.** 그냥 넘기면 늘
  0.2초 일찍 잘려 훅 마지막 말이 칠판으로 넘어간다(13개 topic이 어긋나 있었다).
- **결론 구간의 시간표 행은 "하나만" 둔다.** 여러 개면 품목 배지만 줄줄이 바뀌고, 아예 빼면
  **앞 행이 영상 끝까지 늘어나** 4초 클립을 수십 초로 늘리게 된다(둘 다 실제로 겪었다).
- **부위 점등 클립은 도입부에 쓸 때 줌을 끄고 점등을 일찍 켠다.** 기본값은 1.3초에 켜지기
  시작해 2.7초에 만개하는데, 도입부는 앞 2.6초만 잘라 쓰므로 절반이 깜깜하다.
- **클립 색인의 `avoid`를 어기지 마라.** "목 안쪽 장기엔 안 맞는다"고 적힌 클립을 성대 구간에
  썼고, 그 클립엔 5초쯤 팔이 하나 더 생기는 결함까지 있었다.
- **조립된 영상은 deploy 폴더까지 넣어야 끝이다.** 만들어만 두면 업로드 쪽에선 없는 것과 같다.
