# 원고 v2 작성 가이드 (2026-09-23)

전량 재제작용. **원고를 쓰는 세션·에이전트는 이 파일을 먼저 읽는다.**
CLAUDE.md의 "원고 고도화 6가지"와 "반투명 인체 포맷"이 실제로 어떻게 맞물리는지를 여기 모았다.

---

## 0. 쓰기 전에 확인할 것 네 가지

```bash
.venv/bin/python3 -m lib.xray_inventory --mech                 # 쓸 수 있는 기전 장면
.venv/bin/python3 -m lib.content_review --hook-pattern <topic>  # 이 topic의 훅 문형
.venv/bin/python3 -m lib.content_review --card-structure <topic>
.venv/bin/python3 -m lib.content_review --connectives <topic>
```

그리고 `data/<topic>/card_news_spec.json`의 **`search_keyword`**를 본다 — 제목과 첫 문장이
그 말로 시작해야 한다(검색으로 들어오는 채널이라 이게 유입의 전부다).

---

## 1. 🚨 기전은 라이브러리에서 고른다 — 자유롭게 쓰고 나중에 찾지 않는다

원고를 먼저 자유롭게 쓰면 라이브러리에 없는 기전이 쏟아지고, 그때 **비슷한 클립으로 때우는
사고**가 난다(화면과 나레이션이 어긋나는 건 이 채널에서 반복된 실패다).

- 🚨 **고르기 전에 `assets_library/xray/CLIP_NOTES.md`를 먼저 읽는다** — 이름과 내용이 다른
  클립(`m_follicle_weakening`은 난포가 아니라 모낭)과 구간 주의사항(`act_toss_turn`은 5초를 넘기면
  뇌가 켜진다)이 모여 있다.
- `lib.xray_inventory --mech`에 있는 기전(현재 115종) 중에서 고른다. 이름이 곧 장면이다:
  `m_vasoconstriction`(혈관 수축), `m_mucus_barrier`(점막 보호층), `m_nerve_compression`(신경 눌림)…
- **원고의 각 항목은 그 기전 장면으로 보여줄 수 있어야 한다.** 보여줄 수 없는 설명은 항목이 아니라
  곁가지다.
- 정말 없으면 **지어내지 말고** `data/<topic>/clip_requests.json`에 적고 그 topic은 멈춘다
  (형식은 CLAUDE.md "없는 클립은 요청한다" 절). `check_xray_clips()`가 안 받은 요청을 실패로 잡는다.

---

## 2. 분량과 구조

| | |
|---|---|
| 길이 | **62~85초** (공백 제외 380~521자, 실측 6.13자/초) |
| 도입 | 훅 **한 문장**, 공백 제외 40자 이내 — 증상을 구체적으로 묘사해 "내 얘기다" 싶게 |
| 통념 반박 | 훅 바로 다음 **독립 문장**으로 |
| 항목 예고 | "방아쇠는 A, B, C예요" — 세 가지를 **이름만 먼저 나열** |
| 본문 | 항목 3개, "먼저 / 두 번째로는 / 마지막으로" |
| 마무리 | 해결책 + 병원 신호 |

## 2-1. 🚨 본보기 — `data/소화_14/narration.txt`(위경련)

사용자가 "이건 그나마 괜찮다"고 한 유일한 원고다. **쓰기 전에 열어서 읽어라.** 6요소를 채웠는데도
"허전하다"는 말을 듣는 원고와 이것의 차이는 셋뿐이다.

1. **독자가 실제로 하는 행동을 짚고 뒤집는다.** "이럴 때 탄산음료로 트림을 내는 분이 많은데,
   질병관리청은 오히려 위에 가스가 더 차서…" — 통념 반박을 항목 속에 묻지 말고 훅 다음에 바로 둔다.
   시청자가 "어, 나 그거 하는데" 하는 순간이 없으면 계속 볼 이유가 없다.
2. **항목을 이름으로 먼저 깐다.** "명치를 쥐어짜는 방아쇠는 스트레스, 빈속 커피, 술이에요."
   이게 없으면 "이 세 가지를 확인하세요" 하고 곧장 "먼저"로 들어가 뭘 들을지 모른 채 따라가게 된다.
3. **숫자를 체감되게 계산까지 해준다.** "하루 400mg인데, 커피전문점 커피 250mL 한 잔이 132mg이라
   4잔이면 넘어갑니다." 기준치만 던지면 자기 일로 안 읽힌다.

도입과 첫 항목 사이에 **요약**을 끼워 넣지 말 것 — 위 1·2는 요약이 아니라 반박과 예고다.

---

## 3. 6가지를 다 넣는다 (`check_content_depth()`가 검사)

1. **실행 가능한 수치 3개 이상** — 단위까지. "줄이세요"❌ / "하루 400mg 이하"⭕
2. **통념 반박 1개** — "~가 좋다고 알려졌지만 오히려 …". 시청자가 아는 말만 하면 볼 이유가 없다
3. **기전 한 줄** — 왜 그렇게 되는지(1번의 기전 클립과 짝을 이룬다)
4. **해결책은 "얼마나·언제·어떻게" 중 2개 이상**
5. **병원에 가야 할 신호 1줄** — 구체적 증상으로
6. **기관명을 문장 안에** — "질병관리청은…", "서울아산병원은…". 못 쓰겠으면 그 수치를 뺀다

---

## 3-1. 🚨 공공데이터가 이미 5.1GB 받아져 있다 — 웹에서 긁기 전에 여기부터

`~/Desktop/project/public-data/`에 공공데이터포털 카탈로그 8,766건과 **실제 CSV 5,894개**가
받아져 있다. 건강보험심사평가원 진료 통계, 질병관리청 감염병 발생현황, 국민건강보험공단 지역별
진료 정보 같은 것들이다. **기관명을 문장 안에 넣어야 하는 이 채널에 가장 잘 맞는 자료인데
지금까지 한 번도 안 썼다**(2026-09-24 확인).

```bash
cd ~/Desktop/project/public-data
python3 find.py 대상포진 진료          # 여러 단어면 많이 맞을수록 상위
python3 find.py 흡연 --cols           # 컬럼명까지 검색
python3 find.py --head 15089624       # 그 데이터셋 앞 5줄
```

- 목록의 `○`는 **이미 받아둔 것**, `×`는 카탈로그에만 있는 것이다. `×`는 링크가 만료돼 실패하는
  경우가 많으니 `○`부터 본다.
- 수치를 쓸 땐 **그 파일에서 직접 센 값**을 쓰고 "건강보험심사평가원 자료로…"처럼 기관을 밝힌다.
  웹 기사에서 본 숫자보다 이쪽이 정확하고, 출처 검사도 한 번에 통과한다.

## 4. 리서치는 처음부터 다시 하지 않는다

같은 topic의 기존 캡션·`blog_seo` 본문에 **이미 검증된 주장과 출처**가 들어 있다. 그걸 먼저 읽고
"이미 나온 주장 후보"로 삼은 뒤, WebSearch는 "이 주장을 뒷받침하는 자료가 있나"로 좁혀 쓴다.

⚠️ 다만 **그대로 옮기지는 않는다** — 네이버 원고는 나레이션의 확장판이 아니라 별도 장르다
(재사용률 40%를 넘으면 `check_naver_blog_quality()`가 잡는다).

---

## 5. 금지

- 🚫 "저장부터 하세요", "주목하세요", "지금 확인하세요", "주목!" — 제목·나레이션 어디에도
- 🚫 헷지 어미 남발("~수 있어요"). 생활습관·음식 설명은 단정형으로
- 🚫 대체품 추천(술→무알코올맥주, 담배→전자담배). **영양제·도구처럼 실제로 도움이 되는 품목**을
  권하고, 그 품목은 브랜드커넥트에 있는 것만 쓴다
- 🚫 제목에 가운뎃점(·)·이모지
- 🚫 출처 없는 수치

---

## 6. 다 쓰고 나서

```bash
.venv/bin/python3 -m lib.content_review <topic>          # 기계 검사 전체
.venv/bin/python3 -m pytest tests/test_content_rules.py -q
```

`card_news_spec.json`에 **`"content_v2": true`**를 넣어야 깊이 검사가 켜진다.

그다음 TTS → 영상 조립 순서는 CLAUDE.md "반투명 인체 포맷" 절을 따른다. **TTS는 비용이 드는
단계라 위 검사를 전부 통과한 뒤에 부른다.**

## 제목·상단 띠·썸네일 — 자리마다 하는 일이 다르다

🚨 **세 자리를 같은 말로 채우지 마라.** 2026-09-24 실측 사고: 비뇨기_16이 업로드 제목·상단 띠·썸네일
전부 "크레아틴"이었다. 크레아틴은 **원인 하나**일 뿐이고 시청자가 겪는 건 탈모·배탈·신장 수치인데,
화면 어디에도 그게 없었다.

| 자리 | 넣는 것 | 왜 |
|---|---|---|
| 업로드 제목(`card_news_spec.title`, `platform_captions.title`) | **검색어** | 검색으로 노출된다. 검색어로 시작해야 한다 |
| 영상 상단 띠(`xray.json`의 `top_title`) | **증상** | 이미 들어온 사람에게 "네 얘기다"를 말하는 자리 |
| 썸네일(`card_news_spec.thumb_word`) | **증상** 한 덩어리 | 누르게 만드는 건 물질 이름이 아니라 자기 증상이다 |

- 검색어가 **이미 증상·질환**이면(이명·대상포진·지방간) 세 자리가 같아도 된다.
- 검색어가 **물질·제품·음식**이면(크레아틴·위고비·콜라겐·알룰로스·옥수수) 상단 띠와 썸네일은
  **반드시 증상으로 바꾼다.** 그 물질은 원인 중 하나일 뿐이다.
- 상단 띠는 훅의 증상 묘사를 그대로 줄여 쓰면 된다
  (훅 "위고비 맞은 다음 날, 속이 메스껍고" → 상단 "맞은 다음 날 토할 것 같다면").

### 목적어를 문장 맨 앞에 두고 동사를 멀리 보내지 마라

"뭘 하는지" 주어·목적어를 밝히려다 목적어를 맨 앞에 박으면, 그 뒤에 부사구가 길게 붙었을 때
**문장이 토막처럼 들린다.** 실측 지적(2026-09-24): `크레아틴을 첫 주에 하루 20그램씩 몰아 넣는 걸
로딩이라고 해요.` → "'크레아틴을'이 왜 짤리냐, ~을 이러면서 시작하네".

- ✅ `첫 주에 크레아틴을 하루 20그램씩 몰아 넣는 걸 로딩이라고 해요.` — 시간 표현을 앞에 둔다.
- 목적어로 시작해도 **동사가 바로 붙으면 멀쩡하다**(`술을 마신 날은…`, `물을 자주 마시고…`).
  문제는 목적어와 동사 사이가 먼 경우다 — 기계로 못 가르니 소리 내어 읽어보고 판단한다.
