# 반투명 인체 숏폼 — 확정 포맷 (견본: `소화_14`)

2026-09-24, 사용자가 `소화_14`를 보고 확정했다 — *"싱크도 잘맞춰줬고 내용자체도 나쁘지 않았다,
영상까지 색인해서 맞춰줬던거같다, 써머리쪽도 데이터와 함께 해결책 제공하는거 꽤 괜찮다."*

**이 문서 하나가 기준이다.** 육아 건강 채널·댕냥사전도 같은 포맷을 쓰므로, 경로·품목만 자기
프로젝트로 바꾸고 구조는 그대로 가져간다. 견본 영상은 `assets_library/xray/견본_소화_14.mp4`,
설정은 `data/소화_14/xray.json`.

---

## 1. 화면 구성 — 세 덩어리

```
0 ──────── 도입부 ────────┬──────── 설명 ────────┬──── 결론 ────
   훅 + 통념 반박           항목마다 칸 하나        칠판 전체
   전체 화면 Flow           (왼쪽 행동·오른쪽 기전)   (칸 없음)
```

| 덩어리 | 언제까지 | 화면 |
|---|---|---|
| 도입부 | `opening_until` | 도입 클립이 **화면 전체**를 덮는다 |
| 설명 | 결론 구절 전까지 | 칠판 + 위쪽 칸(960×680) |
| 결론 | `summary_from`부터 | 칠판 전체 버전이 덮는다 |

### 🚨 도입부는 통념 반박까지 덮는다

훅만 덮고 칠판으로 넘기면 **통념 반박 문장이 위쪽 칸(기전)과 아무 상관이 없어** 화면과 말이 따로
논다 — *"기전에 대한거 설명 전에 하는 멘트가 위 영상이랑 하나도 맞지 않아서 짜친다."*

- `opening_until`은 **나레이션 시각**으로 적는다(자막에서 통념 반박 문장이 끝나는 시각).
  빌더가 제목 카드 길이를 더해 넘긴다 — 그냥 넘기면 늘 0.2초 일찍 잘린다.
- 길어진 만큼은 도입 클립을 **잘라 쓰지 말고 통째로** 쓴다(`range`를 빼면 된다). 범위를 두면
  3~6배를 늘려야 해 정지 화면이 된다. 통클립이면 1.0~1.6배로 자연스럽다.
- 🚨 **시간표 첫 구간이 `opening_until`보다 먼저 시작하면 안 된다.** 전체 화면 도입 영상 위에
  칸이 겹쳐 그려진다 — *"누가 이 영상 두개를 영상위에 올려놓으라했어."*

### 위쪽 칸 — 왼쪽 행동 · 오른쪽 기전

```
[ 이 행동이 ]            │            [ 몸 안에선 ]
   행위 클립 476         │8            기전 클립 476
                         │                    [ 부위 ]  ← 오른쪽 아래
```

- 칸 960×680, 구분선 8px, 좌우 476씩. **8px인 이유는 반칸이 짝수로 떨어져야 해서**다(6px이면
  477이 홀수라 결과가 958px로 2px 모자란다).
- 🚨 **클립을 자르지 않는다.** 높이를 칸에 맞추고(720×1280 → 382×680) 남는 좌우를 클립
  배경색으로 메운다. 폭을 먼저 맞추면 전신 클립에서 **발이 잘려 동작의 무게중심이 안 보인다.**
- **부위 라벨은 오른쪽 아래**(기전 쪽). 왼쪽에 두면 행위 클립 이름처럼 읽힌다 —
  *"기전에 해당하는게 그 부위라서, 행동이랑은 별개로."* 구간마다 다른 부위를 적는다.
- 구간 길이는 **반복이 아니라 재생 속도**로 맞춘다. 4배까지 늘리고, 그래도 모자라면 반복하지 말고
  **구간을 쪼개거나 클립을 하나 더 요청**한다.

### 칠판 아이콘은 전부 켠다

항목 아이콘을 가로로 나열하되 **전부 같은 크기로 점등**한다. 지금 말하는 항목만 밝히던 건 문단
경계로 **추정**한 값이라 정확할 수 없고, 어긋나면 엉뚱한 아이콘이 혼자 밝아져 더 헷갈린다 —
*"아이콘 네개 다 점등하자, 싱크 맞추는게 불가능하겠다."*

### 🚨 해결책이 시작되는 자리에서 칠판으로 넘긴다 (2026-09-24 확정)

**영상 칸은 원인·기전 구간에만 붙인다. 해결책부터는 칸 없이 칠판만 나오는 씬으로 넘긴다** —
사용자 지시: *"여기부터는 문제에 대한 해결책이잖아, 기전쪽만 영상을 붙이고 해결책 부분은 뒤로
넘겨서 칠판만 나오는 씬에서 하나하나 언급하라니까. 씬 전환이 되어야 이해가 잘되지."*

- `summary_from`은 **해결책 첫 문장**을 가리킨다("관리는 ~", "오늘부터는 ~", "그러니 ~").
  병원 신호 한 줄만 남기고 그 앞의 해결책 문장들까지 칸을 붙이면, 보는 사람은 그게 원인
  설명인지 해결책인지 구분하지 못한다.
- 그래서 **칠판 구간이 20~30초로 길어지는 건 정상이다.** 그 동안 화면이 안 바뀌는 것도
  설계다 — `verify_output.py`는 칠판 구간의 정지를 지적하지 않는다(설명 구간만 본다).
- 해결책 구간에 쓰려고 받아둔 행위 클립이 있어도 거기엔 안 쓴다. 그 클립은 같은 동작이
  **원인**으로 나오는 다른 topic에서 쓴다.

### 결론

- 시간표 행은 **딱 하나**만 둔다. 여러 개면 품목 배지만 줄줄이 바뀌고, 아예 빼면 **앞 행이 영상
  끝까지 늘어나** 4초 클립을 수십 초로 늘리게 된다(둘 다 실제로 겪었다).
- 배지는 **그 결론이 시키는 행동의 품목**이어야 한다. 원인 품목(맥주·가공육)을 두면 "이걸 하라는
  건가"로 읽힌다 — *"휴지, 변기가 해결책은 아니잖아."*
- 빌더가 칸을 만들지 않는다(칠판이 덮으므로 시간만 버린다).

---

## 2. 원고 — 한 항목은 "행동 → 원인 → 아이템"

| | 말할 것 | 예 |
|---|---|---|
| 행동 | 시청자가 **실제로 하는 것** | "빈속에 커피를 마신다" |
| 원인 | 그래서 **몸에서 무슨 일이** | "위산이 음식 대신 위벽을 그대로 만난다" |
| 아이템 | **뭘 하면 되는지**(칠판 품목과 같은 것) | "커피는 식후로" |

### 🚨 "3가지"는 틀이 아니다 — 증상 하나를 깊게 파는 게 기본 (2026-09-24 확정)

사용자 지시: *"템플릿이 방안 3가지에 한정되어 있으니, 어떤 증상에 대해 깊게 생각할 수 있도록 말해주지
않고 3가지 나열하려다 보니 연관없는 것도 맘대로 집어넣는 느낌. 상단 제목은 구토인데 갑자기 탈모 이야기를
한다거나, 임상 이야기만 좀 하고 치워버려. 무조건 3가지 이 규칙 푼다. 필요할 때 이거로 하되 아닐 때는
아니어도 된다."*

- **항목 수는 소재가 정한다.** 원인이 정말 셋으로 갈리면 셋, 하나를 깊게 풀어야 하면 하나. 칸을 채우려고
  **제목·훅의 증상과 상관없는 항목을 끌어오지 않는다**(대사_14: 제목은 "토할 것 같다면"인데 탈모·췌장을 끼움).
- **임상·통계는 숫자 하나 던지고 끝내지 않는다.** 그 연구에서 **어떤 상황이 벌어졌는지**를 푼다 —
  언제 생기나(증량 직후), 얼마나 가나(며칠), 누구에게 더 오나, 그래서 시청자가 **자기 상황에서 뭘 예상해야
  하나**까지. "오심 43.9%"는 정보가 아니고 "용량이 한 단계 오를 때마다 다시 오고 대개 일주일 남짓 간다"가 정보다.
- 한 증상을 깊게 풀 땐 칸 구성을 **기전의 층위**로 나눈다(예: 위에서 벌어지는 일 → 뇌에서 벌어지는 일 →
  임상에서 언제 몰리는지). 항목 번호("먼저/두 번째로는")는 나열할 때만 쓴다.
- 제목·상단 띠·썸네일·카드 표지도 같은 한 가지 증상을 가리켜야 한다.

### ✅ 대본 견본 — `대사_14`(위고비 메스꺼움), 2026-09-24 사용자 승인

*"훨씬 나아졌네, 그래 그렇게 가야지. 이거 무조건 박아놔라."* — 정본은 `data/대사_14/narration.txt`.
새 대본은 이 짜임을 먼저 보고 쓴다. 전 버전(구토·탈모·췌장 3가지 나열)과 무엇이 달라서 통과했는지:

| | 탈락한 전 버전 | 통과한 버전 |
|---|---|---|
| 범위 | 제목은 구토인데 탈모·췌장까지 셋을 채움 | **처음부터 끝까지 증상 하나**. 췌장은 병원 신호 한 줄로만 |
| 기전 | "위 배출이 늦어져" 한 줄 | **층위로 판다** — 위에서(오래 머묾 → 평소 양이면 넘침) · 뇌에서(구역질 부위에 작용) |
| 임상 | "오심 43.9%" 숫자만 | **그 임상에서 벌어진 일** — 증량 직후에 몰림 · 8일쯤 감 · 다음 단계에서 또 옴 → *"약이 안 맞는 게 아니라 단계가 이른 것"* |
| 해결책 | "의사와 정하세요" | **그 기전에서 곧장 나오는 행동** — 증량 주엔 반으로 나눠 천천히, 배부르면 숟가락 놓기, 기름진 것 피하기(위에 오래 남으니) |

**원칙 한 줄: 시청자가 "아, 그래서 그런 거였구나"를 한 번 겪게 한다.** 숫자·기관명은 그 이해를 받치는 자리에만.

### 🚨 대본은 코디네이터가 검수까지 끝내고 TTS 한 번 → 사용자는 조립본으로 판단 (2026-09-24)

*"매번 TTS나 수정하고 있으니 돈만 나가고 발전은 없다"* → 그래서 한때 "대본을 글로 먼저 승인받자"로 갔는데,
사용자가 바로잡았다: *"대본만 딱 봐서는 문제를 못 볼 확률이 높아. 조립이 된 후에 봐야 전반적인 문제가 다 보여."*

1. **대본 검수는 코디네이터 몫이다** — 제목과 상관없는 칸 채우기·숫자만 던진 임상·해결책 약함을 거르고,
   새 숫자는 전부 그 topic 조사 자료(블로그 원고)와 대조한다. 여기서 걸러질 건 다 걸러야 TTS가 한 번으로 끝난다.
2. TTS 한 번 → 시간표·**카드 항목**(배지가 카드에서 나온다) → 두 벌 조립 → `verify_output` 0건 → 코디네이터가 프레임 확인.
3. **사용자는 조립된 영상으로 본다** — 그래서 조립이 끝나 검사를 통과하면 **바로 미션컨트롤에 올린다**(사용자 지시
   "영상 하나씩 완료될 때마다 미션콘트롤에 올려라"). 승인 표시를 게이트로 걸었다가 밤새 6편이 하나도 안 올라간 적이 있다 — 걸지 말 것.

### 🚨 문장 사이에 이음새를 넣는다 (2026-09-24)

*"역접이나 순접 같은 게 없네. 그리고, 그러나, 그래서, 그렇지만, 그렇기 때문에 이런 접속사가 없어서 너무
부자연스러운 경우가 많구나."* — 실측: 대본 25편이 18문장 안팎에 문장 첫 이음새 0~4개. "~해요. ~해요. ~해요."로
끊겨 원인→결과, 통념→반박 흐름이 귀로 안 들어온다.

- **논리가 꺾이는 자리에** 넣는다: 원인→결과 `그래서·그러니` · 통념→반박 `하지만·그런데` · 새 질문 `그렇다면 ~은 왜
  ~일까요?` · 덧붙임·예외 `특히·다만·물론` · 이유 `~거든요`·`~기 때문에`. 모든 문장에 붙이지 않는다(19문장에 7개 정도).
- 기준: `data/대사_22/narration.txt`(수정 전 `대본검토/이음새/대사_22_before.txt`).
- `check_connective_flow()`가 문장 첫 이음새 5개 미만을 잡는다(preflight·미션컨트롤 게이트 포함).

### 🚨 해결책이 "수치를 낮게 나오게 하는 법"이면 안 된다

비뇨기_16 초판이 "검사 사흘 전 크레아틴 끊고 물 두 컵 더"로 끝났다. 그건 문제를 푸는 게 아니라
**숫자를 가리는 것**이다 — *"내용자체가 뭐를 해결하는게 아니라 신장 수치가 낮게 나오는 방법으로
가버리면 내용이 개병신같이 들어가버린다."* 고친 뒤 각도는 "내 신장이 진짜 나쁜지 가려내는 법".

**기계로 못 잡는다. 원고를 쓰고 스스로 물어야 한다 — "이건 문제를 푸는가, 숨기는가?"**

### 기관명·수치

- 🚫 문장마다 "○○은 ~라고 해요"를 붙이지 마라 — 한 원고에 질병관리청이 다섯 번 나왔다.
- 🚫 **수치를 위해 구간을 따로 만들지 마라.** 통계만 말하는 구간을 끼우면 위쪽 영상이 그 수치와
  무관해져 헷갈리기만 한다. 수치는 **설명을 받치는 자리**에 붙인다.
- ✅ 쓸 자리: 퍼센트·비중처럼 "얼마나 흔한지"를 정확히 말해야 할 때.

### 제목·상단 띠·썸네일은 자리마다 하는 일이 다르다

| 자리 | 넣는 것 |
|---|---|
| 업로드 제목 | **검색어**(노출되는 자리) |
| 영상 상단 띠 | **증상**("네 얘기다"를 말하는 자리) |
| 썸네일 | **증상** 한 덩어리(문장 금지) |

검색어가 이미 증상·질환이면(이명·대상포진) 세 자리가 같아도 된다. **물질·제품·음식이면**
(크레아틴·위고비·콜라겐) 상단과 썸네일은 반드시 증상으로.

---

## 3. 숫자는 한글로 풀어 TTS에 보낸다

아라비아 숫자를 그대로 보내면 **읽기가 뭉개지거나 끊긴다** — *"40 이런거 숫자로 넣는거 맨날
끊긴다."* 음량을 재보면 무음이 아니라 발음이 무너지는 것이다.

- `lib/korean_numbers.to_speech()`가 API로 보낼 텍스트만 바꾼다. **자막은 원문(숫자) 그대로** —
  화면에 "사백 밀리그램"이 뜨면 읽기 힘들다.
- 고유어/한자어를 가른다: `4잔`→"네 잔", `40세`→"사십 세", `250mL`→"이백오십 밀리리터".
- 발화 속도는 **보이스·배속을 바꿀 때마다 다시 잰다.** 상수 하나로 환산하면 틀린다 —
  mp3가 있으면 `ffprobe`로 직접 재는 쪽이 항상 맞다.

---

## 3-1. 클립을 더 받아야 할 때 — 작업 자리는 한 곳이다

🚨 **경로도 파일 이름도 고정이다. 정리 욕심으로 바꾸지 말 것**(`tests/test_xray_worksheet_path.py`가 막는다).

```
assets_library/xray/작업/
  0_작업지시.md   ← 사람이 여는 유일한 파일(번호·프롬프트·몇 번이 Flow만인지)
  1_스틸/         ← 번호 붙은 스틸. 미드저니로 새로 뽑은 것도 여기
  2_완성클립/     ← Flow 결과를 클립 이름 그대로
```

`scripts/prep_clip_worksheet.py`가 시트를 다시 만들고, `scripts/collect_clips.py --commit`이
받은 영상을 라이브러리로 들인다. 한 번 이름을 바꿨다가 사용자가 열어둔 파일이 사라져
"내가 뽑을 게 몇 번이었지"를 다시 물어야 했다 — 그 경로는 매일 여는 자리다.

---

## 4. 조립 전에 이것부터 — `scripts/preflight_xray.py`

축 10개를 topic 전체에 한 번에 돌린다. **여기서 0건이 나와야 조립할 자격이 있다.**

| 축 | 막는 사고 |
|---|---|
| 시간표 해석 | 원고 고치고 시간표를 안 고쳐 조립이 죽는 것 |
| 배치 | 문장 중간 컷 / 8초 빈 구간 / 도입부에 칸 겹침 |
| 결론 | 요약이 여러 행으로 쪼개져 품목만 바뀜 |
| 기전 다양성 | 같은 클립 3번 이상 → 정지 이미지로 보임 |
| 행위 커버리지 | 설명 구간에 행위 클립이 없음 |
| 쉬운 말 | 수치 없는 기관명 / 설명 없는 전문용어 |
| 카드 정합 | 원고 고치고 카드 방치 |
| 클립 존재 | 없는 클립 참조 / 미수령 요청 |
| 내용 깊이 | 수치·통념 반박·병원 신호·길이 |
| 검색어 | 제목이 검색어로 시작 |

조립이 끝나면 **deploy 복사 → 개별 대시보드 → 미션컨트롤 동기화가 자동으로 돈다**(`xray_build._publish`) —
만들어만 두면 업로드 쪽에선 없는 것과 같다. 미션컨트롤엔 **지금 올릴 수 있는 것만** 뜬다(영상·카드 존재 +
음성·원고보다 새것 + preflight 0건, 못 올리게 되면 sync가 지운다).

### 조립 뒤 — `scripts/verify_output.py`

완성된 mp4를 직접 잰다: 원본(음성·자막 포함)보다 낡음 · 길이 불일치 · 무음 · **설명 구간** 정지 · 4배 가까운
늘림 · 네이버 클립 캡션 누락 · deploy 사본 불일치. 칠판 구간 정지는 설계라 안 센다.
**여기도 0건이어야 하고, 그다음 프레임을 눈으로 본다** — 팔이 셋 나오는 클립 같은 건 기계가 못 잡는다.
음성을 다시 뽑았으면 먼저 `scripts/fix_opening_drift.py --commit`(문장 시각이 밀려 도입부 경계가 어긋난다).

### 기계로 못 잡는 둘

- **해결책이 문제를 푸는지 숨기는지** (위 2번)
- **클립이 그 구간 말과 맞는지** — 어휘 겹침으로 재봤더니 130건 중 거의 전부 오탐이었다. 임상
  용어로 적은 클립 설명과 입말 나레이션은 원래 어휘가 안 겹친다. **프레임을 직접 보는 수밖에 없다.**

---

## 5. 견본 `소화_14`의 실제 값

```
opening_until 13.6   (훅 4.3초 + 통념 반박 "탄산음료로 트림을 내는 분이 많은데…" 13.6초까지)
summary_from  "오늘부터는"
top_title     "명치가 쥐어짜듯 아프다면"        ← 증상. 검색어(위경련)는 업로드 제목에만

14.3-19.2  커피  act_grab_belly      m_pressure_buildup  [위]
19.2-27.8  긴장  act_hold_head       m_nerve_overdrive   [위 신경]
27.8-36.2  커피  act_drink_caffeine  m_acid_secretion    [위산]
36.2-43.5  맥주  —(클립 요청 중)      m_toxin_load        [위 점막]
43.5-      물컵  —                   m_nerve_overdrive   [위 신경]   ← 결론, 칠판이 덮는다
```

구간이 전부 4.9초 이상인 점에 주의 — "먼저 스트레스예요" 같은 **1~2초짜리 항목 선언 자막은
단독 구간으로 두지 않는다.** 칸 최소 길이(3초)에 못 미쳐 화면이 안 바뀐다. 선언 줄을 구간 시작으로
삼고 뒤따르는 설명 문장을 같은 구간에 넣는다.

---

## 6. 육아 트랙 — 같은 포맷, 세 가지만 다르다 (2026-09-24)

화면 구성·시간표·조립 전 검사는 **위 1~5절 그대로**다. 아기라서 갈리는 건 셋뿐이다.

### 🚨 아기가 보이는 장면은 Flow(Veo)에 안 보낸다

미성년자 안전필터는 `infant`·`child` 같은 **단어만으로도** 막는다(내용이 얼마나 무해한지와 무관하게
차단된 전례가 있다). 그래서 아기 몸은 **미드저니 정지 스틸 한 장**으로만 만들고, 움직임은
`scripts/make_part_clip.py`가 푸시인+점등으로 입힌다 — 위험 표면이 미드저니 한 곳으로 줄어든다.

- 캐논 스틸 6장: `baby_canon_organs`(전신, 등 대고 누운 몸을 위에서) · `baby_cu_torso` ·
  `baby_cu_head_side`(**짧고 거의 수평인 이관** — 아기 중이염 기전이 여기 달려 있다) ·
  `baby_cu_airway`(끝이 실처럼 가늘어지는 세기관지) · `baby_cu_mouth_throat` · `baby_cu_skin_layers`.
- 요청은 `data/육아/_shared/clip_requests.json`에 `"kind": "still"`로 적는다(Flow 프롬프트 없음).
  시트는 `assets_library/xray/작업_육아/0_작업지시.md`의 **0부**로 따로 선다.
- 미드저니 프롬프트도 아기 단어를 **먼저 피한다** — "early-developmental body proportions"로 비례를
  묘사하고, 막히거나 어른 비례로 나오면 쓰라고 직접 표현 문구를 대안으로 같이 준다.

### 부위 클립은 캐논 스틸 하나에서 여러 개가 나온다

시트에 따로 안 올라간다 — 스틸이 오면 `scripts/make_part_clip.py`로 세션이 만든다.
🚨 **`--region` 없이 만들지 마라**(전신·몸통 스틸은 피사체 전체가 호박색이 된다). region 값은
실제 스틸을 보고 재서 `assets_library/xray/part_regions.json`에 스틸 파일명으로 적는다.

| 스틸 | 여기서 나오는 부위 클립 |
|---|---|
| `baby_canon_organs` | `part_baby_whole`(전신 발진) · `part_baby_forehead`(열) · `part_baby_hand_foot`(수족구) |
| `baby_cu_torso` | `part_baby_stomach` · `part_baby_intestine` · `part_baby_liver` · `part_baby_belly` |
| `baby_cu_head_side` | `part_baby_middle_ear` · `part_baby_nose` |
| `baby_cu_airway` | `part_baby_bronchiole` |
| `baby_cu_mouth_throat` | `part_baby_mouth` |
| `baby_cu_skin_layers` | `part_baby_skin` |

### 행위 칸은 보호자다

행위 클립 48종이 전부 "혼자 있는 성인이 자기 몸을 만지는" 장면이라 육아엔 하나도 안 맞는다
(화면에 두 사람이 나오는 클립이 0건). 새로 만들되 **둘로 가른다**:

- **비접촉(사물만) → Flow** — 시린지에 약을 재고, 분유를 타고, 이유식을 뜬다. 아기가 프레임에 없다.
- **접촉(아기에게 해주는 동작) → 스틸 + 코드** — 체온 재기·약 먹이기·등 두드리기는 보호자 손이 얹힌
  정지 스틸을 뽑아 `make_part_clip.py`로 돌린다. Flow를 안 거치므로 필터를 안 탄다.

### 기전 칸은 그대로 쓴다

기전 클립 115종은 몸속 확대라 나이가 안 드러난다 — 색인 전수 조사에서 topic당 4~6종이 그대로 맞았다.
새로 필요한 건 13종뿐이다(`m_fever_setpoint_rise`·`m_rbc_breakdown`·`m_narrow_tube_contrast`·
`m_gas_bubble_trap` 등).

### 자리

| | 육아 | 기존 |
|---|---|---|
| 스펙 | `data/육아/<topic>/` | `data/<topic>/` |
| 산출물 | `output/육아/<topic>/` | `output/<topic>/` |
| 작업 시트·스틸 | `assets_library/xray/작업_육아/` | `.../작업/` |
| 캐논 스틸 | `assets_library/xray/stills/baby/` | `.../stills/` |

🚨 **topic 이름은 `육아_1`로 평평하다 — 접히는 건 경로뿐이다.** 이 저장소에서 topic 안의 `/`는 이미
**언어**를 뜻해서(`가슴쓰림_1/en`), `육아/육아_1`로 부르면 `육아_1`을 언어 코드로 읽는 코드가 조용히
깨진다. 경로 해석은 `lib/tracks.py`가 전담하고 `tests/test_tracks.py`가 이 규약을 막는다.
