# 지금 할 일 (2026-09-23)

**남은 건 Flow 22개뿐이다.** 업스케일도 미드저니도 끝났다.

| | 상태 |
|---|---|
| 업스케일 24개 | ✅ 완료 — 받아서 교체함 |
| 미드저니 6장 | ✅ 완료 — `RENDER/`에 저장함 |
| **Flow 22개** | ⬜ **`2_Flow.md` 보고 돌리면 됨** |

## Flow 돌리는 법

`2_Flow.md` 맨 위 표에 번호·넣을 파일·길이가 다 있다. 파일은 전부 `../RENDER/` 안에 있다.

- **행위 7개**(32·35·38·56·57·61·70) — `<번호>_start.jpg` + `<번호>_end.jpg` 두 장
- **기전 15개** — `<번호>_start.jpg` 한 장만, **끝 프레임은 비움**
- 결과는 `RENDER/<번호>.mp4`로 저장. 다운로드 폴더에 두고 말만 해주면 세션이 번호 맞춰 정리한다.

## 이번에 고친 것

- **두피·코·눈처럼 겉면을 켜라고 했더니 뇌·골반이 켜졌다** → 행위 클립에선 점등을 아예 뺐다. 점등은 부위 클립이 맡는다.
- **열쇠·집게·수도꼭지·비커·망치·수조·톱니바퀴·빵** 같은 세계관에 없는 사물이 들어갔다 → "도구·기계 부품·실험 기구는 없다, 조직과 체액만 있다"를 프롬프트에 박았다.
- 발·컵이 복제되던 것 → 동작을 바꾸고 `--no`에 복제 관련 금지어를 넣었다.

## 참고

- 끝부분만 이상한 클립 17개는 다시 안 뽑는다 — 세션이 잘라내고 속도로 길이를 맞춘다.
- `3_업스케일/`은 이미 반영 끝났다. Flow까지 끝나면 지워도 된다.
- 원본 프롬프트 전체는 `../_지난시트/`, 번호↔이름은 `../RENDER/_이름표.json`.
